@AGENTS.md

## Notas específicas deste app

- **(US-028) Qualificação B2C: a evidência por critério (US-006/US-024, `buscarPessoasB2C`) já existia — esta história só ACRESCENTOU a recusa de termo de categoria sensível e uma hipótese de dor própria de B2C, sem tocar em B2B.** `lib/sensivel.ts` (não protegido por `verificar-padrao.sh` — só `lib/ai.ts`/`lib/store.ts`/etc. estão na lista `INFRA`, `lib/sensivel.ts` nunca esteve) ganhou `CATEGORIAS_SENSIVEIS` (lista FECHADA, as sete categorias do art. 5º, II da LGPD) e `termoSensivel(texto)`, ao lado do `SENSIVEL = false` que já existia (conceito diferente: aquele é "este app salva histórico sensível?", este é "este texto cita uma categoria protegida?"). **Termos entram em RADICAL** (`"religi"`, não `"religião"`) — achado real do teste desta história: a frase-modelo sem IA de `gerarHipoteseDor` monta `"enfrentando <dor>"` com a `dor` do ICP já em minúsculas/flexionada (ex.: "convicção **religiosa**"), e um termo com sufixo fixo (`"religião"`) não batia nela.
  - **`lib/execucao-prospeccao.ts:omitirEvidenciasSensiveis`, chamada só em `buscarPessoasB2C`**, filtra a lista de evidências (as `avaliarCriterios` objetivas + a `avaliarCriterioInterpretativo` de "Contexto") checando `termoSensivel` no `valor` do critério E no `trecho` citado pela IA — qualquer um dos dois causa a omissão (nunca um "nao_atende"/"atende" com o termo dentro), com o motivo em `console.error`, nunca na tela. Roda ANTES do filtro "sem nenhuma evidência verificável não entra na lista" (US-024): uma pessoa com Localização/Ocupação normais e só o Interesse/Contexto sensível continua entrando (com as evidências não-sensíveis), só a evidência problemática some — confirmado no teste de ponta a ponta (ICP com `interesses: ["ioga", "evangélica", "culinária"]`, as duas evidências que citavam "evangélica" — Interesses e Contexto — foram omitidas, Localização/Ocupação continuaram normais).
  - **`gerarHipoteseDor` (`lib/qualificacao-ia.ts`) ganhou um terceiro parâmetro `jornada: Jornada = "b2b"`** (todo call-site antigo continua igual — só `etapaQualificar` passa `jornada`, lida do `icp?.jornada`). Em `"b2c"`: (1) sinais e dores com termo sensível são filtrados ANTES de virar `sinalPrincipal`/entrar no prompt — nunca chegam nem à frase-modelo sem IA nem ao texto enviado à IA (defesa em profundidade: o termo nem sai do processo); (2) o `SYSTEM` trocado (`SYSTEM_HIPOTESE_DOR_B2C`) exige "necessidade ou momento de vida", proibindo característica pessoal protegida explicitamente. Sem nenhum sinal SEGURO sobrando (todos os sinais do lead eram sensíveis), devolve `null` — mesmo comportamento de "sem sinal nenhum" da US-025, não um caso novo.
  - **Nenhuma AC de captura de tela nesta história** (nenhuma rota nova, nenhuma tela nova) — verificação só por script `.mts` descartável: `termoSensivel` cobrindo as 7 categorias (1 termo cada, achado do radical "religi" corrigido no meio do teste) + `gerarHipoteseDor` com sinal/dor mistos (seguro + sensível) provando que B2C filtra e cita só o sinal seguro, que B2C sem nenhum sinal seguro devolve `null`, e que B2B (parâmetro padrão) continua idêntico a antes; e um teste de ponta a ponta pelo PIPELINE de verdade (`lib/workspace.ts` semeando produto/ICP B2C com interesse e contexto sensíveis + `iniciarExecucao`, sem HTTP, modo demonstração): evidências "Interesses"/"Contexto" omitidas, `console.error` registrado, lead ainda criado com as evidências não-sensíveis.
- **(US-027) Ficha do lead (`/leads/[id]`): um componente de CONTEÚDO puro (`components/FichaLead.tsx`) reaproveitado por DUAS molduras diferentes**, exatamente como a US-018/US-026 já previam. `components/PainelLateral.tsx` (novo, extraído do que era local a `ExploracaoEmpresa.tsx`) é só a moldura (backdrop + slide-over); `components/FichaLeadPagina.tsx` (Topbar + `<Link href="/leads">‹ Leads</Link>` + `.card`) é a outra. `ExploracaoEmpresa.tsx` (US-018) trocou seu painel manuscrito (critérios+sinais+hipótese+`<select>` de papel, tudo duplicado) por `<PainelLateral><FichaLead leadId={...} onLeadAtualizado={...} /></PainelLateral>` — o editor de papel (US-026) MOROU de lá para cá, não foi duplicado.
  - **`FichaLead` é AUTOCONTIDO**: recebe só `leadId` (+ `onLeadAtualizado?`, prop opcional de retorno, mesmo padrão já registrado no Codebase Patterns raiz) e busca `GET /api/leads/[id]` (rota estendida — antes só tinha `PUT`/`DELETE`) sozinho; funciona idêntico vindo de uma página (`/leads/[id]`) ou de dentro de um painel (`ExploracaoEmpresa.tsx`) sem o pai precisar saber nada do lead além do id. O payload da rota (`{ lead, conta, icpPersonas, jornada }`) é o mesmo par de informações que `GET /api/prospeccoes/[id]/andamento` já expõe para o mesmo fim (explicar o papel, decidir se o campo aparece em B2C), só que para UM lead: `conta` vem de `lead.contaId` (nulo em B2C ou quando ainda não se sabe a empresa — aba "Sobre a empresa" mostra "Esta pessoa não está vinculada a uma empresa conhecida.") e `jornada`/`icpPersonas` do ICP da prospecção do lead.
  - **As três abas ("Visão geral"/"Sinais"/"Sobre a empresa") usam o mesmo `TabButton` local (não exportado) já usado em `reunioes-ia/components/EntradaTranscricao.tsx`** (`role="tab"`/`aria-selected`, `border-b-2` colorida quando ativo) — não existe um componente de abas compartilhado em `components/ui.tsx`, e não foi criado um agora (só este arquivo usa abas nesta suíte até aqui).
  - **"Adicionar à lista" (rodapé, ação secundária) só aparece quando `lead.status === "novo"`** ("botão que não faria nada naquele estado não fica desligado, ele sai" — Codebase Patterns raiz): chama o MESMO endpoint de "Adicionar à prospecção" (`POST /api/prospeccoes/[id]/selecionar-pessoas`), só que com `leadIds: [leadId]` — não foi criada uma rota nova. Demais status (`pesquisado`/`qualificado`/...) já contam como "na lista" (mesmo critério de `STATUS_JA_NA_PROSPECCAO` de `ExploracaoEmpresa.tsx`) e o botão simplesmente não renderiza.
  - **"Criar abordagem" (primária) aponta para `/leads/[id]/abordagem`, uma CASCA "Em breve"** (`components/AbordagemLead.tsx`) — mesmo padrão de `Produtos`/`Prospeccões`/`Leads` na US-002 e `ProdutoNovo`/`ProdutoEditar` na US-004: a estratégia de verdade é a US-029 (Fase 5, ainda não implementada). Ao implementar a US-029, **substituir** o conteúdo desta casca, não criar um segundo par de arquivos.
  - Testado de ponta a ponta no `.next/standalone` isolado (conta de teste + `npx tsx` semeando produto/ICP/prospecção/conta/leads DIRETO em `lib/workspace.ts`, com `DATA_DIR` passado na LINHA DE COMANDO — ver gotcha no Codebase Patterns raiz sobre `process.env.DATA_DIR` no corpo do script) e captura por CDP (script reconstruído em `/tmp/shot.mjs`, não sobrevive entre sessões) em desktop 1400×900 e celular 390 px: ficha como página (lead B2B com conta, sinal datado e hipótese; lead B2C sem conta e sem seletor de papel), painel lateral a partir de `ExploracaoEmpresa.tsx`, clique real em "Adicionar à lista" fazendo o botão sumir depois de promovido, casca de "Criar abordagem" e o estado "Esta pessoa não existe mais." — zero erro de console, zero rolagem horizontal em todas. **Gotcha de teste**: `Runtime.evaluate` com uma expressão tipo `"botao.click(); await new Promise(...)"` (await solto, sem função assíncrona) não disparou o clique de forma confiável; envolver em uma IIFE assíncrona explícita (`"(async()=>{ ...; await ...; })()"`) resolveu.
- **(US-026) Papel de cada pessoa: derivação cruzando cargo + personas do ICP, explicação em uma frase e edição manual persistida.** `lib/qualificacao.ts:inferirPapel(cargo, personas)` ganhou um segundo parâmetro (todos os ~5 call-sites de `lib/execucao-prospeccao.ts` passam `icp?.personas ?? []`, incluindo `buscarPessoasChaveUnica`/`buscarPessoasOportunidadesEmpresas`, que antes não recebiam `icp` nenhum — assinatura estendida): decisor/influenciador (palavra-chave no cargo, US-018) continuam tendo prioridade; só quando NENHuma bate, um cargo que casa com uma persona do ICP (`personaDoCargo`, comparação nas DUAS direções — cargo contém a persona OU persona contém o cargo, porque cargo extraído de busca costuma ser uma palavra genérica como "Analista" e a persona é a frase mais específica) vira `"champion"`. `motivoPapel(papel, cargo, personas)` (mesmo arquivo) gera a frase da AC ("o title explica a inferência") citando o cargo ou a persona encontrada; `null` para `"desconhecido"` (nada a explicar).
  - **Edição manual (AC "editável pelo vendedor na ficha") mora só no painel lateral de `components/ExploracaoEmpresa.tsx`** ("Entender por que essa pessoa", modo `empresa_unica`) — é a única "ficha" de um lead que já existe no app antes da US-027 trazer `/leads/[id]`; quando essa história nascer, MOVER o `<select>` para lá, não duplicar. Nas outras listas (`components/ProspeccaoAndamento.tsx`, modos "pessoas" B2B e "oportunidades" B2B) o papel é só leitura (`ChipPapel`, local ao arquivo): chip com o rótulo (ou nada, para "desconhecido") e `title` explicando a inferência — `Chip` (INFRA) não aceita `title`, por isso o `<span>` embrulhando em vez de passar a prop direto.
  - **`LeadProspeccao.papelManual` (novo, `lib/workspace.ts`: coluna `papel_manual INTEGER DEFAULT 0`, mesmo padrão `ALTER TABLE` de `demo`/`apagado_em`) existe só para a EXPLICAÇÃO não mentir depois de uma edição manual**: sem esse flag, `motivoPapel(papel, cargo, personas)` recalcularia uma "explicação" a partir do cargo ORIGINAL e do papel NOVO (editado à mão), produzindo uma frase fabricada tipo "Cargo com palavra de liderança/decisão" para um cargo que não tem nenhuma — confirmado no teste manual (editar para "decisor" um lead cujo cargo é "Analista de Manutenção Predial"). Com `papelManual: true`, os três lugares que mostram a explicação (`ChipPapel`, a linha da lista e o painel, ambos em `ExploracaoEmpresa.tsx`) trocam a frase por "Definido manualmente pelo vendedor." — decisão de UI, não fica em `motivoPapel` (que continua puro: só descreve o que o cargo/persona justificariam, nunca sabe se foi editado).
  - **`PUT /api/leads/[id]` (novo) só aceita `{ papel }`** (400 se não for um dos 4 valores de `ROTULO_PAPEL`, 404 se o lead não existir) e sempre grava `papelManual: true` — é a ÚNICA escrita de `papel` fora do pipeline. "A edição é preservada em nova execução da prospecção" (AC) não precisou de nenhum mecanismo extra: "Repetir" cria uma `Prospeccao` NOVA, e o dedup por produto (`chaveLead`, US-014) nunca recria o MESMO registro de lead — o registro editado, intocado, continua existindo com o papel manual.
  - **`GET /api/prospeccoes/[id]/andamento` passou a devolver `icpPersonas: string[]`** (`icp?.personas ?? []`) só para a explicação (`motivoPapel`) citar a persona certa nas telas — a derivação em si já aconteceu no pipeline; este campo nunca recalcula `LeadProspeccao.papel`.
  - Testado de ponta a ponta: script `.mts` descartável (`inferirPapel`/`motivoPapel` isolados — decisor/influenciador por palavra-chave, champion por persona nas duas direções, `null` sem cargo) e no `.next/standalone` isolado (conta de teste + `curl` semeando produto/ICP B2B com personas `["Analista de Manutenção", "Diretor de Operações"]` + prospecção `empresa_unica` em demonstração, com um lead ajustado à mão via `lib/workspace.ts` para cargo "Analista de Manutenção Predial" só para EXERCITAR champion visualmente — os cargos fictícios de `pessoasChaveDemo()`/`CARGOS_PESSOA` são todos genéricos demais, sempre batendo decisor/influenciador antes de chegar no cruzamento de persona): `PUT /api/leads/[id]` (200 com `papelManual: true`, 400 valor inválido, 404 id inexistente), edição via `<select>` no painel atualizando o chip e a explicação em tempo real, prospecção "pessoas" B2B real (3 leads com decisor/influenciador), e prospecção B2C confirmando que nenhum chip de papel aparece. Captura por CDP (`WebSocket` nativo do Node, sem o pacote `ws`) em desktop 1400×900 e celular 390 px nas quatro telas (lista + painel, antes e depois da edição manual): zero erro de console, zero rolagem horizontal.
- **(US-025) Hipótese de dor: ponto ÚNICO e CENTRAL na etapa 5 do pipeline (`lib/execucao-prospeccao.ts:etapaQualificar`), não espalhado pelos ~6 lugares que criam um `LeadProspeccao`.** A etapa 5 já iterava `listarLeads(prospeccaoId)` para promover status; passou a `async` e, para CADA lead (`atualizarLead(lead.id, { status, hipotese })`), chama `lib/qualificacao-ia.ts:gerarHipoteseDor(lead.sinais, icp?.dores ?? [])` — os SINAIS que o próprio lead já tem (nunca as evidências, que ficam em bloco/campo separado: "a ficha nunca mistura hipótese e evidência no mesmo bloco") e as dores do ICP da prospecção (`icp`, já estava em escopo em `executarPipeline`, só precisou virar parâmetro de `etapaQualificar`). Sem NENHUM sinal, `gerarHipoteseDor` devolve `null` **sem chamar a IA** (não invoca `aiEnabled()`/`askJSON` nenhuma) — é o que a UI usa para mostrar "Ainda sem sinais públicos suficientes para uma hipótese" em vez de qualquer coisa inventada.
  - **O laço da etapa 5 NÃO filtra por status antes de gerar a hipótese** (só o `status` gravado depende de `lead.status === "pesquisado"`): isso inclui os leads "novo" de "Explorar uma empresa" (US-018), que a etapa 5 nunca promove — eles também ganham hipótese, mesmo sem fazer parte "de fato" da prospecção ainda. Decisão deliberada: a hipótese é sobre o LEAD, não sobre o status dele na prospecção, e não haveria onde recalculá-la depois (a US-018 nunca reprocessa um lead "novo" depois de criado).
  - **Sem IA configurada (`!aiEnabled()`), `gerarHipoteseDor` NÃO devolve `null` quando há sinal** — cai numa frase-modelo determinística que já respeita as mesmas regras (condicional: "Pode estar enfrentando `<dor>`, a julgar por `"<sinal>"` (`<data>`)."; sem nenhuma dor no ICP, cai numa segunda frase que só cita o sinal, sem inventar uma dor) — é o que mantém "modo demonstração sempre funciona: sem nenhuma chave" (regra do `prd.json`) também para este bloco novo. Confirmado nos testes: as seis pessoas do exemplo do modo "oportunidades" em demonstração (sempre tem sinal, por regra da US-020) ganham hipótese sem `OPENROUTER_API_KEY` nenhuma.
  - **`gerarHipoteseDor` ordena os sinais por data (mais recente primeiro) e usa só o `sinalPrincipal`** para a frase-modelo sem IA — com IA, TODOS os sinais do lead entram no prompt (um por linha, com data), e é o modelo quem escolhe o que citar; o `SYSTEM_HIPOTESE_DOR` proíbe inventar fato fora dos sinais e exige frase condicional ("pode estar", "provavelmente", "talvez"), no mesmo espírito do `SYSTEM_ABORDAGEM` (`lib/abordagem.ts`) — mudança de prompt conferida interceptando `globalThis.fetch` só para `openrouter.ai` e lendo a mensagem `system`/`user` enviada (mesma técnica já documentada no Codebase Patterns raiz), nunca lendo o código à mão.
  - **UI: bloco "Hipótese de dor" (ícone de lâmpada, `<HipoteseDor>`) é SEMPRE um bloco PRÓPRIO, separado de `EvidenciasLista`** — acrescentado nos três lugares de `components/ProspeccaoAndamento.tsx` que já mostravam `EvidenciasLista`/sinais de um `LeadProspeccao` (pessoas B2C, pessoas B2B, oportunidades) e, como um `<div>` a mais dentro do painel lateral "Entender por que essa pessoa" de `components/ExploracaoEmpresa.tsx` (que já tinha "Critérios atendidos" e "Sinais" como blocos separados — a hipótese virou o terceiro). Não existe `<HipoteseDor>` compartilhado entre os dois arquivos (duplicado localmente, pequeno o bastante para não justificar extração — mesmo critério já usado para outros componentes pequenos e locais desta suíte). Renderiza `null` (nada) quando `hipotese` é `null` E o lead tem sinal (falha silenciosa da IA, já logada em `console.error` por `gerarHipoteseDor`) — só mostra a frase fixa quando `sinais.length === 0` de verdade, nunca confundindo "IA falhou" com "sem sinal".
  - `Conta` NÃO ganhou campo de hipótese (só `LeadProspeccao` tem `hipotese` no schema desde a US-001) — a AC fala só de "cada lead", nunca de empresa; os blocos "empresas"/contas de "oportunidades" continuam sem esse bloco.
  - Testado de ponta a ponta: script `.mts` descartável (sem sinal → `null` sem chamar IA; com sinal e sem IA → frase condicional citando o sinal, com e sem dor no ICP; com IA mockada via `globalThis.fetch` interceptando só `openrouter.ai` → conferido o `system`/`user` enviados E que a hipótese devolvida pelo mock é a usada; pipeline completo em demonstração no modo "oportunidades" → toda pessoa ganhou hipótese; pipeline completo em "empresa_unica" → leads "novo" também ganharam hipótese, sem serem promovidos). E no `.next/standalone` isolado (conta de teste + `curl` semeando produto/ICP + 3 prospecções — "oportunidades", "pessoas" B2B sem sinal, "empresa_unica" — + captura por CDP com o `WebSocket` global do Node): bloco preenchido e em itálico quando há sinal, frase cinza fixa quando não há, e o painel lateral mostrando os três blocos separados (critérios, sinais, hipótese); desktop 1400×900 e celular 390 px, zero erro de console, zero rolagem horizontal em todas as capturas.
- **(US-024) Aderência ao ICP com evidências item a item.** `Evidencia` ganhou `trecho?: string` (`lib/types.ts`), só preenchido por um critério interpretativo avaliado por IA. Critérios OBJETIVOS continuam em `lib/qualificacao.ts` (determinístico, sem IA): "Porte" ganhou a ÚNICA comparação numérica de verdade (`avaliarPorte` — faixa do ICP `"51-200"`/`"mais de 500"` x nº de funcionários extraído do texto por regex; sem faixa OU sem número extraível de um dos dois lados, cai no term-match de sempre, que nunca decide "não atende"). Critérios INTERPRETATIVOS (texto livre do ICP — "Outros critérios" em B2B, "Contexto" em B2C) moram em `lib/qualificacao-ia.ts`, um arquivo NOVO e SEPARADO de `lib/qualificacao.ts` de propósito: importa `lib/ai.ts` (que importa `lib/store.ts`, `node:sqlite`) e só pode ser chamado por código de servidor (`lib/execucao-prospeccao.ts`), nunca por um Client Component — `lib/qualificacao.ts` é importado por `ExploracaoEmpresa.tsx`/`ProspeccaoAndamento.tsx` (`sinalAntigo`) e não pode puxar `node:sqlite` para o bundle do navegador. Sem IA configurada, `avaliarCriterioInterpretativo` cai no mesmo term-match (sem `trecho`, que não há IA para citar) — a demonstração continua funcionando sem nenhuma chave.
  - **"Lead sem nenhuma evidência verificável não entra na lista"** foi aplicada só em `buscarPessoasB2C` (a evidência ali é da PRÓPRIA pessoa). Nos demais modos (`buscarPessoasReais`/`buscarPessoasChaveUnica`/`buscarPessoasOportunidadesEmpresas`) o lead herda `conta.evidencias` (evidência da EMPRESA, não da pessoa) — filtrar por ela esconderia toda pessoa de uma conta sem site encontrado, contradizendo a AC da US-018 ("nenhuma pessoa é adicionada sem seleção explícita" pressupõe que ela aparece na lista para poder ser selecionada). Não estranhar isso como esquecimento; é fronteira documentada no topo de `lib/execucao-prospeccao.ts`.
  - UI: `ROTULO_FIT`/`ROTULO_RESULTADO_EVIDENCIA`/`NIVEL_CHIP_EVIDENCIA` (novos, `lib/rotulos.ts`) — o `Chip nivel={fit}` sem `children` só mostrava "Alta"/"Média"/"Baixa" (nível cru do banco, contra a regra do prd.json); agora todo `<Chip nivel={conta.fit}>` recebe `{ROTULO_FIT[conta.fit]}` como filho ("Alta aderência", sentence case). `EvidenciasLista` (novo componente local em `ProspeccaoAndamento.tsx`) substitui a lista de texto puro "Como este dado chegou aqui" por um chip de resultado + trecho citado quando existe.
  - Testado com o `.next/standalone` isolado (conta de teste + `curl` semeando produto/ICP B2B com "Outros critérios" preenchido + prospecção modo "empresas" em demonstração, sem nenhuma chave) e captura por CDP (`Emulation.setDeviceMetricsOverride`, script `.mjs` descartável usando o `WebSocket` global do Node em vez do pacote `ws`, que não está instalado no projeto) em desktop 1400×900 e celular 390 px: zero erro de console, zero rolagem horizontal, chips de aderência e de evidência renderizando com o rótulo certo. `avaliarPorte` também conferido isolado via `npx tsx -e "..."`: "500 funcionários" fora de "51-200" → `nao_atende`; "120 funcionários" dentro → `atende`; texto sem número → `nao_verificavel`.
- **(US-023) Teto de consultas por prospecção e cache de páginas de 24h, os DOIS dentro de `lib/descoberta.ts`** (duas tabelas próprias, `cache_paginas`/`consultas_prospeccao`, mesmo `app.sqlite`, acessor `abrirBanco()` de `lib/store.ts` — mesmo padrão de "dona do esquema" de `lib/workspace.ts`, só que para uma tabela que não é uma das seis entidades do workspace). `buscarNaWeb`/`lerPagina`/`perfilDePessoa`/`descobrirEmLote` ganharam um último parâmetro opcional `prospeccaoId?: string`, propagado por TODOS os ~14 pontos de chamada de `lib/execucao-prospeccao.ts` (cada função de etapa já tinha `prospeccaoId` no escopo — nenhuma assinatura nova precisou ser inventada, só passar o valor adiante). Sem `prospeccaoId` (as chamadas de `lib/abordagem.ts`/`lib/produto-ia.ts`, fora de uma execução de pipeline), a chamada nunca conta para teto nenhum e nunca lança — só o cache de páginas se aplica a elas (bônus, não exigido pela AC, mas `lerComMarkdown` não tem como saber a diferença e não precisa saber).
  - **O ÚNICO ponto que efetivamente faz a chamada de rede real (`chamar()`, dentro de `lib/descoberta.ts`) é o único ponto que precisa reservar/contar uma consulta** (`reservarConsulta(prospeccaoId)`, primeira linha de `chamar()`) — `buscarNaWeb` (busca) E `lerComMarkdown` (leitura de página/perfil, compartilhada por `lerPagina`/`perfilDePessoa`) passam por ali, então as DUAS capacidades contam para o mesmo teto sem duplicar a lógica em cada uma. O cache (`lerCache`/`gravarCache`, também em `lerComMarkdown`) é verificado ANTES de `chamar()`: um acerto de cache nunca conta consulta nenhuma, porque `chamar()` nem é invocado.
  - **`reservarConsulta` lança `TetoConsultasAtingido` (nova, exportada) ANTES de fazer a chamada que estouraria o teto** (lê a contagem atual, compara com o teto configurado — `BRIGHTDATA_TETO_CONSULTAS` em Opções avançadas, padrão 60 — e só incrementa se ainda houver orçamento): com teto 2, a 1ª e a 2ª chamada reais sempre acontecem, a 3ª é bloqueada ANTES do `fetch`, nunca depois. Isso significa que o número de "empresas"/páginas processadas com sucesso antes de parar pode ser MENOR que o teto (ex.: teto 60 = até 60 chamadas reais, mas cada empresa consome 1 busca + 1 leitura, então até ~30 empresas, não 60) — confirmado por teste de ponta a ponta, não documentado antes na PRD, útil para quem for calibrar o "60" da Open Question 3.
  - **`TetoConsultasAtingido` propaga através de TODOS os `try/catch` "isolados" de `lib/execucao-prospeccao.ts`** (os que dizem "falha ao ler uma candidata não derruba a etapa inteira") via um helper novo, `propagarTeto(err)` (`if (err instanceof TetoConsultasAtingido) throw err`), chamado como PRIMEIRA linha de cada um desses catches — sem isso, o teto seria engolido pelo catch local (log + `continue`/`break`) e a próxima iteração tentaria de novo, sempre lançando de novo, sem nunca chegar ao catch mais externo que sabe marcar a prospecção. É um padrão MECÂNICO (uma linha por catch), não uma reestrutura: qualquer catch novo ao redor de `buscarNaWeb`/`lerPagina`/`perfilDePessoa` que uma história futura acrescentar precisa do mesmo `propagarTeto(err)` na primeira linha, ou o teto silenciosamente para de funcionar para aquele caminho.
  - **`executarPipeline` (catch mais externo) distingue `TetoConsultasAtingido` do erro genérico de etapa**: em vez da mensagem padrão ("Não foi possível concluir a etapa..."), grava `erro: "Paramos em ${err.teto} empresas para não consumir sua cota."` (literal da AC/PRD, com "empresas" fixo mesmo em modos que buscam pessoas — é a frase exata entre aspas da PRD, não vale a pena variar por modo sem uma AC pedindo isso) e `estado: "pronta"` (nunca `"falhou"` — mesma regra já estabelecida para qualquer erro de etapa desde a US-013: o que já foi encontrado antes do teto fica preservado). A tela já mostra isso de graça: `estado === "pronta"` com `erro` preenchido já renderiza `Aviso tom="warn"` desde a US-013/US-014, nenhuma mudança em `components/ProspeccaoAndamento.tsx` foi necessária.
  - **`descobrirEmLote` (ainda sem nenhum chamador no app — só a interface que a PRD pede) ganhou um orçamento de TEMPO** (`TEMPO_MAXIMO_LOTE_MS = 20_000`, checado a cada iteração do laço de leitura, ANTES de cada `lerPagina`): é a implementação literal de "chamada assíncrona do fornecedor... faz no máximo N consultas espaçadas e, esgotado o tempo, segue com o que já tem, sem travar a etapa" (Technical Considerations da PRD) — como a v1 não usa o dataset assíncrono real da Bright Data (Open Question 4), o "tempo esgotado" aqui é sobre a soma de chamadas sequenciais demorando, não sobre polling de um job. Sem uso real ainda, então sem teste de ponta a ponta além do `tsc`/lint.
  - Testado de ponta a ponta com um fornecedor Bright Data falso local (`http.createServer`, `BRIGHTDATA_BASE_URL`): (1) cache — ler a mesma URL duas vezes faz UMA chamada só; (2) teto padrão (60) — a 61ª chamada real lança `TetoConsultasAtingido` com `teto: 60`, exatamente 60 chamadas reais acontecem; (3) teto configurável (`BRIGHTDATA_TETO_CONSULTAS=3`) — para exatamente na 4ª tentativa; (4) sem `prospeccaoId` — 65 leituras seguidas, nunca lança, nunca conta. E de ponta a ponta pelo PIPELINE de verdade (`lib/workspace.ts` semeando produto/ICP/prospecção + `iniciarExecucao`, sem HTTP): com teto 2 e um fornecedor que devolve 12 resultados de busca (cabem numa página só) + institucional real, a prospecção processa 1 busca + 1 leitura (1 conta criada) e para na 2ª leitura, terminando `estado: "pronta"`, `erro: "Paramos em 2 empresas para não consumir sua cota."`; regressão do modo demonstração (sem `BRIGHTDATA_API_KEY`) confirmada sem mudança nenhuma (3 contas fictícias, `erro: null`). Sem AC de captura de tela nesta história (nenhuma rota nova, nenhuma tela nova) — nenhuma verificação por CDP foi necessária.
- **(US-022) Apollo passa a ser fonte também no modo "Explorar uma empresa"** (`lib/execucao-prospeccao.ts:buscarPessoasChaveUnica`, `lib/leads.ts:buscarPessoasDaEmpresa`, novo): antes só o modo "pessoas" (US-019) usava a Apollo; agora, com ela conectada, `buscarPessoasChaveUnica` chama a Apollo filtrando por nome da empresa em vez da busca pública `site:linkedin.com/in "<empresa>" (diretor OR gerente OR head OR coordenador)` — mesmo critério já estabelecido no modo "pessoas": Apollo é fonte ÚNICA quando conectada (sem fallback para a busca pública em caso de erro/0 resultados; falha é engolida com `console.error`, prospecção termina `"pronta"` sem leads novos, nunca `"falhou"`). Cargo e nome vêm dos campos ESTRUTURADOS da Apollo (`p.title`/`p.name`), não mais parseados heuristicamente do título de um resultado de busca — é o que a AC quer dizer com "preenchimento de cargo e empresa" (empresa sempre foi `conta.nome`; o que muda é a confiabilidade do cargo).
  - **`Conta.resumo` (campo que já existia desde a US-001/US-017, mas nunca era exibido em lugar nenhum da tela) passou a aparecer em `components/ExploracaoEmpresa.tsx` sob o rótulo "Sobre a empresa"** — é o destino do fato que a Apollo devolve sobre a organização (fundação/setor/porte, mesma construção de `sinalApollo`, reaproveitada por `buscarPessoasDaEmpresa` sob o nome `sobreEmpresa`), nunca um `SinalProspeccao` (que exige data e fonte, regra da US-020): "o sinal montado a partir dos campos da Apollo deixa de ser chamado de sinal e vira 'Sobre a empresa' na ficha" (AC). A Apollo só ENRIQUECE o resumo quando a etapa 2 (`buscarContaUnicaReal`) não conseguiu ler a página institucional (`conta.resumo` igual a `RESUMO_SEM_SITE` ou `RESUMO_SITE_ILEGIVEL`, constantes novas no topo do arquivo) — nunca sobrescreve um resumo real já extraído da própria página (`resumoDaPagina`), que continua sendo a fonte preferida.
  - **`lib/leads.ts:sinalApollo` (função interna, não exportada) NÃO foi renomeada nem alterada** — o fluxo ANTIGO (`buscarLeads`, `app/page.tsx` coluna "Sinal", CSV, `lib/ferramentas.ts`/MCP `buscar_leads`) continua chamando esse fato de "sinal" exatamente como antes (AC "a ferramenta MCP `buscar_leads` continua funcionando com o comportamento atual"); só o workspace novo (via `buscarPessoasDaEmpresa`, que reaproveita a mesma função internamente) rotula o mesmo tipo de fato como "Sobre a empresa". Duas nomenclaturas coexistindo de propósito — não é inconsistência a corrigir.
  - **`buscarPessoasDaEmpresa` (novo em `lib/leads.ts`) usa `q_organization_name` (não documentado oficialmente — Apollo não está sob a regra "confira documentação oficial" do `prd.json`, que vale só para a Bright Data) + `person_titles: ["Diretor", "Gerente", "Head", "Coordenador"]`**, mesmo recorte de cargos da busca pública que substitui. Lança `ErroApollo` em falha (mesmo formato de `buscarLeads`); quem chama sempre engole.
  - Testado de ponta a ponta com um fornecedor Bright Data falso local + `globalThis.fetch` mockado para `api.apollo.io` (script `.mts` descartável, apagado ao final): (1) sem Apollo — busca pública inalterada; (2) com Apollo e leitura institucional OK — cargo/papel/fonte corretos, `resumo` preservado da página real; (2b) com Apollo e leitura institucional falhando (500 simulado) — `resumo` enriquecido com o fato da Apollo (“Fundada em 2010, atua em Tecnologia, cerca de 80 funcionários.”); (3) Apollo recusando a chave (401) — erro engolido, prospecção `"pronta"`, 0 leads; (4) `buscarLeads` (fluxo antigo/MCP) com Apollo mockada — `Lead.sinal` idêntico ao formato de sempre, provando ausência de regressão. Sem AC de captura de tela nesta história (não há "Verificar no navegador..." na lista de critérios); nenhuma verificação de UI por CDP foi feita.
- **(US-021) Jornada B2C completa: modo "Encontrar pessoas" ganha descoberta real, retenção própria e "Apagar dados desta pessoa"** (`lib/execucao-prospeccao.ts:buscarPessoasB2C`) — era a última combinação modo/jornada ainda fictícia (ver comentário de topo do arquivo, agora "todo modo/jornada faz descoberta de verdade"); `criarPessoasFicticias` foi removida por inteiro (ficou inalcançável, mesmo critério de `criarContasFicticias`/US-020). Usa a mesma técnica de busca pública `site:linkedin.com/in` + `perfilDePessoa` de `buscarPessoasOportunidadesB2C` (US-020), mas os termos comparados são os do PERFIL (localização/ocupação/interesses, critérios da US-006), não sinais de intenção — por isso, diferente de "oportunidades", uma pessoa sem nenhuma evidência ainda entra na lista (fit "media"): este modo promete "combina com o perfil ideal", não "sinal de que este é o momento certo". Sinais do ICP ainda são extraídos quando aparecem no texto (alimentam a coluna "Sinal"), mas nunca são condição de entrada. `contaId` sempre `null` e `papel` sempre `"desconhecido"` (B2C não tem empresa como unidade nem classifica papel de decisão).
  - **Retenção diferenciada por JORNADA, não por tabela** (`lib/workspace.ts`, `DIAS_RETENCAO_B2B = 180`/`DIAS_RETENCAO_B2C = 90`): como `leads` não tem coluna de jornada própria, `limparExpirados()` decide pelo `icps.jornada` da prospecção do lead (`LEFT JOIN prospeccoes → icps`) — ICP já apagado (`apagarICP` é `DELETE`, não soft delete) cai no ramo "não é b2c" (180 dias), nunca apaga cedo demais um lead cuja jornada não dá mais para confirmar. `contas` não têm jornada própria (só existem em prospecções B2B — `deveCriarContas`) e continuam na retenção única de 180 dias. A mesma subquery (`SUBQUERY_LEADS_EXPIRADOS`) é reaproveitada nos dois `DELETE` (abordagens e leads), evitando duplicar a condição.
  - **"Apagar dados desta pessoa" é uma rota nova (`DELETE /api/leads/[id]`, `app/api/leads/[id]/route.ts`), não reaproveita `DELETE /api/prospeccoes/[id]`**: apaga só aquele lead (e as abordagens dele, `apagarLead` já cascateava desde a US-001), sem tocar no resto da prospecção — mesmo padrão 404/200 de `DELETE /api/prospeccoes/[id]` (US-014). Precisou de `npx next typegen` antes do `tsc --noEmit` (rota `[id]` nova, gotcha já documentado abaixo).
  - **A lista B2C do modo "pessoas" (`components/ProspeccaoAndamento.tsx`) é um bloco PRÓPRIO, separado do bloco B2B existente** (`andamento.jornada === "b2c"` vs `"b2b"` — a rota `andamento` passou a devolver `jornada: icp?.jornada`, já que `Prospeccao` nunca persiste jornada, ver decisão da US-010): layout "Pessoa | Fit | Sinal | Contexto | Status" (`ROTULO_STATUS_LEAD`, novo `Record<StatusLead, string>` em `lib/rotulos.ts`), bloco "Como este dado chegou aqui" (uma linha por evidência: critério, valor, `fonte` do lead e a data de criação) e o botão "Apagar dados desta pessoa". A linha fixa sobre dado público/sensível ("Só entram dados que a própria pessoa publicou...") é a mesma frase, adaptada, já usada por `ICPForm.tsx` (US-006) — e também entrou no README (seção "Retenção de dados"), como pedia a AC.
  - Testado de ponta a ponta: script `.mts` descartável com um fornecedor Bright Data falso local (perfil real com ocupação/localização/interesses/sinal datado no texto → fit alta, evidências e sinal extraídos corretamente, `contaId: null`, `papel: "desconhecido"`) e em modo demonstração (3 pessoas fictícias, `demo: true`); teste isolado de `limparExpirados()` confirmando que um lead B2C com 100 dias sem atualização é apagado (retenção 90 dias) enquanto um B2B com a mesma idade permanece (retenção 180 dias). No `.next/standalone` isolado (conta de teste + `curl` semeando produto/ICP B2C e B2B + `DELETE /api/leads/[id]`: 401 sem cookie, 404 id inexistente, 200 apagando de verdade) e captura por CDP (`/tmp/shot.mjs` reconstruído mais uma vez — não sobrevive entre sessões) em desktop 1400×900 e celular 390 px da lista B2C, mais uma captura de regressão do modo "pessoas" B2B (inalterado): zero erro de console, zero rolagem horizontal nas três capturas.
- **(US-020) Modo "Encontrar oportunidades" ganha descoberta real nas DUAS jornadas** (`lib/execucao-prospeccao.ts:buscarOportunidadesEmpresas` para contas B2B, `buscarPessoasOportunidadesEmpresas`/`buscarPessoasOportunidadesB2C` para pessoas) — antes disso "oportunidades" era o único modo totalmente fictício ao lado de "pessoas" em B2C (que ganhou descoberta real na US-021, ver nota acima). A consulta é montada a partir dos SINAIS do ICP/critérios (`sinaisERecorte`), não de segmento/porte em geral — "empresas/pessoas que os apresentem" (AC), não "que combinam com o perfil" (isso já é o modo "empresas"/"pessoas").
  - **Uma candidata só vira `Conta`/`LeadProspeccao` quando sobra pelo menos 1 sinal depois do filtro "somente recentes"** (`filtrarSinaisRecentes`, novo): sem isso ela não é uma oportunidade, é só mais uma empresa/pessoa do segmento. "Sinal sem fonte é descartado" (regra do `prd.json`) já era garantido estruturalmente por `sinaisEncontrados` (só gera sinal com `origem` preenchida) — nenhuma mudança adicional foi necessária para essa AC.
  - **`lib/qualificacao.ts` ganhou duas capacidades novas, usadas por TODOS os modos que já geravam sinal, não só "oportunidades":** (1) `sinalAntigo(sinal)`/`DIAS_SINAL_ANTIGO = 90` — calculado na LEITURA, nunca gravado (mesmo padrão de todo status derivado do tempo); (2) `sinaisEncontrados` passou a chamar `dataDoSinal(conteudo, termo, consultadoEm)`, que procura uma data no formato brasileiro (`dd/mm/aaaa`) a até 120 caracteres do termo encontrado no texto — quando existe (ex.: "Abriu vaga... em 12/05/2026"), essa é a data do sinal; sem ela (ou com uma data no futuro, não confiável), cai no comportamento de sempre (`consultadoEm`, a data da própria consulta). É uma mudança estritamente aditiva: nenhum conteúdo de demonstração dos modos anteriores (US-017/018/019) embute uma data no texto, então o comportamento deles não muda.
  - **Conteúdo de demonstração é PRÓPRIO do modo** (`conteudoDemoDaOportunidade`/`conteudoDemoDaPessoaOportunidade`, mesmo motivo já registrado para "empresas"/"empresa_unica": precisam citar um sinal DE VERDADE, com uma data no texto, para `sinaisEncontrados`/`dataDoSinal` terem o que achar). Como a busca de demonstração (`resultadosBuscaDemo`) sempre devolve as MESMAS 3 candidatas fixas, `diasDemoOportunidade(indice)` usa só 3 índices para exercitar os três resultados possíveis do modo: índice 0 = sinal recente (15 dias), índice 1 = sinal "antigo" (115 dias, mais de `DIAS_SINAL_ANTIGO`), índice 2 = sem sinal nenhum (candidata descartada). A data é sempre relativa a HOJE (`diasAtras(n)`, novo, mesma ideia do helper homônimo — não exportado — de `lib/demo.ts`), nunca um texto solto ou uma data fixa.
  - **Pessoas do lado B2B (`buscarPessoasOportunidadesEmpresas`) NÃO reaproveitam `buscarPessoasChaveUnica` (US-018) diretamente**, apesar de fazerem a mesma busca `site:linkedin.com/in`: aqui a pessoa nasce `status: "pesquisado"` (a etapa 5 promove a "qualificado" como em qualquer modo automático), não `"novo"` (que exige a seleção manual de "Explorar uma empresa", uma busca pontual que "oportunidades" não tem). O laço roda para até `TETO_CONTAS_PESSOAS_OPORTUNIDADE = 5` contas (não 50, o teto de "quantidade alvo"): sem esse teto, uma busca ampla multiplicaria em até 250 buscas de pessoa numa única prospecção (sem orçamento ainda, US-023). O "já visto" (`jaVistos`) é compartilhado por TODAS as contas do laço, não reiniciado a cada uma — dedup entre contas da mesma prospecção, não só entre execuções.
  - **Pessoas do lado B2C (`buscarPessoasOportunidadesB2C`) são o PRIMEIRO uso de `perfilDePessoa`** (`lib/descoberta.ts`) no app: até aqui toda leitura de página (institucional ou de perfil) passava por `lerPagina`, porque nenhum modo anterior lia o perfil de uma pessoa isolada, sem conta. Um resultado de demonstração de `perfilDePessoa` é descartado explicitamente (mesma regra já aplicada a `lerPagina` desde a US-015): o conteúdo genérico (`perfilPessoaDemo`) nunca vaza para dentro de uma busca já real, substituído por `conteudoDemoDaPessoaOportunidade`. Papel sempre `"desconhecido"` (B2C não classifica papel de decisão).
  - **UI (`components/ProspeccaoAndamento.tsx`): novo bloco para `modo === "oportunidades"`**, mostrando as contas (B2B) e/ou os leads (B2C, sem conta) com TODOS os sinais (não só os 3 primeiros, diferente do bloco "empresas") via um novo `ChipSinal` (descrição + data + "· Antigo" em cinza quando `sinalAntigo`, verde/positivo quando recente) — reaproveitado também no bloco "empresas" (antes mostrava só a descrição, sem data) e no painel lateral de `components/ExploracaoEmpresa.tsx` ("item datado na ficha", AC da própria US-020 aplicada ao único lugar que já existia parecido com uma "ficha" de lead, já que `/leads/[id]` ainda não existe — US-027).
  - **`criarContasFicticias` foi removida por inteiro** (não só esvaziada): com "oportunidades" também real, o único modo que ainda cai no fallback de `etapaProcurarEmpresas` é "pessoas" em B2B (que já tinha um `return` próprio, sem criar conta nenhuma ali) — o branch ao qual `criarContasFicticias` pertencia ficou permanentemente inalcançável, mesmo critério já usado para apagar `ULTIMO_PASSO_PRONTO` na US-012.
  - Testado de ponta a ponta: script `.mts` descartável (demonstração B2B com `somenteRecentes` ligado/desligado, demonstração B2C, ICP sem nenhum sinal → nada é buscado, e um fornecedor Bright Data falso local provando que `dataDoSinal` extrai a data de dentro do texto real — sinal datado de ~10 dias atrás, não "agora" como seria sem essa extração) e o `.next/standalone` isolado com conta de teste + `curl` semeando produto/ICPs (B2B e B2C) + captura por CDP nas prospecções de oportunidades B2B/B2C (desktop 1400×900 e celular 390 px) e no painel lateral de "Explorar uma empresa" (para confirmar que o item datado não regrediu): zero erro de console, zero rolagem horizontal, chip "Antigo" aparecendo corretamente só no sinal com mais de 90 dias.
- **(US-019) Modo "Encontrar pessoas" em B2B ganha descoberta real** (`lib/execucao-prospeccao.ts:buscarPessoasReais`, chamada só quando `modo === "pessoas" && jornada === "b2b"` — em B2C o modo ganhou sua própria descoberta real na US-021, `buscarPessoasB2C`, ver nota no topo do arquivo). A etapa 2 (`etapaProcurarEmpresas`) passou a NÃO criar nada para este modo (`if (modo === "pessoas") return;`): diferente de "empresas"/"empresa_unica", a `Conta` de cada pessoa nasce sob demanda dentro da própria etapa 4, porque só ali se sabe a empresa de cada pessoa encontrada.
  - **Duas fontes, nunca misturadas na mesma pessoa**: com `apolloEnabled()`, a Apollo é a fonte de CONTATO (`lib/leads.ts:buscarLeads`, mesmo caminho da busca de leads de hoje) — cada pessoa vem com nome/cargo/cidade/linkedin/empresa/site da organização; sem Apollo, as pessoas vêm de uma busca pública `site:linkedin.com/in "<cargo>" "<segmento>" "<localização>"` (mesma técnica de `buscarPessoasChaveUnica`/US-018), com fallback de demonstração PRÓPRIO (`pessoasChaveDemo()`, não o `resultadosBuscaDemo()` genérico, que não gera nomes de pessoa) quando a busca não está conectada. **Não há combinação/backfill entre as duas fontes**: se a Apollo está conectada, ela é a ÚNICA fonte de contato (0 resultados da Apollo não caem para a busca pública) — decisão deliberada para manter as duas branches simples, documentada aqui para quem for medir cobertura.
  - **"Combinado com os sinais públicos" (AC) é implementado no `contaPara()` interno**: quando a pessoa (via Apollo) traz o site da organização (`org.website_url`), a `Conta` correspondente é qualificada de verdade — lê a página institucional (`lerPagina`) e roda a MESMA qualificação de `buscarContasReais` (`avaliarCriterios`/`sinaisEncontrados`/`calcularFit`) antes da pessoa ser criada, então o lead já nasce com `fit`/`evidencias`/`sinais` da própria conta. No caminho público (sem Apollo), a busca `site:linkedin.com/in` não devolve o site da empresa (só o perfil da pessoa), então `contaPara` é chamada com `site: null` e a conta fica sem qualificação (`fit: null`) — igual à linha de base anterior a esta história; qualificar essas contas também exigiria uma segunda busca por empresa (fora do orçamento desta história).
  - **`contaPara()` é um cache local por nome de empresa (`Map`, minúsculas), não `lib/workspace.ts`**: evita criar duas `Conta`s para a mesma empresa quando duas pessoas da Apollo pertencem a ela — `listarContas(prospeccaoId)` (vazia, já que a etapa 2 não cria nada para este modo) inicializa o cache antes do loop.
  - **`fonte` de cada pessoa segue o formato exato da AC** (`origemPessoa()`, novo): `"encontrado em <domínio>, em dd/mm/aaaa"` quando há domínio (o site da empresa, via Apollo; ou `linkedin.com`, no caminho público — o domínio de onde a pessoa foi encontrada, não inventado); `"encontrado via Apollo"` quando a Apollo não devolveu site algum; `null` em modo demonstração (não há origem real para descrever). **Não existe tela de "ficha do lead" ainda** (`/leads/[id]` é da US-027): a AC "aparece na ficha" foi satisfeita mostrando a mesma frase na lista de resultados de `components/ProspeccaoAndamento.tsx` (novo bloco só para `modo === "pessoas"`, que também é a PRIMEIRA vez que este modo tem alguma UI de resultado — antes caía no vazio entre os blocos de "empresas" e "empresa_unica"). Quando a US-027 nascer, mover essa exibição para lá é natural, não repetir o formato.
  - **`pessoaDoResultado()` ganhou um terceiro campo (`empresa`)** a partir do título do resultado de busca (`"Nome - Cargo - Empresa | LinkedIn"`, terceiro segmento) — `buscarPessoasChaveUnica` (US-018) ainda ignora esse campo (já sabe a empresa, é a única que está explorando), só `buscarPessoasReais` usa.
  - **Falha da Apollo (`ErroApollo`) é ENGOLIDA dentro da etapa** (`console.error` + `return`, sem repassar 0 leads como erro do pipeline) — mesmo padrão já estabelecido por `buscarPessoasChaveUnica` (US-018) para a etapa 4 ("encontrando pessoas-chave"): diferente da etapa 2 ("procurando empresas compatíveis"), onde uma falha de busca vira `erro` da prospecção. Falha da busca pública (sem Apollo) segue o mesmo padrão.
  - Testado de ponta a ponta com um fornecedor Bright Data falso local + `globalThis.fetch` mockado só para `api.apollo.io` (script `.mts` descartável, apagado ao final): (1) sem Apollo — 2 pessoas de empresas distintas, papel certo, `fonte` com domínio `linkedin.com`, contas sem qualificação (`fit: null`, esperado); (2) com Apollo — 2 pessoas da MESMA conta (dedup por nome de empresa), conta com `fit: "alta"` e sinal real extraído da página institucional simulada, `fonte` com o domínio da empresa; (3) 100% demonstração — 3 pessoas fictícias com empresas próprias (`demo: true`, `fonte: null`); (4) Apollo recusando a chave (401) — prospecção termina `"pronta"` sem erro, 0 leads (comportamento engolido, documentado acima). Também testado no `.next/standalone` isolado (conta de teste + `curl` semeando produto/ICP + captura por CDP, `/tmp/shot.mjs` reconstruído mais uma vez — não sobrevive entre sessões; desta vez com `.next/static`/`public` copiados para a RAIZ do standalone, não para `prospeccao-ia/`, porque este build não é do monorepo com subpasta): resultado real (sem Apollo, Bright Data real) e modo demonstração, desktop 1400×900 e celular 390×900, zero erro de console, zero rolagem horizontal nas quatro capturas.
- **(US-018) "Explorar uma empresa" (`empresa_unica`) ganha descoberta real de CONTA e de PESSOAS** (`lib/execucao-prospeccao.ts:buscarContaUnicaReal`/`buscarPessoasChaveUnica`, chamadas só quando `modo === "empresa_unica"`; os modos `pessoas`/`oportunidades` continuam com `criarContasFicticias`/`criarPessoasFicticias`, inalterados): a conta usa a mesma qualificação por evidências de `buscarContasReais` (US-017), mas os CRITÉRIOS comparados vêm do `ICP` (segmento/porte/localização), nunca de `criterios` — o passo 4 deste modo só pede o nome da empresa (US-012), sem redigitar os critérios do perfil. As pessoas vêm de uma busca `site:linkedin.com/in "<empresa>" (diretor OR gerente OR head OR coordenador)`, parseando o título do resultado (`pessoaDoResultado`: "Nome - Cargo - Empresa | LinkedIn"); evidências/sinais da pessoa são uma CÓPIA das da própria conta (não há orçamento nesta história para ler o perfil de cada pessoa — isso é `perfilDePessoa`, que só entra na US-019).
  - **Nasce com `status: "novo"`, não "pesquisado"**: a etapa 5 (`etapaQualificar`) só promove `"pesquisado" → "qualificado"`, então as pessoas encontradas aqui NUNCA são promovidas automaticamente — ficam de fora da prospecção "de verdade" até a pessoa marcar a caixa e confirmar "Adicionar à prospecção" (`POST /api/prospeccoes/[id]/selecionar-pessoas`, `"novo" → "selecionado"`). É a única exceção a "toda pessoa encontrada já é lead da prospecção" no app hoje — documentar isso se uma história futura (Leads/US-036, por exemplo) precisar decidir quais status contam como "lead ativo".
  - **Papel (`decisor`/`influenciador`/`champion`/`desconhecido`) ganha sua primeira implementação real** (`lib/qualificacao.ts:inferirPapel`, heurística por palavra-chave no cargo — nenhuma keyword aponta para `champion`, que fica sempre `"desconhecido"` por essa via) e rótulo em `lib/rotulos.ts:ROTULO_PAPEL` (`Record<Papel, string | null>`, `desconhecido: null` = nenhum chip). A US-026 ("Papel de cada pessoa") é quem deve trazer a derivação completa cruzando as personas do ICP e a edição manual preservada entre execuções — não antecipei esse desenho aqui.
  - **Conteúdo de demonstração é PRÓPRIO do modo** (`conteudoDemoDaEmpresaUnica`/`pessoasChaveDemo`, mesmo motivo do `conteudoDemoDaCandidata` da US-017: o `conteudoPaginaDemo` genérico de `lib/descoberta.ts` não cita os critérios pedidos). Diferente da lista "empresas" (que varia fit entre candidatas), aqui há só UMA empresa — a que a pessoa escolheu — então o conteúdo demo sempre confirma os critérios que existirem (fit tende a "alta").
  - **Painel lateral ("Entender por que essa pessoa") é local a `components/ExploracaoEmpresa.tsx`**, não um componente compartilhado — mesmo padrão de "backdrop + `absolute right-0` `role=dialog`" do menu mobile de `components/ui.tsx` (que não pode ser alterado), reimplementado aqui porque é a primeira tela a precisar disso fora do menu. Se a US-027 (ficha do lead, que também cita "painel lateral") precisar do mesmo padrão, é aqui que ele deve ser extraído para um componente compartilhado.
  - Testado de ponta a ponta com um fornecedor falso local (`http.createServer` em `/request`, `BRIGHTDATA_BASE_URL` apontando para ele): conta com fit alta/evidências "atende"/sinal encontrado, 4 resultados de busca por pessoa com 1 duplicado de propósito → 3 pessoas únicas, papel inferido certo por cargo (decisor/influenciador/desconhecido). Também em modo demonstração (sem Bright Data): nome da empresa preservado literal, 3 pessoas fictícias variadas. `POST /api/prospeccoes/[id]/selecionar-pessoas` testado por `curl` (200 com pessoas promovidas a "selecionado", 400 sem `leadIds`, 401 sem cookie, 404 prospecção inexistente, id desconhecido ignorado em silêncio) e clicando de verdade no navegador via CDP (`Runtime.evaluate` + `.click()` nas caixas e no botão). Capturas em desktop 1400×900 e celular 390 px (lista de resultados, painel lateral aberto, confirmação após adicionar): zero erro de console, zero rolagem horizontal.
- **(US-017) O modo "Encontrar empresas" é o PRIMEIRO a fazer descoberta de verdade** (`lib/execucao-prospeccao.ts:buscarContasReais`, chamado só quando `modo === "empresas"` — os outros modos continuam com `criarContasFicticias`, o código antigo, inalterado): monta a consulta (`"empresas" + segmento + localização + porte`), pagina `buscarNaWeb` (novo parâmetro `pagina` em `lib/descoberta.ts`, soma `&start=<pagina*10>` à URL do Google — paginação padrão do Google, não específica da Bright Data) até atingir a "quantidade alvo" (10/25/50, novo campo no passo 4 e em `criterios.quantidade`) ou esgotar 5 páginas, deduplica por domínio (`lib/qualificacao.ts:dominioDe`) e lê a página institucional de cada candidata (`lerPagina`). Falha ao ler UMA candidata não derruba a etapa (mesmo espírito de `descobrirEmLote`); falha na própria busca (ex.: `chave_recusada`) propaga como erro normal do pipeline (o `try/catch` de `executarPipeline` já cobria isso, nenhuma mudança lá além do `await`).
  - **`lib/qualificacao.ts` (novo) é a primeira implementação de fit/evidências do app**: `avaliarCriterios(conteudo, [{criterio, valor}])` só gera uma `Evidencia` para critério que TINHA valor (campo vazio não vira evidência nenhuma) e nunca marca `"nao_atende"` — sem o termo no texto é `"nao_verificavel"` ("critério sem dado nunca conta como atendido", regra do `prd.json`, não dá para afirmar ausência com segurança a partir de uma página institucional). `calcularFit`: 0 evidências → `"media"` (sem base pra julgar); 0 atendem → `"baixa"`; todas atendem → `"alta"`; misto → `"media"`. **Chips usam literalmente `Chip nivel={fit}`** (`.chip-alta`/`.chip-media`/`.chip-baixa`, `components/ui.tsx`, INFRA) — o mapa de cor desse arquivo é contraintuitivo para aderência (`chip-alta` = vermelho/perigo, `chip-baixa` = verde/ok, porque essas classes nasceram para severidade genérica e foram reaproveitadas aqui por instrução explícita do `prd.json` > `regras`: "fit em `.chip-alta|media|baixa`"); não é bug, é a regra do produto.
  - **Conteúdo de demonstração do modo "empresas" é PRÓPRIO** (`conteudoDemoDaCandidata`, dentro de `execucao-prospeccao.ts`), não o `conteudoPaginaDemo` genérico de `lib/descoberta.ts` (esse é usado por QUALQUER leitura de página sem saber o que está lendo, e produz um texto solto tipo "Este é um texto de exemplo..." que não cita segmento/porte/localização — vira sempre fit `"baixa"` e um nome de empresa feio, o título genérico do resultado de busca demo). Por isso, quando a busca devolve `demo: true`, o nome também troca para `nomeEmpresaFicticia` (a mesma função já usada pelos modos fictícios) em vez do título do resultado de busca. 2 de cada 3 candidatas demo "atendem" a todos os critérios (fit alta) e 1 não tem nada verificável (fit baixa) — mesma variação que `lib/demo.ts:criarProspeccaoExemplo` já usa para o exemplo semeado.
  - **"Ver pessoas" (por conta, na lista de resultados) NÃO abre uma tela nova**: cria uma segunda prospecção no modo `empresa_unica` (`POST /api/prospeccoes`, `criterios: {empresaNome: conta.nome, segmento, localizacao, porte}` vindos da própria conta) e navega para ela — mesmo caminho de "Explorar uma empresa" que a pessoa já usaria manualmente, mesmo padrão de "Repetir prospecção" (POST + `router.push`) já usado nesta tela. Como `empresa_unica` ainda é fictício (US-018 é quem implementa a descoberta de pessoas de verdade), o resultado dessa segunda prospecção continua com pessoas fictícias — fronteira documentada, não esquecimento.
  - **`GET /api/prospeccoes/[id]/andamento` passou a devolver `contas: Conta[]` inteiro** (além de `contasEncontradas`, que continua a contagem) — a lista de resultados do modo "empresas" usa isso direto, sem uma segunda rota; o volume é o mesmo teto da quantidade alvo (até 50), não é um problema de payload.
  - **Sem teto de gasto ainda** (a AC cita "respeitado o teto de gasto da US-023", mas essa história é POSTERIOR a esta — US-023 ainda não existe): "quantidade alvo" 50 pode gerar até 5 chamadas de busca + até 50 leituras de página numa única prospecção. Aceitável por ora (execução em segundo plano, não bloqueia a resposta HTTP); a US-023 é quem deve somar um limite de verdade aqui.
  - Testado de ponta a ponta com um fornecedor falso local (`http.createServer` em `/request`, `BRIGHTDATA_BASE_URL` apontando para ele — script descartável, apagado ao final): 6 candidatas com um domínio duplicado de propósito (dedup → 5 contas), fit variando entre alta/média/baixa conforme o conteúdo de cada página fake, sinal só na candidata cujo texto cita o termo, paginação parando sozinha quando a "página 2" devolve `organic` vazio, e os três códigos de erro (`chave_recusada`/`limite_do_plano`/`servico_fora`) virando o erro padrão do pipeline. Também testado em modo demonstração (sem Bright Data configurada): 3 contas com nomes fictícios plausíveis, fit variado, resumo e sinal coerentes. `curl` cobriu 401 (sem cookie) e 404 (prospecção/produto inexistente); "Ver pessoas" testado clicando de verdade no navegador via CDP (`Runtime.evaluate` + `.click()`), confirmando a navegação para a nova prospecção. Capturas em desktop 1400×900 e celular 390 px da lista de resultados e do passo 4 do assistente (campo "Quantidade alvo" novo): zero erro de console, zero rolagem horizontal.
- **(US-016) `lib/integracoes.ts:BRIGHTDATA` virou "Pesquisa de mercado e sinais"; o campo único `BRIGHTDATA_ZONE` saiu da UI e virou `BRIGHTDATA_ZONE_BUSCA`/`BRIGHTDATA_ZONE_LEITURA` em "Opções avançadas"** (padrões `serp_api1`/`web_unlocker1`) — `lib/descoberta.ts` já tinha o fallback pronto desde a US-015 (`zonaLeitura()` cai para `BRIGHTDATA_ZONE` quando `BRIGHTDATA_ZONE_LEITURA` está ausente), então quem já tinha configurado o campo antigo continua funcionando sem editar nada, mesmo sem o campo aparecer mais na tela. `testar()` (novo `testarZona()`) testa as duas zonas em paralelo e devolve UMA `mensagem` combinando o resultado de cada uma ("Busca: conectada. Leitura: recusou a chave...") — `Integracao.testar` (tipo compartilhado, `lib/setup-comum.ts`) só tem um campo `mensagem`, não dá para devolver duas mensagens separadas.
  - **`Integracao.testar` fala DIRETO com o endereço real do fornecedor** (`https://api.brightdata.com/request` hardcoded, não `lib/descoberta.ts`/`BRIGHTDATA_BASE_URL`) — é assim desde antes desta história (também vale para `APOLLO.testar`). Testar com um fornecedor falso local exige mockar `globalThis.fetch` num script `.mts` que importa a constante `Integracao` direto, não um `http.createServer` apontado por variável de ambiente.
  - **`Campo.tipo === "text"` com `padrao` sempre renderiza como `placeholder`, nunca como valor pré-preenchido** (`components/setup.tsx`, INFRA, `CampoSetup`) — confirmado lendo `value`/`placeholder` do DOM real via CDP. Já era assim para o campo único `BRIGHTDATA_ZONE`; não é regressão desta história, é o padrão da suíte inteira para todo campo `avancado` com `padrao`.
  - **`components/Inicio.tsx` ganhou `<Topbar resumo="Modo demonstração: leads, sinais e mensagens exibidos são exemplos.">`** — este app era o único (dos 12 já na fundação nova) sem essa prop na tela principal. O popover só abre quando `!status.ai` (Topbar, INFRA, não sabe nada sobre `descoberta`), então o aviso sobre pesquisa de mercado em demo só aparece junto do aviso de IA em demo — mesma limitação que os outros 11 apps já aceitam.
  - **`lib/status-do-app.ts` ganhou `descoberta: descobertaAtiva()`** — distinto de `integrations.brightdata` (calculado pelo genérico a partir de `INTEGRACOES`, que só olha se a CHAVE está salva, sem checar zona nenhuma).
  - **Reconstruindo o script de captura por CDP do zero (não sobrevive entre sessões):** `GET /json/version` devolve o target BROWSER, que recusa `Page.*`/`Runtime.*`/`Emulation.*` com `-32601 "wasn't found"`. Use `PUT /json/new?about:blank` para abrir uma aba e pegar o `webSocketDebuggerUrl` de um target PAGE de verdade.
- **(US-015) `lib/descoberta.ts` (novo) é a única camada que fala com a Bright Data: quatro capacidades — `buscarNaWeb(consulta)`, `lerPagina(url)`, `perfilDePessoa(url)` e `descobrirEmLote(criterios)` — cada uma com implementação real e fallback de demonstração (`descobertaAtiva()`), no mesmo formato de `ErroApollo`/`lib/leads.ts` (`ErroDescoberta`: `codigo` ∈ `chave_recusada`/`limite_do_plano`/`servico_fora`/`sem_resultado`, `status`, `acao?`). `app/api/erros.ts` já trata `ErroDescoberta` ao lado de `ErroApollo`, mesmo sem nenhuma rota lançando um ainda (as duas chamadoras atuais, `lib/abordagem.ts`/`lib/produto-ia.ts`, sempre capturam por dentro — quem lançar de dentro de uma rota nova, a partir da US-017, já tem onde cair).
  - **Endereços e formatos CONFERIDOS na documentação oficial em 18/09/2026** (topo de `lib/descoberta.ts`, não repetir aqui de memória numa mudança futura): `POST https://api.brightdata.com/request`, `Authorization: Bearer <chave>`, corpo `{ zone, url, format: "raw" }` — igual para Web Unlocker e SERP API, só a zona muda. **Achado que não estava na PRD nem no código anterior**: o Web Unlocker converte HTML em markdown do próprio lado do serviço com `data_format: "markdown"` no corpo (`scraping-automation/web-unlocker/features`) — `lerPagina`/`perfilDePessoa` usam isso, então `lib/abordagem.ts`/`lib/produto-ia.ts` **pararam de limpar HTML na mão** (o `.replace(/<script.../gi, ...)` de antes da US-001 já era enriquecimento próprio duplicado, some nesta história). Busca (SERP) é uma URL de busca do Google (`https://www.google.com/search?q=<consulta>`) com `&brd_json=1` **na própria query string do alvo**, não no corpo do POST — é isso que faz o serviço devolver `{ organic: [{ title, link, description }] }` já em JSON em vez do HTML da página de resultados.
  - **`descobrirEmLote` NÃO chama a API de datasets (disparo assíncrono + consulta de andamento) da Bright Data** — decisão da própria PRD (Open Question 4, `naoFazer` do `prd.json`): a interface já existe, mas a implementação de v1 é `buscarNaWeb` + `lerPagina` de cada resultado (teto de 5 páginas, `TETO_LOTE`), sequencial, sem identificador nem polling. Se uma história futura trouxer o dataset de verdade, é aqui que a implementação muda — os chamadores não precisam saber.
  - **Duas zonas, uma variável nova, comportamento antigo preservado**: `zonaBusca()` lê só `BRIGHTDATA_ZONE_BUSCA` (capacidade nova, sem UI ainda); `zonaLeitura()` lê `BRIGHTDATA_ZONE_LEITURA` **ou, se ausente, o `BRIGHTDATA_ZONE` de sempre** — é o que faz quem já tinha a Bright Data configurada (só `BRIGHTDATA_API_KEY`+`BRIGHTDATA_ZONE`, o único par que existia até ontem) continuar funcionando sem tocar em nada. A US-016 ("Um cartão só de dados no `/setup`") é quem decide se isso vira dois campos na tela ou se o campo único continua preenchendo os dois — por ora as duas variáveis só entram por ambiente (linhas novas no README).
  - **`enriquecerSite` (`lib/abordagem.ts`) descarta explicitamente um resultado `demo: true`** (`if (pagina.demo) return ""`), em vez de repassar o texto de demonstração para dentro de uma abordagem gerada com IA de verdade — a checagem antiga (`brightdataEnabled() && lead.site`) sumiu porque `lerPagina` já decide sozinha entre real e demonstração; o `if (lead.site)` continua sendo a única condição do lado de fora. Mesma ideia em `sugerirProdutoDoSite` (`lib/produto-ia.ts`): `pagina.demo` vira o aviso "a leitura de páginas não está conectada" (frase idêntica à de antes), e só um resultado real vira `contexto`. Provado com um fornecedor falso (`http.createServer`, `BRIGHTDATA_BASE_URL` apontando para ele) chamando as duas funções sem `OPENROUTER_API_KEY`: nenhuma delas muda de comportamento comparado ao código anterior à US-015.
  - **Teste funcional (script `.mts` descartável, apagado ao final, mesma técnica de sempre)**: fornecedor falso local cobrindo os 5 modos (`ok`/`401`/`429`/`500`/`vazio`, organic vazio → `sem_resultado`), as 4 capacidades em modo real e em modo demonstração, o fallback `BRIGHTDATA_ZONE_LEITURA` ausente → `BRIGHTDATA_ZONE`, e as duas chamadoras (`escreverAbordagem`/`sugerirProdutoDoSite`) sem IA configurada. Sem UI nesta história (nenhuma AC de captura de tela) — nada a verificar no navegador.
- **(US-014) Cancelar, repetir e apagar.** `POST /api/prospeccoes/[id]/cancelar` marca `estado: "cancelada"` (404 se a prospecção não existe, 400 "Esta prospecção não está em execução." se `estado !== "executando"`) e devolve o registro inteiro atualizado. `DELETE /api/prospeccoes/[id]` (novo `app/api/prospeccoes/[id]/route.ts` — antes só existia a subrota `andamento`) chama `apagarProspeccao` (cascata já pronta desde a US-001: abordagens → leads → contas → prospecção), 404 se não existir.
  - **`foiCancelada(prospeccaoId)` (novo em `lib/execucao-prospeccao.ts`) é checada ANTES de cada etapa do pipeline** (e de novo antes do `catch` gravar `"pronta"` com erro): se alguém cancelou entre duas etapas, o pipeline para na hora, sem sobrescrever `"cancelada"` de volta para `"pronta"`/`"falhou"`, preservando o que já foi gravado. **Como o pipeline inteiro ainda roda no mesmo tick** (nenhuma etapa tem I/O real, herança da US-013), essa checagem quase nunca intercepta a tempo num uso real — ela existe para o dia em que a US-015 trouxer I/O de rede de verdade entre as etapas. Para provar visualmente/pelo `curl` que "Cancelar" funciona, semeei uma `Prospeccao` com `estado: "executando"` + 1 conta + 1 lead já gravados **direto via `lib/workspace.ts`** (mesma técnica de "em andamento" já documentada pela US-013) e cancelei por fora — mesmo padrão, não invenção nova.
  - **Dedup de "Repetir prospecção" (AC "sem duplicar leads já existentes") vive no PIPELINE, não no botão**: `chaveLead(nome, empresa, linkedin)` (novo em `lib/execucao-prospeccao.ts`) — LinkedIn tem prioridade quando existe; sem ele, nome+empresa normalizados (minúsculas, trim), mesmo espírito de `chavePerfil` em `lib/leads-vistos.ts`, só que pela identidade da PESSOA, não pelo perfil da busca. `etapaEncontrarPessoas` monta o conjunto de "já vistos" a partir de `leadsDoProduto(produtoId)` (novo em `lib/workspace.ts`, join `leads`+`prospeccoes` por `produto_id` — a busca é escopada ao PRODUTO, não à prospecção nem global) e pula (`return` antes do `criarLead`) qualquer candidato cuja chave já apareça antes, na mesma execução ou em qualquer prospecção anterior do mesmo produto. **Como os dados fictícios são determinísticos** (mesmo índice → mesmo nome, sem aleatoriedade), repetir a mesma prospecção com os mesmos critérios reconhece TODO mundo como já visto e cria zero leads novos — esse é o comportamento esperado da demonstração (provado no teste), não um bug.
  - **Bug real encontrado e corrigido durante o teste desta história**: `listarContas()` ordenava só por `ORDER BY criado_em DESC`; como as 3 contas de uma prospecção nascem no mesmo milissegundo, o SQLite não garantia ordem estável entre chamadas, e `etapaEncontrarPessoas` usa essa ordem para parear índice-de-pessoa↔empresa — a ordem mudando entre a 1ª execução e a repetição fazia o mesmo nome fictício de pessoa cair em empresas diferentes a cada rodada, e a chave de dedup (nome+empresa) nunca coincidia. Corrigido com um tiebreak `rowid DESC` (`ORDER BY criado_em DESC, rowid DESC`, nos dois `SELECT` de `listarContas`) — mesma leitura "mais recente primeiro", só que determinística. Vale conferir esse mesmo sintoma (ordem instável dentro do mesmo milissegundo) se uma história futura ordenar `listarLeads`/`listarICPs`/`listarProspeccoes` por algo que dependa de posição relativa entre linhas criadas no mesmo tick.
  - **UI (`components/ProspeccaoAndamento.tsx`)**: "Apagar" (com `useConfirmacao`, mesmo padrão de "Apagar produto") fica no topo da tela, ao lado do `h1`, visível em QUALQUER estado — ao confirmar, `DELETE` + `router.push("/prospeccoes")`. "Cancelar" (`btn-ghost`) aparece só quando `estado === "executando"`. "Repetir prospecção" (`btn-link`) aparece quando `estado === "pronta"` (ao lado de "Ver leads") — além do "Repetir" que já existia dentro do `Aviso` de `estado === "falhou"` (US-013); os dois chamam a MESMA função `repetir()`, sem duplicar lógica.
- **(US-013) A `Prospeccao` nasce de verdade: `POST /api/prospeccoes` cria com `estado: "executando"` e devolve na hora (201), sem `await` do trabalho** — a rota chama `void iniciarExecucao(prospeccao.id)` (`lib/execucao-prospeccao.ts`) e responde imediatamente. O pipeline roda as 5 etapas fixas de `lib/execucao-etapas.ts` (`ETAPAS_PROSPECCAO`, arquivo client-safe importado tanto pela tela quanto pelo servidor) em sequência, gravando `Prospeccao.etapa` = a chave da etapa em andamento **antes** de fazer o trabalho dela (para o polling mostrar "atual" certo mesmo se o processo morrer no meio). Como nenhuma etapa faz I/O de verdade (tudo é leitura/escrita síncrona em SQLite), o pipeline inteiro roda dentro do mesmo tick do `void iniciarExecucao(...)` — na prática a prospecção já está `"pronta"` antes mesmo do primeiro poll da tela chegar. Isso é o comportamento esperado ("progresso honesto": zero `setTimeout` decorativo), não um bug; só fica perceptível como "em andamento" numa captura de tela se a etapa for seguida manualmente (ver nota de teste abaixo).
  - **Fronteira exata do que esta história gera** (para US-015/017-020/024-026 substituírem sem reler `lib/execucao-prospeccao.ts` inteiro): etapa 2 cria `Conta` (só quando o modo tem empresa: `empresas`/`empresa_unica` sempre, `pessoas`/`oportunidades` só em B2B — `deveCriarContas`) com nome fictício plausível (`nomeEmpresaFicticia`, a partir do segmento dos critérios) ou o nome literal de `criterios.empresaNome` em `empresa_unica`; **sempre** `fit: null`, `evidencias: []`, `sinais: []`, `demo: false`. Etapa 3 **lê e filtra** `criterios.sinais` + `icp.sinais` (função `analisarSinais`, chamada e descartada — nenhum `SinalProspeccao` é persistido, porque exigiria origem real, que é da US-020: "sinal sem fonte é descartado"). Etapa 4 cria `LeadProspeccao` (só quando o modo tem pessoa: todo modo exceto `empresas` — `deveCriarPessoas`): 2 pessoas por conta já criada (`contaId` vinculado, nome fictício via `nomePessoaFicticia`) ou 6 pessoas soltas com `contaId: null` quando não há conta (B2C `pessoas`/`oportunidades`); sempre `linkedin: null`, `fonte: null`, `papel: "desconhecido"`, `fit: null`, `evidencias: []`, `sinais: []`, `status: "pesquisado"`, `demo: false`. Etapa 5 só promove `status` de `"pesquisado"` para `"qualificado"` (`atualizarLead`) — nenhum `fit`/`evidencias` é calculado em lugar nenhum do pipeline.
  - **Erro no meio do pipeline nunca vira `estado: "falhou"`** (esse estado é reservado à recuperação de inicialização, ver abaixo): o `try/catch` amplo de `executarPipeline` grava `estado: "pronta"` + `erro: "Não foi possível concluir a etapa \"<rótulo>\"; os resultados encontrados até aqui foram mantidos."` mesmo quando uma etapa lança — o que as etapas anteriores já gravaram no banco continua lá. Testado forçando uma exceção temporária em `etapaEncontrarPessoas` (revertida antes do commit): a prospecção terminou `pronta` com as 3 contas da etapa 2 preservadas e 0 leads, e o `erro` citou "Encontrando pessoas-chave" pelo rótulo.
  - **`recuperarProspeccoesTravadas(limiteMinutos = 30)` (nova em `lib/workspace.ts`) roda em `instrumentation.ts`** ao lado de `limparExpirados()`: `UPDATE ... WHERE estado = 'executando' AND criado_em < <corte>` vira `estado: "falhou"` com a mensagem fixa `MENSAGEM_EXECUCAO_TRAVADA` (exportada para o teste reaproveitar a mesma string). É o único jeito de uma `Prospeccao` chegar a `"falhou"` nesta história — o pipeline em si nunca grava esse estado.
  - **`GET /api/prospeccoes/[id]/andamento`** (novo, `force-dynamic`) é a única fonte de dados de `components/ProspeccaoAndamento.tsx`: serve tanto a carga inicial da tela quanto o poll a cada 2 s (por isso sair da tela e voltar, ou recarregar uma prospecção já `"pronta"`, mostra o estado certo de cara — AC4 não precisou de nenhum código extra). `produtoNome`/`icpNome` vêm de `obterProduto`/`obterICP` (sem filtro de apagado, mesmo critério de `GET /api/inicio`); `contasEncontradas`/`leadsEncontrados` são `listarContas(id).length`/`listarLeads(id).length` — sem cache, sempre a contagem real no momento da consulta.
  - **O poll da tela (`INTERVALO_POLL_MS = 2000`) segue o padrão de `whatsapp-atendente/components/ConexaoWhatsApp.tsx`**: o `useEffect` do `setInterval` depende do **primitivo** `andamento?.prospeccao.estado` (não do objeto `andamento` inteiro), para não recriar o intervalo a cada poll; a guarda `document.visibilityState === "visible"` fica DENTRO do callback do `setInterval` (não no efeito), e o poll para sozinho assim que `estado` deixa de ser `"executando"` (o efeito recebe `undefined`/`"pronta"`/`"falhou"` e não recria o timer). `carregar` é `useCallback` com dependência só em `prospeccaoId` (id estável).
  - **"Repetir" (estado `"falhou"`) faz um novo `POST /api/prospeccoes` com os mesmos `produtoId`/`icpId`/`modo`/`criterios` lidos da própria `prospeccao` já carregada** e navega (`router.push`) para o novo id — sem nenhuma deduplicação de leads/contas já vistos (isso é escopo da US-014, que ainda não existe). Quem implementar a US-014 deve adicionar a lógica de dedup no PIPELINE (`lib/execucao-prospeccao.ts`) ou numa checagem antes de criar a nova `Prospeccao`, não duplicar o botão "Repetir" que já existe aqui.
  - **`components/ProspeccaoNova.tsx` (passo 4, US-012) trocou o `<Aviso tom="ok">Em breve...</Aviso>` por um `POST /api/prospeccoes` de verdade** seguido de `router.push(/prospeccoes/<id>)`; erro (400/404) vira `Aviso tom="danger"` inline sem navegar, e o botão mostra "Enviando…" e fica desabilitado durante o `fetch` (estado `enviando`, substituiu o antigo `buscaIniciada` que só controlava o aviso decorativo).
  - **Teste de "em andamento" na captura de tela**: como o pipeline conclui no mesmo tick (nenhuma etapa faz I/O real), não dá para capturar um `POST` real "no meio" de uma etapa. Para provar visualmente que a tela desenha concluída/atual/futura corretamente, semeei uma `Prospeccao` com `estado: "executando"`/`etapa: "procurando_empresas"` **direto via `lib/workspace.ts`** (mesmo mecanismo que o próprio pipeline usa para gravar `etapa`, só que chamado à mão num script `.mts` descartável) — não é um estado inventado pela tela, é um dado real que ela sabe ler; documentando aqui para o próximo Ralph não estranhar um "andamento" que não veio de um `POST` cronometrado.
- **(US-012) O passo 4 do assistente ("Critérios da busca") ganhou conteúdo real, e é o ÚLTIMO passo (`TOTAL_PASSOS = 4`)** — a constante `ULTIMO_PASSO_PRONTO` (que distinguia "passo com conteúdo real" de "casca Em breve") foi removida por inteiro: como ela sempre chegava a `TOTAL_PASSOS`, o branch "passo > ULTIMO_PASSO_PRONTO" ficou permanentemente inalcançável (`lerPassoDoEndereco()` já limita `passo` a `1..TOTAL_PASSOS`) e foi apagado junto, em vez de deixado como código morto. Os campos de `components/CriteriosProspeccao.tsx` (novo) mudam conforme o modo escolhido no passo 3, sempre pré-preenchidos a partir do ICP escolhido no passo 1 (`criteriosIniciais(icp, modo, jornada)`): `empresas` (segmento/localização/porte/sinais), `pessoas` B2B (cargo/empresa ou segmento/localização), `pessoas` B2C (localização/ocupação/interesses em `CampoLista`/contexto), `empresa_unica` (só nome da empresa, o único campo sem valor do ICP — por isso é o único modo com validação de preenchimento obrigatório: `podeContinuarPasso4` exige `empresaNome` não vazio) e `oportunidades` (sinais + um "recorte" cujo rótulo já é "Segmento" ou "Localização" dependendo da jornada). **Editar um campo aqui nunca escreve de volta no ICP** — `CriteriosProspeccaoForm` só lê `icp` para o valor inicial.
  - **Sinais de intenção são chips SELECIONÁVEIS (toggle), não uma lista livre como `CampoLista`**: `SinaisSelecionaveis` (local a `CriteriosProspeccao.tsx`, não exportado) mostra todo `icp.sinais` como `<button aria-pressed>` com `.chip-positivo` quando incluído e `.chip-cinza` quando não, mais um campo "+ Adicionar" para incluir um sinal que não estava no ICP (que nasce já selecionado). Só aparece nos modos `empresas`/`oportunidades` (os únicos com o campo "sinais" na AC) — junto da chave "Incluir apenas quem tem sinais recentes (90 dias)" (checkbox simples, mesmo padrão de `OptInGuardar`/`components/ui.tsx`), que nasce **ligada** só em `oportunidades` (`criteriosIniciais`: `somenteRecentes: modo === "oportunidades"`).
  - **`CriteriosBusca`/`criteriosIniciais` NÃO persistem em lugar nenhum** (mesma decisão já tomada para `jornada`/US-010 e `modo`/US-011): a entidade `Prospeccao` só nasce na US-013, que decide como este objeto entra em `Prospeccao.criterios` (`Record<string, unknown>`, já existe em `lib/types.ts` desde a US-001). Os critérios são recalculados (nunca sobrescrevendo uma edição já feita) só quando a combinação perfil+modo muda de fato, via `chaveCriteriosRef` (`` `${icpEfetivo.id}:${modoEfetivo}` ``) — mesma técnica de guarda por `useRef` + `setTimeout(0)` já usada em `ICPForm.tsx`/US-006 para não disparar `set-state-in-effect`.
  - **O botão primário do passo 4 já diz o que a busca vai fazer** (`ROTULO_ACAO_MODO`, novo em `lib/rotulos.ts`: "Buscar empresas"/"Buscar pessoas"/"Explorar empresa"/"Buscar oportunidades") em vez de "Continuar" — mas clicar nele **não dispara nada de verdade ainda**: só mostra um `<Aviso tom="ok">Em breve: a busca com estes critérios.</Aviso>` inline (estado `buscaIniciada`, sem navegação). Criar a `Prospeccao` de fato (`POST /api/prospeccoes`, que ainda não existe) é a US-013 — ao implementá-la, o `onClick` do botão final deve TROCAR esse comportamento (POST + navegar para a tela de execução), não empilhar em cima dele.
  - Testado de ponta a ponta no `.next/standalone` isolado (conta de teste + `curl` semeando 1 produto com um ICP B2B e um ICP B2C + captura por CDP, `/tmp/shot.mjs` reconstruído mais uma vez): os 4 modos B2B (empresas/pessoas/empresa única/oportunidades) e o modo pessoas B2C, todos com os campos certos pré-preenchidos; toggle "sinais recentes" ligado por padrão só em oportunidades; botão "Explorar empresa" desabilitado com o nome vazio e habilitado ao digitar; `?passo=4` direto na URL (sem passar pelos passos 1-3) mostra o aviso "Escolha o produto, o perfil e o tipo de busca..." em vez de quebrar. Medido com `getBoundingClientRect`: botão "Buscar empresas" (o modo com mais campos) com `bottom: 796px` em 1400×900. Desktop e celular (390px), zero erro de console, zero rolagem horizontal em todas as capturas.
- **(US-011) O passo 3 do assistente ("Como encontrar oportunidades") ganhou conteúdo real em `components/ProspeccaoNova.tsx`** (`ULTIMO_PASSO_PRONTO` subiu de 2 para 3): cartões grandes do tipo de busca (`ModoProspeccao`, já existia em `lib/types.ts` desde a US-001 — nada novo no tipo), mesmo padrão de acessibilidade/estilo dos cartões da jornada (US-010): `role="radiogroup"`/`role="radio"`, `.card` com `border-accent bg-accent-soft` quando selecionado. `MODOS_POR_JORNADA`/`ROTULO_MODO`/`DESCRICAO_MODO` (novos em `lib/rotulos.ts`) decidem QUANTOS cartões aparecem e com que texto: 4 em B2B (`empresas`/`pessoas`/`empresa_unica`/`oportunidades`), 2 em B2C (só `pessoas`/`oportunidades`, com descrição própria da jornada — os mesmos dois valores do `ModoProspeccao` têm textos DIFERENTES por jornada, por isso `DESCRICAO_MODO` é `Record<Jornada, Partial<Record<ModoProspeccao, string>>>`, não um `Record<ModoProspeccao, string>` simples).
  - **Ao contrário da jornada (US-010), o modo NÃO tem pré-seleção**: a AC pede seleção obrigatória, e não há de onde derivar um padrão (a jornada vem do ICP; o modo não vem de lugar nenhum). `modoEfetivo` existe mesmo assim (`modo && modosDisponiveis.includes(modo) ? modo : null`) só para invalidar em silêncio uma escolha que deixou de existir (ex.: escolheu "Explorar uma empresa" em B2B, voltou ao passo 1, trocou para um ICP B2C) — sem isso "Continuar" ficaria habilitado com um modo que sumiu da tela.
  - **`modo` (escolha do passo 3) também NÃO ganhou coluna em `prospeccoes`/campo em `NovaProspeccao`**, pelo mesmo motivo já registrado para `jornada` na US-010: a entidade só nasce de verdade na US-013. Vive em `useState` deste componente; a US-013 decide como produto/ICP/jornada/modo entram em `NovaProspeccao` juntos.
  - Testado de ponta a ponta no `.next/standalone` isolado (conta de teste + `curl` semeando 1 produto com um ICP B2B e um ICP B2C + captura por CDP, `/tmp/shot.mjs` reconstruído mais uma vez — não sobrevive entre sessões): navegar direto para `?passo=3&produtoId=...&icpId=...` (ICP B2B) mostra os 4 cartões, nenhum selecionado, "Continuar" desabilitado; clicar em "Encontrar oportunidades" via `Runtime.evaluate` habilita "Continuar" e destaca o cartão; o mesmo endereço com o ICP B2C mostra só 2 cartões com os textos próprios da jornada. Desktop 1400×900 e celular 390×900 nos dois casos, zero erro de console, zero rolagem horizontal (`scrollWidth === clientWidth` nas quatro capturas).
- **(US-010) O passo 2 do assistente ("Quem você quer encontrar") ganhou conteúdo de verdade em `components/ProspeccaoNova.tsx`** (`ULTIMO_PASSO_PRONTO` subiu de 1 para 2): dois cartões grandes (`role="radiogroup"`/`role="radio"`, mesmo padrão de acessibilidade do seletor compacto de `ICPForm.tsx`, mas em cartões — `.card` com `border-accent bg-accent-soft` quando selecionado), usando `ROTULO_JORNADA`/`DESCRICAO_JORNADA` (`lib/rotulos.ts`, a segunda é nova nesta história). **`jornadaEfetiva` é DERIVADA no render** (`jornada ?? icpEfetivo?.jornada ?? null`), mesmo padrão já usado para `icpEfetivo` (US-009) — nunca sincronizada por `useEffect`. Trocar a jornada aqui mostra `Aviso tom="warn"` com `acao.onClick` (não `url`, já que é troca de passo do wizard, não navegação) voltando ao passo 1 ("Escolher outro perfil"), sempre que `jornadaEfetiva !== icpEfetivo.jornada`.
  - **`jornada` (escolha do passo 2) NÃO ganhou coluna em `prospeccoes`** nem campo no tipo `Prospeccao`/`NovaProspeccao`: como a entidade `Prospeccao` só nasce de verdade na US-013 (`estado: "executando"`), gravar a jornada num registro que ainda não existe inventaria a persistência daquela história antes da hora — mesma leitura já registrada pela US-009 para produto/ICP. A escolha vive só em `useState` deste componente (memória, some ao recarregar a página no passo ≥ 2, limitação já documentada); a US-013 é quem deve decidir como `jornada` entra em `NovaProspeccao` quando a prospecção for criada de verdade.
  - **Botão "Voltar" nasceu genérico para qualquer passo ≥ 2** (`irParaPasso(passo - 1)`, ao lado de "Continuar", que também passou a ser genérico: `irParaPasso(passo + 1)`) — antes "Continuar" ia direto para `irParaPasso(2)` (hardcoded) e só a casca "Em breve" tinha "Voltar". Qualquer passo novo (US-011, US-012) que seguir esse layout ganha os dois botões de graça, sem repetir a lógica.
  - **`SUBTITULO_PASSO` (`Partial<Record<number, string>>`) substitui o texto fixo "Produto e perfil ideal"** da linha de apoio: só os passos já implementados (1 e 2) têm título — passo 3/4 (ainda cascas) mostram só "Passo X de 4", sem inventar o título das histórias futuras (US-011/012 devem completar esse `Record` ao nascer, não criar um mecanismo novo).
  - A casca "Em breve" (passo > `ULTIMO_PASSO_PRONTO`) passou a mostrar a jornada escolhida na linha de resumo (`· Empresas e decisores`) ao lado de produto/perfil — só quando `jornadaEfetiva` existe (sempre existe, a menos que produto/ICP nem tenham sido escolhidos ainda).
  - Testado de ponta a ponta no `.next/standalone` isolado (conta de teste + `curl` para semear 1 produto com um ICP B2B e depois um segundo ICP B2C + captura por CDP, `/tmp/shot.mjs` reconstruído): passo 1 → passo 2 com "Empresas e decisores" pré-selecionado (ICP B2B) → trocar para "Pessoas/consumidores" mostra o aviso + "Escolher outro perfil" volta ao passo 1 → escolher o ICP B2C no passo 1 e voltar ao passo 2 pré-seleciona "Pessoas/consumidores" sem aviso → avançar ao passo 3 (casca) mostra a jornada no resumo. Desktop 1400×900 e celular 390×800, zero erro de console, zero rolagem horizontal.
- **(US-009) O assistente de nova prospecção mora em `/prospeccoes/nova`** (renomeado de `/prospeccoes/novo`, a casca da US-003 — todos os links foram atualizados; a rota antiga não tem redirect porque só existia há duas histórias e nenhum link externo aponta para ela) e é um wizard de 4 passos (`components/ProspeccaoNova.tsx`). Só o **passo 1** (produto + perfil ideal) está implementado; passo ≥ 2 mostra um resumo do que foi escolhido + "Em breve", mesmo padrão de casca já usado em outras telas. **O estado do wizard (produto/ICP escolhidos) vive só em memória (`useState`) — não há `sessionStorage` nem criação antecipada da `Prospeccao`** (essa entidade só nasce na US-013, com `estado: "executando"`); recarregar a página no passo ≥ 2 perde a escolha. Cada história seguinte (US-010 a US-013) decide se isso precisa mudar; não antecipei essa solução.
  - **O passo atual mora na barra de endereço (`?passo=1..4`)**, no mesmo par `history.pushState`/`popstate` já documentado no Codebase Patterns raiz (nunca `useSearchParams`) — `lerPassoDoEndereco()` cai em 1 para qualquer valor fora de 1..4. `produtoId`/`icpId` também chegam por querystring (lidos uma vez na abertura, não resscritos a cada troca de passo).
  - **`GET /api/produtos` (US-004, já embute os `icps` de cada produto) é a única consulta desta tela** — nenhuma chamada por produto. O ICP "padrão" pré-selecionado é o mesmo critério já usado por `ProdutoEditar.tsx` (US-006): o mais antigo (`criadoEm` mínimo) entre os do produto. `icpEfetivo` é **derivado no render** (`icpsDoProduto.find(id) ?? padraoIcp`), não sincronizado por `useEffect` — evita o efeito colateral de escrever `icpId` toda vez que `produtos`/`produtoId` mudam.
  - **"Sem nenhum produto cadastrado, o passo 1 é o cadastro" (AC) embute `<ProdutoForm aoSalvar={...} />` diretamente na tela** (novo prop opcional em `ProdutoForm.tsx`, só usado aqui — sem ele o formulário continua redirecionando para `/produtos` como antes). O `aoSalvar` recebe o produto criado, atualiza `produtos`/`produtoId` local (sem um novo `GET`) e a tela continua no passo 1, agora mostrando o aviso "sem perfil ideal ainda" — é assim que "o fluxo continua de onde parou" acontece sem inventar uma segunda tela.
  - **`?voltar=<rota>` é o mecanismo geral de "volta para o fluxo" desta história**, somado a `ProdutoForm.tsx`, `ProdutoComIA.tsx` e `ICPForm.tsx`: ao salvar com sucesso, em vez do destino padrão (`/produtos`, `/produtos/[id]`), redireciona para `<voltar>?produtoId=<id>` (ou `&icpId=<id>` também, em `ProdutoComIA`/`ICPForm`). "Cancelar" e "Preencher manualmente" (dentro de `ProdutoComIA`) também propagam o mesmo `voltar`, lido por um `useEffect(setTimeout(0))` (padrão de `components/setup.tsx`, nunca direto no corpo do efeito). Isso é o que faz "Criar perfil ideal" (produto sem ICP) e "Criar novo com IA" (produto novo com perfil, via US-007) devolverem a pessoa ao passo 1 já com a escolha pronta, em vez de largá-la em `/produtos`. Qualquer história futura que precise de "criar X e voltar para onde eu estava" deve seguir o mesmo parâmetro, não inventar um segundo mecanismo.
  - Testado de ponta a ponta no `.next/standalone` (conta de teste + `curl` + captura por CDP, `/tmp/shot.mjs`): sem produto (cadastro embutido) → sem ICP (aviso + "Criar perfil ideal", round trip completo) → um produto com um ICP (padrão pré-selecionado, "Continuar" habilita) → dois produtos (seletor com descrição) → um produto com dois ICPs ("Usar perfil existente" troca o cartão de resumo) → "Criar novo com IA" (round trip completo, produto+ICP novos pré-selecionados ao voltar) → avançar para o passo 2 (resumo + "Em breve") → botão voltar do navegador (volta ao passo 1 com a mesma seleção). Desktop 1400×900 e celular 390×900, zero erro de console, zero rolagem horizontal em todas.
- **(US-008) Demonstração completa do workspace: `lib/demo.ts:criarProspeccaoExemplo()` (da US-003) foi ESTENDIDA, não duplicada.** As seis tabelas de `lib/workspace.ts` ganharam a coluna `demo` (migração `ALTER TABLE ... DEFAULT 0`, mesmo padrão já usado para `apagado_em`); os seis tipos `Novo*` de `lib/types.ts` ganharam `demo?: boolean` (opcional, default `false` dentro de cada `criar*` — nenhum chamador existente precisou mudar) e os seis tipos de leitura ganharam `demo: boolean` (sempre presente). Toda entidade semeada por `criarProspeccaoExemplo()` (produto, ICP, prospecção, as 3 contas, os 6 leads, a abordagem) nasce com `demo: true`.
  - **`limparProspeccaoExemplo()` (nova, `lib/demo.ts`)**: lê `PROSPECCAO_EXEMPLO_ID` (a mesma chave que já controlava "não duplicar"), chama `apagarProspeccao` (cascata: contas/leads/abordagens) + `apagarICP` (hard delete, já existia) + `apagarProduto` (soft delete, mesmo comportamento de "Apagar produto" na tela) só nos TRÊS ids gravados por aquela chave, e por fim limpa a própria chave (`setConfig(chave, "")`, que já significa "remover" em `lib/store.ts`). Nunca varre a tabela procurando `demo = 1`: só mexe no que ela mesma criou, então um produto real cujo nome coincida com o do exemplo (improvável, mas possível) não é tocado.
  - **`GET /api/inicio` ganhou `temExemplo: boolean`** (`prospeccoes.some(p => p.demo)`) — é o que a tela usa para decidir se mostra "Limpar exemplo"; não depende de ler a config diretamente, então continua correto mesmo se a chave e os dados algum dia divergirem.
  - **`DELETE /api/inicio/exemplo` (novo, ao lado do `POST` já existente da US-003)** chama `limparProspeccaoExemplo()` e devolve `{ ok: true }` (não há "o registro" para devolver — é uma limpeza de várias entidades, não a atualização de uma).
  - **`?exemplo=1`/`?captura=1` em `components/Inicio.tsx`**: `?exemplo=1` é o MESMO caminho do botão "Ver uma prospecção de exemplo" (`verExemplo`, agora em `useCallback` porque o efeito do atalho também o chama), com a guarda `useRef` (`autoIniciado`) e **sem** `return () => clearTimeout(...)` no `useEffect` (PADRAO.md: o Strict Mode de `next dev` mataria o atalho só em desenvolvimento). `?captura=1` **não precisou de nenhum código novo**: é a infra já pronta e compartilhada `useScrollToResult`/`podeRolarAutomaticamente` de `components/ui.tsx` (não pode ser alterado) — só precisei dar `id="stage"` à seção "Prospecções recentes" e chamar `useScrollToResult(exemploCriado)`, onde `exemploCriado` vira `true` só depois que o exemplo é semeado (nunca num carregamento normal da página, para não rolar a tela sozinha para quem já tem dados de verdade).
  - **Chip "Exemplo" (`Chip nivel="neutral"`, texto fixo)** ao lado do nome em `Inicio.tsx` (prospecção recente) e `Produtos.tsx` (card do produto) — as únicas duas telas que já existem e listam entidades que podem ser `demo`. `Prospeccoes.tsx`/`Leads.tsx` ainda são cascas "Em breve" (US-035/036): quando essas histórias nascerem, mostrar o mesmo chip nas contas/leads/abordagens (`Conta.demo`/`LeadProspeccao.demo`/`AbordagemRegistro.demo` já existem no tipo, só falta a tela).
  - **"Limpar exemplo" usa `useConfirmacao`** (mesmo diálogo de "Apagar produto"), fica ao lado de "Ver todas" no cabeçalho de "Prospecções recentes", e só aparece quando `resumo.temExemplo` é `true`.
- **(US-007) `/produtos/novo?ia=1` abre `components/ProdutoComIA.tsx`** em vez de `ProdutoForm` (escolha feita em `ProdutoNovo.tsx`, lendo `?ia=1` com o padrão já usado por `components/setup.tsx` — `new URLSearchParams(location.search)` dentro de `useEffect(() => setTimeout(..., 0))`, nunca `useSearchParams()`). Fluxo de dois passos, tudo no mesmo componente: "entrada" (textarea única — aceita endereço de site OU parágrafo colado) → `POST /api/produtos/analisar` (`lib/produto-ia.ts:sugerirProdutoDoSite`) → "editar" (formulário com os campos de `ProdutoForm` MAIS os do ICP — nome do perfil, setor/porte/localização, personas/dores/sinais em chips — tudo editável, nunca salvo com a resposta da IA). Salvar é sequencial: `POST /api/produtos` e só depois `POST /api/icps` com o `produtoId` da resposta (dois `fetch`, sem rota combinada nova) — jornada sempre `"b2b"` (site de empresa), sem seletor.
  - **`lib/produto-ia.ts` (novo) NÃO usa `lib/descoberta.ts`** porque essa camada só nasce na US-015: por ora ele tem sua PRÓPRIA cópia pequena do fetch à Bright Data (`lerPaginaSimples`), no mesmo padrão já usado por `lib/abordagem.ts:enriquecerSite` — é duplicação deliberada e temporária; a US-015 deve apagar as duas e passar os dois arquivos a usar `lerPagina` de `lib/descoberta.ts`. Entrada que "parece endereço" (regex simples: começa com `http`/`https`, ou não tem espaço e tem um `.`) tenta ler a página quando `BRIGHTDATA_API_KEY`+`BRIGHTDATA_ZONE` estão configurados; sem eles, ou se a leitura falhar, a análise segue só com o texto digitado e a tela mostra um `Aviso tom="warn"` (`avisoLeitura`) — nunca trava o fluxo.
  - **Sem `OPENROUTER_API_KEY`, a sugestão é sempre o mesmo exemplo fixo do CMMS da Zetta Manutenção Industrial** (`SUGESTAO_DEMO` em `lib/produto-ia.ts`, os mesmos textos/critérios de `lib/demo.ts:criarProspeccaoExemplo`, mas como literal solto — não chama a função que persiste, porque aqui é só uma sugestão a editar). `Origem` (`components/ui.tsx`, compartilhado) é reaproveitada com `insumo: "seu site"` (frase monta "a partir de seu site" — funciona sem artigo porque é possessivo, ao contrário de um substantivo comum) e `demoTexto` fixo apontando o exemplo.
  - **`components/CampoLista.tsx` (novo) foi extraído de `ICPForm.tsx`** (antes local, não exportado) para ser reaproveitado aqui: qualquer novo campo de chips (personas/dores/sinais/interesses) deve importar dali, nunca duplicar a lógica de novo.
- **(US-006) `components/ICPForm.tsx` (novo) é o formulário compartilhado por `/produtos/[produtoId]/icps/novo` e `/produtos/[produtoId]/icps/[icpId]`**, mesmo par `ICPNovo.tsx`/`ICPEditar.tsx` embrulhando com `Topbar`+título que `ProdutoNovo`/`ProdutoEditar` (US-005). `app/api/icps/route.ts` (`POST`) e `app/api/icps/[id]/route.ts` (`GET`/`PUT`) validam nome (obrigatório) e jornada (union: ausente cai em `"b2b"`, valor desconhecido é 400 — mesma regra do Codebase Patterns raiz); a leitura de `criterios`/`personas`/`dores`/`sinais` do corpo é compartilhada pelas duas rotas em `app/api/icps/comum.ts` (`lerCriterios`, `lerLista`), padrão "arquivo comum ao lado das rotas que mexem no mesmo registro".
  - **`GET /api/produtos/[id]` passou a embutir `icps: listarICPs(id)`** na resposta (mesmo padrão de `GET /api/produtos`, que já embutia os ICPs de cada card): `ProdutoForm` ignora a chave extra (só lê `nome`/`descricao`/`site`/`propostaValor`), e `ProdutoEditar.tsx` faz sua PRÓPRIA chamada a essa rota (independente da que `ProdutoForm` já faz por dentro) só para listar os ICPs do produto abaixo do formulário — duas chamadas ao mesmo endpoint por tela, aceitas aqui porque são dois componentes com responsabilidades diferentes (editar o produto vs. listar seus perfis) que não compartilham estado.
  - **"Padrão" não é uma coluna nova em `icps`**: como a AC só pede que a lista "mostre qual é o padrão" (não pede uma escolha explícita da pessoa), o padrão é calculado na LEITURA como o ICP mais antigo (`criadoEm` mínimo) entre os do produto — `ProdutoEditar.tsx` faz isso com `reduce`. Se uma história futura (US-009, que já prevê "o padrão, quando só há um") precisar de uma escolha explícita, isso vira uma coluna `padrao` de verdade; até lá, não inventar UI para escolher padrão que a AC não pediu.
  - **Personas, dores, sinais e (em B2C) interesses são chips adicionados um a um**, nunca textarea livre: o widget (`CampoLista`, local a `ICPForm.tsx`, não exportado — só este arquivo tem 4 campos assim) junta um `input` (Enter ou "Adicionar" inserem) com os chips já adicionados, cada um usando o `Chip nivel="neutral"` de `components/ui.tsx` com um `×` dentro do próprio `children` (o `Chip` aceita `children` livre, não precisou de prop nova). Duplicado (mesmo texto já na lista) é ignorado em silêncio, não vira erro.
  - **Critérios de B2B e B2C são o MESMO objeto `CriteriosICP`** (já definido na US-001, com os dois conjuntos de campos convivendo): trocar a jornada no formulário só troca quais campos aparecem, nunca limpa os valores do lado que sumiu — se a pessoa for e voltar, o que já tinha digitado continua lá. O seletor de jornada é o mesmo padrão `role="radiogroup"` + botões de `CampoSetup` (`components/setup.tsx`), reimplementado local a `ICPForm.tsx` (não extraído para `ui.tsx`, que não muda) porque aqui são só 2 opções fixas, sem a lógica de `LIMITE_BOTOES`/`select` de `setup.tsx`.
  - O aviso de dado público/sem categoria sensível (AC "a tela avisa, em uma linha, que em B2C só entram dados públicos") é um `Aviso tom="warn"` normal (mesmo padrão de nota estática não-erro já usado em `components/Rotinas.tsx`), mostrado só quando `jornada === "b2c"`.
- **(US-005) `components/ProdutoForm.tsx` (novo) é o formulário compartilhado por `/produtos/novo` e `/produtos/[id]`**: sem `produtoId` cria (`POST /api/produtos`), com `produtoId` busca (`GET /api/produtos/[id]`) e edita (`PUT /api/produtos/[id]`) — `ProdutoNovo.tsx`/`ProdutoEditar.tsx` (que **substituíram** as cascas "Em breve" da US-004, como o CLAUDE.md já pedia) só desenham `Topbar`+título e embrulham o formulário. Validação de nome/proposta obrigatórios é IGUAL nas duas rotas (`POST`/`PUT`), sempre `{ error: "..." }` (o `codigo`/`acao` opcionais do envelope `lerErro`/`ErroLido` não fazem sentido aqui — não há ação a oferecer para "preencha o campo", a pessoa já está no formulário). Descrição e site continuam opcionais, só nome e proposta são obrigatórios, dos dois lados (`required` no HTML e a mesma checagem no servidor). `app/produtos/[id]/page.tsx` virou `async`, lendo `PageProps<"/produtos/[id]">` e passando `produtoId` como prop — `ProdutoEditar` não usa `useParams()` para isso.
  - **"Produto no topo" depois de salvar (AC da US-005) não ganhou coluna `atualizado_em`**: `listarProdutos()` continua `ORDER BY criado_em DESC` (igual à US-004), porque `contas`/`leads` já estabelecem o precedente de que `atualizado_em` existe só para retenção (`limparExpirados`), nunca para reordenar lista (`listarContas`/`listarLeads` também ordenam por `criado_em`). Um produto recém-criado já nasce no topo por essa ordenação; editar um produto antigo não o move — leitura deliberada da AC (documentar em vez de inventar comportamento que nenhuma outra entidade do app tem), não uma omissão.
  - Testado de ponta a ponta com `.next/standalone` + conta de teste + `curl` (criação válida, nome vazio, proposta vazia, `GET`/`PUT` de um id existente e de um inexistente, todos 400/404 com frase de negócio) + captura por CDP (`/tmp/shot.mjs`) em `/produtos/novo` e `/produtos/[id]` (pré-preenchido), desktop 1400×900 e celular 390 px: zero erro de console, zero rolagem horizontal, campo pré-preenchido corretamente no editar. O banner de erro (`Aviso tom="danger"`) foi conferido forçando a validação do servidor via JS injetado no `Runtime.evaluate` do script de captura (setar o campo e chamar `form.requestSubmit()` no mesmo `Runtime.evaluate` síncrono lê o estado React de ANTES do `onChange` aplicar — é um artefato do script de teste, não bug do app; um clique real do usuário não tem esse problema porque o campo já foi digitado em interações anteriores, com o React já re-renderizado).
- **(US-004) `/produtos` lista os produtos em cards (`components/Produtos.tsx`, `GET /api/produtos`, que já embute os ICPs de cada produto — `listarICPs()` sem filtro + `.filter` em memória, para não fazer uma chamada por card).** Apagar um produto (`DELETE /api/produtos/[id]`) é **soft delete**: `lib/workspace.ts` ganhou a coluna `apagado_em` (migração `ALTER TABLE` num `try/catch`, mesmo padrão já usado em `pdi-time` para `historico.resumo`) e `apagarProduto` só grava essa coluna, nunca `DELETE FROM produtos`. `listarProdutos()` filtra `apagado_em IS NULL` (não aparece mais em `/produtos`), mas `obterProduto(id)` continua sem filtro — é por isso que `app/api/inicio/route.ts` resolve o nome do produto de cada prospecção recente com `obterProduto(p.produtoId)` (chamada por item, só 3 no máximo) em vez de um `Map` construído a partir de `listarProdutos()`: o `Map` excluiria produtos apagados e o nome cairia para o fallback genérico "Produto", quebrando a promessa do diálogo de confirmação ("as prospecções existentes ficam, com o nome do produto preservado"). Nenhuma cascata: ICPs e prospecções do produto apagado continuam intactos, só o produto sai da lista ativa.
  - `lib/rotulos.ts` (novo, próprio deste app, sem `node:*`): `ROTULO_JORNADA: Record<Jornada, string>` (`"Empresas e decisores"`/`"Pessoas/consumidores"`, os mesmos textos que a US-006/US-010 vão usar) — primeiro arquivo desse tipo aqui, mesmo padrão já registrado no Codebase Patterns raiz (Record completo em arquivo client-safe, nunca um ternário na tela).
  - **`/produtos/novo` e `/produtos/[id]` nasceram como cascas mínimas** (`components/ProdutoNovo.tsx`/`ProdutoEditar.tsx`, mesmo texto "Em breve" das cascas da US-002), só para as ações "Criar produto"/"Criar com IA a partir do meu site" (estado vazio) e "Editar" (cada card) terem destino em vez de 404 — o formulário de verdade nasce na US-005 (cadastro) e ganha o caminho de IA na US-007. Ao implementar essas histórias, **substituir** o conteúdo dessas cascas, não criar um segundo par de arquivos.
  - Ação "Apagar" de um card usa `text-danger` **depois** de `btn-link` na mesma `className` (`btn-link text-[13px] text-danger`) — mesmo padrão já usado em `pdi-time/components/Rotinas.tsx`; a ordem das classes no JSX não importa aqui porque `text-danger` é gerado na camada `utilities` do Tailwind 4, que vence sobre a cor de `.btn-link` (`@layer components`) na cascata, independente de qual delas aparece primeiro na string.
- **(US-003) A tela única antiga acabou em `/`: `app/page.tsx` agora só embrulha `components/Inicio.tsx`** (Hero com "Nova prospecção" + quatro números + as três prospecções mais recentes, tudo vindo de `GET /api/inicio`, que agrega `lib/workspace.ts` — nenhuma tela calcula nada sozinha). O antigo `export default function Page()` (formulário de busca + `Previa`/`CartaoEntrada`/`Passos`) foi apagado por não ter mais chamador nenhum; `Resultado`/`ConteudoLeads` (e as funções que só elas usam: `OrigemLeads`, `porteCurto`, `resumoBusca`, `leadsParaTexto`, `exportarCSV`, `chavePerfil`, `ReceberLeadsSemanais`, `AbordagemSalva`) continuam em `app/page.tsx` porque `app/r/[id]/page.tsx` e `app/imprimir/[id]/page.tsx` importam os dois de lá (`from "../../page"`) — não mover para `components/` só por causa desta história. `AbordagemView` foi removida (função órfã, nenhuma tela mais chama `onEscrever` para chegar até ela); `Resultado`/`ConteudoLeads` continuam aceitando `onEscrever`/`onEnviarCRM` como props opcionais para quando uma tela de leads voltar a oferecê-las (Fase 3/4), mas hoje nenhum caminho os passa — prop opcional não usada não é código morto para o `tsc`/`eslint`, só uma função inteira sem chamador é.
- **(US-003) "Nova prospecção" aponta para `/prospeccoes/novo`, casca mínima nova** (`components/ProspeccaoNova.tsx`, mesmo padrão "Em breve" de `Produtos`/`Prospeccoes`/`Leads` da US-002) — o passo 1 de verdade nasce na US-009. "Ver uma prospecção de exemplo" (estado vazio do Início) chama `POST /api/inicio/exemplo` → `lib/demo.ts:criarProspeccaoExemplo()`, que semeia 1 produto + 1 ICP B2B + 3 contas + 6 leads + 1 abordagem (mesmo exemplo CMMS da Zetta Manutenção Industrial já usado pela busca antiga) direto em `lib/workspace.ts`, uma única vez (chave `PROSPECCAO_EXEMPLO_ID` em `getConfig`/`setConfig`, mesmo padrão de "chave de controle" do Codebase Patterns raiz). **Isto NÃO é a US-008 completa**: falta a coluna/campo `demo` nas seis tabelas (para marcar `demo: true` em toda tela onde a prospecção aparecer) e o botão "Limpar exemplo" — a US-008 deve estender `criarProspeccaoExemplo()`, não recriar um segundo semeador.
- (US-002) **Cinco destinos no header, exceção ao PADRAO.md** (ver `tasks/prd-prospeccao-workspace.md`, P4/US-041): `lib/navegacao-prospeccao.ts` (próprio deste app, não comparado por `verificar-padrao.sh`) exporta `NAVEGACAO_PROSPECCAO` (Início, Produtos, Prospecções, Leads, Configurações) e é passado via `navegacao={NAVEGACAO_PROSPECCAO}` a todo `<Topbar>` que este app controla (`app/page.tsx`, `app/r/[id]/page.tsx` e as três telas novas). `lib/navegacao.ts` e `components/ui.tsx` continuam idênticos ao `pdi-time` — o destaque do item ativo em subrotas (`ativo()` em `Topbar`, `pathname.startsWith(href + "/")`) já existia e não precisou mudar. `/setup` é a única exceção: nasce de `components/setup.tsx` (compartilhado, não alterado) e continua com o `Topbar` de três destinos padrão (Início/Histórico/Configurações), como a PRD define ("Configurações: `/setup` como já é hoje") — o link a `/historico` que aparece só ali funciona porque a rota virou redirect, não porque foi atualizado.
- (US-002) `app/historico/page.tsx` virou um redirect (`redirect("/prospeccoes")`, Server Component) — a área "Prospecções" ainda não existe (chega na US-035), mas nenhum link do app deve mais apontar para `/historico` diretamente (só `app/page.tsx`, que agora aponta para `/prospeccoes`). `app/api/historico/route.ts` e `lib/historico.ts` continuam existindo e em uso por `/r/[id]` e `/imprimir/[id]`; não removidos por não serem lidos mais só pela tela aposentada.
- (US-002) `app/produtos`, `app/prospeccoes` e `app/leads` nasceram como cascas mínimas (`components/Produtos.tsx`/`Prospeccoes.tsx`/`Leads.tsx`, só `Topbar` + título + uma linha "Em breve") só para o header ter onde navegar; o conteúdo de verdade de cada uma chega nas histórias próprias (US-004, US-035, US-036) — não antecipar campos/ações que essas histórias ainda vão desenhar.
- (US-002) Testado com `.next/standalone`: **o servidor real fica em `.next/standalone/server.js`** (não em `.next/standalone/<nome-do-app>/server.js`) e só serve CSS/JS se `.next/static` for copiado para `.next/standalone/.next/static` além de `public/` para `.next/standalone/public` — sem o primeiro passo a página carrega sem nenhum estilo (HTML "cru") e pode dar falso positivo de rolagem horizontal (o texto sem Tailwind estoura a largura). Confirme sempre a captura com `Runtime.evaluate` medindo `scrollWidth`/`clientWidth` DEPOIS de conferir visualmente que o CSS carregou.
- `components/ui.tsx`/`lib/ai.ts`/`lib/formato.ts`/`lib/sensivel.ts`/`lib/historico.ts`/`instrumentation.ts`/`components/setup.tsx`/`lib/setup-comum.ts` são cópias literais de `pdi-time`. Só `app/r/[id]/page.tsx`, `app/imprimir/[id]/page.tsx` e os `not-found.tsx` são próprios daqui, porque dependem do `Resultado`/tipo salvo neste app (`tipo: "leads"`, `entrada = DadosBusca`, `saida = ResultadoBusca { fonte, leads }`).
- `SENSIVEL = false` (prospecção não está na lista de apps sensíveis do PRD — só Contratos e Financeiro), então `POST /api/leads` sempre salva no histórico, sem opt-in.
- Só a **busca de leads** (`POST /api/leads`) gera `meta`+`id` e salva no histórico; é o único "Resultado" com link permanente (`/r/[id]`, `/imprimir/[id]`) e com `Entregar` (que traz o extra "Exportar CSV", já que CSV é sobre a lista inteira, não sobre um lead). A **abordagem por lead** (`POST /api/abordagem`) também devolve `meta` (por isso `AbordagemView` tem sua própria `Origem`), mas não é persistida separadamente — evita duas "fontes de verdade" de resultado dentro do mesmo fluxo.
- `Resultado`/`ConteudoLeads` (`app/page.tsx`) aceitam `onEscrever` como prop **opcional**: quando vem de um Server Component (`app/r/[id]/page.tsx`), não dá pra passar uma função (Server Component não serializa handlers para Client Component), então a coluna de ação "Escrever abordagem" simplesmente não é adicionada ao array de colunas nesse caso.
- **Limitação conhecida, investigada e não resolvida** ao gerar o PDF de `/imprimir/[id]` com a tabela de 5 leads: `chromium --print-to-pdf` pagina 1 linha por página quase inteira em branco, em vez de várias linhas por página A4. Não é causada por `break-inside` (testado com `auto !important` em `.card` inteiro e o resultado do PDF não mudou nem um byte). Ver detalhes da investigação em `pdi-time/CLAUDE.md`. Tratar como problema em aberto da fundação compartilhada, não específico deste app.
- (US-038) `lib/demo.ts`/`app/api/abordagem/route.ts`: "Seu nome"/"Sua empresa" (campos em `MaisDetalhes` de `DadosBusca`) seguem o mesmo padrão "auto-lembrado" de `NOME_USUARIO` em `pdi-time` (chaves `REMETENTE_NOME`/`REMETENTE_EMPRESA` em `lib/store.ts`, gravadas por `POST /api/abordagem` quando preenchidas, lidas por `GET /api/leads` para pré-preencher o formulário). A abordagem de demo (`abordagemDemo`) monta a assinatura a partir desses dois campos com fallback "Equipe comercial" quando nenhum foi informado — nunca deixa um marcador tipo `[seu nome]` na saída. `Abordagem.gancho` (o `.summary` da tela) é só o gancho do e-mail; LinkedIn e WhatsApp têm frases de abertura próprias (`ganchoLinkedin`/`ganchoWhatsapp` dentro de `abordagemDemo`), para não repetir a mesma frase nos três canais — mesma regra reforçada no `SYSTEM_ABORDAGEM` para o caminho real de IA.
- (US-049) `lib/leads.ts` (`buscarLeads`) e `lib/abordagem.ts` (`escreverAbordagem`) concentram toda a lógica que antes vivia dentro de `app/api/leads/route.ts`/`app/api/abordagem/route.ts` (Apollo real + `leadsDemo`, enriquecimento via Bright Data + `abordagemDemo`); as rotas HTTP só validam entrada, chamam essas funções e (no caso de leads) salvam no histórico. `lib/ferramentas.ts` (MCP) chama exatamente as mesmas funções — nunca duplicar prompt/lógica de busca ao mexer em qualquer uma das duas pontas. `lib/leads.ts` exporta `ErroApollo`, lançada só quando a Apollo responde com erro (desde a US-037 ela carrega `codigo`/`status`/`acao`, como `ErroIA`, e quem monta a resposta HTTP é `app/api/erros.ts`).
- (US-038) `ConteudoLeads`/`Resultado` (`app/page.tsx`) ganharam seleção múltipla (checkbox por linha, dentro da própria célula "Nome" porque só há um slot `papel: "chip"` no cartão mobile) e um cache `abordagens: Record<leadId, {abordagem, meta}>` mantido em `Page` (fora do union `Estado`, para sobreviver a trocas de fase): ao voltar para a fase "lista", os chips "Abordagem pronta" continuam visíveis porque vêm desse cache, não do estado da tela. "Escrever para os selecionados" reaproveita a mesma função de rede (`gerarAbordagemParaLead`) usada pelo clique individual, processando um lead por vez (sequencial, não paralelo) para não sobrecarregar a IA/demo.
- (US-069) Primeira rotina de verdade deste app (`leads-semanais`, "Leads novos toda semana"): precisou trazer `components/Rotinas.tsx` (copiado sem alterar de `pdi-time`) e adicionar `NOTIFICACOES` a `lib/integracoes.ts` (nenhum dos dois existia aqui ainda — nenhuma história anterior tinha registrado uma rotina neste app). Também precisou atualizar `lib/formato.ts` para a versão atual de `pdi-time` (tinha ficado para trás, sem o parâmetro `comAno` de `data()`, usado por `Rotinas.tsx`); `lib/formato.ts` não está na lista `ARQUIVOS` de `scripts/verificar-padrao.sh`, então diverge silenciosamente entre apps sem o script acusar — vale conferir manualmente contra `pdi-time` ao trazer qualquer componente que dependa dele para um app que ainda não o tinha atualizado.
- (US-069) O botão "Receber leads novos toda semana" fica dentro do próprio `Resultado`/`ConteudoLeads` (componente `ReceberLeadsSemanais`, auto-contido: busca `/api/setup` e `/api/rotinas` sozinho), não em `Page` como o "Receber resumo..." de `agente-kanban`/`financas-ia` — porque o "perfil buscado" (`dados: DadosBusca`) que vira `parametros` da rotina só existe no resultado de uma busca específica. Como `Resultado` já é renderizado tanto pela tela principal (client) quanto por `app/r/[id]/page.tsx` (Server Component, sem callbacks), `ReceberLeadsSemanais` funciona idêntico nos dois lugares — ele não recebe nenhuma função como prop, só o `dados` (serializável), e busca seu próprio estado via `fetch` no `useEffect`, o mesmo truque já usado por `AutomatizarProximosMeses` em `financas-ia`.
- (US-069) `app/api/rotinas/route.ts` (compartilhado, `ARQUIVOS` de `verificar-padrao.sh`) ganhou um campo `parametros` opcional no corpo do `POST` (antes sempre gravava `{}`): mudança genérica e mínima, replicada idêntica nos 10 apps na mesma história (mesmo critério já usado para o `id={i.id}` de `CartaoIntegracao`, US-036). Isso permite qualquer rotina futura (ex.: US-070/076) carregar dados próprios sem precisar de uma rota HTTP própria fora do padrão compartilhado. Quem cria uma rotina com `parametros` sempre faz isso por um botão próprio do app (nunca pelo formulário genérico de `/setup`, que não sabe quais campos pedir) — o executor (`lib/rotinas-do-app.ts`) deve validar os campos esperados e devolver um texto amigável em vez de quebrar quando `parametros` vier vazio (caso alguém crie o tipo pelo formulário genérico mesmo assim).
- (US-073) `lib/crm-mcp.ts` (novo, próprio deste app, não compartilhado): mapeia as duas ações "criar contato"/"criar negócio" para uma ferramenta do CRM conectado via `MCP_CRM` (`lib/setup-comum.ts`, compartilhado, mesmo par de campos URL+código de `MCP_TAREFAS`) por aproximação de nome/descrição — mesmo método de `agente-kanban/lib/quadro-mcp.ts` (`sugerirMapeamento`), só que sem persistir a escolha em "Opções avançadas" (a AC desta história não pede isso; se um CRM real ambíguo precisar de override manual, seguir o padrão de `MapeamentoMCP.tsx`). `montarArgumentos(schema, candidatos)` preenche cada propriedade do schema remoto com o primeiro valor do lead cujo padrão (regex) bate no nome da propriedade — reaproveitar esse helper (ou um igual) para qualquer nova ferramenta MCP externa cujo schema não é conhecido de antemão. `POST /api/leads/[id]/crm` segue o padrão spread+`atualizarSaida` já usado por `POST /api/ata/[id]/quadro` (US-064): lê o registro, chama uma vez por lead selecionado (nunca em lote na chamada ao MCP), marca `Lead.noCRM: true` só nos que tiveram sucesso, devolve `resultados[]` por lead. O botão "Enviar para o CRM" (linha) e "Enviar selecionados para o CRM" (barra de seleção, ao lado de "Escrever para os selecionados") seguem o padrão de 3 estados já documentado no Codebase Patterns raiz: nada enquanto `crmConfigurado === undefined`, botão quando `true`, `<a href="/setup#mcp-crm">` quando `false`; o botão em lote também some sozinho quando todos os selecionados já estão `noCRM` (`leadsSelecionadosSemCRM.length === 0`), sem precisar de um estado "desabilitado" à parte. Testado de ponta a ponta com um servidor MCP de teste descartável (dois tools fake, `criar_contato_crm`/`criar_negocio_crm`) rodando fora do repo — não há CRM real disponível neste ambiente.
- (US-069) "Excluir leads já entregues" usa uma tabela própria (`lib/leads-vistos.ts`, não compartilhada) chaveada por `segmento+cargo+localização+porte` normalizados (minúsculas, sem espaço nas pontas) — não pelo texto exato digitado. Como o pool de leads de demonstração é fixo (15 pessoas, `lib/demo.ts`), a rotina em modo demo esgota leads "novos" depois da primeira execução para o mesmo perfil (a segunda em diante devolve 0 leads novos); isso é o comportamento esperado do dado fictício, não um bug — comprovado ao rodar "Executar agora" duas vezes seguidas no mesmo perfil. Em produção com a Apollo conectada, o pool real não se esgota. `lib/leads.ts` (`buscarLeads`) parou de restringir a quantidade a `QUANTIDADES_VALIDAS` (5/10/15, ainda validado nas rotas que chamam a função) e passou a aceitar qualquer número de 1 a 30, para a rotina poder pedir 10/20/30 sem essa validação interna cortar para 10.
- (US-037) **Migração para a fundação nova (conta, erros orientados, desenho de 15/09/2026).** Todos os arquivos de `ARQUIVOS` (`scripts/verificar-padrao.sh`) vieram do `pdi-time` sem alteração; `app/conta`, `app/entrar` e `app/historico` nasceram com a marca do app; `public/ilustracoes/` recebeu os ícones e a ilustração de Vendas. O acento trocou de `#0f4c81` para `#0b5789` (Vendas, `tasks/paleta-segmentos.json`) — a AC dizia "acento `#0f4c81` mantido", texto anterior à decisão de paleta por segmento, e venceu a regra genérica da Fase 0, como nas US-033/034/035/036. Nenhuma rota pública nova: este app não tem `/s/**`, `/webhook/**` nem sala por link, e o `proxy.ts` compartilhado (copiado sem alterar) já cobre o resto.
- (US-037) **Contagem dos limites de texto da tela principal** (`PROMESSA` em `app/page.tsx`): título do hero "Sua lista de leads com a abordagem pronta" = 8 palavras; apoio = 17 palavras; prévia com 5 itens de 5, 4, 6, 4 e 5 palavras; uma linha de ajuda por campo no máximo (o hint do campo "O que sua empresa vende" virou o começo do próprio placeholder, para o botão primário caber acima da dobra); descrição de cada cartão de `/setup` em uma linha (`beneficio` de `lib/integracoes.ts`). Botão primário medido em y=875 a 1400x900.
- (US-037) **A `Origem` compartilhada não serve para a lista de leads** e foi trocada por `OrigemLeads` (próprio, em `app/page.tsx`): a lista é *buscada* numa base, nenhuma IA a escreve (a `Origem` diria "Gerado com IA a partir de..."), e o "exemplo" dela vem da falta da busca de leads, não da IA (a `Origem` diria "Conecte a IA para usar os seus dados", falso quando a IA já está ligada). Pelo mesmo motivo o `SeloIA` saiu do resultado da lista e ficou só na `AbordagemView`, onde a IA escreve de verdade. Quem tem duas fontes independentes com fallback (aqui: busca de leads e IA) nunca deve depender só de `meta.demo` — mesmo achado da US-036 em `custos-ia`.
- (US-037) **Mensagens da busca de leads e do CRM.** `lib/leads.ts` ganhou `ErroApollo` no formato de `ErroIA` (`codigo`/`status`/`acao`) e `interpretarFalhaApollo(status, detalhe)`, o único ponto que traduz a resposta da Apollo: 401/403 → "A busca de leads recusou a chave. Confira em Configurações › Busca de leads"; 402/429 → "A conta da Apollo atingiu o limite de créditos do plano"; resto e falha de rede → "A busca de leads não respondeu; tente de novo em um minuto". `lib/crm-mcp.ts` ganhou `ErroCRM` pelo mesmo motivo: as frases de `lib/mcp-cliente.ts` falam de "o serviço" e nunca devem chegar à tela. `app/api/erros.ts` (`responderErro`) é o único `catch` das rotas próprias — traduz `ErroApollo` e delega o resto a `respostaErro` (`lib/ai.ts`), reescrevendo o 500 genérico com a frase do app.
- (US-037) `lib/acoes.ts` (novo, módulo puro sem `node:*`): as ações "o que fazer agora" (`/setup#apollo`, `/setup#mcp-crm`, `/setup#notificacoes`) moram lá e são importadas tanto por `app/page.tsx` (Client Component) quanto por `lib/leads.ts`/`lib/crm-mcp.ts` (servidor) — assim o rótulo do aviso e o da `acao` devolvida pela rota não divergem, e o caminho de Configurações sai de `app/page.tsx`, onde `scripts/verificar-jargao.mjs` acusa `/setup` escrito como valor de objeto (como `href="..."` ele não acusa).
- (US-037) `lib/ultima-busca.ts` guarda o último perfil buscado (`ULTIMA_BUSCA`, JSON em `lib/store.ts`) para a rotina "Leads novos toda semana" criada pelo cartão genérico de `/setup` nascer pré-preenchida — o formulário genérico não sabe pedir segmento, cargo e proposta. O `validar` do `TipoRotina` (`lib/rotinas-do-app.ts`) recusa a criação enquanto não houver perfil nem nos parâmetros nem no último buscado, para a rotina não falhar em silêncio no dia marcado.
- (US-037) **Tabela no palco de meia tela:** com o desenho de duas colunas, empresa e cidade saíram de colunas próprias e foram para dentro da célula do nome; o chip de porte virou "55 func."; o sinal ficou com `linhas: 5`. Medido com Playwright: 0 "Ver mais" em 15 linhas (eram 15 antes do ajuste). Quem mexer nas larguras (`31%` no nome, `96px` no porte, `132px` na ação) deve medir de novo `table button:has-text("Ver mais")`, não olhar a captura.
- (US-037) "Escrever para os 5 melhores" ordena por `maisMaterial` (sinal longo, LinkedIn, site e cargo): a busca não devolve nota de qualidade do lead, então "melhores" só pode significar "os que têm mais material para personalizar" — e a tela diz isso em uma linha abaixo do botão. O botão some quando os cinco já têm abordagem pronta, e o rótulo mostra quantos faltam.
