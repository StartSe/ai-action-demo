# Sobe só este app: docker compose up --build  (http://localhost:3005)
# Nenhuma variável é obrigatória: abra /setup no navegador para conectar a IA e as integrações.
services:
  prospeccao-ia:
    build: .
    ports:
      - "3005:10000"
    volumes:
      - dados:/app/data
    restart: unless-stopped

volumes:
  dados:
