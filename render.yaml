# Blueprint único da suíte IA para Executivos: publica os 14 apps de uma vez.
# Cada app também tem seu próprio Blueprint (branch deploy-<app> em https://github.com/StartSe/ai-action-app-deploy).
# Imagens públicas publicadas pelo GitHub Actions em ghcr.io/startse/<app>:latest.
# Nenhuma chave é necessária aqui: após publicar, abra /setup em cada app e conecte a IA.
# Gerado por scripts/gerar-deploy.mjs a partir de catalogo.json. Não edite à mão.
services:
  - type: web
    name: pdi-time
    runtime: image
    image:
      url: ghcr.io/startse/pdi-time:latest
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
    # Para manter a configuração feita em /setup entre deploys (exige plano pago):
    # disk:
    #   name: dados
    #   mountPath: /app/data
    #   sizeGB: 1

  - type: web
    name: agente-kanban
    runtime: image
    image:
      url: ghcr.io/startse/agente-kanban:latest
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
    # Para manter a configuração feita em /setup entre deploys (exige plano pago):
    # disk:
    #   name: dados
    #   mountPath: /app/data
    #   sizeGB: 1

  - type: web
    name: entrevista-ia
    runtime: image
    image:
      url: ghcr.io/startse/entrevista-ia:latest
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
    # Para manter a configuração feita em /setup entre deploys (exige plano pago):
    # disk:
    #   name: dados
    #   mountPath: /app/data
    #   sizeGB: 1

  - type: web
    name: posts-sociais
    runtime: image
    image:
      url: ghcr.io/startse/posts-sociais:latest
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
    # Para manter a configuração feita em /setup entre deploys (exige plano pago):
    # disk:
    #   name: dados
    #   mountPath: /app/data
    #   sizeGB: 1

  - type: web
    name: prospeccao-ia
    runtime: image
    image:
      url: ghcr.io/startse/prospeccao-ia:latest
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
    # Para manter a configuração feita em /setup entre deploys (exige plano pago):
    # disk:
    #   name: dados
    #   mountPath: /app/data
    #   sizeGB: 1

  - type: web
    name: whatsapp-atendente
    runtime: image
    image:
      url: ghcr.io/startse/whatsapp-atendente:latest
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
    # Para manter a configuração feita em /setup entre deploys (exige plano pago):
    # disk:
    #   name: dados
    #   mountPath: /app/data
    #   sizeGB: 1

  - type: web
    name: contratos-ia
    runtime: image
    image:
      url: ghcr.io/startse/contratos-ia:latest
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
    # Para manter a configuração feita em /setup entre deploys (exige plano pago):
    # disk:
    #   name: dados
    #   mountPath: /app/data
    #   sizeGB: 1

  - type: web
    name: reunioes-ia
    runtime: image
    image:
      url: ghcr.io/startse/reunioes-ia:latest
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
    # Para manter a configuração feita em /setup entre deploys (exige plano pago):
    # disk:
    #   name: dados
    #   mountPath: /app/data
    #   sizeGB: 1

  - type: web
    name: financas-ia
    runtime: image
    image:
      url: ghcr.io/startse/financas-ia:latest
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
    # Para manter a configuração feita em /setup entre deploys (exige plano pago):
    # disk:
    #   name: dados
    #   mountPath: /app/data
    #   sizeGB: 1

  - type: web
    name: voz-do-cliente
    runtime: image
    image:
      url: ghcr.io/startse/voz-do-cliente:latest
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
    # Para manter a configuração feita em /setup entre deploys (exige plano pago):
    # disk:
    #   name: dados
    #   mountPath: /app/data
    #   sizeGB: 1

  - type: web
    name: radar-sinais
    runtime: image
    image:
      url: ghcr.io/startse/radar-sinais:latest
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
    # Para manter a configuração feita em /setup entre deploys (exige plano pago):
    # disk:
    #   name: dados
    #   mountPath: /app/data
    #   sizeGB: 1

  - type: web
    name: bussola-ia
    runtime: image
    image:
      url: ghcr.io/startse/bussola-ia:latest
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
    # Para manter a configuração feita em /setup entre deploys (exige plano pago):
    # disk:
    #   name: dados
    #   mountPath: /app/data
    #   sizeGB: 1

  - type: web
    name: simulador-vendas
    runtime: image
    image:
      url: ghcr.io/startse/simulador-vendas:latest
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
    # Para manter a configuração feita em /setup entre deploys (exige plano pago):
    # disk:
    #   name: dados
    #   mountPath: /app/data
    #   sizeGB: 1

  - type: web
    name: custos-ia
    runtime: image
    image:
      url: ghcr.io/startse/custos-ia:latest
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
    # Para manter a configuração feita em /setup entre deploys (exige plano pago):
    # disk:
    #   name: dados
    #   mountPath: /app/data
    #   sizeGB: 1
