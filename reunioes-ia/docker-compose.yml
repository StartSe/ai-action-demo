# Sobe só este app: docker compose up --build  (http://localhost:3008)
# Nenhuma variável é obrigatória: abra /setup no navegador para conectar a IA e as integrações.
services:
  reunioes-ia:
    build: .
    ports:
      - "3008:10000"
    volumes:
      - dados:/app/data
    restart: unless-stopped

volumes:
  dados:
