// Tipos de rotina deste app: nenhum nesta v1 — a atualização do negócio é sempre por ação da pessoa
// (analisar → revisar → confirmar), nunca por rotina agendada. Arquivo mantido (não copiado sem
// alterar entre apps) para o cartão "Rotinas" continuar existindo na infraestrutura compartilhada.
import type { TipoRotina } from "./rotinas";

export const TIPOS_ROTINA: TipoRotina[] = [];
