# Opcional: as conexões podem ser configuradas pela interface.
# Dados, conta, mapas e credenciais persistem neste diretório.
DATA_DIR=./data
AI_PROVIDER=openrouter
# OPENROUTER_API_KEY=
# AI_MODEL=openrouter/auto
# A .venv local é detectada automaticamente após npm run setup:youtube.
# PYTHON_PATH=/caminho/para/outro/python
# Proxy residencial HTTP/HTTPS, se o YouTube bloquear o IP do servidor (Render).
# Use a URL fornecida pelo seu provedor, com usuário/senha codificados para URL.
# YOUTUBE_PROXY_URL=
# Vídeos públicos de qualquer canal: configure pela interface em YouTube.
# Chave criada em https://aistudio.google.com/apikey (não é o segredo OAuth).
# GEMINI_API_KEY=
# GEMINI_VIDEO_MODEL=gemini-3.8-flash
# Modos: gemini, oauth ou public. Salvar a chave na interface seleciona gemini.
# YOUTUBE_IMPORT_MODE=gemini
# API oficial de legendas do próprio canal: também pode ser configurada em Configurações → YouTube.
# Cliente OAuth do tipo Aplicativo da Web, com YouTube Data API v3 ativada.
# Defina as duas variáveis juntas; quando presentes, a interface não as altera.
# YOUTUBE_CLIENT_ID=
# YOUTUBE_CLIENT_SECRET=
# Opcional: origem pública canônica. No Render, RENDER_EXTERNAL_URL é detectada.
# APP_URL=https://seu-mapify.onrender.com
# Recuperação da conta: definir temporariamente e remover após entrar.
# NOVA_SENHA_ADMIN=
