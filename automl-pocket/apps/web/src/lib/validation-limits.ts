/**
 * Limites de caracteres compartilhados entre os inputs (maxLength nativo)
 * e a validação das server actions — os dois lados devem usar a MESMA
 * constante para a mensagem de erro do servidor nunca divergir do client.
 */
export const NAME_MAX_LENGTH = 100;
