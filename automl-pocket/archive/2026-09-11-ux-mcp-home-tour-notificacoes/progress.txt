# Ralph Progress Log
Started: 2026-09-04
---

## Codebase Patterns
- Migrations drizzle: editar apps/web/src/db/schema.ts, depois `npx drizzle-kit generate --name <slug>` e `npm run db:migrate` (ambos em apps/web). Config em apps/web/drizzle.config.ts, saída em apps/web/drizzle/.
- Backfill/SQL manual numa migration drizzle: anexar no FIM do .sql gerado precedido de uma linha `--> statement-breakpoint` (o migrator divide o arquivo por esse marcador); conferir o efeito no psql com um `count(...) FILTER (WHERE ...)` logo após `npm run db:migrate`.
- Banco local: container `automl-postgres-1`, user/db `akkio`, porta 5433 (`docker exec automl-postgres-1 psql -U akkio -d akkio -c "..."`).
- Quality checks web: `npm run typecheck`, `npm run lint`, `npm test` em apps/web. Existe 1 warning pré-existente de lint (react-hooks/incompatible-library) — não é erro.
- `npm run lint` NÃO checa formatação: rodar `npx prettier --check <arquivos tocados>` (em apps/web) antes do commit e `--write` se reclamar.
- Textos de CTA/copy compartilhados vivem como constantes em `src/lib/*.ts` (client-safe) travadas em teste unitário (inclusive guarda contra termos vetados) — nunca string literal no JSX quando o texto já tem constante.
- Em pg_constraint, confdeltype 'n' = SET NULL (não "no action", que é 'a').
- Testes do worker: rodar em container (comando completo no CLAUDE.md do worker, seção "Testes"); ~70s, 301 testes.
- Eventos duráveis do worker: usar `_log_training_event` (model_train.py) — INSERT em audit_logs SEM commit, participa da transação do chamador; user_id fica fora do INSERT (NULL).
- Drizzle: em `select` de tabela única (sem join) as colunas saem SEM o nome da tabela; subselect correlacionado dentro de `sql\`...\`` (`${a.orgId} = ${b.orgId}`) vira `"org_id" = "org_id"` e conta tudo. Filtrar o subselect por parâmetro ou usar `alias()`; confirmar com um teste que insere nada e espera 0.
- Testar server action (`"use server"`) no vitest: `vi.mock("@/lib/session")` com o usuário num `vi.hoisted`, `vi.mock("next/headers", () => ({ headers: async () => new Headers() }))`, e `await import()` da action dentro do `beforeAll`.
- No multiclasse, selectionMetric é "f1_macro" mas os candidatos reportam a chave "f1" — qualquer lookup de métrica primária precisa desse fallback (`_primary_metric`).
- Validar SQL novo do worker: rodar no psql dentro de BEGIN/ROLLBACK (não precisa de linha existente para testar sintaxe).
- Web dev local para smoke: `cd apps/web && DATABASE_URL=postgresql://akkio:akkio@localhost:5433/akkio REDIS_URL=redis://localhost:6379 AUTH_SECRET=x BETTER_AUTH_URL=http://localhost:3000 RESEND_API_KEY= npx next dev -p 3000` (não há .env em apps/web; o da raiz usa hostnames do Docker e tem RESEND real). Módulos só-stdlib do worker rodam do host com `python3` a partir de apps/worker.
- No Bash da sessão o `cd` PERSISTE entre chamadas: nunca encadear `cd apps/x && …` sem voltar — arquivos relativos (progress.txt, prd.json) acabam gravados no diretório errado. Preferir caminhos absolutos.
- Validar consultas de docs: extrair blocos ```sql com awk, `docker cp` para o container e `\i` dentro de BEGIN + INSERTs sintéticos + ROLLBACK — testa sintaxe E semântica sem sujar o banco.
- Fixtures SQL sintéticas: `users.org_id` é NOT NULL — inserir em `organizations` antes; `llm_usage.feature` é o enum `llm_feature` (`(SELECT enum_range(NULL::llm_feature))[1]` pega um valor válido sem hardcode).
- Metadata durável de auditoria: eventos com projectId devem levar projectName junto (o cascade apaga projects). Finders de deployment (lib/deployments.ts) já fazem innerJoin com projects e expõem projectName nos tipos PublishedWebApp/ApiPredictTarget/McpPredictTarget.
- Colunas `numeric` no drizzle são string no TS (insert e select) — formatar com toFixed antes de gravar; `pgEnum` exporta `.enumValues` para derivar o union type sem duplicar a lista.
- Rota de API com lógica testável: handler em `src/lib/**` com objeto de deps (sessão, rate limit, orçamento, LLM, sandbox) e `route.ts` só com o wiring — `api-predict.ts` e `explore/chat-handler.ts`. Nunca exportar helpers de um `route.ts` (o build do Next valida os exports).
- Rodar código server-side de verdade contra o Postgres local: script em `apps/web/src/x.tmp.ts` + `DATABASE_URL=postgresql://akkio:akkio@localhost:5433/akkio npx tsx --tsconfig tsconfig.json src/x.tmp.ts`; apagar o arquivo depois (o tsconfig só resolve `@/` dentro de src).
- Se o módulo (ou algo que ele importa) tem `import "server-only"`, o script tsx precisa de `NODE_OPTIONS="--conditions=react-server"`; no vitest é `vi.mock("server-only", () => ({}))` antes do import.
- Módulo de `src/lib` que Client Components importam NÃO pode puxar `@/db` (pg entra no bundle do browser). Padrão: helpers puros num arquivo client-safe (`template-links.ts`, `training-eta-format.ts`) e o arquivo server-only re-exporta tudo dele (`spreadsheet-template.ts`, `training-eta.ts`).
- UI condicional a um tipo de erro (ex.: CTA só em "limite atingido"): a rota/action devolve um `code` estruturado ao lado de `error` e a UI guarda `errorCode` junto do `error` (zerando os dois juntos) — nunca fazer matching no texto da mensagem. Referência: `QUOTA_LIMIT_CODES`/`quotaLimitCodeFrom` em lib/enterprise-lead.ts.
- Hooks do Better Auth: `hooks.after` global roda ANTES dos after hooks de plugin (que rodam na ordem do array `plugins`), e cada hook pode trocar `ctx.context.returned`. Lógica que depende do resultado final do sign-in (ex.: auditoria de login com 2FA) vai num plugin inline registrado depois do `twoFactor` (`loginAuditPlugin` em auth.ts). Se um `before` hook lança, os after hooks não rodam.
- Smoke test de auth: exportar `REDIS_URL=redis://localhost:6379` (o `.env` da raiz usa o hostname Docker `redis:6379` e qualquer hook com `hitRateLimit` trava na conexão) e `AUTH_SECRET` do `.env`; não fazer `source .env` (tem linha que o zsh não parseia) — passar as variáveis inline no comando.
- Smoke test de auth server-side: script tsx com `getAuth().api.*` (`asResponse: true` para ler status/set-cookie). Conta credential precisa de `issuer = "local:credential"` e `accountId = user.id`; o `secret` do totpURI vem em base32 (`base32.decode` de `@better-auth/utils/base32` antes de `auth.api.generateTOTP`). Inserir user/org direto pelo drizzle evita o seed de datasets do databaseHook; apagar audit_logs do user antes do cleanup (FK é SET NULL).
- Rota de API/MCP que precise logar um request: `requestLogContext(request)` (audit.ts) — nunca `request.url` cru (o `?token=` do MCP é a chave em claro); URL para log sempre passa por `scrubUrl`.
- Rate limit de rota nativa do Better Auth (ex.: `/change-password`): usar `rateLimit.customRules[path] = { window, max }` em auth.ts (por IP, memória, ligado só em produção por padrão) — formato diferente do `{ limit, windowSec }` do `enforceRateLimit`/Redis. Testar a config lendo o fonte de auth.ts (instanciar `betterAuth()` exige banco).
- Emails transacionais disparados por hooks do Better Auth: template como função pura em `lib/email-templates.ts` (escapar texto vindo do usuário antes de interpolar no HTML; datas com `Intl.DateTimeFormat("pt-BR", { timeZone: "America/Sao_Paulo" })` — o servidor roda em UTC) e envio dentro do módulo de `src/lib` que o hook chama, com `send` injetável (`typeof sendEmail`) e try/catch + `console.error` — nunca `sendEmail` direto em auth.ts, para o teste cobrir "envia no sucesso, não envia na falha" sem Better Auth.
- Erros do Better Auth chegam ao after hook como `APIError` com `body.code` (ex.: `INVALID_PASSWORD`, `PASSWORD_TOO_SHORT`) — decidir auditoria/UI por `code`/`status`, nunca pela `message`. Endpoints que giram a sessão (`revokeOtherSessions`, `/two-factor/disable`) deixam `ctx.context.newSession` preenchido e a sessão da requisição apagada: usuário = `newSession?.user ?? getSessionFromCtx(ctx)?.user`.
- Rotas públicas no proxy: `PUBLIC_PATHS` usa `startsWith` — rota raiz `/` é pública por checagem exata `pathname === "/"` (nunca adicionar "/" à lista). Sessão opcional em página pública: `getOptionalSession()` (lib/session.ts) — não relaxar `requireSession`.
- `page.tsx`/`layout.tsx` do App Router só podem exportar o que o Next espera (default, metadata, etc.) — constantes de copy vão para módulo client-safe em `src/lib/**` (ex.: `lib/marketing/copy.ts`). Tipos `PageProps<"/x">`/`LayoutProps<"/x">` são globais (gerados pelo `next typegen`).
- Depois de mover/apagar uma page, `npm run typecheck` pode falhar por `.next/dev/types/validator.ts` desatualizado: `npx next typegen` regenera `.next/types`, mas `.next/dev/types` só é regenerado subindo o `next dev` uma vez.
- Smoke test HTTP de rota com sessão: pegar um `sessions.token` válido no Postgres e montar o cookie `better-auth.session_token=<encodeURIComponent(token + "." + base64(HMAC-SHA256(AUTH_SECRET, token)))>` (assinatura do better-call, base64 padrão). Dev server local: `npx next dev` em apps/web com `DATABASE_URL` trocando `postgres:5432` por `localhost:5433`, `REDIS_URL=redis://localhost:6379` e `AUTH_SECRET` do `.env` da raiz, inline (não há `.env` em apps/web).
- Verificação visual sem Playwright/MCP de browser: `node scripts/screenshot.mjs <url> <saida.png> <largura> <altura>` (apps/web; Chrome headless via CDP, emula a largura de verdade e imprime erros de console/hidratação). `--window-size=375` sozinho NÃO serve (o Chrome impõe janela mínima ~500 px). Dev server para isso: `npx next dev -p 3111` com as env inline (ver item acima).
- Dados fictícios/copy da home: números formatados por helpers determinísticos em `lib/marketing/hero-scenes.ts` (sem `Intl` → sem divergência de hidratação); NBSP sempre como escape `\u00a0` no fonte e nos testes (literal invisível já causou falha de teste).
- Formulário público (sem sessão): handler em `src/lib/**` com deps `insertLead`/`audit`/`ip`/`rateLimitStore`/`now`; honeypot lido da entrada BRUTA antes do parse e respondido como sucesso falso + evento próprio; rate limit por IP via `hitRateLimit("<scope>:<ip>")` e fail-open sem IP (referência: `lib/marketing/homepage-lead-request.ts`, `auth-email-rate-limit.ts`).
- zod v4: `z.email()` é top-level; normalizar antes de validar formato com `z.string().trim().toLowerCase().pipe(z.email())` (checks rodam em ordem).
- Verificação interativa da home sem Playwright: copiar a cabeça de `apps/web/scripts/screenshot.mjs` (Chrome headless + CDP) e dirigir a UI com `Runtime.evaluate` (`el.click()`) + `Input.insertText` após `focus()` (funciona com inputs controlados do React; setar `.value` direto não dispara o onChange). Modelo em progress.txt, US-007.
- Radix: Dialog aberto de dentro de um Sheet empilha (o Sheet fica aberto atrás); `DialogClose asChild` sobrescreve o `data-slot` do Button para `dialog-close`.
- Índice parcial no drizzle: `uniqueIndex(...).on(...).where(sql`...`)` com `sql` de "drizzle-orm" — o drizzle-kit gera o `WHERE` sozinho; o `ON CONFLICT` correspondente precisa repetir o predicado (`ON CONFLICT (a, b) WHERE b IS NOT NULL`).
- Copy compartilhada entre web e worker: JSON em `src/lib/__fixtures__/*.json` importado pelo módulo TS (`resolveJsonModule` ligado) e lido pelo worker em Python — alterar texto = editar o fixture.
- `npx shadcn@latest add <comp>` gera `import { cn } from "cn"` e instala um pacote `cn` indevido: corrigir o import para `@/lib/utils`, `git checkout package.json package-lock.json` e `rm -rf node_modules/cn`. Arquivos de `components/ui/` ficam no estilo shadcn e NÃO passam no prettier — não incluir no `--check/--write`.
- Lint `react-hooks/static-components`: componente vindo de chamada de função no render (`const Icon = fn(x)`) é erro; lookup em record (`MAP[x]`) passa — exportar o record.
- Arquivo de fora do contexto de build (ex.: fixture do web dentro da imagem do worker): `build.additional_contexts` no compose + estágio vazio `FROM scratch AS <nome>` no Dockerfile que o contexto nomeado sobrepõe (`COPY --from=<nome> / dest/`); build avulso continua passando com a pasta vazia. Testar o resultado com `docker compose run --rm --no-deps -T worker python -c ...`.
- Testes do worker que precisam do fixture do web: montar o arquivo no container e apontar `NOTIFICATION_COPY_PATH` (o container só monta apps/worker; `repo_fixture_path()` devolve None lá porque `/src/jobs` não tem raiz de repo acima).
- Shell zsh nas ferramentas: `echo =====` quebra (`=cmd` é expansão de caminho) — separar saídas com `echo '---X'`; o `cd` de um comando persiste para os próximos, então use caminhos absolutos.
- Diálogo aberto por search param do servidor (ex.: `/settings?ativar-2fa=1`): a page lê o param e passa um booleano; o componente usa `useState(defaultOpen)` como valor INICIAL e, ao fechar, `history.replaceState` remove o param (a prop do servidor não muda sem reload). Radix `Portal` só monta no client, então `open=true` no primeiro render não gera divergência de hidratação.
- Ação de modal que grava no servidor: fechar o modal SÓ depois do `await` da action (erro → `toast.error` e o modal fica aberto); estado `pending` desabilita o rodapé inteiro.

- Smoke test com sessão: `requireSession` redireciona para `/aceite-politica` quem não tem linha em `policy_acceptances` para `CURRENT_POLICY_VERSION` (lib/policy.ts) — escolher um usuário que já aceitou (e `approved_at` não nulo) antes de montar o cookie; fazer o driver CDP abortar se `location.pathname` não for a rota esperada.
- `psql ... RETURNING` em script: usar `-Aqtc` (o `-q` esconde o rótulo `INSERT 0 1`, que senão vem colado no id).
- Simular aba oculta no CDP: `Object.defineProperty(document, "hidden", { get: () => true, configurable: true })` + `document.dispatchEvent(new Event("visibilitychange"))` (e o inverso para voltar) — não há comando CDP para `document.hidden`.
- Toast global: `toast()` do sonner (o `Toaster` já está montado em `app/layout.tsx` via `ToasterMount`); ler no CDP por `[data-sonner-toast]`, título em `[data-title]`, ação em `[data-button][data-action]`.
- Disco cheio (ENOSPC) derruba a ferramenta Bash inteira (não cria o arquivo de saída); o Monitor ainda roda e devolve stdout — use-o para `pkill` do `next dev` + `rm -rf apps/web/.next/dev/cache /tmp/cdp-profile-*`. Todo driver CDP deve apagar seu `/tmp/cdp-profile-*` ao terminar.
- Contraste de tokens: `--background` do tema claro é `#f8f9fb` (não branco). Exigência AA sobre cores do tema → calcular a partir do globals.css (`contrastRatio` em `lib/__tests__/marketing-footer.test.ts`), nunca assumir o par do shadcn.
- Tailwind v4: `rotate-*`, `translate-*` e `scale-*` escrevem nas propriedades CSS `rotate`/`translate`/`scale`, NÃO em `transform` — no CDP ler `getComputedStyle(el).rotate` (etc.); `transform` fica "none". `transition-transform` já cobre as quatro.
- CDP `Input.dispatchKeyEvent`: Enter só ativa botão/summary/link se o keyDown levar `text: "\r"` (Espaço idem com `text: " "`); sem `text` o Chrome não gera o keypress e o clique implícito não acontece.
- Header da home: item novo em `HEADER_NAV` exige remedir a barra completa na menor largura em que ela aparece (hoje `lg` = 1024 px): `docWidth == innerWidth` e `nav.right < ctas.left` via CDP — em 768 px 6 âncoras já não cabiam.
- React 19 no CDP: estado setado num handler de clique só chega ao DOM no microtask — ler `aria-*`/texto após `await Promise.resolve()` ou rAF, nunca síncrono após `el.click()`. Capturar viewport rolado SEM `clip` (clip é em coordenadas do documento).
- O `grep`/`rg` do shell das ferramentas é um wrapper que respeita .gitignore (ignora `node_modules`, `.next`): para procurar em dependências use `/usr/bin/grep -r` direto (o `--include` do wrapper também falha).
- SVG posicionado com `absolute` e só `left`/`right` (ou `top`/`bottom`) não estica: elemento substituído fica com 300×150 intrínsecos. Envolver num `span` posicionado e dar `size-full` ao SVG (referência: conectores de `how-it-works.tsx`). Sempre medir `getBoundingClientRect` do SVG no CDP antes de confiar na captura.
- Verificar cookie httpOnly gravado por server action sem completar o fluxo externo (ex.: OAuth): driver CDP com `Network.enable` + clique no botão + `Network.getAllCookies` (mostra `httpOnly`, `sameSite`, `expires`). Em dev sem GOOGLE_CLIENT_ID o better-auth só loga warning e não redireciona — os cookies ficam visíveis.
- Origem/atributos de um cadastro: nunca coluna nova em `users` — metadata do evento `auth.signup` (hook `user.create.after` em lib/auth.ts, lê body → cookies via `readSignupCookies`) e consulta em docs/metricas-uso.md. Lista fechada em `lib/signup-ref.ts` (`SIGNUP_REFS`/`parseSignupRef`).
- `AnimatePresence mode="wait"`: o estado (e qualquer timer disparado por ele) muda na hora, mas a cena nova só monta depois da saída da anterior (~`DURATION.scene`); sub-timers e probes de CDP que dependem do DOM novo precisam esperar essa saída. Prop `layout` do motion exige `LazyMotion features={domMax}` (`domAnimation` não tem projeção).
- Pausar/retomar timer sem reiniciar: guardar `{ key, remainingMs }` num ref; o cleanup do effect desconta o tempo decorrido quando a pausa entra e o próximo effect recria o `setTimeout` só com o restante. Chave `${estado}:${run}` no `AnimatePresence` permite "clicar na cena atual remonta a cena".
- `useInView` do palco/seção em 375×800: o bloco costuma ficar abaixo da dobra e fica pausado até rolar — no driver CDP, `scrollIntoView({ block: "center", behavior: "instant" })` antes de medir (o `scroll-behavior: smooth` da home faz `scrollTo` levar ~1 s; esperar antes de ler `data-paused`).
- Números animados dentro de um bloco que já controla a própria visibilidade: `AnimatedNumber startOnMount` (senão um KPI abaixo da dobra fica em "0" enquanto o resto do bloco anima).
- Imagem estática SVG na home: arquivo em `public/marketing/**` + `next/image` com `width/height` explícitos — src terminando em `.svg` já sai `unoptimized` (sem `dangerouslyAllowSVG` e sem `<img>` cru, que o lint `no-img-element` reclama). Guardar largura/altura numa constante de `copy.ts` e testar contra o `viewBox` do arquivo com `readFileSync(path.join(process.cwd(), "public", src))` (cwd do vitest é apps/web).
- Página pública fora dos route groups precisa do wrapper `data-marketing-root` para o header compartilhado ter `--marketing-header-height` (`h-(--marketing-header-height)` sem a var vira `height:auto` e a barra colapsa) — usar `PublicPageShell` em vez de repetir o wrapper.
- Uma página só pode ter UM `<footer>` no mesmo nível: dois viram dois landmarks `contentinfo`. Faixa extra de rodapé (linha de versão) vai num `<div>` com as mesmas classes.
- `Page.captureScreenshot` com `clip` usa coordenadas do DOCUMENTO: depois de rolar, `clip: {x:0,y:0}` captura o topo da página (sai em branco/sem a seção). Para fotografar o que está na viewport após `scrollIntoView`, capturar SEM `clip` (modelo em /tmp/us017-shot.mjs).
- Animar um bloco que o SSR já entrega pronto: cópia `invisible`/`aria-hidden` do estado final (`data-*-ghost`, SEM as classes de verificação) reservando a altura + versão viva numa camada `absolute inset-x-0 top-0`, e só sair da fase "final" depois de `useHydrated()` — zero salto de layout, zero divergência de hidratação, conteúdo completo sem JS (referência: `agent-chat-demo.tsx`, `typing-text.tsx`).
- Máquina de fases de animação sem loop: expor a fase num `data-*-phase` no nó raiz e derivá-la de `hydrated`/`reduced`/`useInView({ once: true })` + um `useState` de passo — só o primeiro passo usa `setTimeout`, os seguintes vêm de callbacks (`onDone`). O atributo é o que o probe de CDP lê.
- `m.rect`/`m.*` de SVG com `style={{ originX: 0.5, originY: 1 }}` cresce da base (o motion põe `transform-box: fill-box`); ao terminar em `scaleY: 1` o `transform` computado volta a `none` — no probe, `"none"` significa "cheio", não "sem animação".
- Probe de CDP em bloco animado: `scrollIntoView({ block: "center", behavior: "instant" })` ignora o `scroll-behavior: smooth` da home (60 ms bastam) — esperar ~1 s antes de amostrar engole as primeiras fases.
- Animar `pathLength` com o motion sobrescreve `stroke-dasharray`: para desenhar uma linha tracejada, mascarar a linha estática com uma `m.line` sólida animada (`<mask maskUnits="userSpaceOnUse" x=0 y=0 width=100% height=100%>` — a região padrão é o bbox, altura zero em linha reta). `whileInView` no `m.svg` propaga variantes para filhos dentro de `<defs>`.
- Medir peso de JS no Next 16 (Turbopack): o `next build` NÃO imprime mais "First Load JS". Somar o gzip dos `<script src>` do HTML da rota e descartar o chunk com `noModule=""` (polyfills). Piso desta app: 158 kB gzip numa página quase estática, 179 kB em /login.
- Lighthouse contra localhost: usar `--throttling-method=devtools`. No padrão `simulate` tudo chega antes do primeiro paint e o simulador faz o LCP depender de todos os chunks (3,2 s simulados vs 1,6 s reais).
- Turbopack não faz tree shaking do índice do `framer-motion`: importar qualquer coisa de `motion/react` traz core + features + projeção (~59 kB gzip) para o chunk inicial, mesmo com `LazyMotion` por `import()`. `optimizePackageImports` não muda nada e `domAnimation` economiza ~5 kB — não gaste tempo aí.
- Tirar peso do primeiro carregamento: `const load = () => import("...")` nomeado, usado no `dynamic()` E como prefetch em `onPointerEnter`/`onPointerDown`/`onFocus` do botão que abre (modal de contato, menu mobile, `Toaster` do sonner).
- Dialog/Sheet do Radix montado por `next/dynamic` fica sem `Trigger` e o foco volta para o `<body>` ao fechar: passar a ref do botão e restaurar em `onCloseAutoFocus` (`e.preventDefault(); ref.current?.focus()`).
- `font-display: swap` + fallback com `size-adjust` do next/font muda o NÚMERO DE LINHAS de um título em faixas estreitas de largura (a headline da home vai a 3 linhas com Inter e cabia em 2 no fallback abaixo de 420 px) — reservar as linhas do webfont (`max-[419px]:min-h-[3lh]`) mata o CLS sem espaço em branco. Medir com CDP bloqueando `*.woff2` e cache desligado.
- Verificar `prefers-reduced-motion` sem log no código: instrumentar `window.setTimeout` via `Page.addScriptToEvaluateOnNewDocument` e amostrar `document.getAnimations()` em intervalo.
- Contraste: `bg-primary/10` sob `text-primary` de 12 px e `text-muted-foreground/80` de 10 px reprovam o AA; usar `/5` e o token cheio.
- Radix `DropdownMenuItem` que abre um `AlertDialog`: guardar só um `useState` de confirmação no pai e montar o diálogo controlado (`open`/`onOpenChange`) FORA do `DropdownMenu`; o menu fecha, o diálogo abre e ao cancelar o `body` volta a `pointer-events: auto` (verificado via CDP). O foco volta ao `<body>` (não ao gatilho) — restaurar na mão se importar.
- Cabeçalho `sticky top-0` de tela de projeto gruda no topo do `<main overflow-y-auto>` do layout (abaixo da ProjectNavbar, que fica fora do scroll). Barra `fixed bottom-0` é relativa à viewport; conteúdo precisa de `pb-24` no mesmo breakpoint em que a barra aparece.
- Verificar tela autenticada via CDP: `INSERT INTO sessions (user_id, token, expires_at)` com token conhecido + `Network.setCookie` de `better-auth.session_token` = `encodeURIComponent(token + "." + base64(HMAC-SHA256(AUTH_SECRET, token)))` (script em /tmp/cookie.mjs; driver em /tmp/us001-verify.mjs e /tmp/us001-publish.mjs, que clicam em botões, itens de menu e diálogos). Apagar a sessão no fim.
- Fixture de modelo publicável no banco local: `getModelDeployFields` só devolve campos se `models.training_job_id` aponta para um `training_jobs` com `config.target` (ou se há `training_snapshot`); os modelos fixture locais não têm job — criar um job `succeeded` com `{"target": ...}` e apontar o modelo para ele (e reverter depois). Colunas extras em `dataset_columns` com `position >= 100` para não colidir.
- Se o Docker Desktop travar (VM parada no log `~/Library/Containers/com.docker.docker/Data/log/host/com.docker.backend.log`, `docker ps` pendurado, `docker desktop restart` falha com "processes still running"): `pkill -9 -f "com.docker.backend|com.docker.build|Docker Desktop"` + `open -a Docker`; Postgres volta em ~15 s. `docker exec` pendurado também some. Usar `timeout` em todo comando docker/psql.
- `docker exec` só recebe heredoc/stdin com `-i` (`docker exec -i automl-postgres-1 psql ...`); sem o flag o psql roda vazio e o comando "passa" sem executar nada — sempre conferir com um SELECT no fim.
- Driver CDP: Enter só ativa `<summary>`/botões com `text: "\r"` no `keyDown`; `el.focus()` programático só dispara `onFocus` (React/Radix Tooltip) com `Emulation.setFocusEmulationEnabled({ enabled: true })`; `rotate-*`/`translate-*`/`scale-*` do Tailwind v4 escrevem nas propriedades CSS `rotate`/`translate`/`scale` — ler essas no `getComputedStyle`, não `transform`.
- `echo ===` (ou qualquer palavra começando com `=`) quebra o comando em zsh ("= not found") e aborta a cadeia — usar `echo ---`.
- Radix Dialog aberto sem `DialogTrigger` (a partir de popover/toast): passar a ref do gatilho e restaurar o foco em `onCloseAutoFocus`; `DialogContent` só recebe `max-w-*` com prefixo `sm:` (o base `max-w-[calc(100%-2rem)]` é a margem do mobile).
- Prop "abrir por id" controlada de fora num componente com estado interno: `useEffectEvent` lê o estado atual sem entrar nas deps do effect; qualquer setState vai num `.then`/callback (regra do React Compiler: nunca síncrono no corpo do effect).
- Dois ramos de layout condicionais que renderizam o mesmo tipo de elemento na mesma posição (ex.: `<details>`, `<input>` não controlado) precisam de `key` diferente no container — o React reaproveita o nó DOM e o estado nativo (`open`, texto digitado, scroll) vaza de um layout para o outro.
- Contagem de elementos interativos visíveis via CDP: `el.checkVisibility({ contentVisibilityAuto: true, visibilityProperty: true })` — descendentes de `<details>` fechado (Chrome usa `content-visibility: hidden`) ainda devolvem `getBoundingClientRect` não nulo. Instrumentar `Element.prototype.scrollIntoView` via `Page.addScriptToEvaluateOnNewDocument` para ver com que argumentos a UI rolou; `Emulation.setEmulatedMedia({ features: [{ name: "prefers-reduced-motion", value: "reduce" }] })` testa o ramo sem animação. Clipboard em headless: `Browser.grantPermissions` com `clipboardReadWrite` e ler com `(async () => navigator.clipboard.readText())()` (`await` solto no `Runtime.evaluate` é SyntaxError).
- Chamadas de Bash disparadas em paralelo não compartilham o `cd` de forma previsível (o cwd pode ser resetado para a raiz): qualquer leitura de `.env` ou script que alimente variável (`AUTH_SECRET`) usa caminho absoluto, e conferir `${#VAR}` antes de subir o dev server — um `AUTH_SECRET` vazio faz o cookie de sessão injetado não bater.
---


## 2026-09-04 23:40 - US-001
- Novo `components/app/deployment-page-header.tsx` (`DeploymentPageHeader`): link de volta, título, badge, ação primária (label/icon/onClick/disabled/pending/disabledReason → `title`) e menu "Mais ações" (`DropdownMenu`, itens com `destructive`); `sticky top-0 z-20 bg-background/95 backdrop-blur border-b`; abaixo de `sm` a ação primária é duplicada numa barra `fixed bottom-0` com `pb-[calc(0.75rem+env(safe-area-inset-bottom))]`, e o link de volta vira só a seta (`sr-only sm:not-sr-only`) para o título caber em 375 px.
- `deployment-key-controls.tsx`: diálogos extraídos em componentes controlados exportados `RotateKeyDialog`, `RevokeKeyDialog` e o novo `UnpublishDialog` (despublicar saiu de botão visível para item de menu — ganhou confirmação), rótulos em `KEY_ACTION_LABELS`; `DeploymentKeyActions` (usado pela tela da API) segue igual por fora.
- `mcp-config.tsx`: header antigo e bloco de botões da sidebar substituídos pelo `DeploymentPageHeader` (rascunho: Publicar + menu "Salvar rascunho"; publicado: Salvar alterações disabled sem `dirty` + menu Rotacionar/Revogar/Despublicar). `disabledReason` derivado das constantes `SERVER_NAME_REQUIRED`/`FIELDS_REQUIRED`, as mesmas mostradas junto dos campos. Conteúdo com `pt-6 pb-24 sm:pb-8`. Handlers inalterados.
- Verificação via CDP (Chrome headless, sessão injetada): 1366×768, 1280×720 e 375×812 em rascunho e publicado, com 20 campos — "Publicar" visível sem rolar e após rolar até o fim; barra inferior em 375 com `pb` de 96 px no conteúdo; menu abre os 3 diálogos com o texto esperado, Cancelar devolve `pointer-events: auto`; nome vazio / nenhum campo desabilitam a ação com `title` correto e a mensagem junto do campo; rotação e despublicação reais pelo menu funcionaram. Zero erros de console/hidratação. Screenshots em /tmp/us001-*.png.
- Files: apps/web/src/components/app/deployment-page-header.tsx (novo), apps/web/src/components/app/deployment-key-controls.tsx, apps/web/src/app/(project)/projects/[projectId]/deploy/mcp/mcp-config.tsx, apps/web/CLAUDE.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Fixture local para a tela MCP: nenhum modelo do banco local tinha `training_job_id`; a receita do job sintético está nos Codebase Patterns (reverter no fim, inclusive a linha em `deployments` criada ao publicar).
  - Em 375 px o toast do sonner (bottom) cobre a barra inferior fixa enquanto está visível — transitório, não tratado nesta story; se incomodar, dar `offset` ao `Toaster` abaixo de `sm`.
  - Pré-existente e fora do escopo: em 375 px a `ProjectNavbar` sobrepõe logo/nome do projeto com as abas (Preparar/Explorar).
  - Após cancelar um diálogo aberto por item de menu, o foco vai para `<body>`; as próximas stories da tela (US-002/US-003) podem passar um `returnFocusRef` aos diálogos se quiserem devolver o foco ao botão "Mais ações".
  - `apps/web/CLAUDE.md` já não passava no `prettier --check` antes desta story (verificado com `git show HEAD:…`); não é regressão.
---

## 2026-09-05 02:30 - US-002
- `mcp-config.tsx`: faixa de 3 cards virou stepper de uma linha (`ol[aria-label="Etapas"]`, "1 Configurar · 2 Conectar · 3 Usar", ícones 16 px, `aria-current="step"` + `text-primary` no passo atual: 1 em rascunho, 2 publicado). Em rascunho o container passa a `max-w-2xl` e mostra só Nome, Descrição e "Selecionar campos" (contador "N de M campos selecionados" abaixo da legenda, lista `max-h-[40vh]`), depois a faixa "Depois de publicar, conecte a:" com 4 chips não clicáveis (`FEATURED_AGENT_IDS` = claude/chatgpt/cursor/vscode) e o chip "e outros" com Tooltip listando Claude Code, Gemini CLI, Outro (genérico); por fim "Tool predict — N parâmetros" e "Boas práticas de segurança — 4 itens" em `<details>` nativos fechados (`CollapsibleSection`, ChevronDown com `group-open:rotate-180`). Publicado mantém as duas colunas (`max-w-6xl`) com os mesmos `<details>` no fim da seção. Aviso âmbar, `disabled={!published}` da busca/cards e o import de `Lock` removidos. Nada mudou em actions.ts, payload, `dirty`/`canSubmit`/`savedSnapshot`.
- `deployment-key-controls.tsx`: `SecurityPracticesList` (só a `<ol>`) e `SECURITY_PRACTICES_TITLE` exportados; `DeploymentSecurityPractices` (card com título, usado pela tela da API) passou a compô-los — texto inalterado.
- Verificação via CDP (Chrome headless, sessão injetada, dev server 3111) em 1280×720 e 375×812 com 19 campos: layout `draft` com `max-width: 672px`, stepper em uma linha, contador "19 de 19 campos selecionados", `max-height` 288 px / 324,8 px (40vh), 5 chips (ícones `aria-hidden`), tooltip abre por hover e por foco (com `Emulation.setFocusEmulationEnabled`) e fecha com Esc, `<details>` fechados por padrão e alternando por Enter/Espaço com o chevron em `rotate: 180deg`, sem "Publique o servidor primeiro", sem "Endpoint MCP", sem SUA_CHAVE_API, sem busca/cards; "Publicar" visível no topo (1280) e na barra inferior (375) antes e depois de rolar. Fluxo real Publicar → layout `published` (passo 2 em `text-primary`, chave revelada, "Salvar alterações" desabilitado) → Despublicar pelo menu → volta ao layout de rascunho. Zero erros de console/hidratação. Screenshots em /tmp/us002-*.png; driver em /tmp/us002-verify.mjs.
- Files: apps/web/src/app/(project)/projects/[projectId]/deploy/mcp/mcp-config.tsx, apps/web/src/components/app/deployment-key-controls.tsx, apps/web/CLAUDE.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Fixture MCP no banco local: `dataset_columns.org_id` é NOT NULL (pegar de `projects.org_id`); `docker exec` precisa de `-i` para receber heredoc no psql — sem isso o comando "roda" sem executar nada. Receita completa (job sintético + 17 colunas `position >= 100` + sessão) e o cleanup ficaram nesta entrada; publicar cria a linha em `deployments` e despublicar NÃO a apaga — apagar no cleanup.
  - No CDP, Enter só ativa `<summary>`/botões com `text: "\r"` no `keyDown` (Espaço com `text: " "` funciona direto); `rotate-180` do Tailwind v4 escreve na propriedade CSS `rotate` (ler `getComputedStyle(el).rotate`, não `transform`); `el.focus()` programático só dispara `onFocus` do React/Radix com `Emulation.setFocusEmulationEnabled({ enabled: true })`.
  - Toast do sonner entra no `innerText` da página: uma checagem "texto X não aparece" logo após uma action pode casar com o toast (ex.: "Endpoint MCP despublicado…" contém "Endpoint MCP").
  - US-003 vai reorganizar o ramo `published` (chave primeiro, instruções abaixo do card, "Configuração do servidor" recolhida): `configForm`, `toolPredictDetails` e `securityDetails` já são JSX reutilizáveis para isso.
---

## 2026-09-05 00:05 - US-003
- `mcp-config.tsx`: o ramo `published` deixou as duas colunas (`max-w-6xl` + aside) e virou a tela "Conectar" em coluna única `max-w-3xl`, na ordem: `DeploymentKeyStatus` → seção "Escolha seu agente de IA" (busca + grade `grid-cols-2 sm:grid-cols-3 lg:grid-cols-4`, sempre habilitada) → link discreto "Ver URL e header genéricos" (`data-mcp-generic-link`, seleciona `other`) → bloco "Conectar ao {agente}" (`data-mcp-instructions="{agentId}"`, `scroll-mb-24 sm:scroll-mb-4`) → `<details>` "Configuração do servidor" (`data-mcp-details="server-config"`, summary "{título} · N campos", `Badge` "Alterações não salvas" `data-mcp-unsaved` quando `dirty`, corpo = `configForm`) → "Tool predict" → "Boas práticas". Bloco "Endpoint MCP" do topo e `endpointUrlWithToken` removidos (a URL só aparece dentro das instruções). `useEffect([agentId])` faz `scrollIntoView({ block: "nearest", behavior: reduce ? "auto" : "smooth" })` no bloco de instruções. `CollapsibleSection` ganhou `detail?: string` e `badge?: ReactNode` (com `{" "}` entre os spans para o nome acessível não sair colado). Containers dos dois ramos com `key="draft"`/`key="published"` (ver Codebase Patterns). Handlers, `dirty`/`canSubmit`/`savedSnapshot` e `handleUnpublish` (que já limpava `agentId`) inalterados.
- `deployment-key-controls.tsx`: `KEY_REVEAL_NOTICE` exportada ("Copie a chave agora — ela não será exibida de novo."); o bloco da chave em claro ganhou `border-primary` (era `/30`), `data-deployment-key="revealed"` e o aviso passou a usar a constante em destaque (`font-medium`, fundo âmbar, `role="status"`). Vale também para a tela da API, que só trocou o texto do toast pela constante.
- Verificação via CDP (Chrome headless, sessão injetada, dev server 3111) em 1280×720 e 375×812 com 12 campos: publicar → layout `published` com `max-width: 768px`, chave com borda `rgb(59, 94, 235)` e aviso em peso 500; grade com 4 colunas (2 em 375); sem "Endpoint MCP" no texto (excluindo o toaster); Claude → bloco "Conectar ao Claude" entre a grade (bottom 425) e o `<details>` (top 728), dentro da viewport, `scrollIntoView` chamado com `{block:"nearest", behavior:"smooth"}` e com `"auto"` sob `prefers-reduced-motion: reduce`; Copiar → toast "Copiado." e clipboard com a URL `?token=dos_live_…`; desmarcar esconde o bloco; link genérico seleciona "Outro (genérico)" com header Authorization e `?token=`; reload → "só o começo é armazenado" e instruções com SUA_CHAVE_API; abrir configuração → Nome, Descrição e 13 checkboxes; desmarcar um campo → badge "Alterações não salvas" e "Salvar alterações" habilitado; salvar → badge some, toast "Alterações salvas…"; despublicar pelo menu → layout `draft`, sem instruções e sem `<details>` vazando aberto. Zero erros de console/hidratação. Interativos visíveis sem agente: 16 com a chave revelada (back link, Salvar, Mais ações, Copiar chave, busca, 7 cards, link genérico, 3 summaries) e 15 após reload — o AC dizia "no máximo 14" mas a soma dele omite o link genérico e os dois `<details>` da US-002 que o mesmo AC exige; registrado em `notes` do prd.json. Screenshots em /tmp/us003c-1280-*.png e /tmp/us003-375-*.png; driver em /tmp/us003-verify.mjs.
- Files: apps/web/src/app/(project)/projects/[projectId]/deploy/mcp/mcp-config.tsx, apps/web/src/components/app/deployment-key-controls.tsx, apps/web/src/app/(project)/projects/[projectId]/deploy/api/api-config.tsx, apps/web/CLAUDE.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Fixture desta rodada: modelo de classificação `eeee0000-…-0008` (projeto "Projeto US008", org 33fdf6cc…, user us008@teste.com) + job sintético `eeee0000-0000-4000-8000-0000000000f3` com `{"target":"cancelou"}` + 10 colunas `us003_campo_NN` em `position >= 100` + sessão de token conhecido; tudo revertido no fim (inclusive `deployments` e os `audit_logs` gerados). O único modelo local com `training_job_id` de verdade é forecasting (não publicável) — a fixture continua necessária.
  - `api-config.tsx` já não passava no `prettier --check` no HEAD anterior; rodar `--write` nele reformata ~6 trechos alheios — reverter e aplicar só a edição pontual se a mudança for mínima.
  - O bloco de instruções em 375 px fica com `scroll-margin-bottom: 96px` (barra fixa) e em 1280 com 16 px — `block: "nearest"` respeita a margem e o bloco não fica escondido sob a barra.
---

## 2026-09-04 23:50 - US-004
- Tabela `notifications` no schema drizzle (user_id cascade, org_id FK, type text, title/body/href, dedupe_key, metadata jsonb, read_at, emailed_at, timestamps) com índice `(user_id, read_at, created_at desc)` e unique parcial `(user_id, dedupe_key) WHERE dedupe_key IS NOT NULL`. Migration 0028_notifications.sql gerada e aplicada no banco local.
- `src/lib/notifications.ts` (client-safe): NOTIFICATION_TYPES, NotificationType, isNotificationType, notificationIcon, notificationActionLabel, formatNotificationTime (reusa formatRelativeAgo), MAX_UNREAD_BADGE, unreadBadgeLabel.
- `src/lib/__fixtures__/notification-copy.json` + `src/lib/notification-copy.ts` (trainingSucceededNotification, trainingFailedNotification, MFA_SUGGESTION_NOTIFICATION).
- Testes em `src/lib/__tests__/notifications.test.ts` (12 testes). Seção "Notificações in-app" adicionada ao apps/web/CLAUDE.md.
- Files changed: apps/web/src/db/schema.ts, apps/web/drizzle/0028_notifications.sql, apps/web/drizzle/meta/*, apps/web/src/lib/notifications.ts, apps/web/src/lib/notification-copy.ts, apps/web/src/lib/__fixtures__/notification-copy.json, apps/web/src/lib/__tests__/notifications.test.ts, apps/web/CLAUDE.md.
- **Learnings for future iterations:**
  - drizzle-orm 0.45 / drizzle-kit 0.31 emitem o `WHERE` de índice parcial: `uniqueIndex(...).on(...).where(sql\`${table.col} is not null\`)` (importar `sql` de "drizzle-orm" no schema). Não foi preciso editar o SQL à mão.
  - `createdAt.desc()` no `index().on()` sai como `"created_at" DESC NULLS LAST` — é o esperado.
  - JSON em `src/lib/__fixtures__` importa direto (`resolveJsonModule` já está ligado) e é tipado pelo TS; o vitest resolve `@/` pelo alias do vitest.config.ts.
  - Para o insert idempotente (US-005/US-009) usar `ON CONFLICT (user_id, dedupe_key) WHERE dedupe_key IS NOT NULL DO NOTHING` — índice parcial exige o predicado no ON CONFLICT.
---

## 2026-09-05 04:05 - US-005
- Módulo server-only `apps/web/src/lib/notifications-server.ts`: createNotification (dedupe por `onConflictDoNothing` no índice parcial; `replaceIfRead` apaga a lida e reinsere em transação), listNotifications (cursor `before`, created_at desc + id desc), countUnread, markRead, markAllRead (audita `notification.read_all` com `{ count }`), resolveByDedupeKey, pollSince (→ `{ unread, items, latestId }`).
- Server actions `apps/web/src/components/app/notifications-actions.ts` ("use server"): pollNotifications, loadNotifications, markNotificationRead, markAllNotificationsRead — escopo por `requireSession().user.id`, ids validados com zod, retorno serializável (`NotificationItem` em `lib/notifications.ts`).
- Helper de banco de teste `src/lib/__tests__/helpers/test-db.ts` (não existia helper para os testes de deployments — foram criados connectTestDb/createTestUser/dropTestUser) + 10 testes de integração em `notifications-server.test.ts` (dedupe, replaceIfRead, paginação com desempate por id, markRead cross-user, markAllRead + audit, pollSince).
- Seção "Notificações in-app" do apps/web/CLAUDE.md ampliada com o módulo server, as actions e o padrão de teste com Postgres real.
- Files changed: apps/web/src/lib/notifications-server.ts, apps/web/src/lib/notifications.ts, apps/web/src/components/app/notifications-actions.ts, apps/web/src/lib/__tests__/helpers/test-db.ts, apps/web/src/lib/__tests__/notifications-server.test.ts, apps/web/CLAUDE.md.
- **Learnings for future iterations:**
  - Testes de integração com Postgres real: `connectTestDb()` no topo do arquivo (top-level await) seta `process.env.DATABASE_URL` ANTES do `getDb()`; o módulo sob teste tem de ser importado com `await import()` no `beforeAll`, senão o pool singleton nasce com a URL errada. `describe.skipIf(!testDb)` mantém `npm test` verde sem Docker.
  - Módulos `import "server-only"` precisam de `vi.mock("server-only", () => ({}))` no teste, senão o import lança fora do runtime Next.
  - `onConflictDoNothing` do drizzle aceita `{ target, where }` — o `where` é obrigatório para casar com o índice único parcial (`dedupe_key is not null`).
  - `dropTestUser` precisa apagar `audit_logs` do usuário antes de deletar o `users` (FK é SET NULL, ficaria rastro anônimo).
  - Arquivo `"use server"` só pode exportar funções async: tipos de retorno das actions vivem em `lib/notifications.ts`.
  - Um `count(*)::int` via `sql<number>` evita o `count` vir como string do pg.
---

## 2026-09-05 04:20 - US-006
- `components/ui/popover.tsx` (shadcn radix-nova sobre `radix-ui`) e `components/app/notifications-bell.tsx`: sino 36 px com badge 18 px (`unreadBadgeLabel`, oculto em 0), `aria-label` via `notificationsBellLabel` (novo helper em lib/notifications.ts), popover `w-[380px] max-w-[calc(100vw-2rem)]` alinhado à direita com cabeçalho + "Marcar todas como lidas" (só com unread > 0), lista carregada ao abrir via `loadNotifications` (skeleton enquanto carrega, erro com "Tentar de novo"), linhas com ícone por tipo/título truncado/corpo line-clamp-1/tempo relativo (`useClientClock`), ponto + `bg-muted/40` nas não lidas, "Carregar mais" enquanto `nextBefore` vier, estado vazio com BellOff + `NOTIFICATIONS_COPY.empty`; clique marca como lida (otimista) e faz `router.push(href)`.
- Layout `(app)`: `<main>` envolto em `div.relative.min-w-0.flex-1` com o sino em `absolute top-4 right-6 z-30` (`countUnread(user.id)`); `pr-12` na linha do cabeçalho de /projects, /datasets e no bloco do título de /settings. `ProjectNavbar` ganhou prop `initialUnread` (do layout `(project)`) e renderiza `<NotificationsBell variant="navbar" />` à esquerda do ProblemReportButton.
- `NOTIFICATIONS_COPY` em lib/notification-copy.ts (título, marcar todas, carregar mais, erro, retry, vazio) + `NOTIFICATION_ICONS` exportado; testes novos em notifications.test.ts (aria-label singular/plural, copy).
- Verificado via CDP (driver /tmp/us006-verify.mjs, dev server em :3111): /projects (gap de 12 px entre sino e "Novo projeto", zero colisões), /datasets, /settings, navbar de projeto (badge "9+", popover sob a navbar escura), popover vazio, 25 fixtures → 20 itens + "Carregar mais" → 25, "Marcar todas" zera badge e persiste após reload, clique no item fecha o popover e navega; 375 px popover com 343 px dentro da viewport. Fixtures/sessão removidas.
- Files changed: apps/web/src/components/ui/popover.tsx, apps/web/src/components/app/notifications-bell.tsx, apps/web/src/app/(app)/layout.tsx, apps/web/src/app/(app)/projects/projects-view.tsx, apps/web/src/app/(app)/datasets/datasets-view.tsx, apps/web/src/app/(app)/settings/page.tsx, apps/web/src/app/(project)/projects/[projectId]/layout.tsx, apps/web/src/components/project/project-navbar.tsx, apps/web/src/lib/notifications.ts, apps/web/src/lib/notification-copy.ts, apps/web/src/lib/__tests__/notifications.test.ts, apps/web/CLAUDE.md.
- **Learnings for future iterations:**
  - `npx shadcn@latest add <comp>` neste projeto gera `import { cn } from "cn"` e instala um pacote npm `cn` — corrigir para `@/lib/utils` e `git checkout package.json package-lock.json` + `rm -rf node_modules/cn`. Arquivos de `components/ui/` ficam no estilo shadcn (sem ponto e vírgula) e não passam no prettier — não rodar `--write` neles.
  - Lint `react-hooks/static-components`: `const Icon = fn(type)` seguido de `<Icon />` é erro; lookup em record (`const Icon = MAP[type]`) passa. Exportar o record em vez de só a função.
  - O layout do `(app)` não é responsivo em 375 px (sidebar fixa de 240 px) — o título do cabeçalho já fica sob o sino ali; não é regressão desta story. Só o popover foi validado em 375.
  - Radix Popover: `PopoverTrigger asChild` sobre o `Button` funciona com `el.click()` no CDP; o conteúdo recebe o foco (`activeEl` = div do popover) ao abrir.
  - Driver CDP reutilizável para telas autenticadas com popover: /tmp/us006-verify.mjs (ações probe/open/full; cookie via /tmp/cookie.mjs).
---

## 2026-09-05 04:30 - US-007
- `components/app/notification-dialog.tsx` (`NotificationDialog`): Dialog `sm:max-w-md` com ícone do tipo em círculo (`NOTIFICATION_ICON_CIRCLE`: `bg-primary/10 text-primary`, âmbar em training.failed), título, linha "há X min · DD/MM/AAAA HH:MM" (`formatNotificationTimeLine`), corpo como `DialogDescription`, bloco `<dl>` de detalhes (`notificationDetails`: projeto, duração via `formatApproxDuration`, métrica via `notificationMetricLabel`/`formatNotificationMetricValue`) e rodapé com `notificationActionLabel(type)` → `router.push(href)` + fecha (ou "Fechar" se não há href). Foco volta ao sino em `onCloseAutoFocus`.
- `notifications-bell.tsx`: `openDetail(item)` fecha o popover, marca lida otimista (badge/linha na hora, `markNotificationRead` confirma) e abre o modal; prop `openNotificationId` (useEffectEvent + item em cache ou `getNotification(id)`); `triggerRef` nos dois botões.
- `getNotificationById(userId, id)` em notifications-server.ts + action `getNotification(id)` em notifications-actions.ts; `NOTIFICATIONS_COPY.details`/`close`; helpers novos em lib/notifications.ts (`isTrainingNotification`, `formatNotificationDateTime`, `formatNotificationTimeLine`, `NOTIFICATION_TIME_SEPARATOR`, `notificationDetails`, `NOTIFICATION_DETAIL_LABELS`, `notificationMetricLabel`, `formatNotificationMetricValue`).
- Testes: 9 unitários novos (data pt-BR com timeZone injetado, linha de tempo, círculo âmbar, métricas em %/2 casas, detalhes presentes/omitidos, MFA vazio, copy) + 1 de integração (`getNotificationById` cross-user/inexistente). Suíte: 73 arquivos, 955 testes.
- Verificado via CDP (driver /tmp/us007-verify.mjs, dev em :3111, 1366×768 e 375×800): clique em item não lido fecha popover, abre modal (`role=dialog`, aria-labelledby/describedby), badge cai 3→2 na hora e persiste após reload; botão primário navega para o href e fecha; treino falho com círculo âmbar; Esc fecha, não reabre o popover e devolve o foco ao sino; item já lido não mexe no badge; em 375 px o modal tem 343 px com 16 px de margem. Sem warnings de acessibilidade do Radix. Fixtures e sessão removidas.
- Files changed: apps/web/src/components/app/notification-dialog.tsx (novo), apps/web/src/components/app/notifications-bell.tsx, apps/web/src/components/app/notifications-actions.ts, apps/web/src/lib/notifications.ts, apps/web/src/lib/notifications-server.ts, apps/web/src/lib/notification-copy.ts, apps/web/src/lib/__tests__/notifications.test.ts, apps/web/src/lib/__tests__/notifications-server.test.ts, apps/web/CLAUDE.md.
- **Learnings for future iterations:**
  - `DialogContent` do shadcn tem `max-w-[calc(100%-2rem)] sm:max-w-sm`: passar `max-w-md` sem prefixo apaga a margem mobile (modal com 375 px em 375 px). Só sobrescrever com `sm:max-w-md`.
  - `useEffectEvent` (React 19.2) passa no lint `react-hooks` e resolve "prop controlada de fora precisa ler estado atual sem reabrir quando o estado muda"; o setState dentro dele ainda precisa ir para um callback assíncrono (`Promise.resolve(cached).then(...)` unifica o caso em cache e o fetch).
  - Metadata gravada pelo worker (US-009): `{trainingJobId, modelId, durationSeconds, metricName, metricValue}`; `projectName` é opcional no modal (o worker ainda não grava) — `notificationDetails` só mostra o que existe. Métricas de fração (accuracy/f1/auc/mape) saem em %; as demais com até 2 casas pt-BR.
  - `Intl.DateTimeFormat("pt-BR", {...}).format()` gera "05/09/2026, 14:04" (com vírgula) — montar de `formatToParts` para o formato "DD/MM/AAAA HH:MM" da story.
  - Verificação do sino/modal via CDP: em `mobile: true` o Chrome headless ainda respeita `Emulation.setDeviceMetricsOverride` de 375 px; o driver /tmp/us007-verify.mjs cobre clique em linha, probe do dialog, ação primária, Esc e foco.
---

## 2026-09-05 04:50 - US-008
- `notifications-bell.tsx`: polling de 30 s (`NOTIFICATIONS_POLL_INTERVAL_MS`) num único `useEffect` com `setInterval` → effect event `poll` (`pollNotifications(baseline.id)`), consulta imediata no mount para fixar a baseline, pausa em `document.hidden`/retoma com consulta imediata em `visibilitychange`, `pollInFlightRef` contra sobreposição; `applyPoll` (effect event) faz todo o setState no `.then`: badge, anúncio `aria-live` (só quando sobe; limpa ao cair), itens novos no topo da lista carregada (`prependNewNotifications`) e `toast()` do sonner com ação "Abrir" → `openDetail(item)` quando `shouldToastNotification` aprova. Região `<p class="sr-only" role="status" aria-live="polite" data-notifications-announcement>`.
- `lib/notifications.ts`: `PollResult` (movido do server-only, que agora só re-exporta o tipo), `NOTIFICATIONS_POLL_INTERVAL_MS`, `NOTIFICATION_TOAST_DURATION_MS` (10 s), `trainingJobIdFromPathname`, `shouldToastNotification`, `unreadAnnouncement` (singular/plural), `prependNewNotifications`. `NOTIFICATIONS_COPY.toastOpen = "Abrir"`.
- Testes: 8 unitários novos em notifications.test.ts (job page com/sem barra final, treino fora/dentro da página do job, metadata sem trainingJobId, MFA, lido, anúncio, merge sem duplicar, copy). Suíte: 73 arquivos, 963 testes.
- Verificado via CDP (driver /tmp/us008-verify.mjs, dev em :3111, usuário us003): inserir `training.succeeded` via SQL com /projects aberta → em 25 s badge "1", aria-label "Notificações, 1 não lida", região live "Você tem 1 notificação não lida", toast com título/corpo/"Abrir"; "Abrir" abre o modal (detalhes Duração/Acurácia, botão "Ver relatório do modelo"), badge zera e `read_at` gravado; `security.mfa_suggestion` sobe o badge SEM toast; com o popover aberto o item novo entra no topo da lista; com `document.hidden` o badge não muda em 36 s e ao voltar a consulta imediata atualiza em 0,5 s (toast do item que chegou oculto aparece). Zero erros de console. Fixtures removidas.
- Files changed: apps/web/src/components/app/notifications-bell.tsx, apps/web/src/lib/notifications.ts, apps/web/src/lib/notification-copy.ts, apps/web/src/lib/notifications-server.ts, apps/web/src/lib/__tests__/notifications.test.ts, apps/web/CLAUDE.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - `useEffectEvent` pode ser chamado de dentro de outro effect event (`poll` → `applyPoll`) e de callbacks de `setInterval`/listeners criados no effect — o lint do React Compiler aceita; o que ele proíbe é chamar de handlers de evento/render.
  - `pollSince` sem `sinceId` devolve `items: []` de propósito; conta sem nenhuma notificação fica com baseline nula, então a primeira notificação da vida do usuário só vira toast porque o client detecta `established && id === null && latestId` e busca a primeira página.
  - Usuários locais de smoke test: us002/us003/us008 têm aceite da política vigente; os `smoke-2fa-*` e us001 caem em `/aceite-politica`.
  - A tela do job (`/projects/{id}/predict/jobs/{jobId}`) não foi testada no browser (nenhum training_job no banco local para esse usuário) — a supressão do toast ali está coberta só pelo teste unitário de `shouldToastNotification`; a US-010 mexe nessa tela e pode validar de ponta a ponta.
---

## 2026-09-05 05:00 - US-009
- Worker grava a notificação de treino na MESMA transação do status: novo `jobs/notification_copy.py` (lê o fixture compartilhado `apps/web/src/lib/__fixtures__/notification-copy.json` por env → raiz do repo → `/app/web-fixtures/`, com `EMBEDDED_COPY` idêntico como fallback; `training_succeeded`/`training_failed` devolvem `(title, body)`; `format_duration` "2 min"/"1 h 05 min"; `format_metric_value` no formato da tela de progresso — F1 2–3 casas, RMSE até 2, MAPE %; `failure_reason` = primeira frase da error_message, ≤ 140 chars, inicial minúscula quando não é sigla; sem métrica primária a frase da métrica sai do corpo).
- `_notify_training_outcome` em model_train.py: INSERT … SELECT em `notifications` para todos os users da org, `dedupe_key = training:<job_id>`, `ON CONFLICT … DO NOTHING RETURNING id`, sem commit; chamado antes do `conn.commit()` no sucesso (href `/projects/{id}/predict`, metadata trainingJobId/modelId/durationSeconds/metricName/metricValue + projectName) e na falha (href da tela do job, metadata trainingJobId + projectName). Ids guardados em `notification_ids` e logados em debug (insumo da US-012).
- Dockerfile do worker: estágio vazio `FROM scratch AS web-fixtures` + `COPY --from=web-fixtures / /app/web-fixtures/`; `docker-compose.yml` e `docker-compose.prod.yml` passam `build.additional_contexts.web-fixtures: ./apps/web/src/lib/__fixtures__`. Build via compose validado (fixture dentro da imagem, `load_copy()` carrega do arquivo e é igual ao embutido); SQL validado no Postgres local em BEGIN/ROLLBACK (1ª execução cria 1 por usuário, 2ª cria 0).
- Testes: `tests/test_notification_copy.py` (novo) e `tests/test_model_train.py` (NotificationsConn com índice único parcial; run_train_job na falha e _train_and_persist no sucesso com trainer falso, checando ordem update → audit → notificação → commit). 458 testes verdes no container.
- Files changed: apps/worker/jobs/notification_copy.py (novo), apps/worker/jobs/model_train.py, apps/worker/tests/test_notification_copy.py (novo), apps/worker/tests/test_model_train.py, apps/worker/Dockerfile, apps/worker/CLAUDE.md, docker-compose.yml, docker-compose.prod.yml.
- **Learnings for future iterations:**
  - Divergência a resolver na web (não tocada aqui): `formatNotificationMetricValue` em lib/notifications.ts trata `f1`/`f1_macro` como porcentagem ("91%") no bloco Detalhes do modal, enquanto o corpo gravado pelo worker (e a tela de progresso) mostra "F1: 0,91". Se alinhar, tirar f1/f1_macro de PERCENT_METRICS lá e ajustar o teste da web.
  - Coolify reescreve o compose no deploy: se o `additional_contexts` não sobreviver, o build NÃO quebra (estágio scratch vazio) e o worker usa `EMBEDDED_COPY`; conferir no primeiro deploy com `python -c "from jobs.notification_copy import *; print(IMAGE_FIXTURE_PATH.is_file())"` dentro do container do worker.
  - `psycopg.types.json.Jsonb(obj)` expõe `.obj` — útil para fakes de conexão inspecionarem a metadata sem serializar.
  - `_train_and_persist` é testável sem banco: monkeypatch de `model_train.upload_dir`, `model_train.pd.read_parquet` e `monkeypatch.setitem(model_train.TRAINERS, ...)` com `_training_result()` (pipeline=None serializa no joblib); a conexão falsa responde por prefixo de SQL (`_SuccessConn` em test_model_train.py).
  - `python3` do host (3.14) roda `jobs/notification_copy.py` direto (sem deps pesadas) — bom para sanity check rápido antes de subir o container.
---

## 2026-09-05 05:10 - US-010
- `training-progress.tsx`: removido o `useEffect` de `router.replace('/projects/{id}/predict')` (e o estado `enterpriseOpen`, que só existia para segurar esse redirect; `onOpenChange` não é mais passado ao `ProductionCtaBanner`). Estado `succeeded` mostra botão primário "Ver relatório do modelo" (→ `/projects/{id}/predict`) e botão ghost "Voltar aos projetos" (→ `/projects`) num wrapper `[data-training-succeeded-actions]`; subtítulo trocado de "Redirecionando…" para "Seu modelo está pronto. Abra o relatório agora ou volte depois pela notificação.". Banner de produção e métricas mantidos.
- `page.tsx` do job: `await resolveByDedupeKey(user.id, \`training:${job.id}\`)` quando o status carregado é `succeeded`/`failed`, antes de renderizar.
- Novo `src/lib/training-progress-copy.ts` (client-safe): `TRAINING_PROGRESS_COPY` (succeededTitle/succeededSubtitle/viewReport/backToProjects), `trainingReportHref(projectId)`, `PROJECTS_HREF`. Teste `training-progress-copy.test.ts` trava `viewReport === notificationActionLabel("training.succeeded")`, proíbe "redirecion" na copy e confere os hrefs. Suíte: 74 arquivos, 967 testes.
- Verificado via CDP (driver /tmp/us010-verify.mjs, dev em :3111, usuário us003fc, job `dddd…0214` do projeto "US-018 multi-série"): notificação `training.succeeded` com `dedupe_key training:<jobId>` inserida via SQL (mesmo formato do worker) com /projects aberta → em 25 s badge "1" + toast "Abrir"; navegar para a tela do job → `read_at` gravado, badge ausente, h1 "Treinamento concluído", dois links com os hrefs certos, banner de produção com MAE/MAPE; 8 s depois ainda na URL do job (sem redirect); "Voltar aos projetos" leva a /projects com badge zerado. Zero erros de console (só os warnings de dev do Better Auth). Sessão e fixtures removidas. O treino real via worker não foi disparado — a gravação da notificação pelo worker já foi validada na US-009 e aqui foi simulada pelo mesmo INSERT.
- Files changed: apps/web/src/app/(project)/projects/[projectId]/predict/jobs/[jobId]/training-progress.tsx, apps/web/src/app/(project)/projects/[projectId]/predict/jobs/[jobId]/page.tsx, apps/web/src/lib/training-progress-copy.ts (novo), apps/web/src/lib/__tests__/training-progress-copy.test.ts (novo), apps/web/CLAUDE.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - O sino faz uma consulta imediata no mount, então uma notificação resolvida no servidor (`resolveByDedupeKey` no `page.tsx`) já chega com badge zerado na navegação full-page — não precisa de evento client-side para "derrubar" o badge.
  - Usuário `us003fc@teste.com` (org `eced2988…`) tem vários `training_jobs` succeeded com modelo apontando para o job (`dddd0000-…-0214`, `-0312`, `-0310`, `-0203`) e aceite da política vigente — serve para smoke test da tela do job.
  - Os jobs fixture não têm `candidates`, então a tela concluída mostra a lista fallback "Aguardando" (pré-existente; em job real o worker preenche).
  - `router` continua necessário em training-progress.tsx para o `router.refresh()` do polling de 2 s e do retry — só o `router.replace` saiu.
---
## 2026-09-05 05:20 - US-011
- `buildTrainingOutcomeEmail({ to, title, body, actionLabel, actionUrl })` em `lib/email-templates.ts`: mesmo layout dos e-mails de senha (marca, h1 = título, um parágrafo, botão azul com link absoluto, "copie e cole", rodapé explicando por que recebeu); assunto = título; título/corpo/rótulo passam por `escapeHtml` (nome do projeto é input do usuário).
- Lógica da rota em `lib/notification-email.ts` (`handleNotificationEmail(request, deps)`, padrão api-predict): `INTERNAL_API_TOKEN` ausente → 503 + `console.error` 1×/processo (`resetNotificationEmailWarnings`); `X-Internal-Token` comparado por `internalTokenMatches` (Buffer + `timingSafeEqual`, tamanhos diferentes → 401 sem comparar); body zod `{ notificationId: z.uuid() }` → 400; `appOrigin()` nulo → 503; notificação inexistente → 404; tipo fora de `training.*` ou `emailed_at` preenchido → 204; `claimEmail` (UPDATE … WHERE emailed_at IS NULL RETURNING) false → 204; envio falhou → `releaseEmail` + 502 com `EMAIL_PROVIDER_ERROR_MESSAGE`; sucesso → 200 `{ sent: true }`. Tudo com `Cache-Control: no-store`.
- `notifications-server.ts` ganhou `getNotificationForEmail(id)` (innerJoin `users.email`, sem escopo de usuário), `claimNotificationEmail(id)` e `releaseNotificationEmail(id)`. `route.ts` em `app/api/internal/notifications/email/` só faz o wiring + try/catch 500.
- `proxy.ts`: `INTERNAL_PATHS = ["/api/internal/"]` checado antes da sessão (fora de PUBLIC_PATHS); `/api/internal` sem barra continua protegido. Teste anti-CORS (`internal-routes-cors.test.ts`) ganhou `/api/internal/` em `EXCLUDED_PREFIXES` (rota server-to-server por token, como /api/v1 e /api/mcp).
- Testes: `notification-email.test.ts` (16: token/503/400/404/envio único + emailed_at/204 na 2ª/MFA 204/já enviada/claim perdido/502 com release/origem nula), +5 em `email-templates.test.ts` (snapshot leve: título, corpo, URL, sem `{`, escape), +2 no proxy, +1 integração em `notifications-server.test.ts` (join com e-mail, claim único, release). Suíte: 75 arquivos, 992 testes. Smoke ponta a ponta com deps reais (Postgres local + `sendEmail` em modo dev-log) via vitest temporário: 401/401/200 (log com assunto, destinatário e URL absoluta)/204, `emailed_at` gravado, 1 e-mail só.
- Docs: `.env.example` da raiz (não existe `apps/web/.env.example`) documenta `INTERNAL_API_TOKEN`; SECURITY.md ganhou o bullet "Rotas internas server-to-server" em "Modelo de acesso" e menção em "CORS e origem"; apps/web/CLAUDE.md descreve o fluxo na seção de notificações.
- Files changed: apps/web/src/lib/email-templates.ts, apps/web/src/lib/notification-email.ts (novo), apps/web/src/lib/notifications-server.ts, apps/web/src/app/api/internal/notifications/email/route.ts (novo), apps/web/src/proxy.ts, apps/web/src/lib/__tests__/{notification-email (novo),email-templates,proxy-public-paths,notifications-server,internal-routes-cors}.test.ts, apps/web/CLAUDE.md, .env.example, SECURITY.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Toda rota nova em `src/app/**/route.ts` cai no teste `internal-routes-cors.test.ts` (exige `OPTIONS` + política de origem). Rota server-to-server por token entra em `EXCLUDED_PREFIXES` lá e é documentada em SECURITY.md "CORS e origem" — senão a suíte quebra com "não exporta OPTIONS".
  - Script tsx com `--conditions=react-server` NÃO funciona para módulos que importam `lib/notifications.ts` (puxa `lucide-react` → `react.createContext is not a function`). Para smoke ponta a ponta com Postgres real, usar um arquivo vitest temporário (`vi.mock("server-only")` + `connectTestDb`) e apagar depois.
  - Ordem claim → send → release: reservar `emailed_at` ANTES do envio evita e-mail duplo em retry concorrente; a falha do provedor desfaz a reserva para o retry do worker (US-012, 1 retry em 5xx) poder reenviar.
  - `vi.stubEnv("X", "")` é o jeito de simular env ausente (o código faz `(env ?? "").trim() === ""`).
  - `notificationActionLabel("training.failed")` = "Ver detalhes do treinamento" — o botão do e-mail reutiliza esse rótulo; sem tipo conhecido cai em "Abrir no AutoML".
---

## 2026-09-05 13:00 - US-012
- `apps/worker/jobs/notifications_email.py` (novo): `dispatch_notification_emails(ids, *, sleep=time.sleep)` faz POST `{WEB_INTERNAL_URL}/api/internal/notifications/email` por id com `X-Internal-Token`, body `{notificationId}`, timeout 5 s, 1 retry após 2 s em erro de rede/timeout/5xx (4xx não repete); nunca lança — toda falha vira `logger.warning` com o id; sem `WEB_INTERNAL_URL`/`INTERNAL_API_TOKEN` avisa UMA vez por processo e não chama (lista vazia nem lê a env). Só urllib (o worker não tem cliente HTTP).
- `model_train.py`: os dois `logger.debug` placeholder de US-009 viraram `dispatch_notification_emails(notification_ids)` imediatamente DEPOIS do `conn.commit()` de sucesso (`_train_and_persist`) e de falha (`run_train_job`, antes do `raise`).
- Compose (dev e prod): `INTERNAL_API_TOKEN` para web e worker (`${…:-}`, opcional: sem ele a rota responde 503 e o worker não chama — não quebra deploy existente), `WEB_INTERNAL_URL: ${WEB_INTERNAL_URL:-http://web:3000}` no worker. Validado com `docker compose config` nos dois arquivos.
- Docs: README.md (tabela de variáveis do Coolify: `INTERNAL_API_TOKEN`, `WEB_INTERNAL_URL`), `.env.example` da raiz (`WEB_INTERNAL_URL` ao lado do token), apps/worker/CLAUDE.md (bullet do módulo). Não existem `apps/worker/README.md` nem `apps/worker/.env.example` — o projeto documenta tudo no README/.env.example da raiz + CLAUDE.md do worker; documentei lá em vez de criar arquivos paralelos.
- Testes: `tests/test_notifications_email.py` (novo, 14: header/body/timeout, barra final, lista vazia, retry rede/timeout/5xx, 4xx sem retry, 2 falhas não propagam, falha de um id não bloqueia o próximo, exceção inesperada, UUID, sem env 1 aviso, `email_endpoint`); `test_model_train.py` ganhou `_record_email_dispatch` (troca o dispatch por item no `log` da FakeConn) e afirma `commit` → `("dispatch-email", ["n-1","n-2"])` no sucesso e na falha, +teste de falha com erro do treino preservado, +retry sem notificação nova → dispatch `[]`. Suíte do worker: 474 passed, 1 skipped (61 s).
- Smoke ponta a ponta (sem browser): `next dev` com `INTERNAL_API_TOKEN` e `RESEND_API_KEY=` vazio, notificação sintética `training.succeeded` inserida no Postgres local, módulo REAL do worker rodado do host (stdlib) contra `http://localhost:3000`: 1ª chamada 200 + e-mail "Modelo de Smoke US-012 pronto" logado no web (De/Para/Assunto/URL absoluta) + `emailed_at` gravado; 2ª chamada 204; token errado → 401 sem retry. Linha sintética apagada depois. NÃO foi feito treino real pelo browser (sem ferramenta de browser; o container do worker em execução usa a imagem/env antigas) — verificação manual pendente: `docker compose up --build worker` com `INTERNAL_API_TOKEN` no `.env`, treinar um projeto e conferir o log do web.
- Files changed: apps/worker/jobs/notifications_email.py (novo), apps/worker/jobs/model_train.py, apps/worker/tests/test_notifications_email.py (novo), apps/worker/tests/test_model_train.py, apps/worker/CLAUDE.md, docker-compose.yml, docker-compose.prod.yml, README.md, .env.example, prd.json, progress.txt.
- **Learnings for future iterations:**
  - O worker roda do HOST com `python3` puro para módulos só-stdlib (`cd apps/worker && python3 -c "from jobs.x import ..."`) — útil para smoke contra o web local sem container; pandas/psycopg exigem o container.
  - `next dev` local: `cd apps/web && DATABASE_URL=postgresql://akkio:akkio@localhost:5433/akkio REDIS_URL=redis://localhost:6379 AUTH_SECRET=x BETTER_AUTH_URL=http://localhost:3000 <envs da história> npx next dev -p 3000` (não há `.env` em apps/web e o da raiz aponta para hostnames do Docker). Primeira rota compila em ~5 s; os avisos "Node.js API … Edge Runtime" de example-datasets.ts são ruído pré-existente.
  - urllib: `HTTPError` é subclasse de `URLError`/`OSError` — capturar `HTTPError` ANTES do `except OSError`, senão 4xx vira "erro de rede" e ganha retry.
  - Os testes de `test_model_train.py` que passam por `run_train_job`/`_train_and_persist` chamam o dispatch real se não fizerem monkeypatch — sem env ele só loga um warning e devolve, mas para afirmar ordem use `_record_email_dispatch`.
  - `.env` da raiz do repo local tem `RESEND_API_KEY` REAL: em smoke de e-mail sempre exportar `RESEND_API_KEY=` vazio inline para não disparar e-mail de verdade.
---

## 2026-09-05 09:59 - US-013
- Colunas `users.mfa_prompt_dismissed_at` e `users.mfa_prompt_snoozed_until` (timestamptz, nulas) no schema; migration `0029_users-mfa-prompt` gerada e aplicada no banco local.
- Módulo puro client-safe `src/lib/mfa-suggestion.ts`: `MFA_SNOOZE_DAYS = 30`, `MFA_SUGGESTION_MIN_ACCOUNT_AGE_MS` (24 h), `shouldSuggestMfa(input)` e `snoozeUntil(now)`; tipo `MfaSuggestionInput` exportado para o módulo server-only da US-014.
- Testes Vitest `src/lib/__tests__/mfa-suggestion.test.ts` (12 casos): Google → false, 2FA ativo → false, 1 h sem/com modelo, 25 h, limiar de 24 h inclusivo, dismissed, snooze futuro/expirado/que vence agora, snooze expirado não sobrepõe dismiss nem conta nova, `snoozeUntil` sem mutar a entrada.
- Files changed: apps/web/src/db/schema.ts, apps/web/drizzle/0029_users-mfa-prompt.sql, apps/web/drizzle/meta/{_journal.json,0029_snapshot.json}, apps/web/src/lib/mfa-suggestion.ts, apps/web/src/lib/__tests__/mfa-suggestion.test.ts, prd.json, progress.txt.
- **Learnings for future iterations:**
  - `shouldSuggestMfa` recebe `now` injetado e compara com `getTime()` — o limiar de 24 h e o vencimento do snooze são inclusivos (`>=` / `snoozedUntil <= now`); a US-014 deve passar `new Date()` uma vez e reutilizar em `snoozeUntil` para não haver drift.
  - Os campos drizzle são `users.mfaPromptDismissedAt` / `users.mfaPromptSnoozedUntil`; `hasPasswordAccount` vem de `accounts.providerId = "credential"` (mesma regra do `SecurityCard`), e `hasTrainedModel` de `training_jobs.status = 'succeeded'` do usuário — o módulo puro só recebe booleanos.
  - Padrão "Origem/atributos de um cadastro: nunca coluna nova em users" (Codebase Patterns) vale para atributos de marketing; estado funcional do usuário (snooze/dismiss) fica em `users` mesmo, como decidido na PRD.
---

## 2026-09-05 10:20 - US-014
- Novo `src/lib/mfa-suggestion-server.ts` (server-only): `maybeCreateMfaSuggestion(userId, now?)` carrega users + conta `credential` + `exists(models where org_id)`, aplica `shouldSuggestMfa`, cria `security.mfa_suggestion` (dedupe `mfa-suggestion`, `replaceIfRead`) e audita `security.mfa_suggested` só quando criou; `resolveMfaSuggestion(userId)`; memo de 1 h `maybeCreateMfaSuggestionOncePerHour(userId, { remember })` com cookie httpOnly `mfa-suggestion-checked` (valor = hash sha256 curto do userId).
- Gatilhos: `loginAuditPlugin` (auth.ts) após `auth.login` com método email (try/catch); `pollNotifications` (remember: true); `(app)/projects/page.tsx` (remember: false). Ao gravar `auth.2fa_enabled`, `resolveMfaSuggestion`.
- docs/metricas-uso.md: linha de catálogo `security.mfa_suggested` + seção 24 "Funil MFA" (2 consultas validadas em BEGIN/ROLLBACK com fixtures sintéticas).
- Teste de integração `__tests__/mfa-suggestion-server.test.ts` (7 casos, helper test-db): elegível cria 1 + audita; repetir não duplica; Google não cria; 1 h sem modelo → não, `now`+24 h → sim; usuário inexistente; resolve idempotente; cookie-memo.
- Verificado no browser (CDP, dev server 3111): login com senha de conta com created_at −25 h → /projects com badge "1", aria-label "Notificações, 1 não lida", popover com "Proteja sua conta com verificação em duas etapas"; cookie `mfa-suggestion-checked` httpOnly/Lax/ttl ~3600 s presente logo após o mount; conta só Google (sessão injetada) → sem badge. Fixtures removidas do banco.
- Files changed: apps/web/src/lib/mfa-suggestion-server.ts, apps/web/src/lib/__tests__/mfa-suggestion-server.test.ts, apps/web/src/lib/auth.ts, apps/web/src/components/app/notifications-actions.ts, apps/web/src/app/(app)/projects/page.tsx, apps/web/CLAUDE.md, docs/metricas-uso.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - `cookies().set` só funciona em server action/route handler — em Server Component (page.tsx) o Next lança. Padrão: a página só LÊ o cookie e passa `remember: false`; a action grava. O sino já faz o primeiro `pollNotifications` no mount (o cookie apareceu antes dos 30 s), então a memória entra na hora.
  - `auth.2fa_enabled` NÃO é decidido em `twoFactorManageAuditAction` (essa cobre disable/backup-codes): vem do ramo `twoFactorVerifyAuditAction` (verify-totp com sessão prévia) em `loginAuditPlugin` — foi ali que entrou o `resolveMfaSuggestion`.
  - `npx prettier --write src/lib/auth.ts` reformatou 4 hunks pré-existentes (o arquivo não estava prettier-clean); o diff extra é só formatação.
  - Subir o dev server com orgs fixture novas dispara `[seed-examples]` (3 datasets por org): o cleanup precisa apagar `dataset_columns`/`dataset_versions`/`datasets` antes de `users`/`organizations` (nenhuma FK para `organizations` é cascade — confdeltype 'a' em todas).
  - Login real via CDP: `#email`/`#password` + `focus()` + `Input.insertText` + clique no `button[type=submit]`; esperar `location.pathname === "/projects"` (~3 s com bcrypt 12 rounds). Conta credential precisa de `email_verified = true`, `approved_at`, `policy_acceptances` na versão atual e `accounts.password` com hash bcrypt (`bcryptjs.hashSync(senha, 12)`). Driver em /tmp/us014-login.mjs.
---
## 2026-09-05 10:25 - US-015
- `lib/two-factor-setup.ts`: `TWO_FACTOR_ENABLE_QUERY_PARAM` ("ativar-2fa"), `TWO_FACTOR_ENABLE_HREF`, `SECURITY_CARD_ID` ("seguranca"), `wantsTwoFactorEnable(param)` (presente e ≠ ""/"0"; aceita array) e `stripTwoFactorEnableParam(search)`. `MFA_SUGGESTION_NOTIFICATION.href` agora deriva de `TWO_FACTOR_ENABLE_HREF`.
- `notification-copy.ts`: `MFA_SUGGESTION_ACTIONS` (activate/snooze/dismiss + `snoozedToast` = "Vamos lembrar você em 30 dias" derivado de `MFA_SNOOZE_DAYS`, `dismissedToast`), `NOTIFICATIONS_COPY.actionError`; `ACTION_LABELS` de notifications.ts reutiliza `activate`.
- `mfa-suggestion-server.ts`: `snoozeMfaSuggestionForUser(userId, now?)` (UPDATE `mfa_prompt_snoozed_until = snoozeUntil(now)` RETURNING org_id → `resolveMfaSuggestion` → audit `security.mfa_suggestion_snoozed` `{ snoozedUntil }`) e `dismissMfaSuggestionForUser` (idem com `mfa_prompt_dismissed_at`, audit `security.mfa_suggestion_dismissed`); usuário inexistente → null/false sem lançar. Actions `snoozeMfaSuggestion()`/`dismissMfaSuggestion()` em notifications-actions.ts (escopo da sessão).
- `notification-dialog.tsx`: rodapé de `security.mfa_suggestion` com "Não sugerir de novo" (ghost) / "Lembrar depois" (outline) / "Ativar agora" (primário); `pending` desabilita os três; fecha e `toast()` só após a action; erro → `toast.error`.
- `two-factor-enable-dialog.tsx`: props `defaultOpen` e `onClosed`, atributo `data-two-factor-enable-dialog`. `security-card.tsx`: prop `autoOpenEnable`, `id="seguranca"` + `scroll-mt-6` + ref no Card; 2FA desligada → `defaultOpen`, ao fechar `replaceState` sem o param; 2FA ativa → `scrollIntoView` em effect. `settings/page.tsx` recebe `PageProps<"/settings">` e lê `searchParams["ativar-2fa"]`.
- Testes: +3 em two-factor-setup.test.ts, +1 em notifications.test.ts, +3 integração em mfa-suggestion-server.test.ts (snooze grava now+30d/lê/audita/não elegível a 29 d/recria a 31 d com id novo e 2º `security.mfa_suggested`; dismiss nunca mais; usuário fantasma). Suíte: 77 arquivos, 1018 testes.
- Docs: apps/web/CLAUDE.md (bullets "Ações da sugestão de MFA" e "Abertura direta do diálogo de 2FA" + parágrafo no card Segurança), docs/metricas-uso.md (2 linhas no catálogo + nota na §24).
- Verificado via CDP (driver /tmp/us015-verify.mjs, dev em :3111, usuário us003fc com sessão injetada, notificação inserida por SQL): popover mostra só o resumo (1 botão = a linha); modal com os 3 botões na ordem certa; "Ativar agora" → `/settings?ativar-2fa=1` com o diálogo "Passo 1 de 4 · Confirme sua senha" e foco no campo de senha; Esc fecha e a URL vira `/settings` (sem entrada de histórico extra); reload não reabre; "Lembrar depois" → modal fecha, toast "Vamos lembrar você em 30 dias", badge zera, `mfa_prompt_snoozed_until` = +30 d, `read_at` gravado, audit snoozed; "Não sugerir de novo" → toast "Você pode ativar quando quiser em Configurações → Segurança", `mfa_prompt_dismissed_at` gravado, audit dismissed; conta só Google em `/settings?ativar-2fa=1` → sem diálogo e sem card Segurança (Conta, Uso e limites, Sessão), página normal; `two_factor_enabled = true` em viewport de 500 px → sem diálogo, `main.scrollTop` 800 e o card Segurança no topo (top 24 px) mostrando "Ativado". Zero erros de console. Fixtures (sessões, usuário Google, notificação, audit) removidas.
- Files changed: apps/web/src/lib/{two-factor-setup,notification-copy,notifications,mfa-suggestion-server}.ts, apps/web/src/components/app/{notifications-actions.ts,notification-dialog.tsx,two-factor-enable-dialog.tsx,security-card.tsx}, apps/web/src/app/(app)/settings/page.tsx, apps/web/src/lib/__tests__/{two-factor-setup,notifications,mfa-suggestion-server}.test.ts, apps/web/CLAUDE.md, docs/metricas-uso.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Conta só Google NÃO tem o card Segurança (decisão anterior da página), então "rola até Segurança" é vacuoso para ela: o param é simplesmente ignorado. O caso "já tem 2FA" é o que rola (`scrollIntoView` no Card via ref — o `Card` do shadcn espalha props no `div`, então `ref` funciona sem `forwardRef`).
  - O sino marca lida (otimista + `markNotificationRead`) ANTES do modal abrir, então snooze/dismiss não precisam mexer no badge; o `resolveMfaSuggestion` dentro deles é redundante no fluxo normal e vale para chamadas fora do modal.
  - Radix `Portal` só monta após `useLayoutEffect` no client: `Dialog open` verdadeiro já no primeiro render (via `useState(defaultOpen)`) não aparece no SSR nem gera aviso de hidratação; o `autoFocus` do input leva o foco na abertura.
  - `history.length` não cresce com `replaceState` — o driver conferiu 3 entradas antes e depois (about:blank → /projects → /settings).
  - Fixture rápida do modal de MFA: INSERT direto em `notifications` com `dedupe_key = 'mfa-suggestion'` (formato igual ao de `maybeCreateMfaSuggestion`), sem precisar de conta com 25 h nem login; `two_factor_enabled` pode ser ligado por UPDATE para testar o ramo "já ativo" porque `getTwoFactorStatus` tem try/catch (sem linha em `two_factors` devolve nulos).
---

## 2026-09-05 10:30 - US-016
- Colunas `users.onboarding_welcome_seen_at`, `onboarding_dismissed_at`, `onboarding_completed_at` (timestamptz, nulas) no schema; migration `0030_users-onboarding` gerada + `UPDATE users ... WHERE EXISTS (models da org)` manual no fim do arquivo; aplicada no banco local (16 users, 4 com modelo → 4 dispensados).
- Módulo puro client-safe `src/lib/onboarding.ts`: `ONBOARDING_STEPS` (4 passos com key/title/description/ctaLabel vindos da copy), `ONBOARDING_TOTAL_STEPS`, `computeOnboarding(input)` → `{ steps, doneCount, showWelcome, showChecklist, justCompleted }`, `nextOnboardingStep(state)`; tipos `OnboardingInput`/`OnboardingState`/`OnboardingStepKey` exportados para a US-017.
- `src/lib/onboarding-copy.ts`: `ONBOARDING_STEP_COPY`, `ONBOARDING_USE_STEP_HINT`, `WELCOME_PANEL_COPY`, `ONBOARDING_CHECKLIST_COPY`, `ONBOARDING_STEP_BANNER_COPY`, `FIRST_PROJECT_NAME_PLACEHOLDER`, `welcomeTitle(nome)`, `onboardingProgressLabel(done, total)` — já cobre a copy das US-018/019/020.
- Testes Vitest `src/lib/__tests__/onboarding.test.ts` (21 casos): usuário novo, cada passo em sequência, 'use' por predição OU endpoint, dispensado, completado/justCompleted uma vez, backfill, copy travada + termos vetados.
- Files changed: apps/web/src/db/schema.ts, apps/web/drizzle/0030_users-onboarding.sql, apps/web/drizzle/meta/{_journal.json,0030_snapshot.json}, apps/web/src/lib/onboarding.ts, apps/web/src/lib/onboarding-copy.ts, apps/web/src/lib/__tests__/onboarding.test.ts, apps/web/CLAUDE.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - `showWelcome`/`showChecklist`/`justCompleted` são todos falsos com `dismissedAt` OU `completedAt` (`closed`) — a US-017 só precisa carregar as 3 datas e 5 contadores por `org_id` (`projects`, `projects.dataset_id not null`, `models`, `inference_usage`, `deployments.status='published'`).
  - A copy dos passos NÃO usa a palavra "dataset" (termo vetado no teste) — o passo chama-se "Enviar dados"; a key interna continua `dataset`.
  - `welcomeTitle` recebe `user.name` cru: faz trim, pega o primeiro token e cai no título sem vírgula quando vazio.
  - Não há UI ainda: verificação no browser fica para as US-018/019.
---

## 2026-09-05 10:40 - US-017
- `src/lib/onboarding-server.ts` (server-only): `getOnboardingState({ id, orgId })` → `OnboardingSnapshot` (`computeOnboarding` + `nextStep` + `nextProjectId`) com UMA consulta agregada (subselects por org: projetos ativos, projetos com dataset, models, deployments publicados; `sum(inference_usage.count)` por usuário) + segunda consulta só para o `nextProjectId` conforme o passo pendente (dataset → mais recente sem dataset; model → mais recente com dataset e sem modelo; use → mais recente com modelo). `markWelcomeSeenForUser`, `dismissOnboardingForUser` (audita `onboarding.dismissed` com `{ doneCount, doneSteps }`), `markOnboardingCompletedForUser` (audita `onboarding.completed`) — `UPDATE ... WHERE col IS NULL RETURNING`, idempotentes.
- Server actions `(app)/projects/onboarding-actions.ts`: `markWelcomeSeen()`, `dismissOnboarding()`, `markOnboardingCompleted()` — sessão + `requestMeta(await headers())`, sem `revalidatePath` (as páginas do (app) são dinâmicas; a UI vai esconder o card localmente).
- Teste de integração `__tests__/onboarding-server.test.ts` (10 casos, banco local): 0 de 4 + showWelcome; projeto → 1 de 4 e nextProjectId; projeto+dataset → 2 de 4; arquivado não conta; modelo → 3 de 4 aponta para o projeto com modelo; endpoint publicado OU inference fecham 4 de 4/justCompleted; as três actions idempotentes com auditoria única; usuário inexistente devolve fechado.
- Files changed: apps/web/src/lib/onboarding-server.ts, apps/web/src/app/(app)/projects/onboarding-actions.ts, apps/web/src/lib/__tests__/onboarding-server.test.ts, apps/web/src/lib/onboarding.ts (comentário do inferenceCount), apps/web/CLAUDE.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Primeira versão dos subselects correlacionava `${projects.orgId} = ${users.orgId}` e TODOS os testes vieram com doneCount 3: no select de tabela única o drizzle omite o nome da tabela e o predicado virou `"org_id" = "org_id"`. Corrigido filtrando por parâmetro (`${user.orgId}`), que é o que a sessão já tem.
  - `inference_usage` é chaveada por (user_id, year_month) com `count` — não tem org_id; o passo "use" soma o `count` do usuário da sessão (o AC pede "do usuário").
  - `exists()`/`notExists()` do drizzle aceitam um subquery builder (`db.select(...).from(models).where(eq(models.projectId, projects.id))`) — aí a correlação FUNCIONA porque a query externa tem só `projects` e a interna só `models`.
  - Sem UI ainda: a US-018/019 consome `getOnboardingState` na `(app)/projects/page.tsx` e as três actions; verificação no browser fica para lá.
---

## 2026-09-05 10:45 - US-018
- `components/app/welcome-panel.tsx` (client): Dialog aberto na montagem quando `showWelcome` (`useState(showWelcome)`), título `welcomeTitle(userName)`, lead `WELCOME_PANEL_COPY.lead`, os 4 passos de `ONBOARDING_STEPS` em `<ol>` grid (1 coluna, `sm:grid-cols-2`) com ícones `ONBOARDING_STEP_ICONS` (FolderPlus/FileUp/BarChart3/Plug, record exportado), rodapé "Explorar por conta própria" (outline, fecha) e "Criar meu primeiro projeto" (fecha + `onCreateProject`). Fechamento por qualquer caminho passa por `close()` com `useRef` de guarda → `markWelcomeSeen()` exatamente uma vez. `DialogContent` com `max-h-[calc(100dvh-2rem)] overflow-y-auto` e `sm:max-w-lg`; atributos `data-welcome-panel`, `data-welcome-step`, `data-welcome-create` para testes.
- `(app)/projects/page.tsx`: `getOnboardingState({ id, orgId })` disparado antes das consultas de projetos e aguardado no fim; `ProjectsView` recebe `onboarding: OnboardingSnapshot` e `userName` e só renderiza `<WelcomePanel>` quando `onboarding.showWelcome` (nada muda para os demais).
- Verificado via CDP (driver /tmp/us018-verify.mjs + /tmp/us018-scroll.mjs, dev em :3111, usuário us009@teste.com com colunas zeradas por SQL e sessão injetada): 1280 px → dialog role=dialog, título "Bem-vindo(a) ao AutoML, US009", 4 passos em 2×2, botões na ordem certa; "Criar meu primeiro projeto" fecha o painel e abre o "Novo projeto" com foco no input (1 diálogo aberto, `pointer-events` volta ao normal após Esc); `onboarding_welcome_seen_at` gravado com exatamente 1 POST de server action; reload não reabre; sessão nova com cookies limpos ("outro navegador") não reabre; Esc, X e "Explorar" fecham e gravam 1 vez cada; 375×640 → passos empilhados (left=32 em todos), dialog 343×606; 375×520 → scrollHeight 606 > clientHeight 488, rodapé visível e clicável após rolar; clique fora fecha e grava. Zero erros de console (só warnings dev do Better Auth sobre o secret local). Capturas em /tmp/us018-*.png. Suíte: 79 arquivos, 1049 testes.
- Files changed: apps/web/src/components/app/welcome-panel.tsx (novo), apps/web/src/app/(app)/projects/page.tsx, apps/web/src/app/(app)/projects/projects-view.tsx, apps/web/CLAUDE.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Fechar um Dialog Radix e abrir outro no MESMO handler de clique funciona sem truque (`close(); onCreateProject()`): só um `role=dialog` fica no DOM e o `pointer-events: none` do body é removido quando o segundo fecha. Não precisa de `onCloseAutoFocus`/setTimeout.
  - Contar chamadas de server action no CDP: `Network.requestWillBeSent` com método POST e header `next-action`. O sino faz `pollNotifications` na montagem (também é uma action) — zerar o contador DEPOIS da página estabilizar e ANTES da interação medida.
  - Em 375 px o círculo "N" que aparece por cima do rodapé do modal é o `NEXTJS-PORTAL` (indicador do dev tools do Next, só em dev) — não é da aplicação; `document.elementFromPoint` identifica.
  - `OnboardingSnapshot` não tem `Date` (só booleans/números/strings), então serializa direto do Server Component para o client; o `import type` de `onboarding-server.ts` (server-only) num client component é apagado pelo TS e não puxa `@/db` para o bundle.
  - Usuário bom para smoke de onboarding no banco local: `us009@teste.com` (eb6b451f…, aprovado, política aceita, 0 projetos) — basta zerar as 3 colunas `onboarding_*`.
---

## 2026-09-05 15:10 - US-019
- `components/app/onboarding-checklist.tsx` (client): Card `max-w-6xl` "Primeiros passos" com rótulo "{n} de 4", barra `role="progressbar"` (aria-valuenow/max/text), 4 linhas (`<ol>` grid 1 col → `md:grid-cols-4`) com CheckCircle2 `text-primary`/Circle `text-muted-foreground`, título + descrição de `ONBOARDING_STEPS`; o primeiro pendente leva o CTA: "Criar projeto" (botão → `onCreateProject`, abre o CreateProjectDialog), "Enviar dados" (Link /projects/{id}), "Treinar modelo" e "Usar predições" (Link /projects/{id}/predict, este com o hint `ONBOARDING_USE_STEP_HINT`). X "Ocultar" (`aria-label`) esconde localmente, toast `dismissedToast` e `dismissOnboarding()` uma vez. `justCompleted` → card "Tudo pronto! Você já domina o fluxo." com `<OnboardingConfetti>` e `markOnboardingCompleted()` num `useEffect`.
- `components/app/onboarding-confetti.tsx`: 3 SVGs (rect/circle/rect, primary/emerald/amber) animados com `motion/react` (0,5 s + stagger 0,05 s = 0,6 s, ease-out), estáticos sob `useReducedMotion()`; carregado via `next/dynamic({ ssr: false })` do checklist.
- `lib/onboarding.ts`: `onboardingStepHref(step, nextProjectId)` e `ONBOARDING_CONFETTI` (testados em `__tests__/onboarding.test.ts`, +3 testes → 1052).
- `projects-view.tsx`: `<OnboardingChecklist>` entre o cabeçalho (busca + "Novo projeto") e as Tabs.
- Verificado via CDP (driver /tmp/us019-checklist.mjs + /tmp/us019-reduced.mjs, dev em :3111): us009 0/4 → CTA "Criar projeto" abre o dialog → projeto criado → 1/4 com "Enviar dados" apontando para o projeto novo → Ocultar remove o card na hora, toast com o texto exato, `dismissed_at` gravado com 1 POST de action, reload → nada; us004 2/4 → "Treinar modelo" → /predict; us009b 3/4 → "Usar predições" + hint; +inference_usage → "Tudo pronto" com 3 confetes animados (transforms finais após 1 s), `completed_at` gravado, reload → card não existe; reduced motion → `data-onboarding-confetti="static"`, 3 SVGs sem `style` nem animações; 375 px → 4 linhas empilhadas (mesmo `left`), card abaixo do cabeçalho (h1/busca/"Novo projeto" intactos) e acima das abas. Banco restaurado pelo cleanup do driver. Capturas em /tmp/us019-*.png.
- Files changed: apps/web/src/components/app/onboarding-checklist.tsx (novo), apps/web/src/components/app/onboarding-confetti.tsx (novo), apps/web/src/lib/onboarding.ts, apps/web/src/lib/__tests__/onboarding.test.ts, apps/web/src/app/(app)/projects/projects-view.tsx, apps/web/CLAUDE.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Componente que só existe no client e usa `useReducedMotion()`: `next/dynamic({ ssr: false })` resolve hidratação E tira o `motion` do chunk inicial da página; no CDP o chunk leva ~200 ms para montar — sondar com `waitFor` no seletor, não logo após `Page.reload` (a primeira sondagem "E" do driver veio vazia por isso).
  - `document.getAnimations().length` na página inteira não serve para provar "sem animação" (spinners/transições de outros componentes contam) — chamar `el.getAnimations()` nos próprios elementos.
  - O layout do `(app)` segue com sidebar fixa de 240 px em 375 px (pré-existente desde a US-006): em mobile o conteúdo tem ~135 px; validar empilhamento por `left` igual entre as linhas, não pela largura do card.
  - Em `/tmp` há drivers `us0NN-*.mjs` de PRDs ANTERIORES com os mesmos IDs (ex.: /tmp/us019-verify.mjs é dos mini-charts da home) — conferir o cabeçalho do arquivo antes de reaproveitar.
---

## 2026-09-05 15:15 - US-020
- `components/project/onboarding-step-banner.tsx` (client): banner discreto `role="status"` com ícone FileUp, título `ONBOARDING_STEP_BANNER_COPY.title` ("Passo 2 de 4 — Envie uma planilha para começar"), `TemplateDownloadLinks source="onboarding" label="Baixar planilha de exemplo"` e botão X (`aria-label` "Fechar") que só faz `setHidden(true)` — estado em memória, sem action nem storage. Atributos `data-onboarding-step-banner` / `data-onboarding-step-banner-dismiss`.
- `(project)/projects/[projectId]/page.tsx`: `Promise.all([findLatestModelScoped, getOnboardingState])`; `stepBanner = !model && showDatasetStepBanner(onboarding)`; banner renderizado acima do `<div class="text-center">` do título, `max-w-2xl mx-auto mb-8`.
- `lib/onboarding.ts`: `showDatasetStepBanner(state)` puro (`showChecklist` && passo `dataset` pendente) + 4 testes em `__tests__/onboarding.test.ts`.
- `lib/template-links.ts`: fonte `"onboarding"` em `TEMPLATE_SOURCES` (a rota valida por `z.enum(TEMPLATE_SOURCES)` e audita o `source`; o teste itera todas as fontes). `TemplateDownloadLinks` ganhou prop `label` (default `TEMPLATE_DOWNLOAD_LABEL` = "Baixar planilha modelo").
- `create-project-dialog.tsx`: prop opcional `isFirstProject` → placeholder `FIRST_PROJECT_NAME_PLACEHOLDER`; `projects-view.tsx` passa `projects.length === 0`.
- Verificado via CDP (driver /tmp/us020-verify.mjs, dev em :3111, us009 com 0 projetos e painel já visto): "Novo projeto" → input com placeholder "Meu primeiro projeto", `value` vazio e foco; criar → redirect para /projects/{id} → banner com título exato, links `.xlsx`/`.csv` com `source=onboarding` e `download`, banner.bottom (186) ≤ h1.top (218); `fetch` do link → 200 `application/vnd...sheet` e `audit_logs.metadata.source = onboarding`; X → banner some, h1 fica, 0 POST de action, `dismissed_at` continua null; reload → banner volta; com 1 projeto o placeholder volta a "Ex.: Previsão de churn"; `onboarding_dismissed_at` preenchido → sem banner; 375 px → banner 327 px de largura acima do título. Zero erros de console. Capturas /tmp/us020-*.png. Suíte: 79 arquivos, 1056 testes.
- Files changed: apps/web/src/components/project/onboarding-step-banner.tsx (novo), apps/web/src/app/(project)/projects/[projectId]/page.tsx, apps/web/src/app/(app)/projects/create-project-dialog.tsx, apps/web/src/app/(app)/projects/projects-view.tsx, apps/web/src/components/app/template-download-links.tsx, apps/web/src/lib/template-links.ts, apps/web/src/lib/onboarding.ts, apps/web/src/lib/__tests__/onboarding.test.ts, apps/web/CLAUDE.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Banner escondido "só nesta visita": `useState(false)` num client component é suficiente — sem `sessionStorage`/`useSyncExternalStore` (isso é o padrão do `production-cta-banner`, que é por SESSÃO). Não confundir os dois.
  - Decisão fora do AC literal: com modelo vigente e dataset excluído (`model` não nulo) o banner NÃO aparece — a tela já trata esse caso como "projeto não é novo".
  - `(project)/projects/[projectId]/page.tsx` redireciona para /prepare quando há `datasetId`: um usuário com dados em algum projeto nunca cai nesta tela pelo projeto com dados, mas pode cair por um segundo projeto vazio — aí o passo `dataset` já está feito e o banner some (correto).
  - Em `Runtime.evaluate` com `awaitPromise: true` dá para `fetch` a rota autenticada de dentro da página (cookies vão junto) e ler status/headers — mais simples que montar o cookie no Node.
  - A navbar do projeto sobrepõe o título em 375 px (pré-existente, visível em /tmp/us020-f-375.png) — não é desta história.
---

## 2026-09-05 15:25 - US-021
- `lib/marketing/copy.ts`: `HEADER_SPY_SECTION_IDS` (ids das âncoras de `HEADER_NAV`: casos-de-uso, como-funciona, agente, recursos), `SKIP_TO_CONTENT_LABEL` ("Ir para o conteúdo") e `SKIP_TO_CONTENT_HREF` (`#inicio`); +2 testes em `__tests__/marketing-copy.test.ts` (43 no arquivo, 1058 na suíte).
- `components/marketing/nav-link.tsx`: prop `active` → `aria-current="location"`, `text-foreground` e sublinhado `after:` de 2 px `bg-primary` (opacidade 0→1, 150 ms, `motion-reduce:after:transition-none`); classes `after:` sempre presentes para a saída também transicionar. Menu mobile não passa `active` (inalterado).
- `components/marketing/site-header.tsx`: `useActiveSection(sectionIds)` → `{ activeId, activate }`; IntersectionObserver com `rootMargin: -<header px> 0 0 0` (var `--marketing-header-height` lida do wrapper e convertida rem→px, fallback 64), thresholds 0…1 a cada 0,1, vence a maior `intersectionRect.height`; `activate(id)` no clique fixa o alvo (`pinnedRef`) até o observer eleger o mesmo id ou o usuário rolar (wheel/touchmove/keydown). Compacto recebe `NO_SECTIONS` (nada observado). Todo setState em callback.
- `app/(marketing)/layout.tsx`: skip link como primeiro filho do wrapper, `sr-only focus:not-sr-only focus:fixed focus:top-2 focus:left-2 focus:z-50 …`.
- Orçamento de JS (soma gzip dos `<script src>` da home no `next build`, sem o chunk noModule; medido com `next start -p 3222` + /tmp/us021-measure.mjs): antes 225 168 B (219,9 kB), depois 225 887 B (220,6 kB) → +719 B (+0,7 kB), 14 scripts nos dois; limite da história 3 kB.
- Verificado via CDP (drivers /tmp/us021-verify.mjs, /tmp/us021-click.mjs, /tmp/us021-shot.mjs contra o next dev :3111, 1280×900): topo → nenhum `aria-current`; scrollIntoView em cada seção → destaque correspondente (top=64 px em todas); trilha de 250 em 250 px: `-, Casos de uso ×4, Como funciona ×3, Agente ×2, Recursos ×4`; no fim da página (#comecar, sem item) fica "Recursos" (ainda é a mais visível das observadas); estilo do ativo: color = foreground, `::after` 2 px `rgb(59,94,235)` (= bg do botão primário), transição 0,15 s, inativo opacidade 0; clique em âncora (`el.click()` e mouse real) → destaque no microtask (leitura síncrona ainda null, `await Promise.resolve()` já "Agente"), único valor visto durante a rolagem suave, seção para em top=64; wheel para cima depois do clique → volta a seguir (Casos de uso); Tab a partir do topo → foco no skip link visível (rect 8,8 154×36, fixed, z 50); Enter → hash `#inicio`, Tab seguinte foca "Começar grátis" dentro do hero; reduced motion → transições 1e-05 s; /docs (compact) → nenhum ativo e sem skip link. Zero erros de console. Capturas /tmp/us021-e-skiplink.png e /tmp/us021-agente-viewport.png.
- Files changed: apps/web/src/components/marketing/site-header.tsx, apps/web/src/components/marketing/nav-link.tsx, apps/web/src/app/(marketing)/layout.tsx, apps/web/src/lib/marketing/copy.ts, apps/web/src/lib/__tests__/marketing-copy.test.ts, apps/web/CLAUDE.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - React 19: setState num handler de clique NÃO está no DOM logo após `el.click()` retornar — é aplicado no microtask (antes da pintura). Em drivers CDP, ler depois de `await Promise.resolve()`/rAF; leitura síncrona dá falso negativo.
  - Scroll spy: `intersectionRatio` é ruim para seções mais altas que a viewport (nunca chega perto de 1); usar `intersectionRect.height`. Clique em âncora com `scroll-behavior: smooth` precisa "fixar" o alvo, senão o observer passeia pelas seções intermediárias.
  - `rootMargin` só aceita px/%: converter `--marketing-header-height` (4rem) com o `fontSize` do `html`.
  - `Page.captureScreenshot` com `clip {x:0,y:0}` depois de rolar sai em branco (clip é em coordenadas do documento); para o viewport rolado, capturar SEM clip.
  - Skip link para `#inicio` sem `tabIndex=-1` na seção: o Chrome move o "sequential focus starting point", então o Tab seguinte já cai no primeiro focável do hero — não é preciso focar a seção.
  - Medição de bundle: `next build` + `next start -p 3222` com env inline e /tmp/us021-measure.mjs (fetch do HTML, soma gzip dos scripts sem noModule). Build e `next dev` convivem (`.next/dev` separado).
---

## 2026-09-05 15:45 - US-022
- `lib/signup-ref.ts`: `SIGNUP_REFS = ["home", "home-mobile-bar"]` (comentário explica cada origem); `lib/marketing/links.ts`: `MARKETING_MOBILE_BAR_REF: SignupRef = "home-mobile-bar"`; `lib/marketing/copy.ts`: `MOBILE_CTA_BAR_LABEL` ("Ações rápidas", nome da region), `MOBILE_CTA_BAR_PRIMARY`/`_SECONDARY` (= rótulos do hero). Testes: signup-ref (+2: aceita o valor novo, recusa `home_mobile_bar`/caixa diferente; lista travada), marketing-links (+1: href `/signup?ref=home-mobile-bar`, com invite junto), marketing-copy (+1). Suíte: 1062 testes.
- `components/marketing/site-header.tsx` (full, < md): grupo `flex items-center gap-1 md:hidden` com "Entrar" (ghost `sm`, `loginHref`, `prefetch={false}`) + botão de menu; `SiteHeaderMenu` perdeu a prop `loginHref`.
- `components/marketing/site-header-menu.tsx`: "Começar grátis" `Button size=lg h-11 w-full` como primeiro item depois do SheetHeader, depois as âncoras, depois só o `ContactDialog` (o link "Entrar" saiu do Sheet — está sempre visível no header). Fechar ao escolher âncora e devolver foco ao botão de menu: inalterados (verificado: dialog fecha, foco = "Abrir menu", hash `#agente`).
- Novo `components/marketing/mobile-cta-bar.tsx` (client): `useBetweenHeroAndFinalCta()` — 2 IntersectionObservers (`#inicio` → `heroOut = !isIntersecting`; `#comecar` → `finalIn = isIntersecting`), criados só quando `matchMedia("(max-width: 47.9375rem)")` casa e refeitos/desfeitos no `change` (desktop → estado zerado no callback); visível = `heroOut && !finalIn`. Barra `fixed inset-x-0 bottom-0 z-30 border-t bg-background/95 backdrop-blur pb-[env(safe-area-inset-bottom)] md:hidden` + `motion-safe:animate-in fade-in slide-in-from-bottom-2`; "Começar grátis" (Link prefetch false, `ref=home-mobile-bar`) e `ContactDialog variant=outline` lado a lado (`flex-1 h-11`). Effect marca/desmarca `data-mobile-cta` no `main` (DOM direto; cleanup remove). Exporta `MOBILE_CTA_ATTRIBUTE`.
- `app/(marketing)/page.tsx`: `main` com `max-md:data-[mobile-cta]:pb-20`; `<MobileCtaBar invite={invite} />` depois do `FinalCta`.
- `docs/metricas-uso.md`: catálogo do `auth.signup` e §23 citam `home-mobile-bar`; consulta de participação ganhou `da_barra_mobile` e conta `IN ('home','home-mobile-bar')` como "da home".
- Orçamento de JS (mesmo método de US-021: `next build` + `next start -p 3222` + /tmp/us021-measure.mjs, soma gzip dos `<script src>` sem noModule): US-021 = 225 887 B → US-022 = 226 814 B (+927 B, 14 scripts). Somando US-021 (+719 B): +1 646 B ≈ +1,6 kB gzip (limite 3 kB). Build tem os mesmos 6 avisos "Ecmascript file had an error" (instrumentation/example-datasets) do build de US-021 — pré-existentes.
- Lighthouse mobile (13.4.1, `--throttling-method=devtools`, build de produção em :3222, relatório /tmp/us022-lh1.report.{json,html}): **Performance 99, Acessibilidade 100**; FCP 1,6 s, LCP 1,6 s, TBT 30 ms, CLS 0,015, Speed Index 2,4 s. Nenhuma auditoria de acessibilidade abaixo de 0,9.
- Verificado via CDP (driver /tmp/us022-verify.mjs contra next dev :3111): **375×800** — header: "Entrar" visível (rect 256,18 59×28, href `/login?ref=home`) ao lado do botão de menu (319,16 32×32), nenhum "Começar grátis" visível no header, docWidth 375 (sem overflow); topo: barra ausente e `main` sem `data-mobile-cta`; scrollY 1241 (hero 40 px acima): barra `fixed` z 30, top 731/bottom 800 (69 px), links `Começar grátis → /signup?ref=home-mobile-bar` + `Falar com nosso time`, `main[data-mobile-cta]` com padding-bottom 80 px; em #agente idem; 20 px antes de `#comecar` entrar: barra ainda visível; `#comecar` no topo (top=64): barra ausente, atributo removido; volta ao topo: ausente. Menu: dialog 320 px, itens na ordem `Começar grátis (w 287, y 94, href /signup?ref=home) → Casos de uso → Como funciona → Agente → Recursos → Docs → Falar com nosso time`, sem "Entrar"; clicar em `#agente` fecha o Sheet, foco volta a "Abrir menu", hash `#agente`. **1280×900** — barra nunca existe no DOM (null em todos os pontos, inclusive após o hero e antes de #comecar), header desktop inalterado ("Entrar" 32 px + "Começar grátis"). Zero erros de console/hidratação nas duas larguras. Capturas /tmp/us022-m-{a-topo,b-apos-hero,c-comecar,e-menu}.png.
- Files changed: apps/web/src/components/marketing/mobile-cta-bar.tsx (novo), apps/web/src/components/marketing/site-header.tsx, apps/web/src/components/marketing/site-header-menu.tsx, apps/web/src/app/(marketing)/page.tsx, apps/web/src/lib/signup-ref.ts, apps/web/src/lib/marketing/links.ts, apps/web/src/lib/marketing/copy.ts, apps/web/src/lib/__tests__/{signup-ref,marketing-links,marketing-copy}.test.ts, apps/web/CLAUDE.md, docs/metricas-uso.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Elemento condicional que só existe abaixo de um breakpoint: além do `md:hidden`, gatear o trabalho (observers/listeners) por `window.matchMedia("(max-width: 47.9375rem)")` e reagir ao `change` — senão em desktop o nó fica no DOM com `display:none` (links inacessíveis mas presentes) e o estado "visível" vaza para quem depende dele (ex.: atributo no `main`). Tailwind v4: `md` = 48rem; variante `max-md:` existe e empilha com `data-[...]:`.
  - Client component pode marcar um Server Component pai (`main`) por atributo via DOM no effect (não é setState) e o pai traduz em classe `data-[attr]:…` — evita converter a page em client só para um padding.
  - No `next dev` o badge "N" do Next (canto inferior esquerdo) sobrepõe a barra fixa nas capturas — é só dev, não existe no build.
  - Lighthouse `--only-categories=performance,accessibility --throttling-method=devtools --chrome-flags="--headless=new" --quiet` contra `next start` roda em ~1 min e bate com a regra do Codebase Patterns (o `simulate` da US-021 deu 0,87; `devtools` deu 0,99 na mesma página).
  - Sheet do Radix: `data-slot="button"` no Link com `asChild` continua sendo "button" (só `DialogClose asChild` sobrescreve para `dialog-close`/`sheet-close`).
---

## 2026-09-05 15:50 - US-023
- `lib/marketing/copy.ts`: `SECTION_IDS.perguntas`, item `{ href: "#perguntas", label: "Perguntas" }` em `HEADER_NAV` antes de Docs (o spy `HEADER_SPY_SECTION_IDS` e o menu mobile ganham o item automaticamente), `FAQ_LABEL` ("Perguntas frequentes"), `FAQ_TITLE` ("O que todo mundo pergunta antes de criar a conta"), tipos `FaqLink`/`FaqAnswer`/`FaqItem`, `FAQ_ITEMS` (6 pares; respostas em texto puro com `links?: [{ label, href }]` — segurança → /politica-de-privacidade, custo → `HERO_CTA_SECONDARY` linkando `#comecar`, uso → /docs) e o helper puro `faqAnswerSegments(answer)` (recorta o texto em trechos/links pela primeira ocorrência de cada label; label ausente é ignorado).
- Novo `components/marketing/faq.tsx` (Server Component, `#perguntas`, `aria-labelledby="faq-title"`): título em `Reveal`; lista `divide-y border-y max-w-3xl` de `<details class="group">` com `summary` `text-base font-medium`, `[&::-webkit-details-marker]:hidden`, `focus-visible:ring-2 ring-offset-2`, ChevronDown `group-open:rotate-180`; resposta `<p>` com `FaqAnswerText` (âncoras `#` em `<a>`, rotas em `Link`). Sem `name` nos details (abrir um não fecha o outro).
- `lib/marketing/seo.ts`: `HomeFaqJsonLd` + `homeFaqJsonLd()` (FAQPage, `mainEntity` com Question/Answer a partir de `FAQ_ITEMS`, `answer.text` puro); `serializeJsonLd` aceita `HomeJsonLd | HomeFaqJsonLd`. `(marketing)/page.tsx`: segundo `<script type="application/ld+json">`, `<Faq />` entre Features e FinalCta, comentário de ordem atualizado.
- Header: com 6 âncoras a barra completa estourava a largura em 768 px (logo 142 + nav 429 + CTAs 195 + 2 gaps de 24 = 814 px para 720 úteis; antes de "Perguntas" já eram ~725). `site-header.tsx` trocou os três `md:` por `lg:` (nav `hidden lg:block`, CTAs `hidden lg:flex`, grupo Entrar+menu `lg:hidden`); `MobileCtaBar` continua em `md` (decisão independente). Comentário explica a razão.
- Testes: marketing-copy (+8: HEADER_NAV com Perguntas antes de Docs, spy inclui "perguntas", FAQ 6 itens/ordem/sem vazio/`?` e `.`/≤ 400 chars, links literalmente no texto e apontando para `FOOTER_LINKS`/`SECTION_IDS`, termos por resposta, `faqAnswerSegments` recompõe o texto, termos vetados, âncora), marketing-seo (+1: FAQPage 6 Question, texto igual a `FAQ_ITEMS`, sem markup, serialização segura). Suíte: 1071 testes.
- Verificado via CDP (drivers /tmp/us023-verify.mjs, /tmp/us023-header.mjs, /tmp/us023-overflow.mjs contra `next dev :3111`): **1280×900** — 2 JSON-LD (SoftwareApplication, FAQPage com 6 Question, sem markup); seção `<section id="perguntas">` com 6 `<details>` fechados, summary 16px/500; clique em "Perguntas" no header → hash `#perguntas`, `section.top = 64 = header.bottom`, título em y=176 (não coberto), `aria-current` em "Perguntas"; Tab/foco no 1º summary → `:focus-visible` com anel `rgb(59,94,235)` 4px + offset; Enter abre (chevron `rotate: 180deg`), Espaço fecha, Tab+Enter abre o 2º; 3º aberto mostra o link "Política de Privacidade" visível; docWidth 1280. **375×800** — menu Sheet lista `Começar grátis → Casos de uso → Como funciona → Agente → Recursos → Perguntas → Docs`; clique em Perguntas fecha o Sheet e rola até top=64; mesmo comportamento de teclado; docWidth 375. **768/1024/1279** (header após a troca para lg): 768 → menu visível, docWidth 768; 1024 → barra completa (nav 225–746, CTAs 805–1000), docWidth 1024; 1279 idem. Zero erros de console/hidratação. Capturas /tmp/us023-1280-{a-ancora,b-aberto-teclado,c-itens-abertos}.png e /tmp/us023-375-*.png.
- Files changed: apps/web/src/components/marketing/faq.tsx (novo), apps/web/src/components/marketing/site-header.tsx, apps/web/src/app/(marketing)/page.tsx, apps/web/src/lib/marketing/copy.ts, apps/web/src/lib/marketing/seo.ts, apps/web/src/lib/__tests__/{marketing-copy,marketing-seo}.test.ts, apps/web/CLAUDE.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - `group-open:rotate-180` funciona com `<details>` nativo, mas o Tailwind v4 escreve em `rotate` (não `transform`) — o probe tem de ler `getComputedStyle(svg).rotate`, e logo após `d.open = true` o valor ainda é `0deg` por causa da transição de 150 ms (esperar ~300 ms).
  - CDP: `Input.dispatchKeyEvent` de Enter precisa de `text: "\r"` para ativar summary/botão (Espaço precisa de `text: " "`); sem isso `open` não muda e parece bug do componente.
  - O RSC payload duplica strings do JSX no HTML (`grep -o 'application/ld+json'` conta 4 para 2 scripts) — contar via DOM no browser, não no HTML cru.
  - US-024 (footer em 3 colunas com âncoras absolutas nas páginas compactas) pode reutilizar `HEADER_NAV`/`SECTION_IDS` — agora com `#perguntas`; as âncoras absolutas devem ser `/#${id}`.
  - US-026 (SectionHeading compartilhado): `faq.tsx`, `features.tsx` e as demais repetem o mesmo bloco `Reveal > p.text-primary + h2` — candidatos diretos à extração.
---

## 2026-09-05 16:10 - US-024
- `lib/marketing/copy.ts`: bloco do footer movido para depois do hero (referencia `HERO_CTA_SECONDARY`; antes da declaração seria TDZ). `FOOTER_LINKS`/`FOOTER_NAV_LABEL` saíram; entraram `FOOTER_TAGLINE` ("Um produto AutoML."), os tipos `FooterItem` (união `link` | `auth` | `contact-dialog`), `FooterColumn`/`FooterColumnId` e `FOOTER_COLUMNS` (Produto = `HEADER_NAV` mapeado para `link`; Conta = auth /login + /signup + contact-dialog; Legal = políticas + `CONTACT_MAILTO`) e o helper puro `footerLinkHref(href, anchorsAbsolute)` (`#x` → `/#x` só quando pedido). Import type de `MarketingAuthPath` (links.ts) e `CONTACT_MAILTO` (lib/contact).
- `components/marketing/site-footer.tsx` reescrito: `grid gap-10 md:grid-cols-3`, cada coluna `<nav aria-label={label} data-footer-column>` com `<h2>` e `<ul>`; `FooterItemView` faz o switch por `kind` (rotas e `/#secao` em `Link prefetch={false}`, `#secao`/mailto em `<a>`; auth via `marketingAuthHref(path)`; `ContactDialog variant="link"` com classes para parecer link). Linha inferior Logo + copyright + tagline. Prop `anchorsAbsolute` (default false). `public-page-shell.tsx` passa `<SiteFooter anchorsAbsolute />`.
- `app/globals.css`: `--muted-foreground` do tema claro 0.556 → `oklch(0.55 0 0)`. Motivo: com `--background: #f8f9fb` o par dava 4,49:1 (abaixo do AA); com 0.55 dá 4,61:1. Diferença visual imperceptível (#737373 → #717171), vale para o app inteiro.
- `app/layout.tsx`: `<html data-scroll-behavior="smooth">` — o Next passou a avisar no console ao navegar /docs → `/#como-funciona` (rolagem suave vem de `html:has([data-marketing-root])`).
- Testes: `marketing-copy.test.ts` bloco do footer reescrito (+5: 3 colunas/ordem/labels únicos, Produto = HEADER_NAV, Conta, Legal, `footerLinkHref` e âncoras existentes em `SECTION_IDS`, tagline); conjunto de hrefs conhecidos da FAQ derivado de `FOOTER_COLUMNS`. Novo `marketing-footer.test.ts`: lê `globals.css`, converte oklch/hex → luminância e exige ≥ 4,5:1 para muted-foreground sobre background (+2). Suíte: 1077 testes.
- Verificado via CDP (/tmp/us024-verify.mjs, cenários home1280 / home375 / docs1280 contra `next dev :3111`): **1280** — 1 `footer`, `grid-template-columns` com 3 faixas de 341 px, navs Produto/Conta/Legal em x=88/469/851 com `aria-label` = h2; hrefs `#casos-de-uso…#perguntas`, `/docs`, `/login?ref=home`, `/signup?ref=home`, botão "Falar com nosso time", `/politica-de-uso`, `/politica-de-privacidade`, `mailto:`; linha inferior "© 2026 AutoML. Um produto AutoML."; clique em "Como funciona" no footer → hash e `section.top = 64 = header.bottom`; docWidth 1280. **375** — colunas empilhadas (1 faixa de 327 px, y=37/307/475), docWidth 375, mesmo comportamento da âncora. **/docs 1280** — âncoras `/#casos-de-uso…/#perguntas`; botão do footer abre o dialog "Falar com nosso time", Esc fecha e devolve o foco ao botão; clique em "Como funciona" → `pathname "/"`, hash `#como-funciona`, seção em top=64 sob o header `full`. Zero erros de console/hidratação nos 3 cenários (após o `data-scroll-behavior`). Cor renderizada dos links `lab(47.8 0 0)` = L* 47,8 → Y 0,166 → 4,6:1 sobre `rgb(248,249,251)`. Capturas /tmp/us024-{home1280,home375,docs1280}-*.png.
- Files changed: apps/web/src/components/marketing/site-footer.tsx, apps/web/src/components/marketing/public-page-shell.tsx, apps/web/src/lib/marketing/copy.ts, apps/web/src/app/globals.css, apps/web/src/app/layout.tsx, apps/web/src/lib/__tests__/marketing-copy.test.ts, apps/web/src/lib/__tests__/marketing-footer.test.ts (novo), apps/web/CLAUDE.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - O disco da máquina encheu no meio da iteração (cache Turbopack em `.next/dev/cache` + perfis Chrome em `/tmp/cdp-profile-*`). Com ENOSPC a ferramenta Bash nem roda (não consegue criar o arquivo de saída) — o Monitor ainda executa e devolve stdout como evento; usei-o para `pkill` + `rm -rf .next/dev/cache /tmp/cdp-profile-*`. Sempre apagar `/tmp/cdp-profile-*` ao fim de cada driver CDP (alguns subdiretórios `Storage/ext` só apagam depois que o Chrome morre de fato).
  - Heredoc `cat > arquivo` com disco cheio falha em silêncio quando encadeado com `&&` que já quebrou antes — se um script "não existe", confira se foi gravado.
  - `getComputedStyle(...).color` de uma cor `oklch()` volta como `lab(L a b)` no Chrome — parser de `rgb(` não serve; L* → Y = ((L+16)/116)^3.
  - shadcn default: `muted-foreground` 0.556 assume fundo branco; qualquer fundo off-white derruba o par para < 4,5:1. Quando a PRD exigir AA, calcule a partir dos tokens (teste em `marketing-footer.test.ts` reutilizável: `contrastRatio(css, css)`).
  - Next 16 avisa "Detected scroll-behavior: smooth on the <html> element" só em navegação client-side entre rotas com hash; a correção é o atributo `data-scroll-behavior="smooth"` no `<html>` do root layout.
  - US-026 (SectionHeading compartilhado + espaçamento único) pode reaproveitar `data-footer-column` para probes; o footer já usa `py-12`.
---

## 2026-09-05 16:15 - US-025
- Removida a faixa "Empresas que confiam": `components/marketing/trusted-by.tsx` e os 4 SVGs em `public/marketing/logos/` apagados (`git rm`); `TRUSTED_BY_LABEL`, `TRUSTED_LOGO_HEIGHT`, `TrustedLogo` e `TRUSTED_LOGOS` saíram de `lib/marketing/copy.ts`; `(marketing)/page.tsx` sem o import/`<TrustedBy />` e com o comentário de ordem atualizado (Hero → UseCases → HowItWorks → AgentShowcase → Features → Faq → FinalCta). Nenhum JSON-LD citava a seção.
- `hero.tsx`: `pb-16 … lg:pb-24` → `pb-12 sm:pb-16` (o `pt-10 sm:pt-16 lg:pt-20` ficou igual). `scroll-margin-top` da `#casos-de-uso` continua vindo de `[data-marketing-root] section[id]` (globals.css), nada a mudar.
- Testes: `marketing-copy.test.ts` perdeu o describe "empresas que confiam" (−3) e os imports de `node:fs`/`node:path`; a guarda do agent-showcase ("nenhuma loja fictícia com nome de empresa real") agora usa a lista literal `["alura","fiap","pm3","automl"]` em vez de derivar de `TRUSTED_LOGOS`. Suíte: 1074 testes.
- `grep -ri trusted apps/web/src apps/web/public`: só sobra `trustedDevice` (2FA — `two-factor-rate-limit.ts`, `auth.ts`, `two-factor-audit-action.test.ts`), sem relação com a faixa; mantido de propósito.
- Verificado via CDP (/tmp/us025-verify.mjs contra `next dev :3111`): **1280×900** — seções do main `inicio, casos-de-uso, como-funciona, agente, recursos, perguntas, comecar` (sem trusted no DOM); hero `padding-bottom` 64 px, casos de uso `padding-top` 80 px, `heroBottom = ucTop = 714` (vão 0 px; distância de conteúdo 144 px), docWidth 1280; clique em "Casos de uso" no header → hash, `section.top = 64 = header.bottom`, título em y=176, `aria-current` no link. **375×800** — padding-bottom 48 px / padding-top 64 px, `heroBottom = ucTop = 1185` (vão 0; conteúdo 136 px), docWidth 375; clique na âncora do footer → `section.top = 64`. Zero erros de console/hidratação. Capturas /tmp/us025-{1280,375}-{a-transicao,b-ancora}.png.
- Files changed: apps/web/src/components/marketing/trusted-by.tsx (apagado), apps/web/public/marketing/logos/*.svg (apagados), apps/web/src/components/marketing/hero.tsx, apps/web/src/app/(marketing)/page.tsx, apps/web/src/lib/marketing/copy.ts, apps/web/src/lib/__tests__/marketing-copy.test.ts, apps/web/CLAUDE.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Driver CDP: a limpeza de `/tmp/cdp-profile-*` agora está no próprio `finish()` (rmSync após `chrome.kill()` + 800 ms); o cache Turbopack `.next/dev/cache` chegou a 195 MB — apagar quando o disco (97 %) apertar.
  - No zsh da sessão, `echo ===x===` quebra com "not found" (glob de `=`): usar aspas ou separar em chamadas.
  - US-026 (SectionHeading compartilhado): com o hero em `pb-12 sm:pb-16` e as seções em `py-16 sm:py-20`, a distância hero → primeiro título é 136/144 px; se o espaçamento único mudar o `py` das seções, reavaliar o `pb` do hero junto.
---

## 2026-09-05 16:30 - US-026
- Ritmo único: `@utility marketing-section { @apply py-16 sm:py-24; }` em globals.css, aplicada em UseCases, HowItWorks, AgentShowcase, Features e Faq (na `<section>`) e em FinalCta (no `div` interno, porque a faixa escura é full-bleed); saíram `py-16 sm:py-20` (5 seções) e `py-20 sm:py-24 lg:py-28` (CTA final). Hero ficou com `pt-10 pb-12 sm:pt-16 sm:pb-16 lg:pt-20` (US-025).
- Novo `components/marketing/section-heading.tsx` (server): `titleId` (obrigatório — o `aria-labelledby` da seção aponta para o H2), `label`, `title`, `subtitle?`, `align?` ("start" | "center") e `className?` via `cn`. Mesmas classes de antes (`text-sm font-medium text-primary` / `text-3xl … sm:text-4xl` / `text-lg text-muted-foreground`), num único `Reveal`. AgentShowcase passa `className="max-w-none"` (antes o Reveal dela não tinha `max-w-2xl`). Todos os `<h2>` locais das 5 seções removidos; `Reveal` deixou de ser importado em faq.tsx e agent-showcase.tsx.
- copy.ts: `HOW_IT_WORKS_DETAIL_LABEL = "Ver detalhes"`, `HOW_IT_WORKS_DETAIL_LINKS = { 1: "#agente", 3: "#recursos" }` (índice 0-based do passo) e `howItWorksDetailLinkLabel(title)` → `"Ver detalhes: <título>"` (aria-label começando pelo texto visível). `FINAL_CTA_BETA_NOTICE = HERO_BETA_NOTICE`.
- how-it-works.tsx: `StepItem` lê `HOW_IT_WORKS_DETAIL_LINKS[index]` e, se houver, renderiza `<a href="#…">` nativo (rolagem suave do CSS) com `ArrowRight size-3.5`, `text-sm font-medium text-primary hover:underline` e anel de foco; passos 1 e 3 não têm link. final-cta.tsx: `<p class="text-base text-white/70">` com o aviso de beta entre o subtítulo e os botões, dentro do mesmo `Reveal`.
- Testes (marketing-copy.test.ts, +6 → 59 no arquivo, suíte 1048): beta do CTA final igual ao hero e na lista de termos vetados; links de detalhe só nos índices 1 e 3, destinos existem em HEADER_NAV e ficam DEPOIS de #como-funciona; nome acessível; leitura de globals.css (`@utility marketing-section` com `py-16 sm:py-24`); leitura dos 6 fontes (className com `marketing-section` sem `py-`, nenhum `sm:py-`/`lg:py-`); os 5 fontes de conteúdo têm `<SectionHeading` e nenhum `<h2`.
- Verificado via CDP (/tmp/us026-verify.mjs contra `next dev :3111`): **1280×900** — padding 96/96 nas 6 seções (`cls: true`), distância conteúdo→conteúdo 192 px em todas as fronteiras (hero→casos 160 px), 5 H2 com `aria-labelledby` resolvendo, label `rgb(59,94,235)` 14 px, H2 36 px, subtítulo 18 px, agente `max-width: none`; links só nos passos 2 (`#agente`) e 4 (`#recursos`) com aria-label; CTA final com 3 `<p>` (18/16/14 px, white/70) e "Estamos em beta" presente; clique nos dois "Ver detalhes" → hash correto, `section.top = 64 = header.bottom`, `aria-current` acompanha; docWidth 1280. **375×800** — padding 64/64, distância 128 px (hero→casos 112), H2 30 px, mesmos links/hash/top 64, docWidth 375. Zero erros de console/hidratação. Capturas /tmp/us026-{1280,375}-{a-pagina-inteira,b-ver-detalhes-passo2,b-ver-detalhes-passo4,c-cta-final}.png.
- Lighthouse mobile (13.4.1, `--throttling-method=devtools`, build de produção em :3222, relatório /tmp/us026-lh.report.report.{json,html}): **Performance 99, Acessibilidade 100**; FCP 1,6 s, LCP 1,6 s, TBT 30 ms, CLS 0,015, Speed Index 2,4 s. Nenhuma auditoria de acessibilidade abaixo de 0,9 — igual à US-022.
- Files changed: apps/web/src/app/globals.css, apps/web/src/components/marketing/section-heading.tsx (novo), apps/web/src/components/marketing/{use-cases,how-it-works,agent-showcase,features,faq,final-cta}.tsx, apps/web/src/lib/marketing/copy.ts, apps/web/src/lib/__tests__/marketing-copy.test.ts, apps/web/CLAUDE.md, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Tailwind v4: `@utility x { @apply py-16 sm:py-24; }` aceita variante dentro do `@apply` e gera a media query certa (confirmado pelo computed style 64→96 px em 375/1280).
  - Teste que lê fontes de componentes é aceitável para travar convenção de layout (classe obrigatória / ausência de `<h2>` local), mas deve restringir o regex à className do wrapper — paddings internos legítimos (o `py-5` do `<summary>` da FAQ) disparam falso positivo com `/py-\d+/` solto.
  - Driver CDP: `console.log("x", await evaluate(\`...\`))` precisa de DOIS parênteses no fim — o erro "missing ) after argument list" aponta para a linha do fecho do template, não para o log.
  - Com hero `pb-12 sm:pb-16` e seções `py-16 sm:py-24`, a fronteira hero→casos fica 32 px mais apertada que as demais (160 vs 192 desktop; 112 vs 128 mobile); visualmente OK porque o palco do hero já tem respiro interno — mexer no hero só se a PRD pedir.
---
