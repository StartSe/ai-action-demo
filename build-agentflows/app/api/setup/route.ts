// A configuração de provedores foi removida a pedido do produto. Use o login oficial ChatGPT.
function removed(){return Response.json({error:'As configurações antigas foram removidas. Conecte o ChatGPT pelo app.'},{status:410});}
export const GET=removed;
export const PUT=removed;
