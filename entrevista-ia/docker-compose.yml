# Sobe só este app: docker compose up --build  (http://localhost:3003)
# Nenhuma variável é obrigatória: abra /setup no navegador para conectar a IA e as integrações.
services:
  entrevista-ia:
    build: .
    ports:
      - "3003:10000"
    volumes:
      - dados:/app/data
    restart: unless-stopped

volumes:
  dados:
