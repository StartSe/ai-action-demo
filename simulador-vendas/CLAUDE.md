@AGENTS.md

## Notas específicas deste app

- 13º app da suíte, criado a partir de uma cópia completa de `pdi-time/` (ciclo "novos-apps", US-014 do prd.json). `components/ui.tsx`/`components/setup.tsx`/`lib/ai.ts`/`lib/store.ts`/`lib/setup-comum.ts`/`lib/historico.ts`/`lib/mcp.ts`/`lib/mcp-cliente.ts`/`lib/mcp-oauth.ts`/`lib/formularios.ts`/`lib/notificacoes.ts`/`lib/rotinas.ts`/`app/mcp`/`app/f`/`app/api/rotinas`/`app/api/setup`/`proxy.ts` são cópias literais (`scripts/verificar-padrao.sh` ganhou `simulador-vendas` na lista `APPS`). `app/globals.css` diverge só nas 3 linhas de cor de acento (`#3f6212`). `app/r/[id]/page.tsx`, `app/imprimir/[id]/page.tsx` e os `not-found.tsx` são próprios daqui, por dependerem do tipo salvo (`tipo: "conversa"`, `entrada = Conversa`, `saida = Analise`).
- Arquivos específicos de PDI (`lib/pdi.ts`, `lib/checkins.ts`, `components/DialogoAutoavaliacao.tsx`, `components/LembrarCheckins.tsx`, `app/api/pdi/**`) foram removidos da cópia. Nenhum tipo de formulário público é registrado ainda (`lib/formularios.ts`/`app/f/**` ficam como infraestrutura pronta, sem `registrarCallback`) — mesmo estado inicial de `prospeccao-ia`/`bussola-ia` antes das respectivas histórias de formulário. Diferente de `bussola-ia` (nasceu sem `rotina`), este app já nasce com a capacidade `rotina` declarada em `catalogo.json` (`TIPOS_ROTINA` tem `resumo-simulador-vendas`, que resume as conversas analisadas desde a última execução, mesmo molde de `pdi-time`).
- `lib/vendedores.ts`/`lib/cenarios.ts` (novos, não compartilhados): mesmo molde de `bussola-ia/lib/questionarios.ts` (tabela própria no `app.sqlite`, id `base64url` de 9 bytes, `criar`/`listar`/`obter`/`apagar`). `lib/cenarios.ts` é o único caso com semeadura automática: os 3 cenários modelo (`lead-frio`, `desconto`, `renovacao`) usam **ids fixos** e `INSERT OR IGNORE`, chamado no início de `listar`/`obter`/`criar` (flag `semeado` em memória evita repetir a query a cada chamada dentro do mesmo processo) — rodar a semeadura várias vezes, mesmo em processos diferentes, nunca duplica, ao contrário de um `INSERT` comum com id aleatório. Se uma história futura precisar de mais cenários modelo, adicionar ao array `CENARIOS_MODELO` com um novo id fixo é suficiente; nunca reaproveitar um id já usado nem remover um existente (isso deixaria uma linha órfã ou quebraria referências salvas em `Conversa.cenarioId`).
- `lib/criterios.ts` (puro, sem `node:*`) só exporta `CRITERIOS_PADRAO`, a lista fixa dos 7 critérios de venda consultiva escolhidos para este app: abertura e rapport, descoberta de necessidades, apresentação de valor, tratamento de objeções, geração de urgência, escuta ativa, fechamento e próximos passos. É importado tanto por `app/page.tsx` (client, valor inicial dos 7 campos de texto editáveis em "Critérios de avaliação") quanto por `lib/analise.ts`/`lib/demo.ts` (server). Os nomes são só o valor inicial: o painel manda a lista editada (renomeada ou não) para a rota a cada análise, então "os 7 critérios" não são uma constante do backend — são um `string[]` qualquer do tamanho que o usuário decidir.
- `lib/conversa.ts` (puro, sem `node:*`): `parseConversaColada(texto)` converte o formato colado ("Vendedor: ..." / "Cliente: ..." uma fala por linha) em `LinhaTranscricao[]`. Só reconhece os prefixos antes do primeiro `:` da linha (normalizados sem acento/maiúscula: `vendedor`/`vendedora`/`eu` viram `"vendedor"`, `cliente` vira `"cliente"`); qualquer outro prefixo (ou linha sem `:`) é silenciosamente ignorado, nunca lança erro — a validação de "conversa vazia" acontece antes, em `app/api/analisar/route.ts`, sobre o texto bruto (`conversaColada.trim()`), não sobre o resultado do parse (uma conversa colada só com linhas não reconhecidas passaria pela validação de string não-vazia e geraria uma análise sobre uma transcrição vazia — cenário não coberto por teste automatizado ainda, mas o comportamento é "a IA recebe uma transcrição vazia e provavelmente devolve notas baixas", não um erro 400).
- `lib/analise.ts` (`analisarConversa`/`gerarAnalise`) concentra a única lógica desta história, reaproveitada por `app/api/analisar/route.ts` e pela ferramenta MCP `analisar_conversa` (`lib/ferramentas.ts`) — mesmo princípio de nunca duplicar lógica entre rota e ferramenta MCP já usado no resto da suíte. **A nota geral nunca vem da IA**: `analisarConversa` sempre recalcula `analise.nota` como a média dos `criterios[].nota` já recebidos (arredondada para 1 casa decimal com `Math.round(x*10)/10`, antes de qualquer formatação), mesmo que o modelo tente devolver uma nota geral própria (o schema pedido à IA nem inclui esse campo). Cada nota individual também é validada/clampada para `[0,10]` no servidor antes de entrar na média. `numero(nota, 1)` (`lib/formato.ts`, vírgula pt-BR) só é usado na hora de exibir (`Destaque`, `Chip`, `analiseParaTexto`) — o valor armazenado em `Analise.nota` continua sendo `number`, nunca a string formatada.
- `gerarAnalise(dados: DadosAnalise)` monta a `Conversa` a partir do texto colado (`parseConversaColada`) exceto em modo demonstração, onde ignora o texto real e usa `conversaDemo()` (`lib/demo.ts`) — mesmo padrão de `pdi-time/lib/pdi.ts:gerarPDI` (a demonstração nunca reflete a entrada real, só personaliza o que for barato de personalizar). `analiseDemo(criterios)` foi escrita para funcionar com qualquer lista de critérios, não só os 7 padrão: tenta casar cada nome (normalizado) contra um dicionário de 7 respostas prontas e, para qualquer nome que não bata (um critério renomeado pelo usuário, ou um oitavo critério hipotético), cai num texto genérico que pede para conectar a IA — assim a demonstração nunca quebra nem fica com campos vazios quando alguém edita os critérios antes de clicar em "Preencher com um exemplo".
- `Conversa.id` é gerado por `lib/analise.ts` (`crypto.randomBytes(9).toString("base64url")`, mesmo gerador de `lib/historico.ts`, mas uma instância própria) e é **independente** do id do registro em `lib/historico.ts` (o id que vira o link `/r/<id>`) — os dois nascem juntos na mesma chamada de `gerarAnalise` mas não há garantia nem necessidade de serem iguais; a `Conversa` é salva inteira como `entrada` do resultado.
- `app/page.tsx`: o select "Vendedor" inclui uma opção `"Cadastrar vendedor"` (`value="__novo__"`) na própria lista de `<option>` — selecioná-la não muda `dados.vendedorId`, só abre um formulário inline (nome/e-mail/equipe, sem modal) logo abaixo do select; salvar chama `POST /api/vendedores`, insere o vendedor retornado direto no estado local (sem recarregar a lista inteira) e seleciona-o automaticamente. Os "Critérios de avaliação" ficam num `MaisDetalhes` com 7 (ou quantos existirem) `<input>` de texto, um por critério, editando `dados.criterios` por índice. Nenhuma das funções usadas dentro do `useEffect([])` de `?exemplo=1` (`gerar`) chama outra função-irmã do componente (o refresh do histórico depois de analisar é um `fetch(...).then(...)` inline dentro do próprio `gerar`, não uma chamada a `carregarHistorico`) — mesmo cuidado com `react-hooks/exhaustive-deps` já documentado no restante da suíte.
- `ConteudoAnalise` (exportado por `app/page.tsx`, reaproveitado por `app/imprimir/[id]/page.tsx`) usa `DataTable` com 4 colunas: `nome` (`papel:"titulo"`), `nota` (`papel:"chip"`, via `<Chip nivel={tomChip(l.nota)}>{numero(l.nota,1)}</Chip>` — o `nivel` só escolhe a cor verde/laranja/vermelho por faixa de nota, o texto exibido é sempre o número, não o rótulo padrão do `Chip`), `evidencia` (`papel:"resumo"`, pode passar de 2 linhas) e `comoMelhorar` (`papel:"detalhe"`, só aparece em "Ver mais"). "Ver a conversa completa" é um `<details>` simples (não usa `DataTable`) com uma linha por fala, mostrando o minuto:segundo (`formatarSegundo`) só quando `LinhaTranscricao.segundo` existe — a conversa de demonstração tem `segundo` em todas as falas; uma conversa colada pelo usuário nunca tem (o formato colado não carrega tempo), então o `<details>` mostra só "Vendedor"/"Cliente" sem o carimbo de tempo nesse caso.
- `scripts/verificar-jargao.mjs`: como `components/ui.tsx` é compartilhado e já passava para os outros 12 apps, nenhuma alteração foi necessária nele. `app/page.tsx` evita "ID"/"token"/"API" em texto visível; os identificadores internos (`vendedorId`, `cenarioId`) nunca aparecem renderizados, só como `value` de `<option>` (atributo técnico, filtrado pelo script).
- (US-015) `lib/integracoes.ts` ganhou `ELEVENLABS_AGENTE` (uma única integração reunindo `ELEVENLABS_API_KEY` + `ELEVENLABS_AGENT_ID`, select com `opcoesDinamicas` chamando `GET /v1/convai/agents`, + `ELEVENLABS_WEBHOOK_SECRET`), no molde de `entrevista-ia/lib/integracoes.ts:ELEVENLABS_LIGACAO` (`testar()` usa `GET /v1/convai/agents/<id>`, não `/v1/user`). Diferente de `entrevista-ia`, aqui é **uma integração só** (não duas), porque este app não faz ligação telefônica (sem `ELEVENLABS_PHONE_NUMBER_ID`/Twilio) — o agente conversa dentro de um widget na sala de simulação (US-016).
- (US-015) **Não existe nenhum precedente de webhook assinado da ElevenLabs no resto da suíte** (`entrevista-ia`/`reunioes-ia` só usam a ElevenLabs para TTS/discagem/transcrição, nunca recebem nada de volta) — a rota `app/webhook/elevenlabs/route.ts` foi escrita do zero a partir da documentação pública da ElevenLabs, não copiada de nenhum app. Formato do cabeçalho `elevenlabs-signature`: `t=<epoch>,v0=<hash hex>[,v0=<hash2>...]` (pode vir mais de um `v0=` numa rotação de segredo, basta um bater); assinatura = `HMAC-SHA256("<t>.<corpo cru>", segredo)`; corpo lido com `req.text()` (nunca `req.json()` antes de validar, senão a assinatura não bate contra os bytes exatos); janela de 30 min contra `Date.now()`; comparação com `crypto.timingSafeEqual` (convertendo o hex para `Buffer` antes, diferente do único outro uso de `timingSafeEqual` da suíte em `lib/mcp.ts`, que compara texto puro). Sem `ELEVENLABS_WEBHOOK_SECRET` configurado, ou assinatura/janela inválida, a rota devolve 401 sem processar nada; com assinatura válida, responde 200 imediatamente e só depois processa (`processar(corpo).catch(...)`, mesmo padrão fire-and-forget de `whatsapp-atendente/app/webhook/route.ts`) — só assim o teste via `curl` recebe 200 rápido mesmo com a análise (que pode chamar IA) ainda rodando.
- (US-015) Só é aceito `type === "post_call_transcription"` (qualquer outro tipo de evento é ignorado silenciosamente, ainda com 200). Mapeamento da transcrição: `data.transcript[].role` "agent" (o agente da ElevenLabs faz o papel do **cliente** simulado neste app) vira `papel:"cliente"`, "user" (quem está ligando: o vendedor) vira `papel:"vendedor"`; `time_in_call_secs` vira `LinhaTranscricao.segundo`; `data.metadata.call_duration_secs` vira `Conversa.duracaoSeg`; `data.dynamic_variables.vendedor_id` vira `Conversa.vendedorId`. `data.dynamic_variables.sala_token` é lido mas **ainda não usado** (não existe `lib/salas.ts` nesta história — é infraestrutura da US-016, que deve usar o token para achar o cenário/vendedor da sessão de simulação antes de montar a `Conversa`).
- (US-015) Nova função `salvarConversaAnalisada(conversa, criterios?)` em `lib/analise.ts`, irmã de `gerarAnalise` mas para uma `Conversa` que já chega estruturada (não colada como texto) — reaproveita `analisarConversa`+`salvar()` sem duplicar a lógica de análise/gravação no histórico. Será reaproveitada pela sala de simulação por texto/voz (US-016).
- (US-016) `lib/salas.ts` (novo, não compartilhado, mesmo molde de `lib/vendedores.ts`/`lib/cenarios.ts`): tabela própria `salas` (`codigo` como chave primária — mesmo nome de campo de `lib/formularios.ts`, evitado "token" só por causa do verificador de jargão, ver abaixo), `criar({vendedorId?,cenarioId?})` gera um link de 30 dias sem limite de usos (diferente de `lib/formularios.ts`, que tem `limite`), e `registrarResultado(codigo, resultadoId)` grava o resultado mais recente da sala — usado tanto pelo fluxo de voz (aviso automático) quanto pelo fluxo de texto (só por consistência, o texto nunca precisa sondar). `limparExpirados()` registrado em `instrumentation.ts` junto dos outros dois (`historico`/`formularios`).
- (US-016) `lib/simulacao.ts` (novo, server-only): `responderComoCliente(transcricao, cenario)` é a única lógica nova de IA desta história — pede à IA (`askText`, não `askJSON`) só a próxima fala do cliente simulado, recebendo a transcrição inteira como uma string formatada a cada chamada (sem estado de conversa no servidor; o cliente da sala manda a transcrição completa a cada turno). Em modo demonstração, um roteiro fixo de 4 falas (`ROTEIRO_DEMO`) ignora completamente o cenário e o que o vendedor disse — escolhido pelo índice de quantas falas do cliente já existem na transcrição (clampado no último item para conversas mais longas).
- (US-016) A sala pública (`app/simular/[token]/page.tsx`, Server Component) decide texto vs. voz **no servidor**, checando `integracaoConfigurada(ELEVENLABS_AGENTE)` (por isso `ELEVENLABS_AGENTE` teve que virar `export const` em `lib/integracoes.ts`, antes era privado do arquivo) — nunca no cliente. Mesmo padrão "Indisponível" de `app/f/[token]/page.tsx`/`entrevista-ia/app/entrevista/[token]/page.tsx` para link inexistente/expirado (uma tela amigável, sem `notFound()` de verdade — a suíte trata "404 amigável" como uma mensagem, não como status HTTP 404; confirmado que é a convenção intencional, não uma lacuna).
- (US-016) **Gotcha de nomenclatura por causa do `scripts/verificar-jargao.mjs`:** como esse script varre `components/*.tsx` inteiro (não só texto visível), `components/SalaSimulacao.tsx` usa `codigo` em vez de `token` para o prop/variável em toda parte (mesma decisão documentada em `entrevista-ia/CLAUDE.md` para `EntrevistaCandidato.tsx`) — as rotas de servidor (`app/api/salas/[token]/**/route.ts`, não varridas) continuam usando `token` normalmente, seguindo a convenção de pasta dinâmica (`[token]`) do resto da suíte.
- (US-016) Widget de voz: primeiro uso de `<elevenlabs-convai>` na suíte (`entrevista-ia`/`reunioes-ia` só ligam por telefone via Twilio, nunca embutem o widget). Precisou de um arquivo de declaração de tipo ambiente (`lib/elevenlabs-convai.d.ts`, `declare module "react" { namespace JSX { interface IntrinsicElements {...} } }`) porque neste projeto (React 19.2.8 + `@types/react` correspondente) o namespace `JSX` mora dentro de `declare namespace React {}`, não mais solto no escopo global — sem esse arquivo o TypeScript não reconhece o elemento customizado. O script oficial (`https://unpkg.com/@elevenlabs/convai-widget-embed`) é injetado uma única vez por `components/SalaSimulacao.tsx` (checa `document.getElementById` antes de criar a tag) e o componente descobre quando o widget está pronto via `customElements.whenDefined("elevenlabs-convai")` — chamar `setState` direto dentro do corpo do efeito (sem passar por um `.then()`/callback) quando o script já existia disparava `react-hooks/set-state-in-effect` ("Calling setState synchronously within an effect can trigger cascading renders"), mesmo sendo um caso claramente condicional; usar `whenDefined(...).then(...)` resolve nos dois casos (script novo ou já carregado) com uma única forma de código.
- (US-016) Como esta sandbox não tem uma conta real da ElevenLabs, o caminho de voz foi verificado só por leitura de código + `tsc`/lint/build + um teste ponta a ponta via `curl` simulando o aviso automático assinado (`elevenlabs-signature` calculado à mão, mesmo HMAC de US-015) com `dynamic_variables.sala_token` apontando para uma sala criada de propósito — confirmado que `app/webhook/elevenlabs/route.ts` acha a sala, herda `vendedorId`/`cenarioId` dela quando a variável dinâmica não os traz, chama `registrarResultado`, e que `GET /api/salas/<código>/ultima` muda de `pronto:false` para `pronto:true` (comparando com o parâmetro `desde`) só depois que a análise (que roda em segundo plano, ~1,2 s em demo) termina. A verificação em navegador (Playwright, obrigatória pela AC) cobriu só o caminho sem integração (texto), que é o único exigido explicitamente pela história.
- (US-015) O componente do cartão "Dados para a equipe técnica" (endereço do aviso automático + "Última conversa recebida há N minutos") **não pode se chamar `Webhook.tsx`/`Webhook()`** como o PRD sugere: `scripts/verificar-jargao.mjs` proíbe a palavra "webhook" isolada (`\bwebhook\b`) em qualquer `components/*.tsx` (exceto `setup.tsx`), e um identificador `Webhook` sozinho bate nesse limite de palavra (diferente de `WebhookWhatsApp`, que não bate porque não há fronteira de palavra entre "Webhook" e "WhatsApp"). Por isso o componente chama-se `components/WebhookElevenLabs.tsx`/`WebhookElevenLabs()`, seguindo o mesmo naming de `WebhookWhatsApp.tsx` (`whatsapp-atendente`). O texto visível também evita a palavra "webhook" por completo (usa "endereço", "aviso automático de pós-conversa"); o campo de ajuda de `ELEVENLABS_WEBHOOK_SECRET` em `lib/integracoes.ts` pode mencionar "Webhooks" à vontade porque esse arquivo não é varrido pelo script (só `app/page.tsx` e `components/*.tsx` exceto `setup.tsx` são). "Última conversa recebida" é calculado no cliente (`Date.now()` teria que rodar fora do render por causa da regra `react-hooks/purity`/`react-hooks/set-state-in-effect` do `eslint-plugin-react-hooks@7` — resolvido calculando o texto já formatado dentro do `.then()` da própria busca, junto com `setDados`, em vez de um segundo estado/efeito derivado).
- (US-017) `lib/painel-equipe.ts` (novo, server-only, não compartilhado): `montarPainelEquipe(dias=30)` é pura agregação sobre `historico.listarPorTipo<Conversa,Analise>("conversa", 1000)` + `vendedores.listar()` — **nunca chama IA** (nem `askJSON`/`askText`), mesmo padrão de `entrevista-ia/lib/entrevista.ts:gerarRanking`. Compara a janela atual (`[agora-dias, agora]`) contra a janela imediatamente anterior (`[agora-2*dias, agora-dias)`, não sobreposta) tanto para a nota geral da equipe (`Destaque`) quanto para a tendência por vendedor (limiar de ±0,3 na diferença de médias; sem dado na janela anterior, tendência cai em "estável" por padrão, nunca undefined). Só entram na tabela vendedores cadastrados com **pelo menos 1 conversa na janela atual**; "critério mais fraco" (por vendedor e da equipe) agrupa por nome de critério normalizado (`trim().toLowerCase()`), mesmo cuidado de comparação de texto já registrado para "título do sinal" em `radar-sinais` (US-009 do prd.json) — nomes de critério são editáveis por conversa, então o agrupamento nunca pode confiar em `===` bruto. `gerarPainelEquipe()` salva o resultado com `tipo: "painel"` (sem `expiraEmDias`, é só agregado, sem dado sensível), reaproveitado por `app/api/painel-equipe/route.ts` (POST) e pela ferramenta MCP `painel_equipe(dias?)`. **US-018 (resumo semanal da equipe) deve reaproveitar `montarPainelEquipe`/os tipos `PainelEquipe`/`VendedorPainel` em vez de recalcular a mesma agregação.**
- (US-017) `app/r/[id]/page.tsx`/`app/imprimir/[id]/page.tsx` passaram a aceitar um segundo `tipo` (`"painel"`, ao lado de `"conversa"`), seguindo o mesmo molde de `entrevista-ia` (`"ranking"` ao lado de `"entrevista"`/`"scorecard"`): busca o registro sem tipar (`obter<unknown,unknown,Meta>`) primeiro só para decidir o branch pelo `tipo`, e cada branch busca de novo já tipado. `ConteudoPainel`/`ResultadoPainel` (exportados de `app/page.tsx`, paralelos a `ConteudoAnalise`/`Resultado`) reaproveitados sem alteração pelas duas páginas.
- (US-017) **Cuidado com `Chip nivel="neutro"` (`components/ui.tsx`): apesar do nome, esse nível renderiza com a cor de **warn** (laranja, `chip-media`/`chip-neutro` em `globals.css`), não uma cor neutra/cinza.** O nível realmente neutro (cinza-azulado, sem carga semântica) é `nivel="neutral"` (grafia diferente, classe `chip-neutral`, já usado em vários apps para tags informativas tipo "Pendente"/"Cobrança agendada") ou `nivel="cinza"` (cinza puro, usado em `voz-do-cliente`). Usado aqui para a tendência "Estável" (`AcoesVendedor`/`VendedoresPainel` em `components/VendedoresPainel.tsx`, junto de `positivo`/`negativo` para "Subindo"/"Caindo") e para o chip "Em breve" do bloco de cartões desabilitados — a AC pedia "chip neutro" no sentido de "sem carga semântica", que é `nivel="neutral"`, não `nivel="neutro"`. Reler a AC com atenção sempre que pedir um chip "neutro" em português comum.
- (US-017) Uma ação de linha de `DataTable` que precisa tanto expandir dado já embutido na própria linha (sem chamada nova) quanto disparar uma mutação (POST) pode ficar inteira dentro do próprio `render()` da coluna, sem nenhum callback prop: `components/VendedoresPainel.tsx:AcoesVendedor` guarda seu próprio `useState` de aberto/fechado e chama `fetch("/api/salas", ...)` diretamente — funciona igual quando `DataTable` é renderizado a partir de `app/page.tsx` (client) ou de `/r/[id]` (Server Component), porque o componente inteiro já está do lado do cliente (todo `app/page.tsx` é `"use client"`); a regra "callback como prop opcional" (registrada mais acima nesta lista, sobre uma coluna de ação que precisa ser omitida quando chamada do servidor) só se aplica quando a função viria de fora como prop — um handler criado dentro do próprio componente client nunca esbarra nesse limite.
- (US-018) `VendedorPainel` (`lib/types.ts`) ganhou `variacao: number | null`: a diferença `notaMedia - notaMediaAnterior` que `lib/painel-equipe.ts:montarPainelEquipe` já computava por vendedor só para decidir a `tendencia` categórica, agora também exposta como número. Sempre que uma AC pedir um "quem mais X" (mais evoluiu, maior queda etc.) a partir de um agregado que já existe só em forma categórica, prefira expor o número bruto usado internamente (mudança mínima, sem duplicar a agregação) a recalcular tudo de novo em outro arquivo — é o que o próprio `CLAUDE.md` já previa para esta história ("deve reaproveitar `montarPainelEquipe`/`VendedorPainel` em vez de recalcular").
- (US-018) `lib/rotinas-do-app.ts:resumo-equipe` chama `gerarPainelEquipe(7)` (não `montarPainelEquipe` direto) mesmo em modo demonstração, só para ter sempre um link real de painel salvo no histórico para a notificação apontar — o *texto* da notificação é que troca para um exemplo fixo rotulado quando `!aiEnabled()`, porque `montarPainelEquipe` nunca tem noção de demo/real (é agregação pura sobre o histórico) e uma instalação nova sem conversas mostraria "0 conversas" em vez de algo que demonstre o recurso. Esse é um padrão novo em relação a `radar-semanal`/`leads-semanais` (onde a própria função de domínio, `montarRadar`/`buscarLeadsNovos`, já sabe devolver dado de exemplo quando `!aiEnabled()`): aqui a decisão "isto é demo" teve que ficar no executor da rotina, não na função de agregação.
- (US-018) Ao popular dados de teste direto no SQLite para um app cujo `next dev` já está rodando de outra sessão: `lsof -p <pid-do-next-dev> | grep sqlite` mostra o `DATA_DIR` **de verdade** em uso, que pode não ser `<app>/data` (o padrão) se quem subiu o servidor passou `DATA_DIR=/tmp/algo`. Também, o padrão já registrado no `progress.txt` da raiz ("Node 22 roda `.ts` nativo, `await import(caminho-absoluto/lib/x.ts)`") não funciona se esse arquivo (ou algo que ele importa) reexporta de outro caminho relativo sem extensão (ex.: `lib/historico.ts` faz `export { SENSIVEL } from "./sensivel"`) — a resolução de módulos do Node exige extensão explícita, e `--experimental-specifier-resolution=node` não resolve isso no Node 22.23. Mais rápido: um script `.mjs` que abre o mesmo `app.sqlite` com `node:sqlite` puro e replica só o `CREATE TABLE IF NOT EXISTS`/`INSERT` do `lib/*.ts` correspondente, sem importar nada do projeto.
- **(US-033) `GET /api/status` devolve `integrations: { "elevenlabs-agente": ... }`** (era `{}`), com a chave igual ao `id` da integração, como nos demais apps que têm integração própria (`exa` no radar-sinais, `prospecthalo`, `higgsfield`). Todo app com integração além de OpenRouter/Notificações precisa informá-la aqui — é o que a tela usa para saber se a versão real está ligada.
- **(US-033) O campo "Colar uma conversa" usa `min-h-28`**, não `min-h-44`: com a área maior, "Analisar a conversa" (botão primário) ficava abaixo da dobra em um monitor de 1400x900. Ao acrescentar campo novo neste painel, medir de novo (o botão fica hoje em ~896 px, com pouca folga).
- **(US-032 do prd.json) Migração para a fundação nova (conta, erros orientados, desenho de duas colunas) + revisão do app.** O que ficou decidido e medido:
  - **Acento: `#3e68cc`** (Vendas, `tasks/paleta-segmentos.json`). O diagnóstico da história sugeria `#15803d`/`#0f766e`, mas a decisão de cor por segmento (US-002) é superior: o `#3f6212` antigo era oliva e brigava com o verde semântico dos chips, e o azul do segmento resolve isso sem inventar cor. `catalogo.json` atualizado, `scripts/gerar-icones.mjs` rodado, `verificar-paleta` ok.
  - **Rota pública nova no `proxy.ts` compartilhado:** `/simular/*` e `/api/salas/*`. Quem abre o link de treino é o **vendedor**, que não tem conta de administrador — sem isso, o link que o gestor manda cai na tela de entrar. Como `proxy.ts` é comparado byte a byte, a linha (com o comentário justificando) nasceu no `pdi-time` e foi replicada nos 8 apps já migrados, igual a `/s/*` e `/webhook/*`.
  - **Economia de texto (contada):** título do hero "Saiba como cada vendedor conduz a conversa" = 7 palavras (≤8); apoio "Cole uma conversa de vendas: a IA dá a nota, as evidências e o que melhorar." = 16 (≤20); os 5 itens da prévia têm 4, 5, 6, 3 e 5 palavras (todos ≤6); uma linha de ajuda por campo (só "Cole a conversa" e "E-mail" do cadastro de vendedor têm `hint`); benefícios dos 4 cartões de `/setup` em uma linha. **"Analisar a conversa" em 1400x900: `bottom` = 810 px** (medido com Playwright) — a folga é de 90 px, remedir ao acrescentar qualquer campo.
  - **Coerência da demonstração:** o `EXEMPLO` de `app/page.tsx` passou a ser a conversa da Beatriz (`lib/demo.ts`) com `cenarioId: "renovacao"`, e `gerarAnalise` **força** `cenarioId = CENARIO_DEMO` quando `!aiEnabled()`. Sem isso, escolher outro cenário em modo demonstração deixava o título ("Conversa: Cliente pedindo desconto") falando de uma conversa que não é a exibida. `Resultado` ganhou `demoTexto` como prop porque a sala de treino precisa de outra frase: lá a conversa mostrada é a real do vendedor, só a avaliação é de exemplo.
  - **`lib/conversa.ts`:** o parser aceita `Atendente`/`Consultor(a)`/`Representante`/`Falante 1`/`Speaker 1` como vendedor e `Comprador(a)`/`Prospecto`/`Falante 2`/`Speaker 2` como cliente, e descarta carimbo de tempo antes do nome (`[00:12]`, `00:12:45`, `(1:03)`) e sufixo entre parênteses/colchetes depois dele. **Linha não reconhecida continua sendo ignorada em silêncio pelo parser** — quem avisa é a rota (`POST /api/analisar`) e a ferramenta MCP, que comparam `parseConversaColada(...).length === 0` e devolvem `AVISO_SEM_FALAS` (a mesma constante, exportada do parser) com 400 antes de gastar uma chamada de IA.
  - **`lib/legendas.ts` (novo, puro):** converte `.txt`/`.vtt`/`.srt` no servidor (`POST /api/analisar/arquivo`, até 2 MB) para o texto do campo. Falas seguidas do mesmo interlocutor são **juntadas numa linha só** — legenda quebra uma frase em várias telas e uma linha por tela deixaria a transcrição ilegível para a análise. O nome de quem fala vem do marcador `<v Nome>` do VTT ou de um `Nome:` dentro da própria fala; um `12:30` no meio do texto não conta como nome (o candidato a nome precisa ter ≤ 30 caracteres e não terminar em dígito). A rota devolve `falas` (quantas o parser reconheceu) para a tela dizer "N falas reconhecidas" em vez de só trocar o texto do campo.
  - **Sala por voz:** `ESPERA_MAXIMA_MS = 90_000` em `components/SalaSimulacao.tsx`; passado o prazo, um `Aviso` substitui o "Analisando sua conversa..." que girava para sempre. "Já terminei, ver minha análise" agora chama `POST /api/salas/<código>/ligacao` antes de começar a sondar, gravando `salas.ultimaLigacaoEm` (coluna acrescentada com `ALTER TABLE` num `try/catch`, mesmo padrão de `resumo` em `lib/historico.ts`) — é o que faz `ligacoesSemAnalise()` conseguir contar "ligação houve, análise não chegou". O cartão "Dados para a equipe técnica" mostra essa contagem e a última tentativa recusada (`lib/aviso-pos-conversa.ts`, `ELEVENLABS_ULTIMO_ERRO_WEBHOOK`), com motivo em linguagem de negócio (segredo não salvo / assinatura ausente / assinatura não confere / análise falhou).
  - **`ULTIMO_ERRO_WEBHOOK` não pode morar no `route.ts` do aviso:** um `route.ts` do Next só aceita exports de handler, e `GET /api/webhook-info` precisa ler a mesma chave — por isso `lib/aviso-pos-conversa.ts` (registrar/ler), mesmo princípio já registrado na suíte para `app/api/<x>/erros.ts`.
  - **Erros na sala falam com o vendedor, não com o gestor:** as rotas `/api/salas/<código>/conversar` e `/analisar` nunca repassam a frase de `ErroIA` (que manda "conectar a IA em Configurações" — coisa que o vendedor não tem como fazer); devolvem 502 com frase própria ("O cliente simulado não conseguiu responder agora...") e o `codigo` técnico só em `console.error`. Regra geral para qualquer tela pública por link: o texto de erro tem que ser acionável por quem está olhando, não por quem administra.
  - **`lib/crm.ts` e `lib/envio-analise.ts` (novos):** o cartão "Levar as notas para o CRM (Em breve)" virou ação real no menu "Mais" do resultado (`Entregar.extras`), junto de "Enviar a análise ao vendedor". `Resultado` ganhou `acoesDoGestor` (padrão `false`): a sala de treino renderiza o mesmo componente para o vendedor, que não tem sessão nem acesso a essas rotas. A escolha da ferramenta remota do CRM segue o mesmo método de `prospeccao-linkedin/lib/crm.ts` (pontuar nome+descrição por palavras-chave, aqui de anotação/atividade em vez de contato) e `montarArgumentos` preenche o schema desconhecido por regex. Mandar uma análise **de exemplo** ao CRM é bloqueado com 400 (`meta.demo`) — nunca deixar dado fictício entrar no sistema de registro do cliente.
  - **Setup da ElevenLabs:** `ELEVENLABS_WEBHOOK_SECRET` ganhou `avancado: true` (sai da grade principal para "Opções avançadas") e o `link` da integração passou a apontar para **API Keys**, não para a lista de agentes — `passosSetup` (compartilhado) monta o passo 1 como `Abra "<link.rotulo>" e copie a chave`, então o link precisa ser o lugar de onde a chave sai, senão o passo mente. O endereço de criação do agente ficou na `ajuda` do campo do agente. O prompt-modelo e as três variáveis dinâmicas (`sala_token`, `vendedor_id`, `cenario`) estão no cartão técnico (com botão de copiar) e no README.
  - `salas.ultimaLigacaoEm` e `ligacoesSemAnalise()` comparam `ultimoResultadoEm < ultimaLigacaoEm` — uma sala reaproveitada (link sem limite de usos) com uma análise antiga continua contando como pendente até a análise nova chegar, que é o comportamento desejado.
- **(US-001, PRD `prd-simulador-vendas-produto-simulacao-sessoes.md`, 17/09/2026) O app virou independente.** A partir do commit `567f2d6` (o último em que este app era uma cópia da camada visual do `pdi-time`), `components/ui.tsx`, `components/setup.tsx`, `components/conta.tsx`, `lib/navegacao.ts`, `lib/ilustracao.ts` e `app/globals.css` passam a ser **próprios deste app**: seis telas (Início | Produtos | Simulações | Equipe | Resultados | Configurações) não cabem no molde de tela única do `pdi-time`. Para voltar ao estado compartilhado de qualquer um desses arquivos, `git show 567f2d6:pdi-time/<caminho>` é a referência. A `INFRA` (conta, banco, IA, MCP, rotinas, formulários, `proxy.ts`, assets) **continua byte a byte igual ao `pdi-time`** e é conferida em toda execução do verificador — independência é da camada de produto, nunca da infraestrutura.
  - **O PRD (US-001) erra ao dizer que "o modelo já existe desde o `whatsapp-atendente`, nenhuma mudança no script".** Não existia: `scripts/verificar-padrao.sh` só conhecia `"padrao": "proprio"` (usado pelo `automl-pocket`), que tira o app **inteiro** da conferência — forte demais, porque perderíamos também a conferência da infraestrutura. O `whatsapp-atendente` nunca teve flag nenhuma e segue o padrão completo até hoje. O script ganhou então a separação que faltava: a antiga lista `ARQUIVOS` virou `INFRA` (conferida em todo app, sempre) + `CAMADA_PRODUTO` (os seis arquivos acima, pulados só nos apps com `"independente": true` no `catalogo.json`). Nenhum outro app mudou de comportamento; a saída do verificador é idêntica à anterior para os 16 demais.
  - `NAVEGACAO` (`lib/navegacao.ts`) traz os seis destinos e **"Histórico" saiu do cabeçalho** — é uma lista de arquivos gerados, não um lugar de trabalho; passa a ser linkado de `/resultados`. O item ativo já era calculado pelo `Topbar` compartilhado (`ativo(href)`, com `startsWith` para as subrotas), sem nenhuma mudança necessária.
  - `app/produtos`, `app/simulacoes`, `app/equipe` e `app/resultados` nascem como telas reais com `Topbar` + `Empty`, cada uma apontando para o que já funciona hoje (o início, para analisar conversa e criar sala de treino; o histórico, para os resultados salvos). Nenhum destino do cabeçalho pode responder 404 — um menu que leva a lugar nenhum é pior que um menu menor.
  - `proxy.ts` **não mudou**: as quatro telas novas nascem privadas por serem rotas não listadas, e `/simular/*` + `/api/salas/*` continuam públicas (D13 do PRD: os endereços públicos não mudam, porque `proxy.ts` é infraestrutura comparada byte a byte nos 17 apps).
- **(US-002) Produto → Simulação → Sessão no banco.** Cinco tabelas novas (`produtos`, `fontes_produto`, `simulacoes`, `participantes`, `sessoes`, `mensagens_sessao`) no mesmo `app.sqlite`, com o comportamento em `lib/produtos.ts`, `lib/simulacoes.ts`, `lib/participantes.ts` e `lib/sessoes.ts`.
  - **`lib/banco.ts` centraliza esquema + migração, e é a única exceção ao `abrir()` privado por arquivo** que `lib/vendedores.ts`/`lib/cenarios.ts`/`lib/salas.ts` usam. Três motivos: (1) trinta sessões simultâneas no mesmo link pedem **uma** conexão, não cinco — por isso `banco()` devolve a conexão de `lib/store.ts` (`abrirBanco()`), serializando as escritas no processo e tirando o `SQLITE_BUSY` da mesa; (2) as cinco tabelas se referenciam, e espalhar os `CREATE TABLE` faria a ordem de criação depender de qual módulo foi importado primeiro; (3) a migração toca todas de uma vez e tem que rodar **depois** do último `CREATE TABLE`. As tabelas antigas continuam com as conexões delas — nada a mexer.
  - **WAL ligado em `lib/banco.ts`** (`PRAGMA journal_mode = WAL`), não em `lib/store.ts`, que é INFRA comparada byte a byte. O modo é persistente no próprio arquivo do banco, então pedir uma vez de qualquer conexão vale para o arquivo inteiro, inclusive para as tabelas que abrem conexão própria.
  - **A migração é idempotente por construção, não por flag:** todo `INSERT` dela usa `INSERT OR IGNORE` com id/código **já conhecido** (o produto de exemplo é `produto-exemplo`; a simulação de um cenário é `exemplo-<id do cenário>`; o participante herda o id do vendedor; a simulação de uma sala herda o **código** da sala). Rodar de novo, em qualquer processo, nunca duplica — mesmo princípio da semeadura de `lib/cenarios.ts`, e o motivo de nenhum id ser sorteado ali. Ela roda dentro de uma transação e, se falhar, faz `ROLLBACK` e só registra no `console.error`: o app precisa subir mesmo com a migração quebrada, senão ninguém entra nem para investigar.
  - **O id do vendedor vira o id do participante.** É o que faz todo `Conversa.vendedorId` já gravado em `lib/historico.ts` continuar apontando para a pessoa certa depois da migração — sem isso, o painel da equipe perderia o histórico inteiro. Pelo mesmo motivo o **código** da sala vira o código da simulação: link já enviado continua abrindo.
  - **`participantes.email` é `UNIQUE` mas aceita `NULL`** (em SQLite vários `NULL` convivem numa coluna `UNIQUE`): é o que permite trazer, sem inventar endereço, quem foi cadastrado só com o nome. `garantir({nome,email,origem})` exige e-mail e **só sobrescreve o nome quando o salvo está vazio** — quem o gestor cadastrou como "Ana Souza" não vira "ana" porque digitou com pressa no link. `participantes.equipe` existe fora da lista da AC de propósito: é a coluna `vendedores.equipe`, trazida para a migração não perder dado (nada escreve nela hoje; a US-026 decide o destino dela).
  - **`lib/vendedores.ts` virou uma casca fina sobre `lib/participantes.ts`.** A AC pedia que a tabela antiga deixasse de ser lida, mas sete módulos importam `vendedores` (`lib/analise.ts`, `lib/painel-equipe.ts`, `lib/crm.ts`, `lib/envio-analise.ts`, `lib/rotinas-do-app.ts`, `app/api/vendedores/route.ts`, `app/simular/[token]/page.tsx`). Reescrever todos no meio do PRD seria um raio de alcance grande sem necessidade: a casca mantém a assinatura `Vendedor` e passa tudo para `participantes`, então a tabela `vendedores` realmente parou de ser lida e nenhum chamador mudou. **A US-026 é quem apaga a casca.** Nenhum `DROP TABLE`: a tabela antiga continua no arquivo, intacta.
  - **`contarPorPersona` conta toda sessão, inclusive abandonada** (quem abriu o link e desistiu já consumiu aquela persona da rodada), mas **`tentativasDe` ignora as abandonadas** (fechar a aba sem conversar não gasta uma chance). As duas contagens servem a perguntas diferentes e não devem ser unificadas.
  - Sessão parada em `preparando` por mais de 30 minutos vira `abandonada` **na leitura** (`marcarAbandonadas()`, chamada por `obter`/`listar*`/`tentativasDe`), sem tarefa agendada — o app não tem agendador e uma sessão esquecida não incomoda ninguém até alguém olhar a lista.
  - **Rodar os `lib/*.ts` fora do Next continua exigindo um resolvedor:** os imports relativos do projeto omitem a extensão (`./store`) e o Node não resolve isso. O teste desta história usou um gancho `register()` de ~15 linhas que acrescenta `.ts` e resolve o atalho `@/` — mais barato que o script `.mjs` que replica os `CREATE TABLE` à mão, sugerido no gotcha da US-018, e testa o código de verdade em vez de uma cópia dele.
- **(US-003) Biblioteca de produtos.** `/produtos` (lista + cadastro inline), `/produtos/[id]` (dados, materiais e, a partir da US-006, a ficha), `GET/POST /api/produtos` e `GET/PUT/DELETE /api/produtos/[id]`.
  - **O produto de exemplo é escondido, nunca apagado.** `produtos.listar()` devolve os de exemplo só enquanto não existir nenhum produto real; `listarTodos()` é a saída para quem precisa resolver o nome do produto de uma simulação. Apagar seria mais simples e está errado: as simulações migradas das salas antigas (US-002) apontam para `produto-exemplo`, e apagá-lo deixaria sem produto um link que o gestor já mandou para o time.
  - **Um produto com simulação ATIVA não é apagado** (409). A frase diz o que fazer ("Encerre o treino em Simulações e apague o produto depois") e vem com `acao` apontando para lá — a regra de "erro acionável por quem está olhando" que o app já segue na sala. Simulação pausada ou encerrada não bloqueia.
  - **Busca inicial de tela sempre em forma de corrente (`fetch().then()`), nunca `await carregar()` dentro do `useEffect`.** A regra `react-hooks/set-state-in-effect` do `eslint-plugin-react-hooks@7` acusa qualquer chamada direta a uma função que mexe em estado no corpo de um efeito, **mesmo sendo assíncrona** — ela não distingue "setState depois de um await". O `carregar()` em `useCallback` continua existindo para recarregar depois de salvar/apagar (chamado de handler, onde a regra não se aplica). Vale para toda tela nova deste PRD.
  - **Gotcha do SQLite achado pelo teste em banco novo:** `prepare()` sobre tabela inexistente **lança na hora**, não na execução. `migrarSalas` preparava `SELECT titulo FROM cenarios` antes de conferir se a tabela existia; numa instalação nova a tabela `salas` existe (o `instrumentation.ts` a cria ao limpar os links vencidos) mas `cenarios` só nasce na primeira leitura de `lib/cenarios.ts`, então a migração inteira caía no `ROLLBACK` — inclusive o produto de exemplo, que some sem qualquer aviso na tela. **Sempre testar migração com o banco vazio, não só com o banco antigo cheio:** o caminho "algumas tabelas antigas existem, outras não" é o mais provável na vida real e o que nenhum dos dois testes óbvios cobre.
- **(US-004) Importar a página do produto.** `lib/extrair-pagina.ts` (server-only, sem dependência nova) + `POST/GET /api/produtos/[id]/fontes` e `DELETE .../fontes/[fonteId]`.
  - **O endereço vem de quem usa o app, então toda busca aqui é uma requisição que um estranho manda o nosso servidor fazer.** A conferência é feita contra o esquema, contra o nome **e contra o IP resolvido** (`dns.lookup(..., { all: true })`, recusando se **qualquer** endereço resolvido for interno) — só olhar o texto do endereço não pega um domínio público apontando para `127.0.0.1` ou para `169.254.169.254` (o metadata da nuvem). Os redirecionamentos são seguidos **na mão** (`redirect: "manual"`, no máximo 3) porque o `fetch` seguindo sozinho não passaria pela conferência: o primeiro endereço é público e o segundo é que seria interno. Esse é o caminho de ataque real, e tem teste.
  - **Entidades acentuadas são decodificadas por composição, não por tabela:** `&aacute;` é "a" + acento agudo (`́`) e `.normalize("NFC")` junta os dois. Sete linhas cobrem a acentuação inteira do português em vez das ~60 entidades do HTML. Sem isso uma página brasileira chegava ao prompt escrita `implanta&ccedil;&atilde;o` — foi o que o teste pegou, e é o tipo de coisa que um teste só com HTML em inglês nunca encontraria.
  - **`prepare`/sintaxe TypeScript que o Node não roda:** propriedade de parâmetro (`constructor(readonly codigo: X)`) é sintaxe que o TypeScript **transforma**, e o Node só sabe **apagar** tipos (`ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX`). O Next compila as duas formas, então isso só aparece ao rodar o arquivo direto pelo Node — que é como os testes deste PRD funcionam. Declarar o campo e atribuir no corpo resolve. Vale para `enum`, `namespace` e decorator também.
  - **Nem toda página deixa ler.** Site com proteção contra robô responde 403 ao nosso `User-Agent` (conferido: `anthropic.com` responde 403; a Wikipédia em português lê inteira, 18 mil caracteres, com acento preservado). Isso não é defeito a consertar: é exatamente por isso que **toda** frase de erro da rota termina mandando colar o texto à mão, que é o caminho garantido. A frase cita o campo "Texto", que só nasce na US-005 — é a única referência para a frente aqui, e está assim porque a AC pede essa frase literal.
  - Importar ou remover material **volta o produto para "rascunho"** quando a ficha já tinha sido gerada: a ficha da US-006 descreve um material que mudou, e treinar o time com ela seria pior que pedir para gerar de novo.
- **(US-005) Materiais por arquivo e por texto.** A mesma rota `POST /api/produtos/[id]/fontes` atende os três caminhos, decidindo pelo `Content-Type`: `multipart/form-data` é arquivo, JSON é `{tipo:"landing"}` ou `{tipo:"texto"}`. Uma rota só porque o destino é o mesmo (texto normalizado em `fontes_produto`) e porque as regras comuns — teto de 10 materiais, voltar o produto para "rascunho" — valem para os três sem repetição.
  - **`EXTENSOES_MATERIAL` é uma lista à parte de `EXTENSOES_ACEITAS`** em `lib/legendas.ts`, e as duas devem continuar separadas: `EXTENSOES_ACEITAS` responde "de onde sai uma conversa para analisar" (uma transcrição, `.txt`/`.vtt`/`.srt`), `EXTENSOES_MATERIAL` responde "de onde sai o que a empresa vende" — e aí um `.md` de apresentação comercial cabe. `textoDoArquivo` já trata `.md` corretamente (extensão desconhecida volta como texto puro, sem BOM), então nada mais precisou mudar.
  - **`.pdf`, `.docx` e `.pptx` ficam de fora** enquanto a Q1 das Open Questions não for respondida (exigem dependência nova e a decisão vale para a suíte). A tela **diz em uma linha** quais formatos lê hoje e põe o campo "Texto" **ao lado** do envio, não escondido atrás de "mais opções": enquanto não lermos esses formatos, colar é o caminho garantido e quem chega com um PDF precisa ver a saída na mesma tela, sem procurar.
  - Remover material devolve o produto a "rascunho" quando a ficha já existia — mesma regra de importar página (US-004), pelo mesmo motivo: a ficha passa a descrever material que não está mais lá.
- **(US-006) A ficha do produto.** `lib/conhecimento.ts` (server-only) + `POST/PUT /api/produtos/[id]/conhecimento` + `components/FichaProduto.tsx`.
  - **REGRA PARA AS HISTÓRIAS SEGUINTES (D12 do PRD): a ficha é a única coisa que o cliente simulado (US-007/US-015) e o avaliador (US-018) recebem sobre o produto.** `fontes_produto` guarda o material bruto **só** para gerar a ficha de novo; nenhuma fonte crua pode entrar em prompt de simulação ou de avaliação. É por isso que a ficha tem tamanho fechado: ela vai inteira, em **todo turno de conversa**, e um campo sem teto viraria um prompt sem teto.
  - **Os tetos são aplicados no código, depois da resposta, nunca só pedidos no prompt** (resumo ≤ 600 caracteres, listas com até 6 itens de ≤ 120). `normalizarConhecimento` roda nos **dois** caminhos — na geração e no `PUT` da tela — porque um campo editado à mão também entra em todo turno, e o prompt não pode crescer porque alguém colou três páginas num campo. Conferido: resumo de 900 caracteres volta com 600, 9 itens voltam 6, item de 200 volta com 120, repetido sai.
  - **`lista()` não completa para chegar ao mínimo.** Inventar item para bater uma contagem é exatamente o que o prompt proíbe; o mínimo de 3 existe só para `fichaMagra()` avisar na tela que falta material. Lista vazia é resposta correta quando o material não diz nada a respeito.
  - **Gerar não marca "pronto"; salvar marca.** A ficha é a verdade da empresa e só vale depois que uma pessoa olhou — por isso `POST` devolve a ficha sem gravar e é o `PUT` que grava e muda o status. Sem isso, um produto ficaria "pronto" com o que o modelo chutou.
  - Sem IA conectada, `gerarConhecimento` devolve `conhecimentoDemo()` com `meta.demo`, e a tela mostra `SeloIA demo` + um `Aviso` de uma linha com link para `/setup` — mesmo molde do resto do app.
  - **Gotcha do `verificar-jargao.mjs`: o endereço das configurações não pode ser escrito dentro de um `components/*.tsx`.** O script proíbe a string `/setup` ali, e só limpa **atributos técnicos** (`href=`, `src=`, `className=`…) antes de varrer — por isso `<Link href="/setup">` em `components/ui.tsx` passa e `acao={{ url: "/setup#openrouter" }}` em `components/FichaProduto.tsx` não passava. A correção certa não é uma exceção no `scripts/jargao-excecoes.json` (que está vazio em toda a suíte): é receber a ação como prop, porque um componente reaproveitável não deve mesmo conhecer as rotas do app. Telas fora de `app/page.tsx` não são varridas, então quem passa a ação é a tela.
- **(US-007) Catálogo de personas e o cliente simulado.** `lib/personas.ts` (puro: sem `node:*`, lido pelo servidor e pela tela) e `lib/cliente-simulado.ts` (`montarPersonagem`), os dois exercitados por `node --test` com o gancho `register()` de resolução de `.ts`.
  - **`montarPersonagem` recebe uma `semente` e quem chama deve sempre passá-la — na prática o id da sessão.** O personagem é remontado a cada turno da conversa (a sessão grava o `personaId`, não o personagem), e um sorteio sem semente trocaria o nome, o cargo e a empresa do cliente **no meio da ligação**. O sorteio é um FNV-1a com um sal por campo, então a mesma sessão devolve sempre o mesmo cliente e sessões diferentes variam.
  - **Os cargos das listas fixas são todos neutros em gênero** ("Gerente de…", "Head de…"), de propósito: o nome vem de outra lista e um "Diretor Camila Duarte" quebraria a ilusão na primeira fala. Ao acrescentar segmento ou cargo, manter a regra. O que varia por segmento é cargo e empresa; o nome não — uma pessoa chamada Renata trabalha em qualquer ramo.
  - **A dificuldade é parâmetro, nunca frase solta no prompt (D7).** `atributosDe()` soma os deltas aos atributos da persona com `clamp(0, 2)`, e a quantidade de objeções sai da regra da dificuldade (fácil 1, realista 2–3, difícil 3–4, com exigência de prova). As objeções vêm da **ficha do produto** (as reais deste produto), giradas pela semente e completadas pelas de reserva só quando a ficha não tem o suficiente.
  - **O `contexto` é o único campo que o vendedor lê antes da conversa e por isso não pode citar o nome da persona** (D2: a persona só é revelada no feedback). O motivo de o cliente ter aceitado a conversa é escrito por persona em `MOTIVOS`, com as palavras do comportamento e nunca com o rótulo — há teste que varre os sete contextos procurando os sete nomes e os sete ids. Ao escrever motivo novo, lembrar que "Cético", "Direto" e "Especialista" são palavras comuns que não podem aparecer ali.
  - As cinco regras invioláveis do prompt (não sair do personagem, não dar dica de vendas, não revelar a persona nem a simulação, 1 a 3 frases em português do Brasil, quem encerra é o vendedor) têm teste que confere frase por frase nos 7 × 3 prompts. Mudar a redação delas quebra o teste de propósito.
  - A dívida da US-002 foi paga: `lib/banco.ts` importa `PERSONAS_IDS` de `lib/personas.ts` em vez da constante local. Um id novo no catálogo passa a valer para as simulações migradas sem tocar na migração.
- **(US-008) Atribuição equilibrada da persona.** `lib/atribuicao.ts` (puro) com `escolherPersona({ simulacao, contagem, jaUsadas })`, chamado de `sessoes.abrir`.
  - **`sessoes` era o nome de DUAS tabelas no mesmo `app.sqlite`** — a de login (`lib/conta.ts`, infraestrutura byte a byte nos 17 apps) e a de treino (US-002). Com o nome repetido, quem criasse primeiro vencia o `CREATE TABLE IF NOT EXISTS` da outra; como o gestor faz login antes de qualquer vendedor abrir um link, na prática o login sempre vencia e `banco()` **inteiro** lançava `no such column: simulacaoCodigo` na primeira tela de produto. A tabela de treino passou a ser `sessoes_treino`. **Ao criar tabela nova neste app, conferir antes os nomes que `lib/conta.ts` e `lib/store.ts` já usam** (`usuarios`, `sessoes`, `config`): a infraestrutura não pode ser alterada para abrir espaço, então quem chega depois é que muda de nome.
  - A renomeação tem migração (`renomearSessoesDeTreino`), com três conferências que não podem sair: `sessoes` existe, `sessoes_treino` ainda não existe, e a `sessoes` encontrada tem `simulacaoCodigo`. A terceira é a que impede a versão nova de renomear a tabela de **login** de uma instalação que nunca rodou o ciclo. Os índices mantêm o nome antigo (`idx_sessoes_*`) de propósito: o `ALTER TABLE ... RENAME TO` leva os índices existentes junto, e um nome novo criaria um segundo índice igual.
  - **"Persona aleatória" é distribuição, não sorteio (D8).** Sorteio limpo erra feio em amostra pequena — com 30 vendedores é comum um perfil sair 9 vezes e outro 1, e a aba Personas (US-024) passaria a dizer que o time "vende mal para o Cético" porque dois vendedores o pegaram. A ordem de desempate é: 1) quantas vezes **este** participante já pegou a persona, 2) quantas sessões ela já teve na simulação, 3) acaso. O critério 1 vem antes do 2 porque o equilíbrio é do gestor mas quem abre o link é o vendedor, e repetir o mesmo cliente é o que faz ele parar de treinar. É **contagem, não filtro**: quem já passou por todas volta para a que usou menos, nunca trava.
  - **A atribuição é na abertura da sessão, nunca na criação do link**: o link é da simulação e vale para o time inteiro, então ao criá-lo não existe sessão para contar. As três consultas de `abrir` (contagem, histórico da pessoa, `INSERT`) rodam na mesma chamada síncrona, sobre a conexão única de `lib/banco.ts` — é o que mantém a contagem honesta com trinta links abertos ao mesmo tempo.
  - `escolherPersona` recebe a contagem pronta em vez de ler o banco, e por isso a distribuição é exercitável sem SQLite. `personasDe([])` cai no catálogo inteiro, então a escolha nunca devolve vazio nem lança — inclusive quando a simulação do link não existe mais.
- **(US-009) Criar simulação em três passos.** `app/simulacoes/nova/page.tsx` (1 Produto → 2 Desafio → 3 Compartilhar, com o `Passos` compartilhado) e `POST /api/simulacoes`.
  - **Os dois primeiros passos vivem inteiros no navegador; só o terceiro grava.** Voltar entre passos não chama nada, e `POST /api/simulacoes` acontece uma vez, no clique de "Criar treino e gerar o link". Sem isso, um gestor que voltasse para trocar a dificuldade deixaria um treino órfão com link já gerado para cada ida e volta — e um link que aparecesse antes da confirmação é um treino criado por engano.
  - **A rota nunca recusa por valor fora da lista: ela cai no padrão.** Metodologia, dificuldade, modo de persona, tentativas e duração passam por `umDe`/lista de aceitos e voltam ao padrão quando vêm torto. Só três coisas são recusadas com 400, porque só elas tornam a simulação inútil: produto inexistente, metodologia personalizada com menos de 3 critérios, e voz e texto desmarcados ao mesmo tempo. A tela impede as três antes (botão desabilitado + frase), então o 400 é rede de segurança, não o caminho normal.
  - **No modo "Clientes variados" a simulação grava o catálogo inteiro de personas, não uma escolha.** A escolha de quem cada vendedor encontra é feita na abertura da sessão (`sessoes.abrir`, US-008): o link é do time todo, e decidir a persona aqui desligaria a distribuição.
  - `POST /api/simulacoes` chama `registrarEnderecoPublico(req)` porque o link do treino é o primeiro endereço público real deste app — é o que permite ao e-mail do resultado (US-020) montar link absoluto sem adivinhar o domínio.
  - **`contarFontes()` (`lib/produtos.ts`) passou a devolver a contagem por tipo** (`{ total, landing, documento, texto }`), e `GET /api/produtos` expõe `landings` ao lado de `materiais`. O passo 1 precisa dizer "3 materiais · página importada · ficha pronta", e o tipo importa na frase: "página importada" é algo que o gestor reconhece, "1 material" não diz de onde veio.
  - **O link gerado aqui ainda não abre: `app/simular/[token]/page.tsx` continua lendo a tabela antiga `salas`**, então um código de `simulacoes` cai em "Este link não existe". É a sequência do PRD — **a US-013 é quem liga a tela pública às simulações** — e os links migrados das salas antigas (US-002) seguem abrindo normalmente porque a linha em `salas` continua lá.
  - O componente `Escolha` (local da tela, genérico) é a escolha em cartões usada três vezes no passo 2: `<input type="radio" className="sr-only">` dentro do `<label>`, então continua sendo um grupo de rádio de verdade para teclado e leitor de tela, com `has-[:focus-visible]:ring-*` devolvendo o anel de foco que o `sr-only` esconderia. **US-010 e US-011 devem reaproveitá-lo** em vez de criar outro molde de opção.
- **(US-010) Metodologias e rubricas.** `lib/metodologias.ts` (puro, como `lib/personas.ts`): `METODOLOGIAS` com `spin` (9 critérios), `consultiva` (os 7 de sempre) e `personalizada` (vazia, os critérios são escritos pelo gestor por simulação), mais `criteriosDe`, `agruparCriterios`, `limparCriteriosPersonalizados` e `CRITERIOS_MIN`/`CRITERIOS_MAX`.
  - **O `id` de um critério é o que a avaliação grava (US-018), então ele nunca muda.** `nome`, `descricao` e `grupo` podem ser reescritos à vontade; um `id` renomeado quebraria a leitura de resultados já salvos. Por isso os ids são prefixados pela metodologia (`spin-situacao`, `consultiva-abertura`), únicos na suíte inteira.
  - **`lib/criterios.ts` virou uma derivação:** `CRITERIOS_PADRAO = METODOLOGIAS.consultiva.criterios.map((c) => c.nome)`. Os **nomes** dos 7 consultivos são chave de compatibilidade — `lib/demo.ts` casa o texto de exemplo pelo nome normalizado e o histórico já tem análises gravadas com eles —, então nome e ordem da consultiva não mudam sem olhar `CRITERIOS_DEMO` antes.
  - **O `grupo` de um critério personalizado é deduzido do texto** (`grupoDeTexto`, por palavras-chave, caindo em "Descoberta" quando nada casa): o gestor não escolhe grupo (seria mais uma pergunta num passo que já tem cinco), mas o vendedor vê quatro números (US-020) e todo critério precisa cair em um. Quando o texto fala de objeção **e** de valor ("sustentar o preço sem dar desconto"), vale a objeção — é o momento da conversa em que aquilo é cobrado; a ordem das pistas em `PISTAS` é essa decisão, e mudá-la muda a classificação.
  - **Personalizada sem critério nenhum cai na régua consultiva** (`criteriosDe`). A rota já impede criar assim, mas JSON gravado torto não pode deixar o vendedor sem nota: ser avaliado pela régua padrão é melhor que não ser avaliado.
  - `POST /api/simulacoes` usa `limparCriteriosPersonalizados` e `CRITERIOS_MIN` do módulo, e a tela usa `METODOLOGIAS_LISTA`/`CRITERIOS_MAX` — nenhuma das três cópias antigas da lista de metodologias sobrou. A **dedupe passou a ignorar acento e maiúscula** (antes era `new Set` de texto cru), então "Escuta ativa" e "escuta ATIVA" contam como um critério só.
  - A tela mostra os critérios do método escolhido num `<details>` fechado ("O que a avaliação vai olhar (N critérios)"), agrupado nos quatro momentos. **O React reaproveita o mesmo `<details>` ao trocar de método**, então ele continua aberto — bom para comparar SPIN e consultiva, mas um teste de navegador que clique no `summary` de novo fecha a lista em vez de abrir.
- **(US-011) Regras da simulação e o link.** Os cinco controles (tentativas, tempo, feedback, voz, texto) moram no passo 2 de `app/simulacoes/nova/page.tsx`, num bloco "Regras do treino"; a rota já os aceitava e validava desde a US-009.
  - **Tentativas e tempo são listas fechadas, nunca caixa de número.** Um campo livre convidaria "30 tentativas" e "90 minutos", que não é treino: é o vendedor tentando até acertar por eliminação e uma ligação que ninguém termina. "Sem limite" viaja como o texto `"sem-limite"` e vira o `null` que `lib/simulacoes.ts` grava — por isso o estado da tela é `string`, não `number | null`.
  - **Voz e texto desmarcados juntos são impedidos na tela**, com a mesma frase que a rota devolveria ("Deixe pelo menos um jeito de treinar: por voz ou por texto.") e com o botão travado. O 400 da rota continua existindo como rede de segurança, mas quem está olhando a tela nunca chega nele — regra geral deste app: erro previsível se resolve antes do envio, não com uma viagem ao servidor.
  - **O passo 3 repete as regras escolhidas numa linha** (`resumoDasRegras`) logo abaixo do link. O gestor cola o link no grupo do time sem voltar para conferir o que combinou, e "3 tentativas · 10 minutos · por voz ou por texto · com feedback" é o que ele precisa relembrar ali.
  - **"Como funciona" virou um `<details>` fechado.** O que o gestor veio buscar nesta tela é o link; os seis passos são para quem vai explicar o treino ao time, e ficavam empurrando o link para cima da dobra no celular.
  - **O link da simulação não expira.** Nada no app encerra uma simulação por tempo — `instrumentation.ts` só chama `salas.limparExpirados()`, que é a tabela antiga. O que existia de expiração (30 dias das salas) foi convertido pela migração da US-002 em `status: "encerrada"` para as que já tinham vencido. Quem tira um link do ar é o gestor, mudando o status.
  - **Gotcha de teste em navegador com o `CopyButton`:** o `onClick` dele é `async` (`await navigator.clipboard.writeText`), então o `click()` do Playwright resolve **antes** de "Copiado" aparecer. Conferir a confirmação com `waitFor()`, nunca com `isVisible()` logo depois do clique — e o contexto precisa das permissões `clipboard-read`/`clipboard-write` para a leitura de conferência funcionar.
- **(US-012) Lista de simulações.** `/simulacoes` (cartões com filtro, busca e menu de ações), `GET /api/simulacoes` e `GET/PATCH /api/simulacoes/[codigo]`.
  - **As três contagens do cartão saem de uma consulta agregada cada, nunca de uma por cartão** (`sessoes.resumoPorSimulacao` e `sessoes.notaMediaPorSimulacao`). Dez treinos de trinta vendedores seriam centenas de leituras por carregamento de tela se a nota fosse buscada por sessão.
  - **`notaMediaPorSimulacao` junta `sessoes_treino` com `resultados` em SQL, sobre o mesmo `app.sqlite`.** A nota mora no resultado gravado por `lib/historico.ts` (o mesmo registro do link `/r/<id>`), que é INFRA comparada byte a byte e **não pode ganhar função nova** — a junção é a saída. A conferência em `sqlite_master` vem antes do `prepare` porque `resultados` pode ainda não existir num banco recém-criado, e `prepare` sobre tabela inexistente lança na hora.
  - **`app/simular/[token]/page.tsx` consulta a simulação ANTES da sala.** Treino pausado e treino encerrado têm cada um a sua tela amigável ("Este treino está pausado" / "Este treino foi encerrado"), nunca "este link não existe": quem recebeu o endereço no grupo do time precisa saber se espera ou se pede outro. A ordem importa porque toda sala migrada (US-002) virou uma simulação com o mesmo código, inclusive as vencidas, que nasceram `encerrada`.
  - **`PATCH /api/simulacoes/[codigo]` só muda o status, de propósito.** Trocar o desafio de um treino que o time já começou tornaria as sessões antigas incomparáveis com as novas; "Duplicar" existe justamente para criar a versão seguinte sem estragar a que já rodou. A cópia leva tudo menos o código e as sessões, abre o passo 2 preenchido e **só grava no passo 3**, como qualquer treino novo.
  - **Filtro (Todas/Ativas/Encerradas) e busca são cálculo sobre a lista que já está na tela**, sem voltar ao servidor: são dez ou vinte treinos, e pedir de novo a cada tecla seria mais lento e mais frágil. A busca compara sem acento e sem maiúscula (`normalizar`), e olha nome **e** produto.
  - **Gotcha do `react-hooks/immutability` (eslint-plugin-react-hooks@7):** uma função declarada **depois** do `useEffect` que a chama vira erro ("Cannot access variable before it is declared"), mesmo com hoisting de `function`. Funções auxiliares usadas dentro do efeito de busca inicial precisam ser declaradas acima dele.
  - **"Ver resultados" aponta para `/resultados?simulacao=<código>` enquanto a US-022 não cria `/resultados/[codigo]`** — nenhum destino da tela pode responder 404. Ao implementar a US-022, trocar o destino nos cartões.
- **(US-013) Identificação do vendedor em um toque.** `lib/sessao-vendedor.ts` (cookie próprio), `lib/entrar-vendedor.ts` (OAuth de identificação), `GET /api/salas/[token]/entrar/[provedor]`, `GET /api/salas/entrar/[provedor]/callback`, `POST/DELETE /api/salas/[token]/identificar` e a tela `app/simular/[token]/Identificacao.tsx`.
  - **`/simular/<código>` finalmente abre um treino do modelo novo.** A página consulta a simulação primeiro e só cai no caminho antigo (`lib/salas.ts` + `SalaSimulacao`) quando **não existe** simulação com aquele código — o que na prática só acontece numa instalação onde a migração da US-002 ainda não rodou. Nada mais responde "Este link não existe" para um código criado em `/simulacoes/nova`.
  - **O endereço de retorno do OAuth não pode ter o código da simulação dentro.** Google e Microsoft exigem `redirect_uri` registrado caractere a caractere, e um endereço por treino seria impossível de cadastrar: por isso o callback é `/api/salas/entrar/<provedor>/callback` (rota estática irmã de `[token]`, que o Next resolve com prioridade sobre a dinâmica) e o código viaja no `state`, como `<aleatório>.<código>`. O trecho aleatório é conferido contra o cookie `sv_state` — é ele que impede uma volta forjada.
  - **O escopo é `openid email profile`, nunca `gmail.send`/`Mail.Send`.** As credenciais são as mesmas variáveis do app (`GOOGLE_CLIENT_ID_APP`/`MICROSOFT_CLIENT_ID_APP`), mas o objetivo é outro: aqui o app só quer saber quem é a pessoa. **Nenhum token do provedor é guardado** — o `id_token` é lido em memória, vira um participante e o acesso é descartado. Conferir `aud`, `iss` e `exp` é obrigatório; a assinatura não precisa ser verificada porque o token chega pelo canal do servidor, direto do provedor (é o que o próprio OpenID Connect dispensa no fluxo de código).
  - **O emissor da Microsoft muda de pessoa para pessoa** (`login.microsoftonline.com/<tenant>/v2.0`, porque o endpoint é o `common`): a conferência é por formato, não por igualdade. O do Google é fixo (`https://accounts.google.com`).
  - **O cookie do vendedor (`sv_sessao`) é assinado com HMAC-SHA256** e um segredo por instância guardado em `lib/store.ts` (`SESSAO_VENDEDOR_SEGREDO`, criado na primeira necessidade). Sem assinatura, trocar o id no cookie faria alguém treinar no lugar de outra pessoa e o painel do gestor registraria a sessão no nome errado. Ele guarda **só** ids (participante e, a partir da US-014, sessão) — nunca nome nem e-mail.
  - **`lerSessaoVendedor` recebe o cabeçalho `cookie`, não a requisição**, porque a tela pública é Server Component: lá o cabeçalho vem de `headers()` e não existe `Request` para passar adiante. As rotas passam `req.headers.get("cookie")`.
  - **`?pronto=1` é a confirmação desta visita, e é o que separa "Continuar como Ana Souza" de entrar na conversa.** Sem esse parâmetro, quem volta ao link com o cookie válido sempre vê a tela de confirmação com "Não sou eu" — entrar direto numa conversa que a pessoa não pediu gastaria uma tentativa por engano.
  - **Os botões de Google e Microsoft só aparecem quando a instância tem as credenciais**, e "ou informe seu nome e e-mail" fica **sempre** logo abaixo (D5). Sem nenhuma credencial a tela vira só o caminho manual, com a frase "Informe seu nome e e-mail para começar". Botão que dá erro não aparece.
  - **Quem gastou as tentativas vê a melhor conversa, não uma conversa nova** (`sessoes.melhorSessaoDe`, mesma junção em SQL de `notaMediaPorSimulacao` e pelo mesmo motivo: `lib/historico.ts` é INFRA e não ganha função nova). A contagem é por pessoa: outro vendedor no mesmo link entra normalmente.
  - **Gotcha de teste sem navegador:** JSX com texto interpolado (`Continuar como {nome}`) vira `Continuar como <!-- -->Ana Souza` no HTML do servidor. Um `includes("Continuar como Ana Souza")` sobre a resposta crua falha; no navegador o texto está correto. Comparar por regex tolerante ao comentário, ou conferir com Playwright.
  - **Pendência de sequência (resolvida na US-014):** depois de identificado, um treino do modelo novo mostrava um cartão "Tudo pronto, <nome>". Hoje mostra a preparação; a conversa é a **US-015**. Um link migrado das salas antigas cai direto na sala de hoje, com o participante identificado no lugar do `sala.vendedorId`.
- **(US-014) Preparação — "Seu cliente".** `POST/PUT /api/salas/[token]/sessao`, `app/simular/[token]/Preparacao.tsx` e `sessoes.emPreparacao`/`sessoes.emAndamento`.
  - **O que a rota devolve ao navegador do vendedor é o personagem, nunca a persona.** `montarPersonagem` devolve `{ nome, cargo, empresa, contexto, instrucoes }`; a resposta leva **só os quatro primeiros**. O `personaId` e as `instrucoes` (o system prompt, que traz comportamento, objeções e dificuldade) ficam no servidor: quem lê a resposta da rota descobriria com que tipo de cliente vai falar antes de começar, e o treino viraria decoreba. Vale para toda rota que a sala por voz (US-015) acrescentar.
  - **Abrir a tela de preparação não pode abrir uma conversa nova a cada visita.** O `POST` reaproveita a sessão que já está em `preparando` (`sessoes.emPreparacao`) — sem isso, recarregar a página gastaria uma tentativa, sortearia outro cliente, e o efeito de montagem do React em desenvolvimento (que roda duas vezes) criaria duas sessões sozinho.
  - **Sessão aberta e ainda não começada não pode ser contada contra o próprio dono.** `tentativasDe` conta toda sessão não abandonada, inclusive a que está em `preparando`: por isso a tela **e** a rota leem `emPreparacao ?? emAndamento` antes e só conferem o limite quando não existe nenhuma aberta. Sem isso, um treino de uma tentativa só barraria o vendedor entre ler a preparação e clicar em "Começar conversa".
  - **`marcarAbandonadas()` roda na leitura, então só limpa o que é lido.** A regra dos 30 minutos vale para quem chama `obter`, `emPreparacao`, `emAndamento`, `tentativasDe` ou as listagens — um teste que envelheça a linha no banco e depois peça uma tela que não faz nenhuma dessas leituras (a identificação, por exemplo) vai concluir errado que a regra não funciona.
  - **O `PUT` ("Começar conversa") lê o id da conversa do cookie e cai para `emPreparacao` quando ele não traz.** O cookie é atualizado pela resposta do `POST`; um navegador que o perdeu no meio (aba restaurada, cookie de sessão apagado) ainda precisa conseguir usar o único botão da tela.
- **(US-015) A sala por voz do navegador (nível 2 — o padrão).** `components/SalaVoz.tsx`, `lib/conversa-sessao.ts`, `lib/sala-do-vendedor.ts`, `lib/fala.d.ts`, `POST /api/salas/[token]/conversar` (caminho novo), `POST /api/salas/[token]/voz` e `POST /api/salas/[token]/encerrar`.
  - **A transcrição mora no servidor; o navegador manda só a última fala.** `POST .../conversar` recebe `{ fala, segundo }`, grava em `mensagens_sessao` e monta o prompt do turno com o personagem da sessão (que já carrega a ficha do produto) mais `ultimasMensagens(sessaoId, 20)`. A fala do vendedor é gravada **antes** da chamada ao modelo: ela é a última linha do histórico que o cliente responde, e assim não se perde se o modelo falhar no meio. O corpo `{ transcricao }` continua atendido, e é o que mantém as salas de antes da US-002 funcionando.
  - **Repetir um turno que falhou é `{ retomar: true }`, não a mesma fala outra vez.** A fala do vendedor já foi gravada na tentativa anterior; reenviá-la duplicaria a conversa. Qualquer botão de "Tentar de novo" num turno de conversa precisa deste cuidado.
  - **Quem decide que o tempo acabou é o relógio do servidor** (`sala-do-vendedor.restanteSeg`), nunca o cronômetro da tela: a hora do navegador de quem treina pode estar errada — ou ter sido mexida para ganhar tempo. Com o tempo esgotado, o próprio turno vira a despedida (`falaDoCliente({ despedir: true })`) e **encerra a sessão ali**, sem depender de nenhum clique.
  - **`conversaAberta` é a conferência única das três rotas da conversa, e o `aceitaEncerrada` existe por uma razão específica:** o pedido do resultado chega **depois** de a despedida já ter fechado a sessão. Sem aceitar `encerrada`/`avaliada` — e sem o `sessoes.ultimaDe` como último recurso — quem treinou até o fim do cronômetro perderia justamente o feedback da conversa que completou, porque `emAndamento` já não a encontra e o cookie da tela recarregada não traz o id da sessão.
  - **`POST .../encerrar` é idempotente: sessão já avaliada devolve o resultado gravado** (`historico.obter`), nunca 409 e nunca uma segunda avaliação. Avaliar de novo gastaria o modelo outra vez e daria duas notas diferentes para a mesma conversa.
  - **A chave da ElevenLabs nunca vai para o navegador.** O link do treino é público, então `POST .../voz` gera o áudio no servidor e devolve `audio/mpeg`; sem chave a rota responde **409** (não 502) e a tela troca para a voz do próprio navegador sem dizer nada a quem treina. A voz por tipo de cliente é a US-028 — hoje é a constante `VOZ_PADRAO`, com `ELEVENLABS_VOICE_ID` como escape.
  - **`SpeechRecognition` não está em `lib.dom.d.ts` (TypeScript 5.9):** os tipos que a sala usa estão em `lib/fala.d.ts`, arquivo **sem `import`/`export`** para se juntar ao escopo global (molde de `lib/elevenlabs-convai.d.ts`). Só navegadores do Chrome implementam, e só com o prefixo `webkit` — as duas entradas de `Window` são opcionais de propósito.
  - **Valor que só existe no navegador entra por `useSyncExternalStore`, não por `useState` + efeito.** O cronômetro e "esta tela tem toque?" eram erro de hidratação no celular (React #418): o servidor escrevia "Toque para falar" e o navegador, "Segure o botão". O terceiro argumento (`getServerSnapshot`) é o que vale no HTML do servidor, e o do navegador entra depois, sem divergência de texto e sem o efeito que o `react-hooks/set-state-in-effect` proibiria.
  - **Escrever em `ref.current` durante a renderização é erro de lint (`react-hooks/refs`).** Os dois caminhos que se chamam de dentro de um manipulador da escuta (`iniciarEscuta` ↔ `conversar`) viajam por referência **atribuída em efeito**.
  - **Trava de fim de conversa levantada na primeira linha, não na última.** Entre a despedida e o pedido do resultado há duas esperas (o modelo e a voz): duas chamadas quase simultâneas passam as duas pela mesma porta, e a segunda encontra a conversa já avaliada. Em vez do feedback, o vendedor vê "esta conversa já foi encerrada" no treino que acabou de completar.
  - **Navegador sem voz instalada aceita `speechSynthesis.speak` e não fala nada — e não avisa.** Depois de 250 ms, se `speaking` e `pending` estão os dois falsos, não há o que esperar. O teto do `onend` é proporcional ao tamanho da fala (3 a 15 s); um teto fixo e generoso deixava a tela parada em "Falando..." sem som.
  - **Links migrados das salas antigas passaram a seguir o mesmo caminho de um treino novo** (preparação e conversa). `components/SalaSimulacao.tsx` continua servindo **só** a instalação onde a migração da US-002 ainda não rodou. O cenário gravado na sala antiga deixou de ser usado: manter uma segunda sala significaria manter duas conversas, duas transcrições e duas avaliações vivas ao mesmo tempo.
  - **Pendência de sequência:** a avaliação aqui é a análise consultiva que já existia (`lib/analise.ts`), só com os critérios da metodologia escolhida pelo gestor (US-010). O avaliador por rubrica é a **US-018** e o feedback que ensina é a **US-019** — as duas trocam o miolo de `POST .../encerrar` e a tela de `fim` de `SalaVoz`. `mostrarFeedback: false` já é respeitado (a avaliação é gravada, e só o gestor a vê).
  - **Gotcha de teste em navegador:** `innerText` de elemento com `uppercase` no CSS volta em MAIÚSCULAS ("SUA VEZ DE FALAR"), e o Playwright só reporta `pointer: coarse` com `hasTouch`/`isMobile` no contexto — sem isso um viewport de 390 px ainda é tratado como computador e a sala abre em mãos livres.
- **(US-016) A sala com o agente conversacional (nível 1).** `components/SalaAgente.tsx`, `lib/avaliacao-sessao.ts`, o caminho por `sessao_id` em `app/webhook/elevenlabs/route.ts` e as rotas `ultima`/`ligacao` passadas para o modelo de sessão.
  - **As variáveis dinâmicas passaram a ser da sessão, não da sala:** `sessao_id`, `simulacao`, `produto`, `persona_instrucoes`, `participante`, `duracao_minutos`. Com um link para o time inteiro, o agente é o mesmo e a **conversa** é que muda — `sessao_id` é o que faz a transcrição encontrar de volta quem treinou, em que treino e com que cliente. O prompt fixo do agente virou uma linha (`Siga {{persona_instrucoes}}`) porque qualquer instrução escrita lá brigaria com o personagem daquele treino.
  - **`persona_instrucoes` é a única exceção à regra de esconder as instruções do personagem (D2), e ela é estrutural:** o widget roda no navegador de quem treina e é ele quem fala com o agente, então o system prompt precisa atravessar. Todo o resto continua valendo — preparação, turno da conversa e resultado seguem devolvendo só nome, cargo, empresa e contexto. Ao mexer nesta sala, não transformar a exceção em precedente: nenhuma **rota** pode passar a devolver `instrucoes`.
  - **`lib/avaliacao-sessao.ts` existe porque a avaliação passou a ter duas portas:** a conversa que termina na tela (`POST .../encerrar`) e a que chega de fora (aviso de pós-conversa). Elas têm de dar o mesmo resultado, com os critérios da metodologia do gestor. **A US-018/US-019 muda esta função, e as duas portas mudam junto.**
  - **Aviso de pós-conversa pode chegar duas vezes** (a ElevenLabs reenvia o que não recebeu 200): sessão com `resultadoId` não é reavaliada, e a transcrição só é gravada quando `mensagens_sessao` está vazia para aquela sessão. Sem as duas guardas, a mesma conversa aparece em dobro e com duas notas diferentes.
  - **`GET .../ultima` e `POST .../ligacao` passaram a responder por sessão** (cookie assinado + dono reconferido no banco), caindo no comportamento antigo de `lib/salas.ts` só quando o código não é de uma simulação. A sondagem não precisa mais do parâmetro `desde` no caminho novo: `sessoes_treino.resultadoId` é nulo até a avaliação daquela sessão existir, então não há resultado antigo para confundir com o atual.
  - **`sessoes.conversasSemAvaliacao()`** conta sessão `voz-agente` **encerrada e sem resultado** — é o número que o cartão "Dados para a equipe técnica" soma ao antigo `ligacoesSemAnalise()` das salas. Fechar a sessão no "Encerrar e ver meu resultado" é o que faz essa conta existir: sem isso, uma conversa cuja transcrição nunca volta desaparece sem deixar rastro.
  - **A queda para o nível 2 é por tempo (10 s) e por escolha.** `Promise.race` entre `customElements.whenDefined("elevenlabs-convai")` e um prazo; vencendo o prazo, a sala renderiza `SalaVoz` com as mesmas props e o motivo vai para o `console.error`. **O widget também carrega e mesmo assim não conecta** (agente apagado do outro lado, configuração recusada — conferido: o widget só registra `Cannot fetch config for agent` no console, sem evento que o app possa ouvir), e por isso "Prefiro conversar por aqui" fica visível: quem treina nunca fica preso numa tela que não responde. As duas trocas gravam o `modo` da sessão pelo `PUT .../sessao`, que é o que o painel do gestor lê depois.
  - **Duas saídas lado a lado não cabem em 390 px:** o rodapé da sala precisa de `flex-wrap` (medido: 446 px de conteúdo numa tela de 390). Vale para qualquer rodapé com dois botões de texto longo.
  - **Verificação sem conta real da ElevenLabs:** 39 conferências de rota com o evento assinado à mão (`curl`/`fetch` com o mesmo HMAC da US-015), 4 do caminho antigo por `sala_token`, 18 em navegador com o widget carregando de verdade do `unpkg` (ele carrega e falha ao buscar a configuração do agente fictício — é o esperado), 12 da queda automática com o script bloqueado por `page.route(...).abort()`, e 24 do cartão de `/setup`.
- **(US-017) Tentativas, retomada e histórico do vendedor.** `lib/retomada.ts`, `lib/sessoes.ts:historicoDe`, `app/simular/[token]/meus-resultados/**`, `app/simular/[token]/Moldura.tsx` e o balanço na tela de identificação.
  - **`/r/<id>` é a tela do GESTOR e não está na lista pública do `proxy.ts`** — mandar o vendedor para lá o joga na tela de entrar, com uma conta que ele não tem, no fim do treino. O feedback dele mora em `/simular/<código>/meus-resultados/<conversa>`, dentro do prefixo público, protegido pelo cookie assinado e com o dono **reconferido no banco** (sem isso, trocar o id na barra abriria a conversa inteira de um colega). Os três lugares que apontavam para `/r/<id>` foram corrigidos: a tela de limite (US-013), a sondagem da sala do agente (US-016) e o fim da conversa. **Toda tela nova para quem treina precisa nascer sob `/simular/*`.**
  - **O tipo de cliente é revelado em dois lugares e em nenhum outro: no fim da conversa e no histórico.** `POST .../encerrar` só acrescenta `tipoDeCliente`/`comportamento` na resposta que **traz o feedback** — com `mostrarFeedback: false` nada é revelado, porque a revelação é parte do feedback. A lista de conversas não revela a que ainda está `em_andamento`. Antes disso a regra D2 continua inteira: preparação e turnos não devolvem persona nenhuma.
  - **A retomada mede o tempo desde a ÚLTIMA FALA, não desde o começo da conversa.** Quem parou há dois minutos no meio de um treino de quinze continua nele; quem começou há quarenta e não fala há trinta, não. Medir pelo começo fecharia conversas que ainda estão acontecendo.
  - **Conversa esquecida com 4 falas ou mais é avaliada com o que houver; abaixo disso vira `abandonada`.** Abandonada não gasta tentativa (`tentativasDe`), então quem mal começou não é penalizado — e dar nota a duas frases seria inventar um resultado. O `encerrar` acontece **antes do primeiro `await`** (mesma trava da US-015): duas abas voltando ao mesmo tempo mandariam a mesma conversa para duas avaliações.
  - **`retomarOuFechar` roda na leitura da tela pública**, como `marcarAbandonadas`: o app não tem agendador. Consequência para testes: envelhecer a linha no banco não fecha nada sozinho — é preciso abrir `/simular/<código>` depois.
  - **O balanço ("Você já treinou 2 de 3 vezes") mora na tela de identificação, não numa tela nova.** Ele só aparece quando **não há conversa em aberto**: com uma conversa em andamento, o botão continua sendo "Continuar como <nome>", que retoma aquela conversa — o `?pronto=1` da US-013 continua separando "confirmo que sou eu" de "entro numa conversa". Quem esgotou as tentativas perde o "Treinar novamente" mas mantém "Não sou eu" (vendedor em computador compartilhado é comum); a tela "Você já concluiu este treino" continua sendo a guarda de quem chega com `?pronto=1` na mão.
  - **`app/simular/[token]/Moldura.tsx` é o molde das telas do vendedor** (marca + cartão, `MARCA`/`NOME_APP`, `largo` para o feedback). Toda tela nova de quem treina deve usá-lo: é o que mantém a promessa de "sem menu do app, sem cabeçalho de gestor, sem caminho para as configurações" verdadeira sem depender de lembrar dela.
- **(US-018) Agente avaliador.** `lib/avaliacao.ts` (substitui `lib/avaliacao-sessao.ts`), `lib/analise.ts:salvarResultado`, `lib/demo.ts:avaliacaoDemo`, `app/page.tsx:ConteudoSessao`/`ResultadoSessao`, o terceiro tipo em `/r/[id]` e `/imprimir/[id]`, `app/api/sessoes/**` e a seção "Avaliações pendentes" em `/resultados`.
  - **O histórico ganhou um terceiro tipo: `"sessao"`.** `"conversa"` continua sendo a conversa real colada no painel (D11) e `"painel"` o resumo da equipe. Toda tela que lê um resultado precisa decidir o que faz com os três — `/r/[id]`, `/imprimir/[id]` e o feedback do vendedor já decidem, e quem criar a quarta tela de leitura tem de lembrar. A saída de um `"sessao"` é `AvaliacaoSessao` (`notaGeral`, `criterios` com `id`/`grupo`/`semEvidencia`, `grupos`, `pontosFortes`, `oportunidade`, `contexto`), **não** `Analise`.
  - **A nota geral não é um campo que a IA devolve, e a nota por grupo também não.** O prompt manda explicitamente *não* devolver nota geral; `montarAvaliacao` calcula a média dos critérios e as quatro médias por grupo com uma casa decimal. Isso é o que permite o gestor comparar dois vendedores: duas conversas avaliadas pelo mesmo modelo em dias diferentes ainda somam do mesmo jeito.
  - **Evidência é citação conferida, não texto de confiança.** O prompt pede o trecho entre aspas; o código extrai as citações, normaliza (sem acento, sem pontuação, sem caixa) e confere contra a transcrição — trecho que não aparece é descartado (`evidencia: ""`, `semEvidencia: true`, **nota mantida**) e a tela diz "Sem trecho da conversa para citar aqui" em vez de exibir uma fala que ninguém disse. Trecho longo vale pelas 8 primeiras palavras, para uma vírgula trocada não derrubar uma citação boa.
  - **`notaDoResultado` (lib/sessoes.ts) lê `saida.nota`, e a avaliação de sessão não tem esse campo.** Ela tem `notaGeral`. As junções em SQL que resolvem nota (`notaMediaPorSimulacao`, `melhorSessaoDe`, `historicoDe`) leem os dois nomes — ao acrescentar campo de nota a qualquer saída nova, conferir essa função antes.
  - **A gravação no histórico mora num lugar só: `lib/analise.ts:salvarResultado`** (tipo, título, resumo, conversa como entrada, 90 dias). `gerarAnalise`, `salvarConversaAnalisada` e `avaliarSessao` passam por ela — nenhuma chama `historico.salvar` direto.
  - **`avaliarSessao` recebe o `id` da sessão, não o objeto.** Ela relê do banco: quem a chama acabou de fechar a sessão, e passar o objeto em mãos convidava a esconder um campo ajustado ali (foi o que acontecia com a duração da ligação do agente). Duração que vem de fora agora entra por `encerrar(id, { duracaoSeg })`, gravada, não por argumento de avaliação.
  - **Falha de IA deixa a sessão em `encerrada` sem `resultadoId` — e isso tem uma tela.** `pendentesDeAvaliacao()` + `GET /api/sessoes/pendentes` + `POST /api/sessoes/[id]/avaliar` ("Tentar de novo", que recusa reavaliar quem já tem resultado) aparecem em `/resultados`. A conversa nunca se perde numa falha de avaliação; quando a US-022 construir o painel por simulação, esta lista se muda para lá.
  - **As ações de entrega (`Entregar`) não podem aparecer numa tela do vendedor:** "Baixar PDF" vai para `/imprimir/<id>` e "Copiar link" para `/r/<id>`, as duas rotas privadas. `ResultadoSessao` tem `entregar` (padrão `true`) e as telas de quem treina passam `entregar={false}`. Mesma armadilha da US-017, agora com um interruptor.
  - **A avaliação de exemplo cita a conversa de verdade.** `avaliacaoDemo` devolve o mesmo formato cru que a IA devolveria (e passa pela mesma `montarAvaliacao`), com as evidências copiadas das falas reais do vendedor — é o que exercita a conferência de evidência de ponta a ponta sem chave de IA. O julgamento é fictício e a tela diz isso (`SeloIA demo` + "as notas abaixo não são um julgamento desta conversa").
  - Verificação: 10 testes de unidade de `montarAvaliacao`/`evidenciaValida` por `node --test` com o gancho `register()` de `.ts`, 53 conferências de rota em servidor de verdade (`next start`, `DATA_DIR` em `/tmp`) e 38 em navegador (1400x1000 e 390 com toque). **Sem chave de IA nesta máquina: o caminho com IA real não foi exercitado**, só o de demonstração — o formato cru é o mesmo nos dois, e é ele que a aritmética e a conferência de evidência recebem.
- **(US-019) Feedback que ensina.** `components/FeedbackVendedor.tsx` (novo), `lib/personas.ts:adjetivosDoCliente`/`frasePerfil`, `lib/sala-do-vendedor.ts:balancoDeTentativas`, o `copiarFrase` de `ConteudoSessao`/`ResultadoSessao` e as duas telas que mostram o feedback (`components/SalaVoz.tsx` e `app/simular/[token]/meus-resultados/[sessao]`).
  - **O feedback do vendedor tem duas portas e uma tela só.** Logo depois da conversa (dentro da sala) e pelo histórico (`/simular/<código>/meus-resultados/<conversa>`) — `FeedbackVendedor` é o componente que as duas renderizam, com `ConversaRegistrada` para quem não vê nota. Mudança de conteúdo do feedback se faz lá, nunca em uma das telas: elas divergiriam sem que nada falhasse.
  - **A revelação é uma frase com o nome do personagem, não um rótulo:** "Cláudia estava no papel de um cliente apressado e exigente." O verbo é "estava no papel de" porque os nomes sorteados em `lib/cliente-simulado.ts` são de pessoas de qualquer gênero e os adjetivos concordam com "cliente" — "Cláudia era um cliente apressado" tropeça na leitura. O rótulo (emoji + nome) continua logo abaixo, com o comportamento.
  - **Na sala, o nome do personagem sai da tela, não da rota** (`POST .../encerrar` devolve só os adjetivos em `perfil`): a sala mostrou o nome durante a conversa inteira. Na tela do histórico ele é remontado por `montarPersonagem` com a **mesma semente** (o id da sessão) — remontar é mais barato que gravar o personagem na sessão, e os dois nunca desencontram.
  - **`balancoDeTentativas` (lib/sala-do-vendedor.ts) é a resposta única de "ainda posso treinar?"**, usada pela rota de encerrar e pela tela do feedback. Ela repete a regra da US-014: uma conversa **em aberto** não fecha a porta para o dono dela, porque `tentativasDe` já a conta. Na rota, o balanço é lido **na hora da resposta** (`balanco()`, não uma constante no topo): enquanto a conversa está aberta ela conta como tentativa em andamento, e o balanço diria "pode treinar de novo" justamente quando aquela era a última chance.
  - **`copiarFrase` é um interruptor, como `entregar`:** o botão "Copiar frase" da oportunidade aparece só nas telas do vendedor. Na tela do gestor a entrega inteira já tem "Copiar texto", e numa folha impressa um botão não faz nada.
  - Com `mostrarFeedback: false` a tela do vendedor não tem número nenhum (nem nota, nem momentos, nem rubrica) e o tipo de cliente **não** é revelado — a revelação é parte do feedback. As duas saídas ("Treinar novamente" e "Ver minhas outras conversas") continuam, porque quem fechou a conversa quer saber o que fazer agora do mesmo jeito.
  - Verificação: 6 testes de unidade da frase do perfil (`node --test` com o gancho `register()`, incluindo os 12 nomes × 7 personas × 3 dificuldades), 44 conferências de rota/tela em servidor de verdade (`next start`, `DATA_DIR` em `/tmp`) e 40 em navegador (1400x1000 e 390 com toque), com a frase realmente copiada para a área de transferência conferida contra a que está na tela.
- **(US-020) E-mail do resultado para o vendedor.** `lib/envio-analise.ts` (ganhou `enviarFeedbackDaSessao`, `textoDoFeedback` e o interruptor), a chamada no fim de `lib/avaliacao.ts:avaliarSessao`, as colunas `envioEmail`/`envioEmailMotivo` de `sessoes_treino` (`lib/banco.ts:garantirColuna`, `lib/sessoes.ts:registrarEnvioEmail`/`falhasDeEnvioEmail`), `app/api/envio-feedback`, `app/api/sessoes/envios`, `components/EnvioFeedbackEmail.tsx` e a seção "Não conseguimos enviar por e-mail" em `/resultados`.
  - **O envio mora na avaliação, não nas rotas.** Quatro portas chegam a `avaliarSessao` (o fim da conversa na sala, o aviso de pós-conversa do agente, a retomada e o "Tentar de novo" do gestor) e o vendedor tem de receber o mesmo e-mail por todas. Rota nova que avalie uma sessão não precisa fazer nada — e não deve enviar por conta própria.
  - **`enviarFeedbackDaSessao` nunca lança, e cada motivo para não enviar é uma volta silenciosa:** gestor desligou o envio, participante entrou sem e-mail, treino com `mostrarFeedback: false` (mandar por e-mail o que a tela esconde contornaria a decisão do gestor) ou nenhuma conta de e-mail configurada. Só o envio **recusado pelo provedor** é gravado. Tratar "não configurado" como falha encheria a tela do gestor de linhas vermelhas para algo que ninguém pediu.
  - **A idempotência vem do banco, não de uma marca de processo:** `sessao.envioEmail === "enviado"` é conferido antes de enviar. É o que segura o aviso de pós-conversa (entregue mais de uma vez por definição) e o "Tentar de novo".
  - **O link do e-mail é o do vendedor** (`/simular/<código>/meus-resultados/<sessão>`), nunca `/r/<id>`: aquela tela é privada e quem clicasse cairia em "entrar". Mesma armadilha da US-017/US-019, agora em texto de e-mail — onde nenhum lint pega. Sem `APP_URL` o e-mail sai sem link, e a nota e o que fazer diferente continuam valendo.
  - **O e-mail não leva a rubrica inteira**, de propósito: nota, até três pontos fortes, a única oportunidade e a frase sugerida. Critério por critério com evidência é a leitura do gestor; numa caixa de entrada viraria uma parede de texto que ninguém abre antes de uma ligação.
  - **Coluna nova em tabela que já existe pede `garantirColuna`** (`ALTER TABLE ... ADD COLUMN`, com a conferência antes porque o SQLite não tem `IF NOT EXISTS` aqui). Sem isso, a instalação que já rodava lança "no such column: envioEmail" na primeira avaliação — e um banco recém-criado não mostraria o problema.
  - O interruptor fica num cartão próprio das Configurações (`EnvioFeedbackEmail`), não no cartão "Notificações": aquele é a conta de e-mail (o *como* enviar), comparado byte a byte na suíte; este é o *se* enviar, decisão deste app. A chave é `FEEDBACK_EMAIL_VENDEDOR` (`"0"` desliga; ligado por padrão).
  - Verificação: 44 conferências de rota em servidor de verdade (`node .next/standalone/server.js`, `DATA_DIR` em `/tmp`) com um **servidor SMTP de mentira local** — nenhum serviço externo é chamado e o corpo do e-mail é conferido de verdade (nota, pontos fortes, oportunidade, frase, link, ausência da rubrica) — e 30 em navegador (1400x1000 e 390 com toque). A migração foi exercitada num `app.sqlite` com a tabela no formato antigo. **`next start` avisa que não funciona com `output: standalone`** (o `next.config.ts` deste app): o servidor de teste é `node .next/standalone/server.js`, com `.next/static` e `public` copiados para dentro de `.next/standalone`.
- **(US-021) Modelo de IA por tarefa.** A infraestrutura nasceu no `pdi-time` e está documentada lá (`lib/ai.ts`: `modelName("avaliacao")`, `askText`/`askJSON` com `model`; `lib/setup-comum.ts`: `openrouter({ avaliacao, rotuloModelo })`). Aqui ela aparece em `lib/integracoes.ts` (`openrouter({ avaliacao: true, rotuloModelo: "Modelo para simulação" })`) e em `lib/avaliacao.ts`, que passa `model: modelName("avaliacao")` e declara o mesmo modelo no `meta`.
  - **Este app é o único da suíte com duas pontas de exigência oposta:** o cliente simulado responde a cada fala do vendedor e precisa ser rápido (`lib/conversa-sessao.ts`, `lib/simulacao.ts`); a avaliação acontece uma vez por conversa e vira nota, então vale um modelo mais capaz. Por isso o cartão da IA em `/setup` tem dois seletores.
  - **Só `lib/avaliacao.ts` usa a tarefa `avaliacao`.** A conversa, a ficha do produto (`lib/conhecimento.ts`) e a análise da conversa real colada pelo gestor (`lib/analise.ts`) continuam no modelo padrão. Rota nova que avalie uma sessão chega por `avaliarSessao` e já herda o modelo certo — não deve escolher modelo por conta própria.
  - Em "Automático" (o padrão dos dois campos) a avaliação usa o mesmo modelo da simulação: nada muda para quem não mexeu em Configurações.
- **(US-022) Painel da simulação — Visão geral.** `lib/painel-simulacao.ts` (novo), `lib/sessoes.ts:avaliacoesDaSimulacao`/`resumoPorSimulacao` (ganhou `ultimaSessao`), `app/api/resultados/[codigo]`, `app/resultados/[codigo]/{page,Painel}.tsx` e a lista de treinos em `/resultados`.
  - **Número de painel é cálculo, frase de painel é IA — e nunca o contrário.** Média, média por competência e comparação entre períodos saem de `montarPainelSimulacao`, que é função pura sobre as conversas já avaliadas; a única chamada de modelo é a frase de "Principal oportunidade do time", escrita **a partir dos números já calculados**, nunca das conversas. É a mesma regra da US-018 um nível acima: pedir um agregado ao modelo faria dois carregamentos da mesma tela discordarem entre si.
  - **A frase é guardada por uma hora ou até chegar conversa nova**, com a chave do cache montada do estado do treino (`avaliadas|sessoes|ultimaSessao`) — sem isso, cada vez que o gestor abrisse a tela o app gastaria uma chamada para reescrever a mesma conclusão. E ela **sempre existe**: sem chave de IA, com a IA fora do ar ou com resposta vazia, vale `oportunidadeCalculada`, que monta a frase a partir da competência mais fraca e da descrição dela em `lib/metodologias.ts`. Esta é a linha que diz ao gestor o que fazer; ficar sem ela é pior que tê-la sem IA.
  - **Recorte de período vale para a evolução, nunca para as competências.** As barras somam **todas** as conversas avaliadas do treino: um treino parado há cinco semanas ficaria com todas as barras em zero se a janela de 30 dias valesse ali, e o gestor abre a tela justamente para saber onde o time trava. A janela (atual contra a imediatamente anterior, não sobrepostas — a regra que `lib/painel-equipe.ts` já usava) existe só para o quarto destaque.
  - **`avaliacoesDaSimulacao` devolve a saída como texto, sem interpretá-la.** Quem sabe falar com o banco é `lib/sessoes.ts`; quem sabe o formato da avaliação é `lib/avaliacao.ts`, que já lê as sessões — interpretar lá fecharia o círculo. Mesma junção em SQL de `notaMediaPorSimulacao`, pelo mesmo motivo: uma consulta, não uma por conversa avaliada.
  - **As barras são um SVG próprio de dois retângulos, com `preserveAspectRatio="none"`** para acompanhar a largura da tela sem recalcular nada no navegador; o arredondamento fica no invólucro, em CSS, porque esticar um `rx` deformaria a ponta. Cada barra carrega um `aria-label` com a nota e quantas conversas ela soma — o número está escrito ao lado, mas a barra sozinha não diz nada a quem não vê a tela.
  - **As listas de "Avaliações pendentes" e "Não conseguimos enviar por e-mail" continuam em `/resultados`, e não dentro do painel de um treino** (a US-018 supunha que se mudariam): as duas são pendências do gestor, não números de um treino, e ele não tem por que adivinhar em qual treino cada uma caiu para encontrá-la.
  - **`tsc --noEmit` numa rota nova falha até o `next build` rodar.** `PageProps<"/resultados/[codigo]">` vem do typegen do Next 16, que nasce no build — o erro é `Type '"/..."' does not satisfy the constraint 'AppRoutes'` e não tem nada a ver com o código. Ordem certa: `npm run build`, depois `tsc --noEmit`.
  - Verificação: 55 conferências de rota em servidor de verdade (`node .next/standalone/server.js`, `DATA_DIR` em `/tmp`), com quatro conversas reais de três vendedores e duas delas envelhecidas no banco para 40 dias atrás, para as duas janelas existirem; e 62 em navegador (1400x1000 e 390 com toque). **Sem chave de IA nesta máquina: a frase escrita pelo modelo não foi exercitada**, só o texto calculado e a queda para ele.
- **(US-023) Aba Equipe.** `lib/painel-simulacao.ts:montarEquipe` (e os tipos `VendedorNaSimulacao`/`ConversaDoVendedor`/`Tendencia`), `lib/sessoes.ts:treinosPorParticipante`, `app/resultados/[codigo]/Equipe.tsx` e `app/resultados/[codigo]/apresentacao.ts` (os formatos que as abas compartilham).
  - **A soma das linhas tem de bater com o cabeçalho.** `equipe[].sessoes` conta **todas** as sessões da pessoa no treino (inclusive abandonada e em andamento), e a soma delas é o mesmo "N sessões" do topo — há conferência disso no teste de rota. Contar só as avaliadas esconderia justamente quem abriu o link três vezes e nunca terminou uma conversa, que é com quem o gestor precisa falar.
  - **A nota da coluna é a média da pessoa no treino inteiro; a nota de cada conversa aparece no detalhe e na planilha.** Quem conferir a tela procurando a nota de uma conversa específica na tabela não a encontra, e isso é intencional — um teste que procurasse "8,6" na tabela falharia sem haver defeito.
  - **O recorte de período vale só para a tendência**, como na visão geral (US-022): a janela atual contra a imediatamente anterior, com o mesmo limiar de 0,3 de `lib/painel-equipe.ts`. Sem conversa na janela anterior, a tendência é "estável", nunca uma seta inventada — e a tela explica o limiar em uma linha, senão a seta vira superstição.
  - **"Maior desafio" só existe quando é de fato pior que o "vai melhor com".** Com um tipo de cliente só — ou com todos empatados, que é o caso da avaliação de demonstração — apontar um desafio com a mesma nota do melhor inventaria uma diferença que não houve.
  - **`treinosPorParticipante()` atravessa simulações de propósito:** o painel é de um treino, mas quem vai conversar com a pessoa quer saber se ela é veterana ou se está na primeira vez. Uma consulta agregada para a tela toda, no molde de `resumoPorSimulacao`.
  - **`app/resultados/[codigo]/apresentacao.ts` é onde moram `nota`, `contagem`, `tomDaNota`, `PREENCHIMENTO` e `normalizar`.** Cada aba é um arquivo e todas mostram nota: a régua de cor ("verde a partir de 7") precisa ser a mesma na barra de competência e na coluna da tabela. **A US-024 e a US-025 devem importar daqui**, nunca copiar.
  - **A planilha é uma linha por sessão** (vendedor, e-mail, treino, tipo de cliente, como conversou, situação, data, duração em minutos com uma casa, nota e o link `/r/<id>`), com `;` e BOM, como as exportações de `custos-ia`/`prospeccao-ia`. Duração arredondada para inteiro escreveria "0" numa conversa de quarenta segundos — e conversa curta demais é exatamente o que o gestor quer enxergar ali.
  - **Gotcha de verificação em navegador: `npm run build` recria `.next/standalone` e apaga o `static`/`public` copiados para dentro dele.** O sintoma é uma tela que fica em "Carregando..." para sempre (o JS do navegador responde 404 e a busca inicial nunca acontece), sem erro visível no servidor. Depois de **todo** build, recopiar antes de subir o servidor de teste.
  - **Entrar pelo formulário de `/entrar` no Playwright corre com a hidratação:** `fill` + `click` logo depois do `goto` dispara o envio nativo do formulário e recarrega `/entrar` com os campos na barra de endereço. Numa conferência cujo alvo é outra tela, o caminho estável é `POST /api/conta/entrar` por `fetch` e `context.addCookies` com o cookie devolvido.
  - Verificação: 64 conferências de rota em servidor de verdade (`node .next/standalone/server.js`, `DATA_DIR` em `/tmp`, quatro conversas de três vendedores, uma envelhecida em 40 dias e duas notas trocadas no banco para as janelas e a ordenação existirem) e 56 em navegador (1400x1000 e 390 com toque), com a planilha realmente baixada e lida linha a linha.
- **(US-024) Aba Personas.** `lib/painel-simulacao.ts:montarPersonas`/`dificuldadeComPersona`/`dificuldadeCalculada` (e o tipo `PersonaNaSimulacao`), `app/api/resultados/[codigo]/personas`, `app/resultados/[codigo]/Personas.tsx` e `app/resultados/[codigo]/BarraNota.tsx` (a barra que a visão geral e esta aba dividem).
  - **"Poucos dados" é medido pelas conversas AVALIADAS, não pelas sessões abertas.** A AC fala em "menos de 3 sessões", mas a média sai das avaliadas: uma persona com seis sessões e uma conversa avaliada mostraria uma nota tirada de uma conversa só — exatamente a conclusão errada que a regra existe para evitar. Como toda persona com menos de 3 sessões tem menos de 3 avaliadas, a régua mais apertada cobre a AC e mais um caso. A linha da tela diz quantas faltam ("2 conversas avaliadas — a nota aparece a partir de 3"), senão "poucos dados" não distingue duas conversas de nenhuma.
  - **`sessoes` e `avaliadas` aparecem lado a lado de propósito:** "quantas sessões" é o número que o rodízio da US-008 equilibrou (o mesmo de `contarPorPersona`, contando inclusive quem abandonou), e a nota só existe para as avaliadas. São perguntas diferentes e juntá-las esconderia um treino muito jogado e pouco terminado.
  - **A frase mora numa rota própria (`/api/resultados/<código>/personas`), buscada quando a aba abre.** A lista vem no painel, porque é cálculo puro e sai de graça; pendurar a segunda chamada de IA na resposta do painel gastaria uma chamada por abertura de tela para o gestor que nunca sai da visão geral. Regra geral para as abas seguintes: **cálculo viaja junto, frase de IA viaja quando a aba abre.** Cache e texto de reserva seguem a regra da US-022 (uma hora ou até chegar conversa nova; a chave inclui a persona de menor nota).
  - **"O time vai pior com X" só pode ser dito quando há com o que comparar.** Com um único tipo de cliente acima do mínimo, o texto calculado diz "é o único tipo de cliente com base suficiente" — apontar um pior entre um inventaria uma diferença que não houve, mesma decisão do "maior desafio" da US-023.
  - **O padrão observado é a competência mais fraca *daquelas* conversas** (`pontosFracos`, agregado por persona), não a competência mais fraca do treino: é o que separa "o time vende mal" de "o time trava na abertura quando o cliente desconfia".
  - **`BarraNota` é componente, não cópia.** A barra da competência (US-022) e a da persona são a mesma; duplicá-la deixaria as duas divergirem sem que lint, build ou teste falhassem. `COR_DA_NOTA` entrou em `apresentacao.ts` pelo mesmo motivo que `PREENCHIMENTO`: a régua de cor do número grande tem de ser a da barra.
  - Verificação: 45 conferências de rota em servidor de verdade (`node .next/standalone/server.js`, `DATA_DIR` em `/tmp`, 13 conversas de 3 vendedores em três treinos, com as notas trocadas no banco para as médias por persona ficarem distintas) e 28 em navegador (1400x1000 e 390 com toque), cobrindo os três estados: dois clientes com nota, um com nota e um com "poucos dados", e nenhum com base. **Sem chave de IA nesta máquina: a frase escrita pelo modelo não foi exercitada**, só o texto calculado.
- **(US-025) Evolução ao longo do tempo.** `lib/sessoes.ts:avaliacoesDosParticipantes`, `lib/painel-simulacao.ts:montarEvolucoes` (e os tipos `EvolucaoDoVendedor`/`MesDaEvolucao`/`SequenciaDeGrupo`, mais o campo `evolucao` de `VendedorNaSimulacao`) e `app/resultados/[codigo]/Evolucao.tsx`, dentro do detalhe da aba Equipe.
  - **A evolução é do participante no app inteiro, nunca de um link.** `avaliacoesDosParticipantes` **não** filtra por `simulacaoCodigo`: quem melhorou em "Objeções" melhorou vendendo, não dentro de um treino, e é esse número que dá ao gestor motivo para manter o time treinando todo mês. Consequência conferida em teste: a mesma pessoa abre com a **mesma** linha nos painéis de dois treinos diferentes, enquanto a nota da coluna (que é do treino) difere entre eles.
  - **É uma consulta agregada para o time inteiro da tela, não uma por pessoa** — mesma razão de `treinosPorParticipante`/`notaMediaPorSimulacao`, e a mesma conferência em `sqlite_master` antes do `prepare` porque `resultados` pode não existir num banco novo.
  - **A janela é de 12 meses cortada por mês inteiro** (`new Date(ano, mes - 11, 1)`), não "365 dias atrás": cortar no meio deixaria o mês mais antigo da linha com a média de um pedaço do mês, comparada com meses inteiros ao lado.
  - **Só entram os meses em que a pessoa treinou.** Um mês vazio no meio viraria um buraco na linha — ou, pior, um zero — e a sequência por competência ganharia um "—" que parece nota baixa e é ausência. O rótulo de cada ponto traz o mês, então a lacuna continua legível.
  - **Menos de dois meses é `suficiente: false`, e a área diz "Ainda sem histórico suficiente".** Uma linha de um ponto só desenha uma reta que qualquer gestor leria como "estagnado" — uma conclusão que o dado não sustenta.
  - **`valores` de cada grupo é alinhado índice a índice com `meses`**, e `null` significa "aquele grupo não foi avaliado naquele mês" (acontece de verdade quando a pessoa treinou em treinos de metodologias diferentes). Grupo que não apareceu em mês nenhum sai da lista inteira.
  - **O gráfico é um SVG próprio com o `preserveAspectRatio` no padrão (`meet`)**, ao contrário da `BarraNota`, que usa `none`: esticar sem manter a proporção deformaria os pontos e os rótulos. O teto `max-w-[340px]` existe porque numa coluna larga de tabela o quadro esticado deixaria os meses maiores que o texto ao redor. Os rótulos saem de `passo` em `passo`, **ancorados no mês mais recente** (`(n - 1 - i) % passo === 0`), para doze meses não encavalarem os nomes; a cor de cada ponto é a régua de `tomDaNota` (`apresentacao.ts`), e o `aria-label` do quadro lista os meses e as notas, porque a linha sozinha não diz nada a quem não vê a tela.
  - **Gotcha de teste: o id da sessão não volta em rota nenhuma** — ele viaja no cookie assinado do vendedor (US-013). Um roteiro que precise envelhecer uma conversa no banco lê o id por `ORDER BY rowid DESC` logo depois de criá-la, nunca da resposta. E, como a avaliação de demonstração dá notas iguais, a evolução só existe no teste se as conversas forem movidas para meses diferentes **e** tiverem a `notaGeral` trocada no `resultados` — mexer no dado gravado, nunca no cálculo.
  - **Gotcha de navegador: `DataTable` renderiza a tabela do computador e os cartões do celular ao mesmo tempo no HTML** (um escondido por CSS). Toda busca de elemento numa conferência da aba Equipe precisa de `.locator("visible=true")`, senão o clique cai no invisível e o teste trava esperando um elemento que nunca aparece.
  - Verificação: 30 conferências de rota em servidor de verdade (`node .next/standalone/server.js`, `DATA_DIR` novo, duas pessoas em três treinos, com conversas movidas para 3 meses, 1 mês, hoje e 20 meses atrás) e 22 em navegador (1400x1000 e 390 com toque), sem erro de console nem rolagem horizontal.
- **(US-026) Equipe (tela) e a conversa real.** `lib/equipe.ts` (novo), `lib/sessoes.ts:resumoPorParticipante`/`sessoesComNotaDe`, `app/api/equipe` e `app/api/equipe/[id]`, a tela `app/equipe/page.tsx` reescrita e `app/equipe/analisar/page.tsx` (a análise de conversa real, vinda inteira do Início).
  - **"Analisar uma conversa real" mudou de endereço, não de comportamento** (D11). O formulário de colar/enviar conversa saiu de `app/page.tsx` e virou `/equipe/analisar`: a rota (`POST /api/analisar`), a ferramenta do assistente (`analisar_conversa`) e o formato do resultado são os mesmos, e o resultado continua sendo o `Resultado` exportado de `app/page.tsx` — a tela que `/r/<id>`, `/imprimir/<id>` e a sala também usam. Não existe uma segunda cópia do formulário em lugar nenhum; o Início agora só aponta para lá.
  - **`/?exemplo=1` continua sendo a porta da suíte e por isso o Início redireciona.** O atalho é usado pelo botão "Testar com um exemplo" de `components/setup.tsx` (INFRA, byte a byte nos 17 apps) e pela captura do catálogo no workflow de publicação (`/?exemplo=1&captura=1`). Como o formulário saiu do Início, o efeito de abertura repassa **a busca inteira** para `/equipe/analisar` (`router.replace`), preservando o `captura=1`. Mover um formulário de tela sem olhar quem chama o atalho quebraria a captura do catálogo sem falhar em teste nenhum.
  - **`lib/vendedores.ts` e `/api/vendedores` foram apagados** (a casca de compatibilidade da US-002 cumpriu o prazo dela). Os seis chamadores passaram a importar `lib/participantes.ts` direto; `lib/types.ts:Vendedor` ficou como a forma que `tituloResultado` recebe, porque `Participante` é compatível com ela. A tabela `vendedores` continua no arquivo, sem `DROP` e sem ninguém lendo.
  - **A nota da tela de Equipe soma treinos e conversas reais; a do painel de um treino, não.** As duas são a média dos critérios em 0–10, e a pergunta desta tela é sobre a pessoa. Consequência a lembrar: a mesma pessoa pode aparecer com 7,4 em `/equipe` e 7,1 no painel de um treino sem que nenhum dos dois esteja errado — é a distinção "número do participante" × "número do treino" da US-025, agora com um caso em que o número maior é o da pessoa.
  - **"Sessão" nesta tela é conversa que aconteceu**: `resumoPorParticipante` exclui `preparando` e `abandonada`, as mesmas exclusões de `historicoDe`. Quem o gestor cadastrou e ainda não treinou aparece com 0, "—" e "Nunca treinou" — ficar de fora da lista esconderia justamente quem precisa de convite.
  - **Apagar uma pessoa tira o nome, nunca o histórico.** `participantes.apagar` só remove a linha dela; as sessões e as análises continuam, e no painel de um treino a coluna passa a mostrar "Sem nome" (`montarEquipe` de `lib/painel-simulacao.ts` já tratava o caso). A confirmação diz isso **com o número na frente** ("3 conversas dela continuam no painel, agora sem o nome") porque "as conversas somem" e "as conversas ficam sem nome" são consequências muito diferentes.
  - **A linha do tempo do detalhe é buscada quando a linha abre**, não junto da lista: são trinta pessoas e cada uma tem o histórico inteiro dela. O treino aparece com o nome do treino e o tipo de cliente; a conversa real, com o título gravado na análise (que já traz o cenário, quando houver) e a etiqueta "Conversa real".
  - **Gotcha que vale para toda tela com `DataTable`: as linhas são numeradas pelo índice (`key={i}`).** Numa lista de onde se pode apagar, o componente de uma linha é **reaproveitado** pela linha seguinte, com o estado dela — o detalhe aberto de quem foi apagado reaparecia no nome de outra pessoa. A correção é dar `key` de identidade ao que a coluna renderiza (`<Detalhe key={p.id} …>`), não mexer no `DataTable`, que é genérico e não conhece a identidade da linha.
  - **Gotcha de navegador: o cartão do celular do `DataTable` não é `<tr>` nem `<article>`.** Ele é filho direto do `div.divide-y`, então o seletor de "a linha desta pessoa" precisa ser `tr, .divide-y > div` com `visible=true`. E esperar por "Linha do tempo" não basta: o título aparece antes de a busca voltar, e a conferência lê "Carregando..." — o que se espera é o `Carregando...` **sair**.
  - Verificação: 67 conferências de rota em servidor de verdade (`node .next/standalone/server.js`, `DATA_DIR` novo) — cadastro, e-mail normalizado, mesmo e-mail sem duplicar, um treino avaliado e uma conversa real da mesma pessoa, a média das duas, a linha do tempo ordenada, apagar e a conversa continuando no painel sem nome, apagar duas vezes, `/api/vendedores` fora do ar e a porta fechada sem sessão — e 60 em navegador (1400x1000 e 390 com toque), com o link do convite lido da área de transferência e o `/?exemplo=1` chegando à análise. Lint, `tsc --noEmit`, build, `verificar-padrao`, `verificar-paleta` e `verificar-jargao` em 0.
