# Ralph Progress Log
Started: Sat Sep 12 16:56:50 -03 2026
---

## Codebase Patterns
- Todos os 10 apps (pdi-time + agente-kanban, contratos-ia, entrevista-ia, financas-ia, posts-sociais, prospeccao-ia, reunioes-ia, voz-do-cliente, whatsapp-atendente) replicam o mesmo esqueleto de `app/page.tsx`: um `useRef` guarda ("autoEnviado"/"autoIniciado") mais um `useEffect([])` que lê `?exemplo=1` da URL e dispara o fluxo via `setTimeout(..., 0)`. Mudanças nesse padrão devem ser copiadas sem alterar nos 10 apps (regra do PRD).
- Cada app roda `next dev` na porta 3000 (não há porta fixa 3001 como o texto de uma acceptance criteria sugere); usar `PORT=xxxx npm run dev` se precisar rodar vários em paralelo.
- Não há script de typecheck no package.json; usar `npx tsc --noEmit -p tsconfig.json` dentro da pasta do app.
- Para verificar comportamento client-side sem servidor de browser MCP: `chromium --headless --disable-gpu --virtual-time-budget=8000 --dump-dom "<url>"` roda timers/fetches por Xms antes de despejar o DOM final — útil para confirmar que o atalho `?exemplo=1` realmente chegou ao estado "pronto" (procurar `class="reveal"` ou texto do resultado).

## [2026-09-12] - US-001
- Corrigido o bug do atalho `?exemplo=1` em `next dev` (React Strict Mode) nos 10 apps da suíte.
- Causa raiz: o `useEffect` marcava `autoEnviado.current = true` de forma síncrona e retornava um `cleanup` que cancelava o `setTimeout` (`return () => clearTimeout(t)`). Em Strict Mode (dev), o efeito roda: setup → cleanup → setup novamente. O cleanup cancelava o timer agendado na primeira chamada; na segunda chamada do setup, o guard `autoEnviado.current` já estava `true`, então o timer nunca chegava a disparar.
- Correção: manter a marcação do ref antes do `setTimeout` (para não duplicar em Strict Mode), mas remover o `return () => clearTimeout(t)`. Assim o único timer agendado (o da primeira invocação do efeito) sobrevive ao ciclo de desmontagem simulada do Strict Mode e dispara normalmente; a segunda invocação do efeito é bloqueada pelo guard e não agenda um segundo timer.
- Arquivos alterados: `pdi-time/app/page.tsx`, `agente-kanban/app/page.tsx`, `contratos-ia/app/page.tsx`, `entrevista-ia/app/page.tsx`, `financas-ia/app/page.tsx`, `posts-sociais/app/page.tsx`, `prospeccao-ia/app/page.tsx`, `reunioes-ia/app/page.tsx`, `voz-do-cliente/app/page.tsx`, `whatsapp-atendente/app/page.tsx`.
- Não toquei no outro `useEffect` de `agente-kanban` (carregamento inicial do quadro, linha ~43) pois não faz parte do atalho `?exemplo=1` e está fora do escopo da história.
- Verificação: `npx tsc --noEmit` e `npm run lint` passando nos 10 apps; testei em `next dev` com `chromium --headless --virtual-time-budget=8000 --dump-dom "http://localhost:3000/?exemplo=1"` em pdi-time (achou "PDI de Marina Costa") e financas-ia (achou `class="reveal"`, ou seja, chegou ao estado "pronto").
- **Learnings for future iterations:**
  - Ver seção "Codebase Patterns" acima.
  - Não existe um app rodando de fato na porta 3001; a menção a 3001 na US-001 não corresponde à config real (`next dev` sempre sobe em 3000 salvo `PORT` explícita).
---

## [2026-09-12] - US-002
- Criado `scripts/gerar-icones.mjs`: lê `catalogo.json` e escreve `<app>/app/icon.svg` (32x32, `rect` com `rx=7` na cor `acento` do app e a inicial do nome em branco, 700, 17px) para os 10 apps; remove `app/favicon.ico` de cada app processado (Next.js App Router detecta `app/icon.svg` automaticamente como ícone via convenção de arquivo, sem precisar declarar em `layout.tsx`).
- `metadata.title` de cada `layout.tsx` dos 10 apps passou para `'{Nome do app} · IA para Executivos'` (script Node ad hoc, não versionado, que leu `catalogo.json` e fez a troca por regex).
- `PADRAO.md`: seção "Novo app na suíte" agora cita o passo de rodar `node scripts/gerar-icones.mjs` e ajustar o título ao criar um app novo.
- Arquivos alterados: `scripts/gerar-icones.mjs` (novo), `PADRAO.md`, `<app>/app/icon.svg` (novo, 10x) e `<app>/app/layout.tsx` (título) nos 10 apps; `<app>/app/favicon.ico` removido nos 10 apps.
- Verificação: `npx tsc --noEmit` e `npm run lint` limpos nos 10 apps; subi `pdi-time` (PORT=3001) e `financas-ia` (PORT=3009) com `next dev` e confirmei via curl que `GET /icon.svg` devolve 200 com o SVG esperado (cor de acento e inicial certas) e que o HTML da página tem `<title>Nome do app · IA para Executivos</title>`.
- **Learnings for future iterations:**
  - Next.js App Router detecta `app/icon.svg` sem nenhuma configuração extra em `layout.tsx` — basta o arquivo existir nessa pasta.
  - Para checar ícone/título sem browser MCP, `curl -s http://localhost:<porta>/icon.svg` e `curl -s http://localhost:<porta>/ | grep -o "<title>[^<]*</title>"` com o `next dev` rodando em background bastam como evidência.
---

## [2026-09-12] - US-003
- `lib/ai.ts` ganhou o tipo `Meta` e a função `meta({ demo, insumo })`, que devolve `{ demo, model: modelName(), geradoEm: new Date().toISOString(), insumo }`.
- `app/api/pdi/route.ts` agora inclui `meta` na resposta, tanto no caminho de demonstração quanto no caminho real de IA (mesmo `insumo` fixo: "entregas recentes e objetivos da empresa").
- Novo componente `Origem` em `components/ui.tsx`: recebe `meta: Meta`, formata a data com `Intl.DateTimeFormat("pt-BR", ...)` (dia/mês/ano e hora/minuto separados e concatenados com "às"), e escolhe o texto conforme `meta.demo`. O nome do modelo só entra no atributo `title` do `<p>`, nunca no texto visível.
- `app/page.tsx`: `Estado` (fase "pronto") trocou o campo `demo: boolean` por `meta: Meta`; `Resultado` renderiza `<Origem meta={meta} />` logo abaixo de `ResultHead`, e o subtítulo perdeu o sufixo `" (exemplo em modo demonstração)"` (essa informação agora vive só na linha Origem).
- Arquivos alterados: `pdi-time/lib/ai.ts`, `pdi-time/components/ui.tsx`, `pdi-time/app/api/pdi/route.ts`, `pdi-time/app/page.tsx`.
- Verificação: `npx tsc --noEmit` e `npm run lint` limpos; subi `pdi-time` com `PORT=3011 npm run dev` e usei `chromium --headless --virtual-time-budget=8000 --dump-dom "http://localhost:3011/?exemplo=1&captura=1"` — confirmei a linha "Exemplo ilustrativo a partir de entregas recentes e objetivos da empresa. Conecte a IA para analisar seus dados" (modo demo, sem chave configurada), o atributo `title="nvidia/nemotron-3-super-120b-a12b:free"` na linha, e que o subtítulo do resultado voltou a ser só "Coordenadora de Marketing, 1 a 3 anos na função" (sem o sufixo antigo).
- **Learnings for future iterations:**
  - `Origem` importa `type { Meta } from "@/lib/ai"` dentro de `components/ui.tsx` — como `lib/ai.ts` também é um arquivo compartilhado replicado sem alteração nos 10 apps (junto com `ui.tsx`), esse acoplamento é seguro e deve ser preservado ao copiar a fundação para os outros apps (US-017 em diante).
  - `meta()` usa `insumo` como uma frase fixa por rota (o que descreve os dados de entrada), definida no próprio `route.ts`; cada app decide seu texto de `insumo` ao adotar o padrão.
  - Não existe ainda o helper `data()` de US-006; a formatação de data em `Origem` foi feita diretamente com `Intl.DateTimeFormat` neste componente. Quando US-006 criar o helper `data(d, { comHora })` em `ui.tsx`, `Origem` deve passar a usá-lo em vez de duplicar a formatação.
---

## [2026-09-12] - US-004
- `DemoNotice` (faixa de largura total abaixo da Topbar) removido de `pdi-time/components/ui.tsx`; a função de avisar sobre modo demonstração migrou para dentro do próprio `Topbar`.
- `Topbar` ganhou a prop opcional `resumo?: string`. Quando `demo` é verdadeiro e `resumo` foi passado, o chip "Modo demonstração" vira um `<button aria-haspopup="dialog" aria-expanded>` que abre um popover (`role="dialog"`, posicionado com `absolute`) contendo a frase `resumo` e o link "Conectar a IA em 1 minuto" para `/setup`. Sem `resumo` (ex.: a Topbar usada em `components/setup.tsx`) ou fora do modo demo, o chip continua sendo um `<span>` estático.
- Popover fecha com Esc (`keydown` no `document`) e com clique fora (`mousedown` no `document` checando `ref` do container); como é um `<button>` nativo, já é focável e ativável por teclado (Enter/Espaço) sem esforço extra.
- O badge do chip ganhou `min-w-[142px]` (`min-w-[110px]` no celular) e `justify-center`, para que a largura mínima (e portanto a altura da barra, que já era fixa) não dependa do texto ("Verificando IA" vs "Modo demonstração" vs "IA conectada" vs "Servidor indisponível").
- `app/page.tsx`: removida a chamada a `DemoNotice` e o import correspondente; `Topbar` agora recebe `resumo="Modo demonstração: o plano exibido é um exemplo."`.
- Arquivos alterados: `pdi-time/components/ui.tsx`, `pdi-time/app/page.tsx`.
- Verificação: `npx tsc --noEmit` e `npm run lint` limpos. Sem ferramenta de browser (MCP) disponível nesta sessão, usei `chromium --headless --disable-gpu --virtual-time-budget=<n> --dump-dom` em duas capturas (budget 1 vs 6000) de `http://localhost:3021/` para comparar o HTML do badge antes e depois de `/api/status` resolver: a classe `min-w-[142px] max-md:min-w-[110px]` é idêntica nos dois casos, só o texto e a cor mudam — confirma que não há salto de layout. Também confirmei que o `aria-haspopup="dialog"`/`aria-expanded="false"` aparece no botão quando o app está em modo demo. Não foi possível simular clique/Esc/clique-fora sem uma ferramenta de browser interativa (puppeteer/CDP não estão disponíveis no ambiente) — a lógica de abrir/fechar foi verificada por revisão de código, mas a interação em si pede verificação manual num browser real.
- **Learnings for future iterations:**
  - `components/setup.tsx` também usa `Topbar` (sem `DemoNotice`/popover, e sem `resumo`) — por isso `resumo` precisa ser opcional em `Topbar`, não obrigatório.
  - Não há ferramenta de browser MCP configurada neste projeto; a verificação visual "Verify in browser using dev-browser skill" nas próximas histórias precisará seguir o mesmo caminho alternativo (`chromium --headless --dump-dom`) ou ser sinalizada como pendente de verificação manual quando envolver interação (clique, popover, formulário) em vez de apenas leitura de estado.
---
