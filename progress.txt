# Ralph Progress Log
Started: Fri Sep 18 08:59:38 -03 2026
Rodada: Entrevistadora IA — Vaga → Candidato → Entrevista, voz em três níveis, pesquisa do candidato (Bright Data via MCP) e parecer para o gestor (ralph/entrevista-ia-vagas-candidatos-entrevistas)
Fonte: tasks/prd-entrevista-ia-vagas-candidatos-entrevistas.md (28 histórias no prd.json, 25 na PRD — a coluna notes de cada história diz de qual US da PRD ela veio; 6 fases)
A rodada anterior ("simulador-vendas-produto-simulacao-sessoes", 31 histórias, todas concluídas) está em archive/2026-09-18-simulador-vendas-produto-simulacao-sessoes/. A seção "Codebase Patterns" dela (semeadura de demonstração, tabelas novas no mesmo app.sqlite, testes de lib/*.ts fora do Next, servidor standalone, app independente) vale quase inteira para esta rodada: leia antes da US-002.
---

## Codebase Patterns

- **O modelo de app independente já existe no `scripts/verificar-padrao.sh`: basta a flag no catálogo.** `"independente": true` na entrada do app em `catalogo.json` faz o verificador conferir só `INFRA` (conta, banco, IA, MCP, rotinas, formulários, `proxy.ts`, `eslint.config.mjs`, assets — byte a byte com o `pdi-time`, em TODO app) e `ESTRUTURA_INDEPENDENTE` (que tira `app/historico/page.tsx` da lista obrigatória). A camada `PRODUTO` (`components/ui.tsx`, `components/setup.tsx`, `components/conta.tsx`, `lib/navegacao.ts`, `lib/ilustracao.ts`, `app/globals.css`, `public/ilustracoes/icones`) deixa de ser comparada. Nenhuma linha do script precisa mudar — a rodada do `simulador-vendas` é que teve de criar essa separação. **Independência é da camada de produto, nunca da infraestrutura.**
- **Destino de cabeçalho que ainda não tem tela nasce como tela real, nunca como 404.** Client component com o mesmo `Topbar` + `Empty`, título e apoio definitivos, e uma `acaoSecundaria` apontando para o lugar que hoje faz aquilo (o início, o histórico). O item ativo sai de graça: o `ativo(href)` do `Topbar` compartilhado já usa `startsWith` para as subrotas.
- **`npm ci` antes de qualquer `lint`/`build` num app que nunca foi rodado nesta máquina.** Cada app da suíte tem o próprio `node_modules`; sem ele o `npm run lint` morre em `sh: eslint: command not found`, o que parece defeito de configuração e não é.
- **Verificação em navegador, receita que funciona aqui:** `npm run build` → copiar `.next/static` e `public` para dentro de `.next/standalone` (o build recria o diretório e apaga o que foi copiado antes) → `DATA_DIR=/tmp/... PORT=<porta livre> node .next/standalone/server.js` → criar a conta por `POST /api/conta` (`{ nome, email, senha, confirmarSenha }`, senha forte) → Playwright com `chromium.launch({ channel: "chrome" })` e o cookie `sessao` em `context.addCookies`. Matar o servidor por porta (`lsof -nP -iTCP:<porta> -t | xargs kill -9`), nunca por nome do processo.
- **Uma porta única para as tabelas novas (`lib/banco.ts`), sobre a conexão de `lib/store.ts`.** `abrirBanco()` já existe; todo módulo de entidade nova entra por ele. Duas conexões para o mesmo arquivo SQLite não convivem com uma transação aberta — por isso nenhuma função que escreve em `resultados` (conexão de `lib/historico.ts`) pode ser chamada de dentro de um `BEGIN`. Quem apaga em cascata devolve os ids órfãos e deixa o chamador apagar **depois**.
- **Regra de unicidade "enquanto o registro vale" é índice parcial, não só checagem no código.** `CREATE UNIQUE INDEX ... WHERE status NOT IN ('cancelada','expirada')` é aceito pelo SQLite e é o que impede duas linhas vivas do mesmo par; a checagem em `criar()` é a cortesia (devolve a existente), o índice é a garantia.
- **Migração idempotente por construção: `INSERT OR IGNORE` com id derivado (hash do texto normalizado), nunca sorteado.** A marca em `config` só evita varrer a tabela antiga a cada subida — não é ela que garante a idempotência. E toda migração tem de ser testada com o **banco vazio** (a tabela antiga pode ainda não existir): `prepare()` sobre tabela inexistente lança NA HORA, então a conferência em `sqlite_master` vem antes do `prepare`.
- **Campo pesado (BLOB, texto de dezenas de milhares de caracteres) não entra no tipo de domínio.** Uma lista de trinta itens carregaria tudo para mostrar trinta nomes. Listagem usa uma lista explícita de colunas leves, com os booleanos derivados resolvidos em SQL (`CASE WHEN length(x) > 0 ...`), e quem precisa do conteúdo chama um acessador próprio.
- **Corte de tamanho e normalização moram no módulo que grava, nunca na rota:** a regra tem de valer igual para a tela, para o assistente (MCP) e para qualquer porta que venha depois.
- **`instrumentation.ts` NÃO é conferido por `scripts/verificar-padrao.sh`** (o `simulador-vendas` já o edita): é o lugar certo para o que tem de acontecer "na subida" — criar tabelas, migrar, semear.
- **No celular o cabeçalho recolhe a navegação num botão:** conferir os seis destinos em 390 px exige abrir o menu (`hasTouch`/`isMobile` no contexto) — e o item ativo não é destacado por classe lá, então a conferência de "item ativo" é do desktop.
- **Cadastro que é "um por instalação" mora em `config` (`lib/store.ts`), não em tabela nova.** Tabela é para lista; `setConfig`/`getConfig` com um JSON já dá cifragem em repouso e prioridade de variável de ambiente de graça. Salvar o formulário todo em branco deve APAGAR a chave, não gravar um registro vazio: "nunca salvou" e "salvou nada" são o mesmo estado para quem lê a tela.
- **Entrada de pessoa e entrada de IA pedem duas funções de tamanho, não uma.** A de pessoa RECUSA o excesso (`{ ok: false, erro }` → 400 com a frase pronta); a da IA (e a leitura do que já está gravado) CORTA. Ler o que está salvo pelo caminho que recusa é o que faz um teto mais apertado numa versão nova apagar dado bom de uma versão antiga.
- **Exemplo em `lib/demo.ts` que precisa do tipo do módulo que o consome: `import type`.** O import de tipo é apagado na compilação, então `cultura.ts` → `demo.ts` (runtime) + `demo.ts` → `cultura.ts` (só tipo) não fecha ciclo.
- **"Este cadastro ainda não foi feito" viaja em `integrations` de `/api/status`, por `statusExtra()` (`lib/status-do-app.ts`).** É o único ponto de `/api/status` próprio de cada app (não comparado por `scripts/verificar-padrao.sh`), e a chave não precisa ser uma integração de verdade — assim várias telas leem a MESMA decisão sobre o que conta como configurado.
- **Cartão de Configurações que não é conexão entra abaixo do `<SetupPage />`, na coluna `max-w-[860px] ... flex flex-col gap-6` (o mesmo arranjo do `simulador-vendas`), com `id` próprio no `<section>`** — o `/setup#<id>` já funciona sem tocar em `components/setup.tsx`. O caminho com `#` mora em `lib/acoes.ts`: `verificar-jargao.mjs` acusa `/setup` em `app/page.tsx` e `components/*.tsx`, mas em `lib/*.ts` só procura `http://localhost:3000`.
- **Playwright não está instalado em nenhum app da suíte.** A verificação em navegador precisa de um `npm i playwright@<versão>` num diretório descartável (`/tmp/verif`) e rodar o script de lá; `npx playwright` sozinho não deixa `import { chromium } from "playwright"` resolver.

---

## 18/09/2026 - US-001 - O app vira independente e ganha os seis destinos

- `catalogo.json`: a entrada de `entrevista-ia` ganhou `"independente": true` (logo abaixo de `captura`, como no `simulador-vendas`). `scripts/verificar-padrao.sh` **não mudou** — o modelo de dois níveis já existia — e passou a imprimir `camada de produto própria (independente: true)` + `ok` para este app.
- `entrevista-ia/lib/navegacao.ts` deixou de ser cópia do `pdi-time` e passou a trazer os seis destinos: Início `/`, Vagas `/vagas`, Candidatos `/candidatos`, Entrevistas `/entrevistas`, Relatórios `/relatorios`, Configurações `/setup`. "Histórico" saiu do cabeçalho; `/relatorios` o linka duas vezes com o rótulo "Relatórios anteriores" (na ação do `Empty` e numa linha de apoio abaixo dele). A tela `/historico` continua existindo e respondendo 200 — só saiu da navegação.
- Quatro telas novas, todas client components com `Topbar` + `Empty` e uma ação para o que já funciona: `app/vagas/page.tsx`, `app/candidatos/page.tsx`, `app/entrevistas/page.tsx` (as três apontam para o início) e `app/relatorios/page.tsx` (aponta para `/historico`). Cada uma tem um ícone SVG próprio, desenhado no arquivo.
- `entrevista-ia/CLAUDE.md` ganhou a nota da independência: data (18/09/2026), o commit `b0139e3` a partir do qual os seis arquivos da camada de produto passam a ser próprios, e como voltar ao estado compartilhado (`git show b0139e3:pdi-time/<caminho>`).
- `proxy.ts` intocado: as quatro telas nascem privadas (conferido: `/vagas` sem sessão responde 307) e `/entrevista/<código>` continua pública (200).

**Arquivos alterados:** `catalogo.json`, `entrevista-ia/CLAUDE.md`, `entrevista-ia/lib/navegacao.ts`, `entrevista-ia/app/{vagas,candidatos,entrevistas,relatorios}/page.tsx`.

**Verificações:** `npm run lint` (só o aviso pré-existente de `<img>` em `components/setup.tsx`), `npm run build`, `npx tsc --noEmit` (depois do build, por causa do typegen), `scripts/verificar-padrao.sh`, `scripts/verificar-paleta.mjs` e `scripts/verificar-jargao.mjs` — todos limpos. Navegador (Chrome via Playwright) nas seis rotas em 1400x1000 e 390: status 200, item ativo correto no desktop, os seis destinos no menu recolhido do celular, nenhum erro de console e nenhuma rolagem horizontal.

**Learnings for future iterations:**
- A AC dizia "o modelo já existe, nenhuma mudança no script" e **desta vez estava certo** — o `simulador-vendas` criou a separação `INFRA`/`PRODUTO` na história equivalente dele e ela já cobre qualquer app novo. Vale conferir com `bash scripts/verificar-padrao.sh` antes e depois: a saída dos outros 16 apps tem de ficar idêntica.
- Este app **não tinha `node_modules`**: `npm ci` é o primeiro passo de qualquer história aqui, senão `npm run lint` falha por `eslint: command not found` e parece problema de configuração.
- `npx tsc --noEmit` só passa **depois** do `next build` (o typegen de `PageProps`/`RouteContext` nasce no build) — a ordem certa continua sendo build, depois typecheck.
- A US-002 (próxima) cria `vagas`, `candidatos`, `fontes_candidato`, `entrevistas` e `mensagens_entrevista` no mesmo `app.sqlite`. Conferir os nomes contra `lib/conta.ts` (`usuarios`, `sessoes`), `lib/store.ts` (`config`), `lib/historico.ts`, `lib/formularios.ts` e `lib/rotinas.ts` antes de escrever o `CREATE TABLE` — a infraestrutura não pode mudar de nome para abrir espaço.
- As quatro telas desta história são placeholders **com texto definitivo de título e apoio**: quando a história de cada uma chegar, o `Empty` some mas o `h1`/apoio ficam. Vale reaproveitá-los em vez de reescrever.
---

## 18/09/2026 - US-002 - Vaga, Candidato e Entrevista no banco

- **`lib/banco.ts`** (novo): as cinco tabelas (`vagas`, `candidatos`, `fontes_candidato`, `entrevistas`, `mensagens_entrevista`) mais os índices, criadas sobre a **conexão de `lib/store.ts`** (`abrirBanco()`), e a migração dos scorecards antigos. `banco()` é a porta única; nenhum módulo novo abre o SQLite por conta própria. Os nomes foram conferidos antes contra `config`, `usuarios`, `sessoes`, `resultados`, `formularios`, `respostas` e `rotinas` — nenhum colide.
- **`lib/vagas.ts`** (novo): `Vaga` com cargo, área, senioridade, modelo, local, faixa salarial (inteiros em reais, nulos quando "a combinar"), desafios, requisitos (um por linha), competências culturais (JSON), tom, número de perguntas (6 a 12, padrão 8), duração (10 a 30, padrão 15), pergunta de pretensão, status e `exemplo`; `criar`/`listar({status})`/`obter`/`atualizar`/`mudarStatus`/`apagar`. Ligar "a combinar" apaga a faixa no próprio módulo, não na tela.
- **`lib/candidatos.ts`** (novo): `Candidato` + `FonteCandidato`, com `criar`/`listar({busca})`/`obter`/`atualizar`/`apagar` e `adicionarFonte`/`listarFontes`. `apagar()` remove fontes, entrevistas e mensagens numa transação e devolve `resultadosOrfaos` (os pareceres ficam para o chamador apagar fora da transação, porque `lib/historico.ts` tem conexão própria). `cvTexto`/`cvArquivo` ficam fora do tipo de domínio — acessadores próprios (`obterCvTexto`, `obterCvArquivo`) e `temCvTexto` resolvido em SQL.
- **`lib/entrevistas.ts`** (novo): `criar` (devolve a existente quando o par já tem entrevista viva), `obter`, `obterPorCodigo`, `listar({vagaId, candidatoId, status, periodo})`, `mudarStatus` (carimba a data do estado), `definirCodigo`, `registrarMensagem`, `transcricao`, `registrarResultado`, `decidir`, `cancelar`, `expirarVencidas()` (chamada na leitura).
- **`instrumentation.ts`**: chama `banco()` na subida, para a migração não cair na primeira leitura de tela.
- **Migração**: cada scorecard antigo (`entrevista`/`scorecard`) virou uma vaga por título normalizado (`encerrada`, requisitos copiados), um candidato por nome e uma entrevista `avaliada` com `resultadoId` apontando para o registro antigo; a conversa antiga foi copiada para `mensagens_entrevista`. `ranking` fica de fora. Nada em `resultados` foi apagado nem alterado.

**Arquivos alterados:** `entrevista-ia/lib/{banco,vagas,candidatos,entrevistas}.ts` (novos), `entrevista-ia/instrumentation.ts`, `entrevista-ia/CLAUDE.md`.

**Verificações:** `npm run lint` (só o aviso pré-existente de `<img>`), `npm run build`, `npx tsc --noEmit`, `scripts/verificar-padrao.sh`, `verificar-paleta.mjs` e `verificar-jargao.mjs` — todos limpos.
Testes de banco com `DATA_DIR` em `/tmp`, **um processo por fase** (o reinício de verdade): criar vaga + dois candidatos + duas entrevistas e transcrição → reiniciar → tudo continua lá (inclusive o BLOB do currículo e o carimbo de `iniciadaEm`); a migração rodou e agrupou os dois scorecards antigos do mesmo título numa vaga só; a segunda criação do mesmo par devolveu **a mesma** entrevista; cancelar liberou o par; convite com prazo vencido expirou na leitura; apagar o candidato levou entrevistas, falas e fontes. Também: banco totalmente vazio (sem a tabela `resultados`) abre sem erro, e rodar tudo de novo não duplicou nada.
Servidor standalone com `DATA_DIR` em `/tmp` semeado com um scorecard antigo: subiu, `/api/health` ok, conta criada por `POST /api/conta`, `/vagas` 200, a migração rodou **no boot** (vaga + candidato + entrevista + 2 mensagens), reiniciar não duplicou, e `/r/<id>` e `/imprimir/<id>` continuam 200 num scorecard antigo completo.

**Learnings for future iterations:**
- **A AC pedia "testes com `curl` num servidor standalone" — e na US-002 isso não existe ainda**: as rotas `/api/vagas` e `/api/candidatos` só nascem na US-005/US-008. O que substituiu (e cobre mais): dois processos Node separados sobre o mesmo `DATA_DIR`, exercitando os `lib/*.ts` de verdade pelo gancho de resolução, **mais** o servidor standalone de verdade para conferir a migração no boot e que `/r/<id>` não regrediu. Quando a AC descreve o teste por um meio que a história ainda não tem, o que vale é a pergunta que ela quer responder ("sobrevive ao reinício?", "duplica?", "apaga em cascata?").
- **Um `/r/<id>` que responde 500 num teste pode ser a fixture, não o código.** Um registro de histórico inventado à mão com `meta: {}` derruba a página em `getFullYear` (o `Origem` espera o formato de `meta()` de `lib/ai.ts`). Gerar a fixture com as mesmas funções do app antes de concluir que houve regressão.
- **A marca `MIGRACAO_ENTREVISTAS_V1` faz a migração rodar UMA vez por instalação** — um scorecard salvo depois dela **não** vira entrevista. É o comportamento certo (da US-013 em diante toda entrevista nasce direto nas tabelas novas), mas quem for testar a migração precisa semear o dado antigo **antes** do primeiro `banco()`.
- **Onde a próxima história encosta:** a US-003 (cultura) grava em `lib/store.ts` sob `CULTURA_EMPRESA` e alimenta `Vaga.competenciasCulturais` (`origem: "empresa"`); a US-004 (`lib/semear-demo.ts`) precisa de um `lib/exemplos.ts` irmão (como no `simulador-vendas`) para a remoção do conjunto de exemplo não fechar ciclo de import — a remoção é chamada de dentro de `criar()` de `lib/vagas.ts`/`lib/candidatos.ts`, então ela não pode importar nada que volte a eles.
- `Candidato.ficha` está tipada como `FichaCandidato = Record<string, unknown>` (JSON opaco) só até a US-009 criar `lib/ficha.ts` — trocar o alias lá, não inventar outro campo.
---

## 18/09/2026 - US-003 - Cultura da empresa em Configurações

- **`lib/cultura.ts`** (novo): tipo `Cultura` (`valores[]` de `{ id, nome, descricao }` até 6, `comportamentos`, `naoCombina`, `atualizadoEm`), guardado como JSON único em `lib/store.ts` sob `CULTURA_EMPRESA`. `obterCultura()` nunca devolve nulo — sem cultura própria devolve `culturaDemo()` com `exemplo: true`. `salvarCultura()` apaga a chave quando tudo vem em branco. `validarCultura()` (recusa, para quem digitou) e `normalizarCultura()` (corta, para a IA e para a leitura do que já está gravado) são funções separadas de propósito. `culturaConfigurada()` alimenta o status.
- **`lib/demo.ts`**: `culturaDemo()` com quatro valores de uma empresa de serviços B2B (Cliente no centro, Dono do resultado, Clareza antes da pressa, Melhora contínua) e os dois textos, `atualizadoEm: ""` (nada disso foi salvo por ninguém).
- **`app/api/cultura/route.ts`** (`GET`/`PUT`, privadas por não estarem na lista pública de `proxy.ts`) e **`app/api/cultura/gerar/route.ts`** (`POST`): o gestor cola a página "Sobre nós" e a IA propõe os campos (`askJSON`, modelo padrão, entrada cortada em 8 mil caracteres); em modo demonstração devolve `culturaDemo()` depois de `esperar(1200)`. A proposta **preenche o formulário e não grava nada**.
- **`components/CulturaEmpresa.tsx`** (novo), registrado em `app/setup/page.tsx` acima de `AcessoMCP`: lista de valores (nome curto + uma frase, "Adicionar valor" até 6, "Remover" por linha), os dois textos, "Gerar a partir de um texto" dentro de um `MaisDetalhes` e "Salvar". Sem cultura salva, o formulário nasce com o exemplo e um `Aviso` dizendo que é exemplo.
- **`lib/status-do-app.ts`**: `integrations.cultura`. **`lib/acoes.ts`**: `ACAO_CULTURA`. **`app/page.tsx`**: sem cultura salva, um `Aviso` acima do formulário da vaga leva ao cartão.

**Arquivos alterados:** `entrevista-ia/lib/cultura.ts` (novo), `entrevista-ia/components/CulturaEmpresa.tsx` (novo), `entrevista-ia/app/api/cultura/{route.ts,gerar/route.ts}` (novos), `entrevista-ia/lib/{demo,status-do-app,acoes}.ts`, `entrevista-ia/app/{setup/page.tsx,page.tsx}`, `entrevista-ia/CLAUDE.md`.

**Verificações:** `npm run lint` (só o aviso pré-existente de `<img>`), `npm run build`, `npx tsc --noEmit`, `scripts/verificar-padrao.sh`, `verificar-paleta.mjs` e `verificar-jargao.mjs` — todos limpos. Servidor standalone com `DATA_DIR` em `/tmp` e conta criada por `POST /api/conta`: `/api/cultura` sem sessão devolve 401; `GET` devolve o exemplo com `exemplo: true`; os três 400 de validação (nome com 41 caracteres, 7 valores, texto com 1001) saem com a frase certa; `PUT` válido salva, descarta a linha em branco e vira `exemplo: false`; `PUT` tudo em branco volta ao exemplo; `POST /api/cultura/gerar` recusa texto curto e devolve o exemplo no modo demonstração. Navegador (Chrome via Playwright) em 1400x1000 e 390: cartão presente e **acima** do `AcessoMCP`, editar + remover + salvar funciona e o aviso de exemplo some, o salvo sobrevive ao recarregar, o 400 do servidor aparece na tela, "Propor os valores" preenche o formulário sem salvar, o aviso do Início some quando existe cultura — sem erro de console e sem rolagem horizontal.

**Learnings for future iterations:**
- **Duas linhas da AC ficaram para as histórias donas das telas, e isso está registrado aqui de propósito.** "Início mostra o passo 'Cadastre a cultura da empresa' em **Comece em 3 passos**" depende do bloco que só nasce na US-022, e "a **vaga nova** avisa que está usando a cultura de exemplo" depende de `/vagas/nova`, que só nasce na US-005. O que dá para entregar hoje foi entregue — o dado (`integrations.cultura`) e o aviso equivalente no Início, com o texto e o destino definitivos. **US-022 deve dobrar esse `Aviso` para dentro do "Comece em 3 passos" (não somar mais um aviso ao lado) e US-005 deve ler a mesma chave.**
- **Playwright não está instalado em app nenhum da suíte**: `npm i playwright@1.63.0` num diretório descartável e rodar o script de lá. `npx playwright` resolve o binário, não o `import`.
- A cultura de exemplo preenche o formulário na primeira visita em vez de deixá-lo vazio. Foi decisão consciente: quem abre a tela pela primeira vez precisa ver o FORMATO da resposta esperada (rótulo curto + uma frase concreta) antes de escrever a da própria empresa. O aviso de "isto é exemplo" é o que impede que ela seja confundida com a da empresa — sem ele, a escolha seria ruim.
- **Onde a próxima história encosta:** a US-004 (`lib/semear-demo.ts`) usa `culturaDemo()` para as quatro `competenciasCulturais` da vaga de exemplo, com `origem: "empresa"` — os `id` dos valores (`cliente-no-centro`, `dono-do-resultado`, `clareza-antes-da-pressa`, `melhora-continua`) já servem de `id` da competência. A US-005 monta as caixas "competências da empresa" a partir de `obterCultura().valores` e precisa avisar quando `exemplo` for verdadeiro.
---
