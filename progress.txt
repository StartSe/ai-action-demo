# Ralph Progress Log
Started: Fri Sep 18 08:59:38 -03 2026
Rodada: Entrevistadora IA — Vaga → Candidato → Entrevista, voz em três níveis, pesquisa do candidato (Bright Data via MCP) e parecer para o gestor (ralph/entrevista-ia-vagas-candidatos-entrevistas)
Fonte: tasks/prd-entrevista-ia-vagas-candidatos-entrevistas.md (28 histórias no prd.json, 25 na PRD — a coluna notes de cada história diz de qual US da PRD ela veio; 6 fases)
A rodada anterior ("simulador-vendas-produto-simulacao-sessoes", 31 histórias, todas concluídas) está em archive/2026-09-18-simulador-vendas-produto-simulacao-sessoes/. A seção "Codebase Patterns" dela (semeadura de demonstração, tabelas novas no mesmo app.sqlite, testes de lib/*.ts fora do Next, servidor standalone, app independente) vale quase inteira para esta rodada: leia antes da US-002.
---

## Codebase Patterns

- **O modelo de app independente já existe no `scripts/verificar-padrao.sh`: basta a flag no catálogo.** `"independente": true` na entrada do app em `catalogo.json` faz o verificador conferir só `INFRA` (conta, banco, IA, MCP, rotinas, formulários, `proxy.ts`, `eslint.config.mjs`, assets — byte a byte com o `pdi-time`, em TODO app) e `ESTRUTURA_INDEPENDENTE` (que tira `app/historico/page.tsx` da lista obrigatória). A camada `PRODUTO` (`components/ui.tsx`, `components/setup.tsx`, `components/conta.tsx`, `lib/navegacao.ts`, `lib/ilustracao.ts`, `app/globals.css`, `public/ilustracoes/icones`) deixa de ser comparada. Nenhuma linha do script precisa mudar — a rodada do `simulador-vendas` é que teve de criar essa separação. **Independência é da camada de produto, nunca da infraestrutura.**
- **Destino de cabeçalho que ainda não tem tela nasce como tela real, nunca como 404.** Client component com o mesmo `Topbar` + `Empty`, título e apoio definitivos, e uma `acaoSecundaria` apontando para o lugar que hoje faz aquilo (o início, o histórico). O item ativo sai de graça: o `ativo(href)` do `Topbar` compartilhado já usa `startsWith` para as subrotas.
- **`npm ci` antes de qualquer `lint`/`build` num app que nunca foi rodado nesta máquina.** Cada app da suíte tem o próprio `node_modules`; sem ele o `npm run lint` morre em `sh: eslint: command not found`, o que parece defeito de configuração e não é.
- **Verificação em navegador, receita que funciona aqui:** `npm run build` → copiar `.next/static` e `public` para dentro de `.next/standalone` (o build recria o diretório e apaga o que foi copiado antes) → `DATA_DIR=/tmp/... PORT=<porta livre> node .next/standalone/server.js` → criar a conta por `POST /api/conta` (`{ nome, email, senha, confirmarSenha }`, senha forte) → Playwright com `chromium.launch({ channel: "chrome" })` e o cookie `sessao` em `context.addCookies`. Matar o servidor por porta (`lsof -nP -iTCP:<porta> -t | xargs kill -9`), nunca por nome do processo.
- **No celular o cabeçalho recolhe a navegação num botão:** conferir os seis destinos em 390 px exige abrir o menu (`hasTouch`/`isMobile` no contexto) — e o item ativo não é destacado por classe lá, então a conferência de "item ativo" é do desktop.

---

## 18/09/2026 - US-001 - O app vira independente e ganha os seis destinos

- `catalogo.json`: a entrada de `entrevista-ia` ganhou `"independente": true` (logo abaixo de `captura`, como no `simulador-vendas`). `scripts/verificar-padrao.sh` **não mudou** — o modelo de dois níveis já existia — e passou a imprimir `camada de produto própria (independente: true)` + `ok` para este app.
- `entrevista-ia/lib/navegacao.ts` deixou de ser cópia do `pdi-time` e passou a trazer os seis destinos: Início `/`, Vagas `/vagas`, Candidatos `/candidatos`, Entrevistas `/entrevistas`, Relatórios `/relatorios`, Configurações `/setup`. "Histórico" saiu do cabeçalho; `/relatorios` o linka duas vezes com o rótulo "Relatórios anteriores" (na ação do `Empty` e numa linha de apoio abaixo dele). A tela `/historico` continua existindo e respondendo 200 — só saiu da navegação.
- Quatro telas novas, todas client components com `Topbar` + `Empty` e uma ação para o que já funciona: `app/vagas/page.tsx`, `app/candidatos/page.tsx`, `app/entrevistas/page.tsx` (as três apontam para o início) e `app/relatorios/page.tsx` (aponta para `/historico`). Cada uma tem um ícone SVG próprio, desenhado no arquivo.
- `entrevista-ia/CLAUDE.md` ganhou a nota da independência: data (18/09/2026), o commit `b0139e3` a partir do qual os seis arquivos da camada de produto passam a ser próprios, e como voltar ao estado compartilhado (`git show b0139e3:pdi-time/<caminho>`).
- `proxy.ts` intocado: as quatro telas nascem privadas (conferido: `/vagas` sem sessão responde 307) e `/entrevista/<código>` continua pública (200).

**Arquivos alterados:** `catalogo.json`, `entrevista-ia/CLAUDE.md`, `entrevista-ia/lib/navegacao.ts`, `entrevista-ia/app/{vagas,candidatos,entrevistas,relatorios}/page.tsx`.

**Verificações:** `npm run lint` (só o aviso pré-existente de `<img>` em `components/setup.tsx`), `npm run build`, `npx tsc --noEmit` (depois do build, por causa do typegen), `scripts/verificar-padrao.sh`, `scripts/verificar-paleta.mjs` e `scripts/verificar-jargao.mjs` — todos limpos. Navegador (Chrome via Playwright) nas seis rotas em 1400x1000 e 390: status 200, item ativo correto no desktop, os seis destinos no menu recolhido do celular, nenhum erro de console e nenhuma rolagem horizontal.

**Learnings for future iterations:**
- A AC dizia "o modelo já existe, nenhuma mudança no script" e **desta vez estava certo** — o `simulador-vendas` criou a separação `INFRA`/`PRODUTO` na história equivalente dele e ela já cobre qualquer app novo. Vale conferir com `bash scripts/verificar-padrao.sh` antes e depois: a saída dos outros 16 apps tem de ficar idêntica.
- Este app **não tinha `node_modules`**: `npm ci` é o primeiro passo de qualquer história aqui, senão `npm run lint` falha por `eslint: command not found` e parece problema de configuração.
- `npx tsc --noEmit` só passa **depois** do `next build` (o typegen de `PageProps`/`RouteContext` nasce no build) — a ordem certa continua sendo build, depois typecheck.
- A US-002 (próxima) cria `vagas`, `candidatos`, `fontes_candidato`, `entrevistas` e `mensagens_entrevista` no mesmo `app.sqlite`. Conferir os nomes contra `lib/conta.ts` (`usuarios`, `sessoes`), `lib/store.ts` (`config`), `lib/historico.ts`, `lib/formularios.ts` e `lib/rotinas.ts` antes de escrever o `CREATE TABLE` — a infraestrutura não pode mudar de nome para abrir espaço.
- As quatro telas desta história são placeholders **com texto definitivo de título e apoio**: quando a história de cada uma chegar, o `Empty` some mas o `h1`/apoio ficam. Vale reaproveitá-los em vez de reescrever.
---
