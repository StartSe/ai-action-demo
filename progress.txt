# Ralph Progress Log
Started: Sun Sep 20 2026
Rodada: Clone de Site — sites como projetos, assets do cliente, agente que edita e publica, métricas e domínio próprio (ralph/clone-site-agente-sites)
Fonte: tasks/prd-clone-site-agente-sites.md (10 histórias no prd.json; a coluna notes de cada história diz de qual US da PRD ela veio)
A rodada anterior ("entrevista-ia-vagas-candidatos-entrevistas", 28 histórias, todas concluídas) está em archive/2026-09-20-entrevista-ia-vagas-candidatos-entrevistas/. A seção "Codebase Patterns" dela (tabelas novas no mesmo app.sqlite, servidor standalone, testes em navegador) continua valendo; os itens que interessam a este app estão resumidos abaixo.
Trabalho feito num worktree separado (../ai-action-demo-clone-site), criado a partir de 33c4795 (build-agentflows/agentflows-v2, que contém a main e a revisão de UX do clone-site de 18/09), porque o checkout principal tinha alterações não commitadas do radar-sinais.

---

## Codebase Patterns

- **Tabelas novas do app entram no mesmo `app.sqlite` por `abrirBanco()` de `lib/store.ts`**, cada entidade no seu módulo (`lib/projetos.ts`, `lib/assets.ts`, …) com `CREATE TABLE IF NOT EXISTS` na primeira chamada. Nunca abrir uma segunda conexão para o mesmo arquivo.
- **Campo pesado (imagem em base64, HTML de dezenas de KB) nunca entra no tipo devolvido às telas**: a listagem faz `SELECT` com colunas explícitas e há uma função própria (`imagemDe(id)`) para quem realmente precisa do dado.
- **Trabalho longo roda em segundo plano dentro do processo** (promessa sem `await`, estado no SQLite, tela consultando a cada 5 s), como `videos-campanha/lib/videos.ts`. Na subida (`instrumentation.ts`, que não é conferido por `verificar-padrao.sh`) tudo o que ficou "em andamento" vira "falhou" com motivo em português.
- **Arquivos `INFRA` (`lib/ai.ts`, `lib/store.ts`, `proxy.ts`, `app/api/status/route.ts`, …) só divergem do `pdi-time` com uma entrada em `scripts/padrao-excecoes.json` explicando o motivo.** Preferir sempre um módulo próprio do app (`lib/motor.ts` para escolher OpenRouter × ChatGPT) a editar o compartilhado.
- **Jargão: `scripts/verificar-jargao.mjs` varre `app/page.tsx` e `components/*.tsx` (menos `setup.tsx`)** procurando "token", "API", "OpenRouter", "webhook", "endpoint", "/setup", " ID". Um cartão de configuração que precisa nomear o provedor pede exceção em `scripts/jargao-excecoes.json`.
- **`npm ci` antes de `lint`/`build` num app novo nesta máquina** (cada app tem o próprio `node_modules`).
- **Verificação em navegador:** `npm run build` → `cp -r public .next/standalone/ && cp -r .next/static .next/standalone/.next/` → `DATA_DIR=/tmp/<app>-dados PORT=<porta livre> HOSTNAME=127.0.0.1 node --disable-warning=ExperimentalWarning .next/standalone/server.js &` → criar a conta por `curl -X POST /api/conta` → Playwright/Chromium para as capturas. A porta 3000 desta máquina é de outro projeto: usar 3115+.

---

## 20/09/2026 - US-001 - Sites como projetos com estado, geração em segundo plano e link por slug
- `lib/types.ts` ganhou `EstadoProjeto`, `OrigemProjeto`, `ErroProjeto` e `Projeto` (sem a imagem). Novo `lib/projetos.ts`: tabela `projetos` (mesmo `app.sqlite`, via `abrirBanco()`), `criar` (valida imagem/marca/briefing; slug único derivado do nome, com sufixo -2/-3 e lista de reservados), `obter`, `obterPorSlug` (slug OU id), `listar` (colunas explícitas, nunca `imagem`), `renomear`, `definirSlug`, `editarPedido` (só rascunho/falhou), `apagar` (leva a página do histórico), `marcarVisto`, `paginaDoProjeto`, `versaoPublicadaDe`, `publicar(id, n?)`, `iniciarGeracao` (promessa sem await), `aguardarGeracao` (para as portas antigas), `encerrarAbandonados({ naSubida })`, `avisos()`.
- Rotas `app/api/sites/**` (POST/GET, [id] GET/PATCH/DELETE, gerar 202/409, publicar, visto, avisos); `lib/resposta-sites.ts` traduz `ProjetoNaoEncontrado`/`PaginaNaoEncontrada` → 404, `ErroDePedido` → 400, o resto → `respostaErro`.
- `/s/[id]` passou a servir a versão publicada do projeto por slug ou id, caindo na última versão de uma página antiga sem projeto; cabeçalhos/CSP/404 foram para `lib/publicacao.ts` (com `'self'` já em `img-src`, para os assets da US-004).
- `POST /api/pagina` e a ferramenta MCP `gerar_pagina` criam o site, disparam e esperam (`aguardarGeracao`), devolvendo `projetoId` e `slug`. `instrumentation.ts` encerra os `gerando` na subida e, no laço de 60 s, os que passaram de 15 min.
- README: seção "Sites: projetos com estado…" com as rotas e o teste por curl.
- Verificado no standalone (porta 3115, demonstração): `POST /api/sites` com `gerar: true` respondeu 201 em 15 ms; 3 s depois `pronto`, `versaoPublicada = 1`, `imagem IS NULL`; `/s/loja-aurora` 200 sem sessão e `/s/nao-existe` 404; `PATCH slug` para `aurora` funcionou e `a` foi recusado; matar o servidor em `gerando` e subir de novo deixou o site em `falhou` "O servidor reiniciou durante a geração" com a imagem mantida, e "Tentar de novo" (202) levou a `pronto`; `POST /api/pagina` antigo devolveu `projetoId`/`slug`; `DELETE` apagou projeto e página.
- **Learnings for future iterations:**
  - **`route.ts` só pode exportar `GET`/`POST`/…/`dynamic`.** Constantes e helpers (CSP, página 404, resolução de slug) têm de morar em `lib/` (`lib/publicacao.ts`), senão o `next build` recusa o export.
  - **`RouteContext`/`PageProps` são tipos gerados em `.next/types`:** num worktree novo `npx tsc --noEmit` falha em todos os `route.ts` até rodar `npx next typegen` (ou um `next build`). Rodar o typegen antes do primeiro `tsc`.
  - **Criar a conta por curl exige `confirmarSenha`** igual a `senha` (`app/api/conta/route.ts`); sem ele a resposta é "As senhas não são iguais."
  - **O nome automático (`NOME_AUTOMATICO`) é trocado pelo título da página ao ficar pronto, e o slug refeito só nesse caso** — quem deu nome ao site mantém o slug que viu ao criar.
---

## 20/09/2026 - US-002 - Tela inicial "Meus sites" e criação por referência, endereço ou briefing
- `app/page.tsx` reescrita: Hero com a PROMESSA nova ("O site da sua empresa, no ar hoje", 7 palavras; apoio 15), Passos (Referência ou briefing · Marca e imagens · No ar com o agente), cartão "Criar um site" com três abas (`role="tablist"`) e o cartão "A sua marca" (nome do site acompanha o nome da marca até a pessoa digitar um próprio). Criar = `POST /api/sites` com `gerar: true`, sem esperar: o site entra no topo de "Meus sites" em Gerando e um Aviso diz que pode sair da tela. `?exemplo=1` cria direto; "Preencher com um exemplo" só preenche.
- Novo `components/MeusSites.tsx` (lista controlada pela tela, consulta a cada 5 s só enquanto houver "gerando"; `ChipEstado`, `TempoGerando`, Tentar de novo/Gerar/Apagar, `Empty` com o exemplo), `components/useAvisos.ts` (sino: `/api/sites/avisos` a cada 15 s, pausa em `document.hidden`, prefixo "(N) " no título), `components/TopbarSite.tsx` (Topbar da suíte + status + avisos, usado por todas as telas do app, inclusive a Server Component `/r/[id]`), `components/ResultadoPagina.tsx` (o antigo `Resultado` de `app/page.tsx`), `app/sites/[id]/page.tsx` (primeira versão: estados rascunho/gerando/falhou/pronto, marca visto ao abrir; vira o workspace na US-006). `.chip-gerando` (ponto pulsante) em `globals.css`.
- `/setup` fica sem o sino: `components/setup.tsx` é compartilhado e renderiza a própria Topbar sem `notificacoes` (decisão registrada; o sino aparece em Início, Histórico, /sites e /r).
- Verificado: lint (só o aviso conhecido de `<img>` em setup.tsx), `tsc`, `build`, jargão limpo; capturas via Chrome headless por CDP (`scratchpad/cdp-shot.mjs`, emulação real de 390 px) em 1400x900 e 390 px: Início com dois sites (Falhou com motivo + Tentar de novo; No ar com Abrir o link), aba "Descrever a empresa", `/sites/[id]` pronto (prévia renderizando) e falhou (ErrorBox + Tentar de novo), sem rolagem horizontal e sem erro de console.
- **Learnings for future iterations:**
  - **`chromium --headless --window-size=390,...` NÃO produz uma tela de 390 px no macOS** (a janela tem largura mínima ~500 e a captura sai cortada). A saída é falar CDP direto com o Chrome (`Emulation.setDeviceMetricsOverride` com `mobile: true`), o que `scratchpad/cdp-shot.mjs` faz em 60 linhas com o `WebSocket` nativo do Node 24 — sem Playwright. O script também imprime `scrollWidth` (rolagem horizontal) e erros de console.
  - **`react-hooks/refs` (eslint-plugin-react-hooks 7) proíbe `ref.current = x` no corpo do componente.** Para um callback estável (um `setState` do pai) basta colocá-lo nas dependências do efeito; o "ref espelho" só é necessário para callbacks instáveis, e aí a atribuição vai num `useEffect`.
  - **Um Server Component (`/r/[id]`) que precisa do sino usa um Client Component casca (`TopbarSite`)** em vez de receber `notificacoes` por props: o hook de consulta periódica só roda no cliente.
---

## 20/09/2026 - US-003 - Criar do zero a partir de um briefing
- `lib/gerador.ts`: `SYSTEM_BRIEFING_TAILWIND`/`SYSTEM_BRIEFING_CSS` (regras de arquivo único da suíte + a estrutura padrão de landing em sete blocos), `montarPromptBriefing` (aceita `blocoAssets` para a US-004) e `gerarDoBriefing()` (motor de texto — `askText` até a US-005 trocar por `lib/motor.ts`; demonstração com `paginaDemo({ briefing })`). `salvarPagina` grava `entrada.briefing` e a instrução da versão 1 vira "Site criado a partir do briefing".
- `lib/demo.ts`: `fraseDoBriefing()` (primeira frase, até 90 caracteres) entra no `<h1>` e no `<title>` da landing fixa. `lib/projetos.ts:executarGeracao` escolhe o gerador pela origem. `lib/ferramentas.ts`: ferramenta MCP `criar_site(nome, briefing, marca?, formato?)` e o helper `criarEEsperar` compartilhado com `gerar_pagina`.
- Verificado no standalone (demonstração): o site "Clínica Vida" que tinha falhado na US-002 foi gerado por "Tentar de novo" (202 → pronto, título e h1 com a frase do briefing); `criar_site` pelo MCP devolveu id, slug `padaria-sol`, links e HTML.
- **Learnings for future iterations:**
  - **Um site que falhou por "recurso ainda não existe" fica no banco esperando a etapa seguinte** — "Tentar de novo" reaproveita o briefing/captura guardados, então a US seguinte pode ser verificada em cima dos registros que a anterior deixou em `falhou`.
  - **`Pedido` exige `imagem`; para o briefing basta `imagem: ""`** na hora de salvar (a entrada gravada nunca leva a imagem, só `tamanhoImagem`).
---

## 20/09/2026 - US-004 - Logo e imagens do cliente
- Novo `lib/assets.ts` (tabela `assets`, base64; `detectarMime` pelos bytes — PNG/JPG/WEBP — ou pelo texto para SVG, recusando script/eventos/foreignObject; um só logo, até 12 imagens, 2 MB; `montarBlocoAssets` monta o bloco "Imagens da empresa" dos prompts). Rotas `GET|POST /api/sites/[id]/imagens` (multipart), `DELETE .../[assetId]` e a pública `GET /s/[id]/a/[assetId]` (cache 1 h, nosniff, CSP para SVG).
- `lib/gerador.ts`: `OpcoesAssets { blocoAssets, demoAssets }` em `gerarPagina`/`gerarDoBriefing`; `montarPrompt`/`montarPromptEdicao`/`editarPagina` aceitam o bloco; REGRAS_COMUNS ganhou a exceção "quando o pedido trouxer Imagens da empresa, use-as". `lib/projetos.ts:opcoesAssetsDe(id)` monta as opções na geração; `apagar` leva os assets. `lib/demo.ts:aplicarAssetsDemo` troca o quadradinho do cabeçalho pelo logo e os blocos `.imagem` (grandes primeiro) pelas fotos.
- UI: `components/PainelImagens.tsx` com `SeletorImagens` (formulário de criação: logo + até 6 imagens com descrição, enviadas entre `POST /api/sites` sem `gerar` e `POST .../gerar`) e `PainelImagens` (workspace: lista/adiciona/remove). `/sites/[id]` ganhou a coluna lateral com o painel.
- Verificado no standalone (demonstração): logo SVG 201, SVG com `<script>` 400, duas fotos PNG 201; o asset público sai com `image/svg+xml`, `public, max-age=3600`, `nosniff` e CSP restrita; o site gerado usa `<img>` do logo (h-9) e das fotos (object-fit cover); `/s/cafe-aroma` renderiza com as imagens e sem violação de CSP no console (o único 404 é o favicon, pré-existente). Capturas 1400 px do workspace com o painel e do formulário com o seletor.
- **Learnings for future iterations:**
  - **Rota dinâmica nova = `npx next typegen` antes do `tsc`**: `RouteContext<"/nova/rota">` falha com "does not satisfy the constraint AppRouteHandlerRoutes" até os tipos serem regenerados; não é erro de código.
  - **`Response` com `Buffer` reclama de tipo em Next 16**: passar `new Uint8Array(buffer)` como corpo.
  - **Miniatura de arquivo local/asset do próprio app usa `<img>` com `eslint-disable-next-line @next/next/no-img-element`** (a regra é aviso, mas o comentário registra a decisão): `next/image` não otimiza URL de objeto nem rota dinâmica com cache próprio.
---
