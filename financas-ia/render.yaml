# Blueprint de publicação de Analista Financeiro (especificação: https://render.com/docs/blueprint-spec)
# Imagem pública publicada pelo GitHub Actions em ghcr.io/startse/financas-ia:latest.
# Nenhuma chave é necessária aqui: após publicar, abra /setup no app e conecte a IA.
# As chaves ficam em SQLite em /app/data. No plano free o disco é efêmero e a configuração se perde a cada deploy.
services:
  - type: web
    name: financas-ia
    runtime: image
    image:
      url: ghcr.io/startse/financas-ia:latest
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "10000"
    # Para manter a configuração feita em /setup entre deploys (exige plano pago):
    # disk:
    #   name: dados
    #   mountPath: /app/data
    #   sizeGB: 1
