# Ralph Progress Log
Started: Tue Sep 22 2026
Rodada: Atendente no WhatsApp 0.3.0 — resiliência, mídia, persona por brief, "por que respondeu assim", memória por contato e painel de atendimento (ralph/whatsapp-atendente-qualidade-ux)
Fonte: tasks/prd-whatsapp-atendente-qualidade-ux.md (20 histórias em 6 fases; a coluna notes de cada história diz de qual US da PRD ela veio)
A rodada anterior ("clone-site-agente-sites", 10 histórias, todas concluídas, trabalhada no worktree ../ai-action-demo-clone-site) está em archive/2026-09-22-clone-site-agente-sites/. As rodadas anteriores deste app estão em archive/2026-09-18-whatsapp-atendente-telas/ e archive/2026-09-17-onboarding-erros-conta/. Leia a seção "Codebase Patterns" desses progress.txt e o whatsapp-atendente/CLAUDE.md antes da primeira história.
Antes de começar: confira a branch (`git branch --show-current`), crie a branch da rodada a partir da main e nunca use `git add -A` (o checkout pode ter alterações de outro app).

---

## Codebase Patterns

- **Camada INFRA não muda nesta rodada** (`lib/ai.ts`, `lib/store.ts`, `lib/modelos.ts`, `lib/setup-comum.ts`, `proxy.ts`, `app/api/status|setup|conta|rotinas|historico`, `app/mcp`, `app/f`). Tudo o que precisar de IA fora de `askText`/`askJSON`/`askVision`/`askWithTools` nasce em módulo próprio do app (`lib/midia.ts`, `lib/persona.ts`, `lib/memoria.ts`), chamando o OpenRouter diretamente como `lib/documentos.ts` faz com embeddings. Divergência de propósito em arquivo INFRA só com entrada em `scripts/padrao-excecoes.json`.
- **Esquema:** colunas novas via `ALTER TABLE` dentro de `try` (padrão de `passou_por_pessoa` em `lib/conversas.ts`); tabelas novas em módulo próprio com `CREATE TABLE IF NOT EXISTS` na primeira chamada, no mesmo `app.sqlite` por `abrirBanco()`. `lib/conversas.ts` continua o único que escreve em `conversas`/`mensagens`.
- **Tipos de tela em `lib/types.ts`** (client-safe, sem `node:sqlite`); rótulos de valores do banco em `lib/rotulos.ts` como `Record` completo, nunca ternário na tela.
- **Jargão:** `scripts/verificar-jargao.mjs` varre `app/page.tsx`, `components/*.tsx` (menos `setup.tsx`) e `lib/*.ts`. Texto de tela fica em `components/` (não em `app/<rota>/page.tsx`) de propósito, para ser varrido. Um componente varrido não pode importar de caminho com "setup" no nome.
- **Testes sem conta real:** z-api falsa com `http.createServer` + `ZAPI_BASE_URL`; IA falsa trocando `global.fetch` para `openrouter.ai`; `node --test tests/*.test.mts` (`npm test`).
- **Verificação em navegador:** `npm run build` → `cp -r public .next/standalone/ && cp -r .next/static .next/standalone/.next/` → `DATA_DIR=/tmp/whatsapp-atendente-dados PORT=<porta livre ≥ 3115> HOSTNAME=127.0.0.1 node --disable-warning=ExperimentalWarning .next/standalone/server.js &` → criar conta por `curl -X POST /api/conta` → capturas desktop 1400x1000 e celular 390 com Playwright/Chromium. A porta 3000 é de outro projeto. O `placeholder` de campos não aparece nas capturas deste Chromium: texto de campo desligado é desenhado, não só `placeholder`.
- **Barra de endereço é a fonte da verdade das telas** (`history.pushState` + `popstate`, nunca `useSearchParams`); componente que consulta sozinho e avisa a tela de fora guarda a prop de retorno num `ref`.

---

## 2026-09-22 — US-001: Mensagens únicas, rajadas agrupadas e guarda contra corrida
- `lib/conversas.ts`: coluna `mensagens.id_externo` (CREATE + `ALTER TABLE` em `try` + índice único parcial `WHERE id_externo IS NOT NULL`); `registrarMensagemCliente({ idExterno })` devolve `MensagemClienteRegistrada` (conversa + `mensagemId` + `duplicada`) e ignora em silêncio um id já gravado; novas leituras `mensagensSemResposta(numero)` e `ultimaMensagemDoClienteId(numero)`.
- `lib/atendente.ts`: `responder()` (simulador/MCP, grava e responde na mesma chamada) passou a delegar para a nova `responderPendente(numero, { config, ultimaMensagemId, comecouEm })`, que responde à sequência inteira do cliente desde a última resposta e confere `podeResponder`/`motivoParaNaoResponder` DUAS vezes (antes da IA e antes de gravar). Retorno tipado `RespostaDoAtendente` (`resposta: null` + `descartada: <motivo>` quando nada sai). Prompt pede "UMA mensagem só" quando há mais de uma pendente.
- Novo `lib/rajada.ts`: `agendarResposta(numero, origem, { janelaMs })` com `Map` de temporizadores por número, `JANELA_RAJADA_MS = 3000`, proteção para janela que fecha com resposta ainda em andamento (`emAndamento`/`pendenteDepois`), envio por `enviarMensagem` com `registrarFalhaEnvio` no catch e `classificarEmSegundoPlano` depois.
- `app/webhook/zapi/route.ts` e `app/webhook/route.ts`: gravam e agendam (não chamam mais `responder()`); z-api passa `messageId` e descarta `isEdit`/`waitingMessage` com log; Meta passa `msg.id`.
- `lib/zapi.ts`: `segundosDigitando()` (1–3 s, `ceil(tamanho/80)`) enviado como `delayTyping` no `send-text` (documentação reconferida em 22/09/2026: segundos, 1 a 15, padrão 0).
- `tests/rajada.test.mts` (7 testes: dedupe, rajada → um `send-text`, assumir na janela, assumir DURANTE a geração, `podeResponder`, simulador síncrono + repetida por id, `delayTyping`). `npm test` = 13/13.
- `README.md` (linha do comportamento + `lib/rajada.ts` no mapa) e `CLAUDE.md` (cinco notas 0.3.0/US-001).
- Verificação: `tsc --noEmit`, `npm run lint` (só o warning pré-existente de `<img>`), `npm run build` ok. Ponta a ponta no standalone (`:3141`, `DATA_DIR=/tmp/wa-dados-us001`, credenciais da z-api por env + `ZAPI_BASE_URL=http://127.0.0.1:3140` apontando para `/tmp/wa-us001/zapi-falsa.mjs`): três avisos em 1 s → UM `send-text` (com `delayTyping: 3` e `Client-Token`), uma mensagem `atendente` depois de três `cliente`; mesmo aviso duas vezes → uma mensagem no banco e log "repetido ignorado"; `POST /assumir` 0,5 s depois do aviso → zero `send-text`, status `humano`, log "descartada: uma pessoa assumiu"; `isEdit`/`waitingMessage` → nenhuma conversa criada, dois logs. Sem tela nova: não há captura.
- Pré-existentes, NÃO desta história: `verificar-jargao.mjs` aponta "OpenRouter" em `components/Assistente.tsx:645` (arquivo intocado, último commit 8d1c712 na main); `verificar-padrao.sh` sai 1 por divergências em `bussola-ia`, `entrevista-ia`, `simulador-vendas`, `videos-campanha` e `lib/mcp-cliente.ts` deste app (também intocado). Vale resolver na US-020 (verificação final) ou fora do Ralph.
- **Learnings for future iterations:**
  - `tsx` não estava em `node_modules` no checkout: `npm ci` antes de `npm test` na primeira iteração de uma máquina.
  - A z-api falsa em `http.createServer` precisa recortar o caminho depois de `/instances/<id>/token/<tok>/` — são CINCO segmentos antes (`split("/").slice(5)`); com `slice(6)` toda chamada vira caminho vazio e o `send-text` "some" da contagem enquanto o app acha que enviou (o fake devolvia `{ value: true }` e `enviarTexto` só lança quando vem `error`). Quando a contagem der zero sem erro no log do servidor, desconfie do fake antes do app.
  - `tempoRespostaMs` de uma resposta por rajada inclui a janela de 3 s (é o tempo que o cliente esperou de verdade); no simulador continua sendo medido do `Date.now()` de antes de gravar, com precisão de ms.
  - Sem `config` salva o standalone responde pela clínica de exemplo: "Oi / preço / limpeza" cai em transferência (`atencao`) e "horário de atendimento" acha o trecho da base. Para um teste ponta a ponta previsível, pergunte algo que esteja na base de exemplo.
  - Fila em memória + status calculado na leitura: `motivoParaNaoResponder` lê `obterRegistro` (status já efetivo), então "resolvida" por 24 h nunca bloqueia uma resposta a mensagem recém-chegada (a mensagem reabre como `ia` antes).
---

## 2026-09-22 — US-002: Transferência com motivo, falha da IA sem silêncio e linha do tempo da conversa
- Novo `lib/transferencia.ts` (folha, client-safe): `MotivoTransferencia` (5 valores), `rotuloMotivo` (Record completo), `MOTIVOS_PARA_O_PROMPT` (explicação por motivo para o prompt; `falha` fica de fora), `lerMotivo` (`[TRANSFERIR]` → `sem_informacao`, `[TRANSFERIR:motivo]`, motivo desconhecido → padrão, marcador só no FIM do texto), `semMarcador`, `FRASE_FALHA_PADRAO`.
- `lib/types.ts`: `PapelMensagem` + `nota`/`evento` e `PAPEIS_DE_CONVERSA`; `Config.fraseFalha?`; `Conversa`/`ConversaCompleta` + `motivoTransferencia`/`esperandoDesde`. `lib/estado.ts:migrarConfig` e `PUT /api/config` aceitam `fraseFalha` (opcional, vazio = padrão).
- `lib/conversas.ts`: colunas `motivo_transferencia`/`esperando_desde` (CREATE + ALTER em `try`, reaterro para `atencao` antigas); `SO_CONVERSA` em `historicoRecente`, `perguntasDoCliente`; fronteira de `mensagensSemResposta` passou a `papel IN ('atendente','humano')`; `registrarResposta({ motivo, atendente })` grava/zera motivo+espera e o evento de transferência; `registrarMensagemCliente` grava `esperando_desde` (COALESCE) em `humano` e o evento "Conversa reaberta pelo cliente" (status EFETIVO resolvida, antes da mensagem); `registrarMensagemHumana` zera espera; `mudarStatus` ganhou `zerarEspera`/`zerarMotivo`/`evento`; `assumir`/`devolver(numero, atendente)`/`resolver` com eventos; `registrarEvento` exportado; `garantirConversa({ motivo })` e `semearExemplosSeVazio({ atendente })` dão motivo, espera e eventos às conversas de exemplo (`ConversaExemplo.motivo` em `lib/demo.ts`).
- `lib/atendente.ts`: prompt com `[TRANSFERIR:motivo]` + lista de motivos; `responderPendente` lê o motivo com `lerMotivo`/`semMarcador`, `try/catch` em `perguntarComFerramentas` → em `origem: "whatsapp"` grava/envia `fraseFalha`, motivo `falha`, `console.error` + `registrarFalhaEnvio`; simulador/MCP relançam. `RespostaDoAtendente.motivo`; `POST /api/simular` devolve `motivo`.
- `lib/metricas.ts`/`GET /api/metricas/exportar`: coluna "Motivo da transferência"; contagens de mensagens só de `SO_CONVERSA`.
- `components/ConversaAberta.tsx`: `LinhaEvento` (pílula centralizada, cinza, com hora); faixa âmbar "{atendente} passou esta conversa para uma pessoa · {motivo}"; par Aprovar/Corrigir calculado sobre `soConversa`. `components/PainelContato.tsx`: "Total de mensagens" e "Última mensagem" ignoram eventos/notas. Rotas `devolver` (passa `getConfig().atendente`) e `GET /api/conversas` (passa `atendente` à semeadura).
- Testes: novo `tests/transferencia.test.mts` (7 testes: lerMotivo, registrarResposta+evento+planilha, assumir/devolver/resolver/reabrir, IA falsa com `[TRANSFERIR:cliente_pediu]`, IA 500 no WhatsApp → frase de reserva + `falha` + `ultimaFalhaEnvio`, IA 500 no simulador → rejeita, caminho local → `sem_informacao`); `tests/rajada.test.mts` ajustado ao evento novo. `npm test` = 20/20; `tsc`, `npm run lint` (só o warning pré-existente) e `npm run build` ok.
- Ponta a ponta no standalone (`:3142`, `DATA_DIR=/tmp/wa-us002/dados`, z-api falsa em `:3140`, IA falsa por `node --import /tmp/wa-us002/ia-falsa.mjs` lendo `/tmp/wa-us002/ia-modo.json`): webhook com resposta `[TRANSFERIR:cliente_pediu]` → um `send-text` SEM o marcador, `atencao`, motivo `cliente_pediu`, evento "Bia pediu ajuda de uma pessoa · O cliente pediu uma pessoa"; IA em 500 → `send-text` com a frase de reserva, `atencao`, motivo `falha`, `WHATSAPP_ULTIMA_FALHA` gravada e erro no log; assumir → mensagem humana → devolver → resolver → nova mensagem do cliente gera os quatro eventos e "Conversa reaberta pelo cliente" antes da mensagem, e a IA volta a responder; planilha com a coluna nova. Capturas (Playwright, `/tmp/wa-us002/*.png`) em 1400x1000 e 390x844 das duas conversas: faixa âmbar com motivo, cinco eventos na linha do tempo, `scrollWidth === clientWidth`, zero erro de console.
- Docs: `README.md` (comportamento + `lib/transferencia.ts` no mapa) e `CLAUDE.md` (quatro notas US-002).
- **Learnings for future iterations:**
  - **IA falsa dentro do standalone**: `node --import <preload>.mjs .next/standalone/server.js` com um preload que troca `globalThis.fetch` funciona — o Next embrulha o `fetch` que já existe quando sobe, então o falso vira o "original". Controlar o modo por um arquivo JSON lido a cada chamada evita reiniciar o servidor entre cenários. Scripts em `/tmp/wa-us002/` (`ia-falsa.mjs`, `subir.sh`, `zapi-falsa.mjs`, `shot.mjs` com `playwright-core@1.47` + `chrome-headless-shell` de `~/Library/Caches/ms-playwright/chromium_headless_shell-1243`; cookie `sessao` no domínio `127.0.0.1`). A conta exige senha com maiúscula, minúscula, número e caractere especial.
  - Toda consulta antiga em `mensagens` que assumia "não é cliente = é resposta" quebra com eventos na tabela: a fronteira de `mensagensSemResposta` foi o caso concreto (um evento depois da mensagem do cliente esvaziava a lista de pendentes). Ao somar um papel novo, procurar por `papel <> 'cliente'` e `papel !== "cliente"` antes de qualquer outra coisa.
  - Os testes da rajada assertavam a lista inteira de papéis; com eventos, filtrar `papel !== "evento"` ou incluir o evento explicitamente (o de "assumir" é determinístico).
  - A classificação de assunto com a IA falsa (texto sem JSON) deixa `assunto` nulo — não é bug da história, é o fake.
  - Não há tela para `fraseFalha` ainda (US-008); até lá só `PUT /api/config` ou a configuração salva alteram a frase.
---
